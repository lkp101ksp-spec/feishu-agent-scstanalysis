# 测试总结+2026-09-04T08-05-56（Phase 26 T5：AgentLoop 状态机）

## 本次测试范围

Phase 26 T5：自研 agentic loop（think→act→observe），按计划
`docs/superpowers/plans/2026-09-03-phase26-t5-agent-loop.md` 严格走 TDD 五步
（Step5 commit 按用户要求跳过，未执行 git commit）。

涉及文件：

- 新建 `orchestrator/coding/__init__.py`（空，目录原先不存在）
- 新建 `orchestrator/coding/agent_loop.py`（AgentLoop/LoopResult/truncate_observation/compress_messages）
- 新建 `tests/unit/test_coding_agent_loop.py`（14 用例）

测试命令：`.\.venv\Scripts\python.exe -m pytest tests/unit/test_coding_agent_loop.py -v --basetemp=.pytest_basetemp_t5`

## 执行记录

| 步骤 | 内容 | 结果 |
|---|---|---|
| Step1 | 写失败测试 + 建包骨架 | 完成 |
| Step2 | 首次跑 pytest | 如期 collection FAIL：`ModuleNotFoundError: orchestrator.coding.agent_loop` |
| Step3 | 实现 agent_loop.py | 完成 |
| Step4a | 第二跑 | 12 passed / 2 failed（见下"与计划差异"） |
| Step4b | 修正后复跑 | **14 passed in 0.16s**（计划写 13 用例，实际 14，全绿） |

## 与计划的差异（测试先行，实现按测试契约修正两处）

1. **arguments 需解析为 dict 再 dispatch**：计划实现把 `function.arguments`
   JSON 字符串原样透传，而测试 `test_two_tool_steps_then_final` 断言
   `seen == [("t1", {})]`（dict 原样传递）。修正：`_execute_tool` 内对 str 先
   `json.loads`（空串按 `{}`），解析失败回 `{"ok": False, "error": "BAD_ARGUMENTS_JSON"}`
   观察而非崩溃。
2. **超时判定 `>` 改 `>=`**：`timeout_sec=0` 时 `time.monotonic() - started > 0`
   可能因同 tick 为 False 导致继续发起 LLM 调用；改为 `>=` 保证零超时立即终止，
   `test_timeout` 断言 `llm.calls == []` 通过。对正常正数超时无实际影响。

## 覆盖点

- 三重终止：final / max_steps / timeout / budget_exhausted（压缩一次仍超优雅收尾）
- 连续 3 败禁用工具（注入"禁用"告知、第 4 轮 tools=[]）、成功重置计数器、禁用后仍调工具 → no_tools
- L2 审批：拒绝回 APPROVAL_DENIED 观察且不 dispatch、放行后正常 dispatch；未知工具缺省按 L1
- 细节：单条观察截断（truncated 标记、<5000 字符）、on_step 回调、model 透传、tool_events 收集

## 后续建议

1. T6/T7 拼装时以本测试的 dispatch 契约为准：**arguments 一律为已解析 dict**，CodeTools.dispatch 侧勿再重复 `json.loads`。
2. `_approx_tokens` 为字符数/4 粗估，接入真实模型后可用 tokenizer 精估替换（保持接口不变）。
3. 未 commit 的工作树含 T5 全部产物，执行总览勾选/提交前请先 `git status` 确认无其他混入改动。
4. `.pytest_basetemp_t5` 已清理。
