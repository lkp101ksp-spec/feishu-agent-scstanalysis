# 测试总结 2026-09-18 18:15

## 本批内容：Phase 65 st_nichenet 集成（空间版 NicheNet 配体活性优先级，st 第 16 工具）

立项探针（probe_st_nichenet2.py 三路判据：空间 A2M rank1+诱饵 0/5、非空间 5/5 tested、
洗牌 5/5 稳定）终判后落地。跨镜像分发复用 st_cellchat_v2 先例：st_nichenet.py 物理在
sc_tools/、l3_spatial 注册 image=bio、st 镜像零增重。空间增量 = BFS kNN 分环
（core=receiver niche 全 spot，r1..max_rings 为 sender，远端 -1），空间约束=候选母体
收窄（min_expr 门控在收窄后 sender 母体统计，远端高表达配体被过滤=诱饵地面真值）。
KB 桥（expr_stats.csv+geneset.csv → nichenetr_bridge.R）与绘图/emit 范式继承 sc 版，
新增 rings.png 身份产物 + ring_counts。timeout 1800，附录 A + SECTION_TITLES 连带同步。

## 验证结果

| 验证项 | 命令 | 结果 |
|---|---|---|
| TDD 新测试 | pytest tests/unit/test_l3_st_nichenet.py + test_l3_spatial.py + test_l3_dispatch_contract.py | **21 passed**（5 新用例 + st 15→16 + 表守护抓漏后补绿） |
| ruff | ruff check 五个 touched 文件 | All checks passed! |
| mypy（CI 口径） | python -m mypy（packages 白名单生效） | Success: no issues found in 196 source files |
| 冒烟真跑（docker 断网） | python scripts/_smoke_st_nichenet.py | **SMOKE OK**：human A2M top10 自证+诱饵 0/5 tested（sender 22/400）+mouse 自动探测 87 配体+四拒收路径全过 |
| 全量回归 | pytest -q -m "not pg" | **1335 passed**, 0 failed（6 deselected pg 层） |

## 过程问题与教训（重要）

1. **冒烟合成数据地面真值被基础表达破坏**：全基因 poisson(1.0) 背景下，诱饵在
   sender 带约 63% 细胞有表达（P(X=0)=e⁻¹≈0.37），sender_pct≥min_expr 空间过滤失效。
   对策：诱饵列在非远端区严格置零，落实"只在远端高表达"的地面真值语义。
   教训：与立项探针首跑同款教训（配体列基线噪声骗过 min_expr），合成空间数据
   必须显式分区控制零表达。
2. **镜像不含新脚本的冒烟挂载盲区**：sc 版冒烟能跑通是因为 nichenet.py 已烘焙进
   bio 镜像 /opt/sc_tools；新脚本 st_nichenet.py 本地/CI 镜像都没有，直接 docker run
   报 No such file。对策：冒烟 BASE 加 `-v {repo}/sandbox/sc_tools:/opt/sc_tools`
   整目录挂载（含 common.py），对齐 handler 的 script_dir 行为——CI 不依赖镜像
   是否包含当次新脚本。
3. **mypy 口径踩坑**：`python -m mypy .` 会绕过 pyproject 的 packages 白名单，
   炸出 tests/scripts 1881 个历史噪音错；正确门禁口径=CI 同款 `python -m mypy`
   （不带参数，packages 生效，196 文件）。st_nichenet.py 两处真错（coords 缺
   ndarray 注解、order 变量 list/ndarray 同名联合推断冲突改 rorder）已修。

## 后续建议

- push 后观察 CI：test/docker-smoke 应快绿；bio-image-smoke 为 manual job（本次
  ci.yml 变更在 test job，不会自动触发镜像构建）；顺带复查 re-run 35329154163
  的 bio-image-smoke 终态（挂账）。
- st_nichenet 真机验证（真实 Visium/Xenium processed.h5ad，先跑 st_process 产
  obsm['spatial'] + spatial_domain 列）留待有真实 st 数据集时做；逐 niche 对
  循环（L4 编排）与 st_commot/st_cellchat_v2 三工具互补联用是后续候选方向。
- 冒烟两坑教训已钉 ROADMAP Phase 65 条目；合成空间数据"分区严格置零"应作为
  后续 st 类冒烟脚本的默认姿势。
