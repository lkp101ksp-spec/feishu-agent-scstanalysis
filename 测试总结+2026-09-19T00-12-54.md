# 测试总结（2026-09-19 00:12）——Phase 69：整合双引擎收官——sc_integrate +harmony & st_integrate 多切片整合工具化（st 第 18 工具）

## 验证一览

| # | 场景 | 结果 | 关键数字 |
|---|------|------|----------|
| 1 | 契约测试（test_l3_st_integrate.py 新增 5 用例） | 5 passed | registered / dispatches_bio_image / defaults / list_passthrough / error_passthrough |
| 2 | L3 清单守护（test_l3_spatial + test_l3_singlecell） | 19 passed | st_* 17→**18 工具**；sc_integrate 转发测试改双引擎枚举锁定（enum=[bbknn,harmony] + harmony 转发断言） |
| 3 | 合成冒烟 _smoke_sc_integrate_harmony.py（5 场景） | SMOKE OK | 600×500 双批（S2 叠 40 技术基因批次效应）：harmony 可校（representation=X_pca_harmony + 产物落盘）/ bbknn 对照（neighbors_within_batch=8）/ 三拒收（batch 缺列、method 非法、batch 单值）断网端到端 |
| 4 | 合成冒烟 _smoke_st_integrate.py（9 场景） | SMOKE OK | 双切片（2×400 spot × 600 共享基因 + 片 B 批次效应）：harmony 默认引擎（600 HVG 全选 + 三产物落盘）/ spatial_offset note 勿喂空间 kNN 提示 / bbknn 引擎 / 六拒收（单片、重复、非法 method、ref 缺失、基因零交集、slice_col 冲突） |
| 5 | 真机对照 p69-5b（4 跑并行全绿，各 ~30s） | 全绿 | 599sub 1000×32285（10 供体分层 ×100，seed=7；counts 回源 combined_10x，frac_int=1.0）+ osccL/osccR（1749 spot 按 x 中位切分 874/875）；sc：bbknn mix_graph **0.93** > harmony 0.91，但 harmony sil_leiden **+0.08** > bbknn -0.02（16 簇 vs 8 簇）；st：bbknn mix_graph **0.55** > harmony 0.39，sil_leiden 双双近 0；指标落盘 _eval/p69b_metrics.json |
| 6 | 门禁 ruff / mypy 双平台 / pytest | 全绿 | ruff All checks passed；mypy win+linux **198 文件 Success**（var-annotated 1 错修复后）；pytest **1340 passed**（178s，not-pg 口径 = 1335 基线 + 5 新用例，零回归；Phase 68 引的 1341 为含 6 pg 用例全集口径） |
| 7 | 终版镜像复验（mypy 修复后 docker 快重建 → 双冒烟重跑） | 全绿 | inline 修复零运行时差异，COPY 层拾取新脚本后两冒烟 SMOKE OK |
| 8 | harmonypy 钉版复验（用户口径"v2 以上"→ 层改 "harmonypy>=2" → 重建+双冒烟） | 全绿 | 钉版层真跑（上下文修正 sandbox/ 后链失效重装）：清华源解析 **harmonypy 2.0.2**（cp312 wheel 12.1MB）；新镜像 df8e7122 双冒烟 SMOKE OK |
| 9 | CI 侧 manual run 验收（bio/st 镜像 job 仅 workflow_dispatch 触发，push run 两 job 自动 skip——ci.yml `if: github.event_name == 'workflow_dispatch'`） | 见下 | 首跑 **35407998472**：test/docker-smoke/st-image-smoke 三 job 绿，bio-image-smoke 死于 NicheNet 先验层 **zenodo 504 Gateway Timeout**（runner 无层缓存每次裸连，单次抖动整层报废，~30min 才暴露）→ bio.Dockerfile 两下载点（zenodo 四件先验+GitHub tarball）加 `dl()` 五次重试+20s 退避（**7b97aeb**，本地镜像内小文件实测 `dl ok: TRUE` 32KB + BuildKit --check 零警告）→ 复跑 **35413460482** |
| 10 | Phase 70 探针：probe_sc_tcr（sc_tcr 免疫组库立项前方法学验证，本机 .venv 纯 pandas/scipy） | 全绿 | 三判据 PASS——①六重过滤（is_cell/high_confidence/productive/链∈{TRA,TRB}/双链齐/barcode 单 clonotype）6 类污染行全灭，4 组织有效细胞 12/3/6/4 精确匹配；②跨组织 clonotype `patient::canonical(cdr3s_nt)`——cloneA 9 细胞跨 Tumor/PBMC 合一、P2 复刻 B 序列单例患者隔离独立成克隆、分级 n>=3:20/n=1:5；③Startrac 指数 expa 双实现对拍（P1 0.1871 手算+scipy 一致/P2 0.0794 scipy 一致）+ migr 手算对拍（P1 0.5510/P2 0.4000，1e-9 容差）——**结论：可立 Phase**；探针插曲：expa_P2 手算值 0.0793900624 曾算错，改 scipy.stats.entropy(base=2) 独立通道对拍根治；判据②d 序列匹配需患者前缀限定（P2 复刻序列撞 P1 期望误 FAIL） |
| 11 | Phase 70 落地：sc_tcr 工具（契约 6 用例+守护 2+冒烟 7 场景+门禁） | 全绿 | 契约 test_l3_sc_tcr.py 6 passed（list[dict] 首例逐元素透传/file 转容器相对路径/多数据根拒收/缺三元组结构化报错）+ 守护 dispatch_contract 2 passed（_synth_args 加 array-of-object 分支按 items.required 合成最小元素，sc_tcr 自动入列）；冒烟 _smoke_sc_tcr.py **7 场景**（①四文件全量 n_cells_kept=25/n_clonotypes=9/expa·migr 探针真值逐位一致+三产物②患者隔离+跨组织合一③单组织 migr=null 降级④空列表⑤缺列 INVALID_INPUT⑥写回 60% 命中三列落盘+clone_size=9 写回⑦10% 命中 TCR_ALIGN_FAILED）；门禁 ruff/mypy win **199 文件**+linux 反模拟 199 Success/pytest **1346 passed**（1340 基线+6 新契约零回归）；**开发两插曲钉档**——①ToolSpec 深嵌套 JSON schema 括号三轮才平（description 行尾多 `}` 把 contig_files 提前关、required 落进 properties——嵌套 dict 字面量超过 4 层时肉眼数括号必错，深度打印+repr 定位）②冒烟断言 cell_id 带 `patient__tissue__` 前缀 startswith("p1t_b") 永假（探针裸 barcode→容器全串 cell_id 的口径漂移，改 contains） |
| 12 | Phase 71 探针：probe_sc_cytosig（sc_cytosig 细胞因子信号预测立项前验证，bio 镜像内 venv） | 全绿 | **安装路线三轮钉档**——data_significance 是 2021 年 GSL 依赖 C 扩展：Windows 无 GSL ✗ / 容器 build-isolation 拉新 numpy 头文件错配 ✗ / **--no-build-isolation 但 pip 安装期仍升 numpy 2.5 ✗ → 终解 = 独立 venv /opt/cytosig_env（numpy<2 与 CytoSig 同命令约束防升级 + apt libgsl-dev build-essential）**；签名体量 **4881 基因×43 因子**（TGFB1 top 基因 GADD45B/JUNB/FSTL3 = 文献钉死 TGFβ 应答基因，签名自带 sanity）；三判据 PASS——①合成注入 2.0×sig[TGFB1]⊗组指示 → TGFB1 beta 排名 **1/43**、A 组 0.0695 vs B 组 -0.0003（断层 3 倍于第二名 GDF11）②纯噪声对照 43 因子仅 1 个 p<0.01（置换假阳率 ~2% 符合预期）③zscore 口径同向 rank 1（beta vs zscore 双口径交叉）——**结论：可立 Phase**，镜像层方案 apt+venv 已验证（pip 清华源自带重试，无需 dl()） |
| 13 | Phase 71 落地：sc_cytosig 工具（契约 4 用例+守护+冒烟 6 场景+门禁） | 全绿（本机口径，CI 挂账见后续建议） | 五连带齐：handler（timeout 1800）+ToolSpec **浅层 schema**（吸取 Phase 70 括号地狱教训，全部标量参数：dataset_ref/groupby 必填、mode enum diff/per_cell、nrand 100..5000）+SECTION_TITLES+附录 A 1800 行+ci.yml 冒烟步；契约 test_l3_sc_cytosig.py 4 用例+dispatch 守护共 6 passed；**venv 桥架构落地**——cytosig.py 主环境读 h5ad（raw 优先+upper 对齐）→ diff 逐群差分谱/per_cell 逐细胞 → tsv → subprocess /opt/cytosig_env 内核 _cytosig_core.py（ridge_significance_test alpha=1e4）→ beta/std/zscore/pvalue 长表+群汇总+热图/top5 条形（beta vs zscore 双口径并列，note 钉注不可混用）；冒烟 **6 场景**（①diff TGFB1 rank 1/43 断层 3.07×+zscore 同向+三产物②per_cell 40 细胞+by_group 群汇总 TGFB1@A 领跑③噪声对照 B 群最深 1/20 细胞显著（口径调整：860 次检验下"≥1 击因子数"纯二项期望≈8 无判别力，改判深度≤4/20）④mode 非法⑤groupby 缺列带候选提示⑥LOW_OVERLAP）；**三插曲钉档**——①签名含 30 个 C10orf88 类混合大小写符号（HGNC 旧式写法），单侧 upper 对齐漏配 770/800 → cytosig.py+内核双侧 upper 化后 800 全量；②zenodo.org 全站 504（nichenetr 先验层），本地全量重建与 CI 35426142997（sc_tcr 验收）同卡——**解耦先行**：FROM 旧 cpu-latest 叠 cytosig 层打 cpu-cytosig 补丁镜像跑绿冒烟，恢复后全量重建补验；③PowerShell 管道 `Select-Object -Last` 会整段缓冲 docker build 输出直到命令结束——误判构建挂起误杀一轮（教训：长命令看进度别接 -Last） |

## 真机对照口径（p69-5b：BBKNN vs Harmony 头对头）

- 三指标：**mix_graph**（connectivities 跨批边占比——引擎无关口径，bbknn 改图、harmony 改 PCA 都落在最终邻居图上）+ **sil_batch / sil_leiden**（各引擎自家整合空间：harmony=X_pca_harmony、bbknn=X_pca；>2000 细胞抽样 2000，seed=7）。
- sc 侧结论：bbknn 混批更激进（0.93 vs 0.91）但结构牺牲更大（sil_leiden -0.02 vs +0.08，簇数 8 vs 16）——**批间差异强时 harmony 保留更多生物学结构**。
- st 侧结论：bbknn 混片更强（0.55 vs 0.39）；表达域结构两者都弱（x 中位切分的"批次"是合成口径，域结构差异由切片本身主导）。
- 经验法则沉淀进工具描述：默认 bbknn（快、稳、存量兼容）；批间差异大/需保留簇结构时传 method=harmony；st_integrate 默认 harmony（多切片通常批差更大）。

## 关键修复与教训沉淀

1. **mypy var-annotated 一击即中**：`x = np.asarray(...)` 中间变量在 strict 模式需显式注解——inline 进 `run_harmony(np.asarray(...), ...)` 调用零运行时差异修复；integrate.py 同款预防性统一（本机过≠CI 过，平台盲区纪律）。教训：strict 模式下中间变量倾向 inline 或显式注解。
2. **真机对照 counts 回源坑**：integration 产物（59900_bbknn）的 X 与 raw 均为 scaled 形态（frac_int=0.85，raw min=0.07）——对照实验的 counts 必须回原始 10x 源（bio_test_data/combined_10x 93665×32285，与 59900 条码 100% 对齐，frac_int=1.0）。探针断言"raw 快照非 counts"先败后查，先验数据形态再建库。
3. **harmonypy 2.x 直调口径**：清华源无 0.3/0.4 旧线、2.x 为重写版（Z_corr=(cells, pcs) 不转置）——代码按 Z_corr 形状自适应 transpose，**不依赖 scanpy.external.pp_harmony_integrate 包装**（其内部有旧版形状假设）；层钉 `>=2` 下限（回滚口径 = 放宽下限或删层重建）。
4. **st_integrate 设计红线：不做空间邻域图**——跨切片坐标无物理意义，合并后只做表达域（UMAP+leiden）聚类；spatial_offset=true 各片 x 平移并排**仅展示用**，offset 产物喂空间 kNN 工具（st_stats/st_nichenet 等）会误判邻居——note 字段显式告警。
5. **new_id 防撞**：`{ref1}__{ref2}[_etc]__{method}` 双下划线分隔 + `\W` 清洗 + 80 长度帽——与 sc 侧 `{id}_{method}` 单下划线风格区分，多 ref 不与单 ref 撞名；>2 片缩写 _etc。
6. **跨镜像分发复用第三例**：st_integrate.py 物理在 sc_tools/、l3_spatial 注册 image=bio——st 镜像零增重（st_cellchat_v2/st_nichenet 先例延续），bio 镜像一层 harmonypy 双工具共享。
7. **CI 镜像 job 冷缓存裸连外网源的抖动脆弱性**：GH runner 无 docker 层缓存，bio 全量构建每次重下 zenodo 先验/GitHub tarball——Phase 64/66/68 三次过属运气，本次首撞 504 整层报废且 ~30min 才暴露。修法 = 下载点统一 `dl()` 重试帮手（5 次×20s 退避 + `file.size>0` 拦半截文件）；R 语法先在本地镜像内小文件实测、BuildKit `--check` 过了才上 CI——**层内下载必须自带重试，构建期网络调用不能赌单次成功**。

## 后续建议

- **zenodo outage 挂账（sc_tcr + sc_cytosig CI 验收同卡）**：manual run 35426142997 挂在 bio 镜像构建步——zenodo.org 全站 504（nichenetr 先验 RDS，dl() 五次重试扛不住持续性 outage，非单次抖动），冒烟步全 skipped 属上游连坐非代码问题；本地全量重建同卡（前置 R 层已缓存）。**守望已挂**：bio_workspace/_eval/_zenodo_watch.py 后台轮询（5 分钟×24h），恢复后自动三连——①rerun 35426142997（sc_tcr 验收）②dispatch main 最新 ci.yml（Phase 71 验收）③本机全量重建 cpu-latest+默认镜像复跑 sc_cytosig 冒烟。期间 Phase 71 冒烟已用 cpu-cytosig 补丁镜像（FROM 旧 cpu-latest 叠 venv 层）先行跑绿。
- **sc_tcr 真机挂账**：VDJ 数据（Cell Ranger filtered_contig_annotations.csv）到位后补真机验收——重点看真实 contig 的列形态（字符串 bool/额外列）、跨组织 clonotype 复用率、写回对齐率实测；paper 口径可对照作者报告 6,503 clonotype 量级。
- **sc_cytosig 真机挂账**：真机数据到位后按 diff 口径跑一轮（预期 TGFB1 在 CAF/肌成纤维等间质群领先），对照 CytoSig 网站排名口径解读 beta；nrand=1000 千级细胞分钟级，无需降档。
- 双冒烟已进 ci.yml bio-image-smoke（st_niche_scan 步后追加）；CI 侧已验收（2026-09-19 manual run 35413460482 四 job 全绿，sc_integrate/st_integrate 冒烟步 step 级 success）。插曲：首轮 35407998472 bio 构建撞 zenodo 504（runner 冷缓存裸连外网，单次抖动整层报废）→ bio.Dockerfile 两下载点加 dl() 五次重试+20s 退避（7b97aeb）后复跑绿。
- harmonypy 已钉 `>=2` 下限（2026-09-19 用户口径"v2 以上"，实测实装 2.0.2 cp312 wheel；代码 Z_corr 形状校验保留为防御兜底）；若未来 3.x breaking（Z_corr 形状再变），冒烟第①场景会立刻拦住，届时再评估加上限。
- st 侧真机对照目前是 oscc 双片 x 中位切分（合成"批次"）；真实多批次空间数据（不同供体/平台）到位后建议复跑 _eval/p69b_metrics.py 三指标对照。
- sil_leiden 在 st 侧双双近 0：切片数少、全局 HVG 口径下表达域聚类的意义待真实多片数据再评；必要时考虑 per-slice HVG 再合并。
- oscc 双片产物（osccL__osccR__{harmony,bbknn}）已留 bio_workspace 作 st_integrate 真机基准，后续 st 整合改动可一键回归。
