# 测试总结（2026-09-19 00:12）——Phase 69：整合双引擎收官——sc_integrate +harmony & st_integrate 多切片整合工具化（st 第 18 工具）

## 验证一览

| # | 场景 | 结果 | 关键数字 |
|---|------|------|----------|
| 1 | 契约测试（test_l3_st_integrate.py 新增 5 用例） | 5 passed | registered / dispatches_bio_image / defaults / list_passthrough / error_passthrough |
| 2 | L3 清单守护（test_l3_spatial + test_l3_singlecell） | 19 passed | st_* 17→**18 工具**；sc_integrate 转发测试改双引擎枚举锁定（enum=[bbknn,harmony] + harmony 转发断言） |
| 3 | 合成冒烟 _smoke_sc_integrate_harmony.py（5 场景） | SMOKE OK | 600×500 双批（S2 叠 40 技术基因批次效应）：harmony 可校（representation=X_pca_harmony + 产物落盘）/ bbknn 对照（neighbors_within_batch=8）/ 三拒收（batch 缺列、method 非法、batch 单值）断网端到端 |
| 4 | 合成冒烟 _smoke_st_integrate.py（9 场景） | SMOKE OK | 双切片（2×400 spot × 600 共享基因 + 片 B 批次效应）：harmony 默认引擎（600 HVG 全选 + 三产物落盘）/ spatial_offset note 勿喂空间 kNN 提示 / bbknn 引擎 / 六拒收（单片、重复、非法 method、ref 缺失、基因零交集、slice_col 冲突） |
| 5 | 真机对照 p69-5b（4 跑并行全绿，各 ~30s） | 全绿 | 599sub 1000×32285（10 供体分层 ×100，seed=7；counts 回源 combined_10x，frac_int=1.0）+ osccL/osccR（1749 spot 按 x 中位切分 874/875）；sc：bbknn mix_graph **0.93** > harmony 0.91，但 harmony sil_leiden **+0.08** > bbknn -0.02（16 簇 vs 8 簇）；st：bbknn mix_graph **0.55** > harmony 0.39，sil_leiden 双双近 0；指标落盘 _eval/p69b_metrics.json |
| 6 | 门禁 ruff / mypy 双平台 / pytest | 全绿 | ruff All checks passed；mypy win+linux **198 文件 Success**（var-annotated 1 错修复后）；pytest **1340 passed**（178s，not-pg 口径 = 1335 基线 + 5 新用例，零回归；Phase 68 引的 1341 为含 6 pg 用例全集口径） |
| 7 | 终版镜像复验（mypy 修复后 docker 快重建 → 双冒烟重跑） | 全绿 | inline 修复零运行时差异，COPY 层拾取新脚本后两冒烟 SMOKE OK |
| 8 | harmonypy 钉版复验（用户口径"v2 以上"→ 层改 "harmonypy>=2" → 重建+双冒烟） | 全绿 | 钉版层真跑（上下文修正 sandbox/ 后链失效重装）：清华源解析 **harmonypy 2.0.2**（cp312 wheel 12.1MB）；新镜像 df8e7122 双冒烟 SMOKE OK |
| 9 | CI 侧 manual run 验收（bio/st 镜像 job 仅 workflow_dispatch 触发，push run 两 job 自动 skip——ci.yml `if: github.event_name == 'workflow_dispatch'`） | 见下 | 首跑 **35407998472**：test/docker-smoke/st-image-smoke 三 job 绿，bio-image-smoke 死于 NicheNet 先验层 **zenodo 504 Gateway Timeout**（runner 无层缓存每次裸连，单次抖动整层报废，~30min 才暴露）→ bio.Dockerfile 两下载点（zenodo 四件先验+GitHub tarball）加 `dl()` 五次重试+20s 退避（**7b97aeb**，本地镜像内小文件实测 `dl ok: TRUE` 32KB + BuildKit --check 零警告）→ 复跑 **35413460482** |
| 10 | Phase 70 探针：probe_sc_tcr（sc_tcr 免疫组库立项前方法学验证，本机 .venv 纯 pandas/scipy） | 全绿 | 三判据 PASS——①六重过滤（is_cell/high_confidence/productive/链∈{TRA,TRB}/双链齐/barcode 单 clonotype）6 类污染行全灭，4 组织有效细胞 12/3/6/4 精确匹配；②跨组织 clonotype `patient::canonical(cdr3s_nt)`——cloneA 9 细胞跨 Tumor/PBMC 合一、P2 复刻 B 序列单例患者隔离独立成克隆、分级 n>=3:20/n=1:5；③Startrac 指数 expa 双实现对拍（P1 0.1871 手算+scipy 一致/P2 0.0794 scipy 一致）+ migr 手算对拍（P1 0.5510/P2 0.4000，1e-9 容差）——**结论：可立 Phase**；探针插曲：expa_P2 手算值 0.0793900624 曾算错，改 scipy.stats.entropy(base=2) 独立通道对拍根治；判据②d 序列匹配需患者前缀限定（P2 复刻序列撞 P1 期望误 FAIL） |
| 11 | Phase 70 落地：sc_tcr 工具（契约 6 用例+守护 2+冒烟 7 场景+门禁） | 全绿 | 契约 test_l3_sc_tcr.py 6 passed（list[dict] 首例逐元素透传/file 转容器相对路径/多数据根拒收/缺三元组结构化报错）+ 守护 dispatch_contract 2 passed（_synth_args 加 array-of-object 分支按 items.required 合成最小元素，sc_tcr 自动入列）；冒烟 _smoke_sc_tcr.py **7 场景**（①四文件全量 n_cells_kept=25/n_clonotypes=9/expa·migr 探针真值逐位一致+三产物②患者隔离+跨组织合一③单组织 migr=null 降级④空列表⑤缺列 INVALID_INPUT⑥写回 60% 命中三列落盘+clone_size=9 写回⑦10% 命中 TCR_ALIGN_FAILED）；门禁 ruff/mypy win **199 文件**+linux 反模拟 199 Success/pytest **1346 passed**（1340 基线+6 新契约零回归）；**开发两插曲钉档**——①ToolSpec 深嵌套 JSON schema 括号三轮才平（description 行尾多 `}` 把 contig_files 提前关、required 落进 properties——嵌套 dict 字面量超过 4 层时肉眼数括号必错，深度打印+repr 定位）②冒烟断言 cell_id 带 `patient__tissue__` 前缀 startswith("p1t_b") 永假（探针裸 barcode→容器全串 cell_id 的口径漂移，改 contains） |
| 12 | Phase 71 探针：probe_sc_cytosig（sc_cytosig 细胞因子信号预测立项前验证，bio 镜像内 venv） | 全绿 | **安装路线三轮钉档**——data_significance 是 2021 年 GSL 依赖 C 扩展：Windows 无 GSL ✗ / 容器 build-isolation 拉新 numpy 头文件错配 ✗ / **--no-build-isolation 但 pip 安装期仍升 numpy 2.5 ✗ → 终解 = 独立 venv /opt/cytosig_env（numpy<2 与 CytoSig 同命令约束防升级 + apt libgsl-dev build-essential）**；签名体量 **4881 基因×43 因子**（TGFB1 top 基因 GADD45B/JUNB/FSTL3 = 文献钉死 TGFβ 应答基因，签名自带 sanity）；三判据 PASS——①合成注入 2.0×sig[TGFB1]⊗组指示 → TGFB1 beta 排名 **1/43**、A 组 0.0695 vs B 组 -0.0003（断层 3 倍于第二名 GDF11）②纯噪声对照 43 因子仅 1 个 p<0.01（置换假阳率 ~2% 符合预期）③zscore 口径同向 rank 1（beta vs zscore 双口径交叉）——**结论：可立 Phase**，镜像层方案 apt+venv 已验证（pip 清华源自带重试，无需 dl()） |
| 13 | Phase 71 落地：sc_cytosig 工具（契约 4 用例+守护+冒烟 6 场景+门禁） | 全绿（本机口径，CI 挂账见后续建议） | 五连带齐：handler（timeout 1800）+ToolSpec **浅层 schema**（吸取 Phase 70 括号地狱教训，全部标量参数：dataset_ref/groupby 必填、mode enum diff/per_cell、nrand 100..5000）+SECTION_TITLES+附录 A 1800 行+ci.yml 冒烟步；契约 test_l3_sc_cytosig.py 4 用例+dispatch 守护共 6 passed；**venv 桥架构落地**——cytosig.py 主环境读 h5ad（raw 优先+upper 对齐）→ diff 逐群差分谱/per_cell 逐细胞 → tsv → subprocess /opt/cytosig_env 内核 _cytosig_core.py（ridge_significance_test alpha=1e4）→ beta/std/zscore/pvalue 长表+群汇总+热图/top5 条形（beta vs zscore 双口径并列，note 钉注不可混用）；冒烟 **6 场景**（①diff TGFB1 rank 1/43 断层 3.07×+zscore 同向+三产物②per_cell 40 细胞+by_group 群汇总 TGFB1@A 领跑③噪声对照 B 群最深 1/20 细胞显著（口径调整：860 次检验下"≥1 击因子数"纯二项期望≈8 无判别力，改判深度≤4/20）④mode 非法⑤groupby 缺列带候选提示⑥LOW_OVERLAP）；**三插曲钉档**——①签名含 30 个 C10orf88 类混合大小写符号（HGNC 旧式写法），单侧 upper 对齐漏配 770/800 → cytosig.py+内核双侧 upper 化后 800 全量；②zenodo.org 全站 504（nichenetr 先验层），本地全量重建与 CI 35426142997（sc_tcr 验收）同卡——**解耦先行**：FROM 旧 cpu-latest 叠 cytosig 层打 cpu-cytosig 补丁镜像跑绿冒烟，恢复后全量重建补验；③PowerShell 管道 `Select-Object -Last` 会整段缓冲 docker build 输出直到命令结束——误判构建挂起误杀一轮（教训：长命令看进度别接 -Last） |
| 14 | Phase 71 真机验证：real_cytosig_599sub.py（599sub_bbknn 千细胞子集，**cpu-latest 正式镜像**） | 全绿 | 链路 ok：43 因子、签名交集 **4599/4881（94%）**（双侧 upper 鼠源对齐兑现）、8 簇、矩阵源 adata.raw；**方向判读命中文献**——簇 5（Col1a1 1.64+Acta2 0.63，CAF/肌成纤维）TGFB3 rank 1（b=+0.052）+ TGFB1 rank 2（b=+0.044）；簇 3（Ptprc 2.35+Cd3e 1.48，T 细胞）TGFB3 rank 1（b=+0.029）；簇 0（Lyz2 髓系）TGFB3 rank 4；**反向对照成立**——纯上皮簇 1/4/6 TGFB3 rank 42/43/43 垫底；**双口径互证**——16 个簇×因子组合 beta_rank 与 z_rank 全部逐一相等；日志存档 _eval/real_cytosig_599sub.log（插曲：python 块缓冲+PowerShell `*>` 重定向竞态只落末行——`python -u`+`Out-File` 重跑补全；ident 显示 `?` 为 CSV 回读 int 化 vs str 键，已修 str(c)） |
| 15 | Phase 72 落地：sc_genescore PROGENy 通路活性打分（契约 test_l3_sc_genescore+冒烟 5 场景+门禁） | 全绿 | decoupler 2.2.0 **MLM 加权回归** 14 信号通路；PROGENy top500 权重快照**构建期预取**（镜像层 fetch_progeny 复用 Phase 50 脚本烘焙，运行时断网可用）；**Zenodo 504 代理修复**兑现（dl() 重试护栏下快照预取层真跑）；冒烟 5 场景（TGFb 权重注入真值回收 rank 1+组均值/方差双判据+三拒收断网端到端）；真机 oscc **1749 细胞 14/14 通路** top=JAK-STAT（groupby=cluster_annotations 直跑，**groupby 自定义分组列**参数随工具落地）；超时档 1800s 进附录 A+section_digest 术语 |
| 16 | Phase 73 落地：sc_metabolism AUCell 升级（契约+冒烟 6 场景+门禁两插曲修复复跑） | 全绿 | **method=aucell 排名 AUC 默认口径**（前 10% 特征窗口，对齐 skill AUCell 方法论；n_up pt1k=2470/oscc=1563 实测）+ mean 均值差保 Phase 32 回归兼容；**groupby 自定义 obs 分组列**（默认 leiden，oscc 病理注释 cluster_annotations 直跑——真机补验 315/320 通路 top=Ribosome）；**Enrichr json 通路重复基因保序去重**（KEGG_2021_Human 实证，防 decoupler net (source,target) 重复行拒收）；**mouse 大写映射 str 收口**（门禁插曲②：mypy no-any-return）；冒烟 6 场景（Glycolysis 2.5×信号注入真值回收+方差 rank+A>B 分离+method/groupby 双拒收断网端到端）；真机 pt1k 301/303 通路；门禁插曲①：E501 行超长（print 拆 2 行 f-string） |
| 17 | Phase 74 落地：sc_cellfreq 组成偏好分析升级（契约+冒烟 7 场景+门禁） | 全绿 | **Ro/e 期望比富集**（chisq 期望 vs 实际逐簇偏好，好/坏双向）+**样本级 Fisher 偏好检验**（2×2 OR+BH）并列+**簇比例 Spearman 共现网络**（\|rho\|>=min_rho 且 BH q<0.05 连边；**igraph 零权重边 bug 修复**+edge_betweenness 社区+圆形布局 png+边表/社区 csv）；cooccurrence/min_rho 双参（默认开/0.6，false 静默关）；CI 宿主侧加 scanpy（仅 cellfreq 冒烟造数用，归属 Phase 74 分拆纪律）；冒烟 7 场景（已知偏向 Fisher/Ro-e 真值回收+三值降级+6 样本共现网络真值+off-switch 静默+拒收断网端到端）；真机 pt1k 组成/共现 4 产物落盘（n=10 样本 BH 校正下**空网络属合法结果**，冒烟 6 样本已证网络机制） |
| 18 | 三阶段分拆提交+push+CI+镜像烘焙复验 | 全绿 | **分拆纪律兑现**：共享文件（l3_singlecell.py/ci.yml/test_l3_singlecell.py）按 phase 归属中间态重建——revert→commit（**acaf580** 72 genescore +700/−6）→restore→commit（**e683130** 73 metabolism +274/−36）→restore→commit（**c5d6600** 74 cellfreq +229/−10），每阶段 hunk 头核对+终态 byte-equal+ruff/pytest 85 用例回归绿；备份 patch 快照用后即删；镜像重建烘焙复验：BuildKit 缓存下仅 COPY sc_tools 层重跑（#34 未缓存），新镜像 sha **4f716f5b**，容器内 /opt/sc_tools/metabolism.py:70 实测含 str 收口；push 插曲：首轮 curl 35 TLS unexpected eof 失败且输出混入"Everything up-to-date"误导——fetch 核实远端仍在 3e81a4a 后重试即过（3e81a4a..c5d6600），预推送钩子质量门两次 PASS；CI push run **35448218429**（c5d6600）test/docker-smoke 绿 + st/bio 镜像 job 自动 skip（设计如此）；manual dispatch 验收见 #19 |
| 19 | CI manual dispatch 验收（bio 全量冷构建+三新冒烟步，**三轮**：35448479226→35454576706→35460402127） | 三轮各修一雷，终全绿 | push run 绿后照 Phase 71 规矩 dispatch（ref=master）——**轮 1** 死于 monocle3 层：TUNA CRAN **44 包同波并行下载（Ncpus=4）同秒抖动**，statmod/futile.logger/snow 各 ~4.6s 失败→limma/BiocParallel 链式缺依赖→R CMD INSTALL 崩（教训 7 同族新变体：**dl() 只护 shell 下载，install.packages 内部下载护不到**）；修法 = **R 级重试封装 ir()**（探缺→只补缺→sleep 15s→6 波兜底 stop）+ monocle3 本体 3-try（插曲：parse 过但首跑 ir('sf') 缺 n 默认秒败——教训 14，补 n=6+utils 真实调用预检）；本地重建全绿（sha f817cf7e，monocle3 1.4.27 一次过，上轮三雷包全装上）→ commit 9f39012；**轮 2**（35454576706）st/test/docker-smoke 三 job 绿、bio-image-smoke 仍死但**换雷**：**CellChat 层（:148）38s 秒挂于 GitHub API 403 匿名限流**（`remotes::install_github` 走 api.github.com 匿名配额 60/h **按出口 IP 计**，CI runner Azure 共享出口 IP 52.234.45.177 被其它租户耗光至 0/60——环境性间歇雷，重试无用（重置 1h）；且 cytotrace2 层当年"codeload 直连不稳故走 api.github.com"的选型在这点上同样吃配额）；修法 = **CI 传 secrets.GITHUB_TOKEN 作 GITHUB_PAT build-arg**（认证请求 5000/h 且按 token 计不按 IP 计，remotes 自动读取；ARG 仅构建期存在不进镜像配置，ephemeral token 无泄漏面；本地空值走代理不受影响）+ CellChat 层补 3-try 对齐 monocle3 纪律 + monocle3 层同喂 GITHUB_PAT；R 代码 parse+真实调用双预检过（教训 14 纪律）；v4 重建验证中时**构建日志 TAIL 又炸出同族第三雷**——nichenetr/cytotrace2 两层裸 `api.github.com/.../tarball` 下载同吃按 IP 匿名配额（CI 必挂）→ 补 edits 5/6（PAT 条件头 `headers=hd`+size>1000 校验，cytotrace2 顺手 3-try 对齐纪律）+ 从 Dockerfile 抽源 parse 预检双过；缓存策略省时：edit 5 层序在 monocle3 之后 → 杀 v4 起 v5 仅 21 层起重跑（~15min）全绿 sha **92c5f02b**，雷区四包 CellChat 2.2.0.9001/monocle3 1.4.27/nichenetr 2.2.1.1/CytoTRACE2 1.1.0 + 三新工具 CLI 冒烟全 ok；ci.yml 喂 `--build-arg GH_PAT=${{ secrets.GITHUB_TOKEN }}`；commit 74fb06a push 质量门全过（ruff/mypy/pytest 1354）→ **轮 3（35460402127）四 job 全绿**：bio 冷构建 89min 过（GH_PAT 修复 CI 生效，四雷区层无一复挂）+三新冒烟步（sc_genescore/sc_metabolism/sc_cellfreq）CI step 级 success——Phase 72/73/74 CI 验收正式收口 |

## 真机对照口径（p69-5b：BBKNN vs Harmony 头对头）

- 三指标：**mix_graph**（connectivities 跨批边占比——引擎无关口径，bbknn 改图、harmony 改 PCA 都落在最终邻居图上）+ **sil_batch / sil_leiden**（各引擎自家整合空间：harmony=X_pca_harmony、bbknn=X_pca；>2000 细胞抽样 2000，seed=7）。
- sc 侧结论：bbknn 混批更激进（0.93 vs 0.91）但结构牺牲更大（sil_leiden -0.02 vs +0.08，簇数 8 vs 16）——**批间差异强时 harmony 保留更多生物学结构**。
- st 侧结论：bbknn 混片更强（0.55 vs 0.39）；表达域结构两者都弱（x 中位切分的"批次"是合成口径，域结构差异由切片本身主导）。
- 经验法则沉淀进工具描述：默认 bbknn（快、稳、存量兼容）；批间差异大/需保留簇结构时传 method=harmony；st_integrate 默认 harmony（多切片通常批差更大）。

## 关键修复与教训沉淀

1. **mypy var-annotated 一击即中**：`x = np.asarray(...)` 中间变量在 strict 模式需显式注解——inline 进 `run_harmony(np.asarray(...), ...)` 调用零运行时差异修复；integrate.py 同款预防性统一（本机过≠CI 过，平台盲区纪律）。教训：strict 模式下中间变量倾向 inline 或显式注解。
2. **真机对照 counts 回源坑**：integration 产物（59900_bbknn）的 X 与 raw 均为 scaled 形态（frac_int=0.85，raw min=0.07）——对照实验的 counts 必须回原始 10x 源（bio_test_data/combined_10x 93665×32285，与 59900 条码 100% 对齐，frac_int=1.0）。探针断言"raw 快照非 counts"先败后查，先验数据形态再建库。
3. **harmonypy 2.x 直调口径**：清华源无 0.3/0.4 旧线、2.x 为重写版（Z_corr=(cells, pcs) 不转置）——代码按 Z_corr 形状自适应 transpose，**不依赖 scanpy.external.pp_harmony_integrate 包装**（其内部有旧版形状假设）；层钉 `>=2` 下限（回滚口径 = 放宽下限或删层重建）。
4. **st_integrate 设计红线：不做空间邻域图**——跨切片坐标无物理意义，合并后只做表达域（UMAP+leiden）聚类；spatial_offset=true 各片 x 平移并排**仅展示用**，offset 产物喂空间 kNN 工具（st_stats/st_nichenet 等）会误判邻居——note 字段显式告警。
5. **new_id 防撞**：`{ref1}__{ref2}[_etc]__{method}` 双下划线分隔 + `\W` 清洗 + 80 长度帽——与 sc 侧 `{id}_{method}` 单下划线风格区分，多 ref 不与单 ref 撞名；>2 片缩写 _etc。
6. **跨镜像分发复用第三例**：st_integrate.py 物理在 sc_tools/、l3_spatial 注册 image=bio——st 镜像零增重（st_cellchat_v2/st_nichenet 先例延续），bio 镜像一层 harmonypy 双工具共享。
7. **CI 镜像 job 冷缓存裸连外网源的抖动脆弱性**：GH runner 无 docker 层缓存，bio 全量构建每次重下 zenodo 先验/GitHub tarball——Phase 64/66/68 三次过属运气，本次首撞 504 整层报废且 ~30min 才暴露。修法 = 下载点统一 `dl()` 重试帮手（5 次×20s 退避 + `file.size>0` 拦半截文件）；R 语法先在本地镜像内小文件实测、BuildKit `--check` 过了才上 CI——**层内下载必须自带重试，构建期网络调用不能赌单次成功**。
8. **mypy 跨模块 Any 退化（no-any-return）**：`upper_gene_map(...) -> list[str]` 签名在跨模块解析不可见时退化 Any，返回处报 no-any-return——`[str(g) for g in ...]` 显式收口即修；与 Phase 69 var-annotated 同族（strict 模式对类型边界敏感），教训：**返回值经过跨模块函数时倾向显式容器推导收口**。
9. **Enrichr json 通路库自带重复基因**：KEGG_2021_Human 实证同通路内有重复符号——decoupler net 按 (source,target) 唯一性校验，重复行直接拒收；修法 = `dict.fromkeys` 保序去重（顺序稳定可复现），**外部通路库进门先去重应成为固定工序**。
10. **igraph 零权重边**：共现网络用 weight 编码 rho，但 rho=0 的合法连边（恰过阈值的边界情形）与"无边"语义在 igraph 布局/统计中混叠——零权重边统一钳到极小正值（或从边表剔除改记 note），**图算法里 weight=0 ≠ 没有边**。
11. **共享文件多 phase 分拆提交法**：三个 phase 改同一组文件时，按归属逐 phase `revert hunks→commit→restore hunks`，每阶段用 `git diff | Select-String "^@@"` 核对 hunk 头数量与内容、终态与原工作树 byte-equal + 全量单测回归；动手前先打备份 patch 快照、收尾即删——**分拆的正确性锚点是"终态不变"，不是"中间态好看"**。
12. **push 失败输出混淆**：TLS 抖动（curl 35 unexpected eof）失败时 git 可能混出"Everything up-to-date"字样——判成败唯一可信锚点是 `git fetch` 后看 `git branch -vv` 的 ahead 数与 `git log origin/master`，**不轻信 push 的 stdout 尾行**；预推送钩子（质量门）PASS 不代表网络传输成功。
13. **R install.packages 并行下载抖动（教训 7 同族新变体）**：Ncpus 并行拉 CRAN 时镜像站连接饱和可整波瞬断（CI 实证 44 包同秒 ~4.6s 齐挂——statmod/futile.logger/snow 失败→上游 limma/BiocParallel 链式缺依赖崩）；dl() 重试只覆盖 shell 下载点，**包管理器内部下载护不到**——修法 = R 级"缺啥补啥"重试封装（每波 requireNamespace 探缺→install.packages 补缺→sleep 退避，N 波兜底 stop），批量下载与单点下载同样不赌单次成功。
14. **Rscript -e 内联代码预检两道坎**：parse 预检只保语法不保调用口径——ir() 首跑秒败于 `ir("sf") argument "n" is missing`（定义 function(p,n) 调用 ir('sf')，语法层面全对、运行时才炸）；修法 = 参数给默认值（function(p,n=6)）+ 用必装包（utils）走一遍真实调用预检——**内联脚本的验证要"定义+调用"成对跑通，parse 完不等于能跑**。
15. **GitHub API 匿名配额是按出口 IP 计的共享雷（CI 专属）**：remotes::install_github / api.github.com tarball 端点全走 api.github.com 匿名配额 60/h——CI runner 的 Azure 共享出口 IP 配额被同 IP 其它租户耗光时**环境性秒挂（403 remaining 0/60），重试无用（重置 1h）**，且表现为"上一轮能过这一轮挂"的间歇性误导；修法 = CI 天然有 secrets.GITHUB_TOKEN，`--build-arg GH_PAT=${{ secrets.GITHUB_TOKEN }}` 喂给 GITHUB_PAT 后 remotes 自动认证（5000/h 按 token 计不按 IP 计）；ARG 仅构建期存在不进镜像配置，ephemeral token 无泄漏面，本地空值走代理不受影响——**凡构建期碰 GitHub API 的层都该喂 PAT + 自带重试，双保险缺一不可**。

## 后续建议

- **Phase 72/73/74 CI 验收 ✅ 已收口（2026-09-19，dispatch run 35460402127 四 job 全绿）**：三轮各拆一雷——轮 1 monocle3 TUNA 抖动（ir() 重试）、轮 2 CellChat GitHub API 按 IP 匿名配额 403（GH_PAT build-arg）、轮 2.5 TAIL 主动扫出 cytotrace2/nichenetr 同族雷（PAT 条件头）；三新冒烟步 CI step 级 success，全程复盘见 #19 + 教训 13/14/15。
- **sc_genescore 真机扩展挂账**：oscc（1749 细胞）14/14 通路 top=JAK-STAT 已绿；599sub 千细胞子集与 TGFb×CAF 交叉验证（与 sc_cytosig TGFB3/TGFB1 联读）待 cytosig 联合分析 Phase 立项时一并做——两工具共享 PROGENy/CytoSig 信号口径，联读是天然的验收对照。
- **sc_metabolism st 侧扩展候选**：当前 metabolism/genescore 均为 sc 工具；Visium spot 级代谢/通路活性（st_genescore）是自然延伸，反卷积权重（RCTD）加权打分口径待设计。
- **组间比较口线下一步**：sc_cellfreq Ro/e 与 Fisher 并列输出已就位，跨条件（NI vs 对照）组成差异的比较框架（compositional analysis，如 scCODA/propeller）可在后续 Phase 评估——当前 min_rho=0.6 共现网络在 n=10 样本 BH 校正下偏保守（空网络合法），样本量上来后建议复跑 pt1k 真机回归看边数变化。
- **zenodo outage 已恢复（挂账兑现 2026-09-19）**：守望进程检出 zenodo 200 → 本地全量重建 rc=0 → cpu-latest 正式镜像 sc_cytosig 冒烟 6/6 复验绿（不再依赖 cpu-cytosig 补丁镜像，real_cytosig_599sub.py IMG 已切回）；push run **35431086802**（e42d089）test/docker-smoke/st-image-smoke 三 job success——bio-image-smoke 是 workflow_dispatch-only job 在 push run 自动 skip（设计如此非故障）→ 手动 dispatch run **35435285914** 补跑 bio 全量构建+sc_tcr/sc_cytosig 两冒烟步（API 插曲钉档：rerun 35426142997 撞 403、dispatch 用 ref=main 撞 422——**本 repo 主分支是 master**，改 ref=master 才 204；多余一轮 35435143136 已 202 取消）。**结论已补记：run 35435285914 completed/success 四 job 全绿，bio-image-smoke 内 sc_tcr 与 sc_cytosig 两冒烟步 step 级 success——sc_tcr CI 验收（35426142997 遗留）与 Phase 71 CI 验收同 run 双兑现**。
- **sc_tcr 真机挂账**：VDJ 数据（Cell Ranger filtered_contig_annotations.csv）到位后补真机验收——重点看真实 contig 的列形态（字符串 bool/额外列）、跨组织 clonotype 复用率、写回对齐率实测；paper 口径可对照作者报告 6,503 clonotype 量级。
- **sc_cytosig 真机验证已兑现（2026-09-19，见验证一览 #14）**：599sub_bbknn 千细胞子集 diff 口径——CAF 簇 TGFB3/TGFB1 rank 1/2、T 细胞簇 TGFB3 rank 1、纯上皮簇 TGFB3 垫底、beta/zscore 双口径 16/16 排名一致；签名交集 4599/4881 证双侧 upper 对齐链路；对照 CytoSig 网站排名口径解读 beta 的用法已验证（nrand=1000 千级细胞约 2 分钟，无需降档）。
- 双冒烟已进 ci.yml bio-image-smoke（st_niche_scan 步后追加）；CI 侧已验收（2026-09-19 manual run 35413460482 四 job 全绿，sc_integrate/st_integrate 冒烟步 step 级 success）。插曲：首轮 35407998472 bio 构建撞 zenodo 504（runner 冷缓存裸连外网，单次抖动整层报废）→ bio.Dockerfile 两下载点加 dl() 五次重试+20s 退避（7b97aeb）后复跑绿。
- harmonypy 已钉 `>=2` 下限（2026-09-19 用户口径"v2 以上"，实测实装 2.0.2 cp312 wheel；代码 Z_corr 形状校验保留为防御兜底）；若未来 3.x breaking（Z_corr 形状再变），冒烟第①场景会立刻拦住，届时再评估加上限。
- st 侧真机对照目前是 oscc 双片 x 中位切分（合成"批次"）；真实多批次空间数据（不同供体/平台）到位后建议复跑 _eval/p69b_metrics.py 三指标对照。
- sil_leiden 在 st 侧双双近 0：切片数少、全局 HVG 口径下表达域聚类的意义待真实多片数据再评；必要时考虑 per-slice HVG 再合并。
- oscc 双片产物（osccL__osccR__{harmony,bbknn}）已留 bio_workspace 作 st_integrate 真机基准，后续 st 整合改动可一键回归。
