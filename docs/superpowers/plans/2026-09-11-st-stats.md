# st_stats 空间统计分析实施计划（Phase 45）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 st_* 工具面新增第 9 个工具 st_stats，单工具承载空间统计三分析（autocorr/cooccurrence/nhood_enrichment）。

**Architecture:** 容器内脚本 `sandbox/st_tools/stats.py`（squidpy 1.8.3 `gr.*`，邻域图优先复用 st_process 已建 `spatial_connectivities`，processed.h5ad 只读）+ orchestrator 注册 `st_stats`（`l3_spatial.py`，timeout 1800s）+ `SECTION_TITLES` 补映射。产物走 `pngs` 聚合键 + `.csv` 键，IM/报告链路零改动。

**Tech Stack:** squidpy 1.8.3 / scanpy / pandas（st 镜像现成，无新依赖，无需改 Dockerfile/锁文件）；pytest TDD。

**Spec:** `docs/superpowers/specs/2026-09-11-st-stats-design.md`（已批准提交 a90ee6b）

**模式锚点：**
- 容器脚本模板：[domains.py](file:///i:/飞书agent/sandbox/st_tools/domains.py)（read_args/fail+SystemExit/load_adata/emit/run 全套）
- common.py 契约（已逐行核对）：`read_args()` 无参、`emit(dict)` 含 ok、
  `fail(code,msg)` 不抛须跟 `raise SystemExit(1)`、
  `load_adata({"dataset_id","file":"processed"})`、`WS_ROOT`、
  `ensure_spatial`；无 emit_error/read_processed/save_fig
- 容器探针已核实（squidpy 1.8.3）：`gr.spatial_autocorr(mode, genes,
  show_progress_bar)` ✓；`gr.co_occurrence(cluster_key, interval)` uns 键
  为 `occ`/`dist`；`gr.nhood_enrichment(cluster_key, n_perms,
  show_progress_bar)` ✓；`pl.spatial_scatter` 需 `return_ax=True`（T7 教训）
- handler/注册模板：[l3_spatial.py st_domains](file:///i:/飞书agent/orchestrator/tools/builtin/l3_spatial.py#L108-L134) + [注册块](file:///i:/飞书agent/orchestrator/tools/builtin/l3_spatial.py#L279-L299)
- 注册测试模板：[test_l3_spatial.py](file:///i:/飞书agent/tests/unit/test_l3_spatial.py)
- `parse_gene_list` 已从 l3_singlecell 导入（l3_spatial L14，st_plot 在用）

**门禁纪律（每次跑 pytest 必带）：**
`--basetemp=I:\飞书agent\.pytest_tmp`；输出落 basetemp 外文件防锁冲突；
同文件多处修改必须串行 SearchReplace（禁并行互覆盖）。

---

### Task 1: st_stats 注册层红灯测试

**Files:**
- Test: `tests/unit/test_l3_st_stats.py`（新建）

- [ ] **Step 1: 写注册层红灯测试**（runner mock 断言 run("stats", ...) 参数透传）

```python
"""st_stats 注册测试（mock BioRunner，不发 docker）。"""
from unittest.mock import MagicMock

import pytest

from orchestrator.tools.bio.bio_runner import BioRunError, BioRunner
from orchestrator.tools.builtin.l3_spatial import register_l3_spatial
from orchestrator.tools.tool_registry import ToolRegistry

ST_IMAGE = "feishu-research-agent/bio:st-cpu-latest"


@pytest.fixture
def runner():
    r = MagicMock(spec=BioRunner)
    r.run.return_value = {"ok": True, "dataset_ref": "abc123",
                          "analysis": "autocorr", "n_genes_tested": 50,
                          "top_gene": "G1", "top_stat": 0.42}
    return r


@pytest.fixture
def reg(runner):
    registry = ToolRegistry()
    register_l3_spatial(registry, runner)
    return registry


def test_st_stats_registered(reg):
    """st_* 第 9 工具注册为 L1_compute，timeout 1800。"""
    spec = reg.get("st_stats")
    assert spec is not None
    assert spec.risk_level == "L1_compute"
    assert spec.timeout_sec == 1800
    props = spec.parameters["properties"]
    assert props["analysis"]["enum"] == [
        "autocorr", "cooccurrence", "nhood_enrichment"]
    assert props["mode"]["enum"] == ["moran", "geary"]


def test_st_stats_forwards_params(runner, reg):
    """全参数透传 + st 镜像 + /opt/st_tools + timeout 1800。"""
    reg.get("st_stats").handler(
        dataset_ref="abc123", analysis="nhood_enrichment",
        cluster_key="spatial_domain", n_perms=500,
        coord_type="generic", n_neighs=8)
    args, kw = runner.run.call_args
    assert args[0] == "stats"
    assert args[1] == {"dataset_id": "abc123",
                       "analysis": "nhood_enrichment",
                       "cluster_key": "spatial_domain",
                       "n_perms": 500,
                       "coord_type": "generic", "n_neighs": 8}
    assert kw["image"] == ST_IMAGE
    assert kw["script_dir"] == "/opt/st_tools"
    assert kw["timeout_sec"] == 1800


def test_st_stats_defaults(runner, reg):
    """默认值：mode moran / n_perms 1000 / grid / 6 / genes [] / key ''。"""
    reg.get("st_stats").handler(dataset_ref="abc123", analysis="autocorr")
    args, _ = runner.run.call_args
    assert args[1]["mode"] == "moran"
    assert args[1]["n_perms"] == 1000
    assert args[1]["coord_type"] == "grid"
    assert args[1]["n_neighs"] == 6
    assert args[1]["genes"] == []
    assert args[1]["cluster_key"] == ""


def test_st_stats_genes_string_parsed(runner, reg):
    """planner 把 genes 序列化成 repr 串时宽松解析（st_plot 同款纪律）。"""
    reg.get("st_stats").handler(dataset_ref="abc123", analysis="autocorr",
                                genes="['G1', 'G2']")
    args, _ = runner.run.call_args
    assert args[1]["genes"] == ["G1", "G2"]


def test_st_stats_error_passthrough(runner, reg):
    """BioRunError → 统一错误输出。"""
    runner.run.side_effect = BioRunError(
        "ST_CLUSTER_KEY_MISSING", "no cluster col")
    out = reg.get("st_stats").handler(dataset_ref="abc123",
                                      analysis="cooccurrence")
    assert out == {"error_code": "ST_CLUSTER_KEY_MISSING",
                   "error_message": "no cluster col"}
```

- [ ] **Step 2: 跑测试确认红**

Run: `.venv\Scripts\python.exe -m pytest tests/unit/test_l3_st_stats.py -q --basetemp=I:\飞书agent\.pytest_tmp`
Expected: FAIL — `reg.get("st_stats")` 返回 None（工具未注册）

- [ ] **Step 3: Commit（红灯记录）**

```bash
git add tests/unit/test_l3_st_stats.py
git commit -m "test(l3): st_stats 注册红灯测试（Phase 45 TDD）"
```

---

### Task 2: l3_spatial 注册 st_stats（绿灯）

**Files:**
- Modify: `orchestrator/tools/builtin/l3_spatial.py`（st_deconvolve handler 之后加 handler；文件末尾注册块之后加注册）

- [ ] **Step 1: 加 handler**（在 `st_deconvolve` handler 结束后、第一个 `registry.register` 之前）

```python
    def st_stats(*, dataset_ref: str, analysis: str,
                 mode: str = "moran",
                 genes: list[str] | None = None,
                 cluster_key: str = "", n_perms: int = 1000,
                 coord_type: str = "grid", n_neighs: int = 6) -> dict[str, Any]:
        """空间统计三分析（Moran/Geary 自相关、共现、邻域富集）。"""
        try:
            out = runner.run(
                "stats", {
                    "dataset_id": dataset_ref, "analysis": analysis,
                    "mode": mode, "genes": parse_gene_list(genes),
                    "cluster_key": cluster_key, "n_perms": n_perms,
                    "coord_type": coord_type, "n_neighs": n_neighs,
                }, image=st_image, script_dir=_ST_SCRIPT_DIR,
                timeout_sec=1800)
        except BioRunError as e:
            return _err(e)
        out.pop("ok", None)
        return out
```

- [ ] **Step 2: 加注册块**（st_deconvolve 注册块之后）

```python
    registry.register(ToolSpec(
        name="st_stats",
        description=(
            "空间统计分析三合一：autocorr（Moran's I/Geary's C 空间自相关，"
            "识别空间可变基因，输出逐基因统计表与 top4 空间分布图）、"
            "cooccurrence（簇间空间共现曲线）、nhood_enrichment（簇间邻域"
            "富集 zscore 热图）。cluster_key 留空自动回退 spatial_domain→"
            "banksy_domain→leiden→clusters。需先跑 st_process；"
            "cooccurrence/nhood_enrichment 建议先跑 st_domains。"
        ),
        parameters={
            "type": "object",
            "properties": {
                "dataset_ref": {"type": "string"},
                "analysis": {"type": "string",
                             "enum": ["autocorr", "cooccurrence",
                                      "nhood_enrichment"]},
                "mode": {"type": "string", "enum": ["moran", "geary"],
                         "default": "moran",
                         "description": "仅 autocorr"},
                "genes": {"type": "array", "items": {"type": "string"},
                          "description": "仅 autocorr：显式基因列表"
                                         "（空=高变基因前 50）"},
                "cluster_key": {"type": "string", "default": "",
                                "description": "仅后两个分析：obs 列名"
                                               "（留空自动回退）"},
                "n_perms": {"type": "integer", "default": 1000,
                            "description": "仅 nhood_enrichment：排列次数"},
                "coord_type": {"type": "string", "enum": ["grid", "generic"],
                               "default": "grid",
                               "description": "邻域图类型（仅 processed 无"
                                              "既有邻域图时兜底生效）"},
                "n_neighs": {"type": "integer", "default": 6},
            },
            "required": ["dataset_ref", "analysis"],
        },
        risk_level="L1_compute",
        handler=st_stats,
        timeout_sec=1800,
    ))
```

- [ ] **Step 3: 跑注册测试确认绿**

Run: `.venv\Scripts\python.exe -m pytest tests/unit/test_l3_st_stats.py -q --basetemp=I:\飞书agent\.pytest_tmp`
Expected: 5 passed

- [ ] **Step 4: SECTION_TITLES 补映射**

`orchestrator/report/section_digest.py` SECTION_TITLES 增加一行（st_deconvolve 行之后）：

```python
    "st_stats": "空间统计分析",
```

Run: `.venv\Scripts\python.exe -m pytest tests/unit/test_report_digest.py -q --basetemp=I:\飞书agent\.pytest_tmp`
Expected: 全绿（7f0cf2e 覆盖测试自动验证 st_stats 已配）

- [ ] **Step 5: 全量回归 + 门禁**

Run: `.venv\Scripts\python.exe -m pytest tests/unit -q --basetemp=I:\飞书agent\.pytest_tmp`
Expected: 全绿（1246+5）
Run: `.venv\Scripts\ruff.exe check orchestrator/tools/builtin/l3_spatial.py orchestrator/report/section_digest.py tests/unit/test_l3_st_stats.py`
Run: `.venv\Scripts\mypy.exe orchestrator/tools/builtin/l3_spatial.py`
Expected: 干净

- [ ] **Step 6: Commit**

```bash
git add orchestrator/tools/builtin/l3_spatial.py orchestrator/report/section_digest.py
git commit -m "feat(l3): 注册 st_stats 空间统计三分析工具（Phase 45）

- handler + ToolSpec（analysis 必填三选一，timeout 1800 排列检验余量）
- SECTION_TITLES 补 st_stats=空间统计分析
- genes 复用 parse_gene_list 宽松解析（st_plot 同款）"
```

---

### Task 3: 容器脚本 stats.py 实现

**Files:**
- Create: `sandbox/st_tools/stats.py`

- [ ] **Step 1: 写脚本**（API 均经容器探针/common.py 源码核实，无猜测）

```python
"""st_stats 空间统计三分析（Phase 45）：autocorr / cooccurrence /
nhood_enrichment。邻域图优先复用 st_process 已建 spatial_connectivities，
缺失时按 coord_type/n_neighs 兜底补建（processed.h5ad 只读不写回）。
产物：csv 落 /ws/{ds}/stats_{analysis}/，图走 pngs 聚合键（宿主收图口径）。

squidpy 1.8.3 容器探针已核实：
- gr.spatial_autocorr(mode, genes, show_progress_bar)
- gr.co_occurrence(cluster_key, interval) → uns[f"{k}_co_occurrence"]
  键为 occ/dist
- gr.nhood_enrichment(cluster_key, n_perms, show_progress_bar)
- pl.spatial_scatter 需 return_ax=True（st_domains T7 教训）
"""
from __future__ import annotations

from common import emit, fail, run

ANALYSES = ("autocorr", "cooccurrence", "nhood_enrichment")
CLUSTER_FALLBACK = ("spatial_domain", "banksy_domain", "leiden", "clusters")
AUTOCORR_DEFAULT_N_GENES = 50


def _build_neighbors(adata, coord_type: str, n_neighs: int) -> None:
    """复用 st_process 已建邻域图；缺失才按参数补建（不写回）。"""
    if "spatial_connectivities" in adata.obsp:
        return
    import squidpy as sq
    if coord_type == "generic":
        sq.gr.spatial_neighbors(adata, coord_type="generic", delaunay=True)
    else:
        sq.gr.spatial_neighbors(adata, coord_type="grid", n_neighs=n_neighs)


def _resolve_cluster_key(adata, cluster_key: str) -> str:
    """显式列名优先；留空按回退链探测；全灭报错列可用 obs 列。"""
    if cluster_key:
        if cluster_key not in adata.obs.columns:
            fail("ST_CLUSTER_KEY_MISSING",
                 f"obs 无列 '{cluster_key}'；可用: "
                 f"{list(adata.obs.columns)}")
            raise SystemExit(1)
        return cluster_key
    for cand in CLUSTER_FALLBACK:
        if cand in adata.obs.columns:
            return cand
    fail("ST_CLUSTER_KEY_MISSING",
         f"无可用簇列（回退链 {CLUSTER_FALLBACK} 全缺失）；"
         f"可用 obs 列: {list(adata.obs.columns)}")
    raise SystemExit(1)


def _do_autocorr(adata, out_dir, mode: str, genes: list[str]) -> None:
    """Moran's I / Geary's C：逐基因统计 csv + top4 空间分布 png。"""
    import matplotlib.pyplot as plt
    import squidpy as sq

    if not genes:
        if "highly_variable" in adata.var.columns:
            genes = list(adata.var_names[adata.var["highly_variable"]])[
                :AUTOCORR_DEFAULT_N_GENES]
        else:
            genes = list(adata.var_names)[:AUTOCORR_DEFAULT_N_GENES]
    else:
        missing = [g for g in genes if g not in adata.var_names]
        if missing:
            fail("INVALID_INPUT",
                 f"基因不在 var_names: {missing[:5]}（共 {len(missing)} 个）")
            raise SystemExit(1)
    sq.gr.spatial_autocorr(adata, mode=mode, genes=genes,
                           show_progress_bar=False)
    df = adata.uns["moranI" if mode == "moran" else "gearyC"]
    df = df.reset_index().rename(columns={"index": "gene"})
    stat_col = "I" if mode == "moran" else "C"
    df = df.sort_values(stat_col, ascending=(mode != "moran"))
    df.to_csv(out_dir / f"autocorr_{mode}.csv", index=False)
    top = df.iloc[0]
    top_genes = list(df["gene"].head(4))
    for g in top_genes:
        stat = float(df.loc[df["gene"] == g, stat_col].iloc[0])
        ax = sq.pl.spatial_scatter(adata, color=[g], return_ax=True)
        ax.set_title(f"{g} ({stat_col}={stat:.3f})")
        ax.figure.savefig(out_dir / f"autocorr_{g}.png", dpi=150,
                          bbox_inches="tight")
        plt.close("all")
    emit({"ok": True, "analysis": "autocorr", "mode": mode,
          "n_genes_tested": len(genes),
          "top_gene": str(top["gene"]), "top_stat": float(top[stat_col]),
          "stats_csv": f"{out_dir}/autocorr_{mode}.csv",
          "pngs": [f"{out_dir}/autocorr_{g}.png" for g in top_genes]})


def _do_cooccurrence(adata, out_dir, cluster_key: str) -> None:
    """簇间空间共现：长表 csv + 共现曲线 png。"""
    import matplotlib.pyplot as plt
    import pandas as pd
    import squidpy as sq

    sq.gr.co_occurrence(adata, cluster_key=cluster_key)
    occ = adata.uns[f"{cluster_key}_co_occurrence"]
    prob, dist = occ["occ"], occ["dist"]
    clusters = list(adata.obs[cluster_key].cat.categories)
    rows = [{"cluster": c, "neighbor_cluster": clusters[j],
             "dist": float(d), "prob": float(prob[i, d_idx, j])}
            for i, c in enumerate(clusters)
            for d_idx, d in enumerate(dist)
            for j in range(len(clusters))]
    pd.DataFrame(rows).to_csv(out_dir / "cooccurrence.csv", index=False)
    sq.pl.co_occurrence(adata, cluster_key=cluster_key,
                        clusters=clusters[: min(6, len(clusters))],
                        figsize=(8, 6), show=False)
    plt.gcf().savefig(out_dir / "cooccurrence.png", dpi=150,
                      bbox_inches="tight")
    plt.close("all")
    emit({"ok": True, "analysis": "cooccurrence",
          "cluster_key": cluster_key, "n_clusters": len(clusters),
          "cooccurrence_csv": f"{out_dir}/cooccurrence.csv",
          "pngs": [f"{out_dir}/cooccurrence.png"]})


def _do_nhood(adata, out_dir, cluster_key: str, n_perms: int) -> None:
    """邻域富集：zscore/count 双矩阵 csv + 热图 png。"""
    import matplotlib.pyplot as plt
    import numpy as np
    import pandas as pd
    import squidpy as sq

    sq.gr.nhood_enrichment(adata, cluster_key=cluster_key, n_perms=n_perms,
                           show_progress_bar=False)
    enr = adata.uns[f"{cluster_key}_nhood_enrichment"]
    z = np.asarray(enr["zscore"], dtype=float)
    cnt = np.asarray(enr["count"], dtype=float)
    cats = list(adata.obs[cluster_key].cat.categories)
    pd.DataFrame(z, index=cats, columns=cats).to_csv(
        out_dir / "nhood_zscore.csv")
    pd.DataFrame(cnt, index=cats, columns=cats).to_csv(
        out_dir / "nhood_count.csv")
    mask = ~np.eye(len(cats), dtype=bool)
    i, j = np.unravel_index(np.nanargmax(np.where(mask, z, np.nan)), z.shape)
    sq.pl.nhood_enrichment(adata, cluster_key=cluster_key, method="ward",
                           figsize=(8, 6), show=False)
    plt.gcf().savefig(out_dir / "nhood_enrichment.png", dpi=150,
                      bbox_inches="tight")
    plt.close("all")
    emit({"ok": True, "analysis": "nhood_enrichment",
          "cluster_key": cluster_key, "n_perms": n_perms,
          "top_pair": f"{cats[i]}~{cats[j]}",
          "top_zscore": float(z[i, j]),
          "zscore_csv": f"{out_dir}/nhood_zscore.csv",
          "count_csv": f"{out_dir}/nhood_count.csv",
          "pngs": [f"{out_dir}/nhood_enrichment.png"]})


def main() -> None:
    """st_stats 容器入口：三分析分发。"""
    from common import WS_ROOT, ensure_spatial, load_adata, read_args

    args = read_args()
    analysis = str(args.get("analysis", ""))
    if analysis not in ANALYSES:
        fail("INVALID_INPUT",
             f"analysis 须为 {ANALYSES} 之一，收到 '{analysis}'")
        raise SystemExit(1)
    mode = str(args.get("mode", "moran"))
    if mode not in ("moran", "geary"):
        fail("INVALID_INPUT", f"mode 须为 moran/geary，收到 '{mode}'")
        raise SystemExit(1)
    genes = args.get("genes") or []
    if isinstance(genes, str):
        genes = [genes]
    n_perms = int(args.get("n_perms", 1000))
    coord_type = str(args.get("coord_type", "grid"))
    if coord_type not in ("grid", "generic"):
        fail("INVALID_INPUT",
             f"coord_type 须为 grid/generic，收到 '{coord_type}'")
        raise SystemExit(1)
    n_neighs = int(args.get("n_neighs", 6))

    adata = load_adata({"dataset_id": args["dataset_id"],
                        "file": "processed"})
    ensure_spatial(adata)
    _build_neighbors(adata, coord_type, n_neighs)

    out_dir = WS_ROOT / args["dataset_id"] / f"stats_{analysis}"
    out_dir.mkdir(parents=True, exist_ok=True)
    if analysis == "autocorr":
        _do_autocorr(adata, out_dir, mode, list(genes))
    elif analysis == "cooccurrence":
        _do_cooccurrence(adata, out_dir, _resolve_cluster_key(
            adata, str(args.get("cluster_key", ""))))
    else:
        _do_nhood(adata, out_dir, _resolve_cluster_key(
            adata, str(args.get("cluster_key", ""))), n_perms)


if __name__ == "__main__":
    run(main)
```

- [ ] **Step 2: ruff + 语法检查**

Run: `.venv\Scripts\ruff.exe check sandbox/st_tools/stats.py`
Expected: 干净

- [ ] **Step 3: Commit**

```bash
git add sandbox/st_tools/stats.py
git commit -m "feat(st): stats.py 空间统计三分析容器脚本（Phase 45）"
```

---

### Task 4: 镜像重建 + 容器冒烟（合成数据三分析真跑）

**Files:**
- Create: `scripts/_smoke_st_stats.py`（验证脚本，留存备复跑）

- [ ] **Step 1: 重建 st 镜像**（stats.py 走 COPY sandbox/st_tools 层，秒级）

Run: `docker build -f sandbox/st.Dockerfile -t feishu-research-agent/bio:st-cpu-latest sandbox`
Expected: 构建成功

- [ ] **Step 2: 写冒烟脚本**（合成 10×10 网格：左半簇 A 右半簇 B；G_grad 左高右低为注入空间模式，G_rand 随机）

```python
"""st_stats 容器冒烟：合成网格数据三分析真跑（断网）。"""
import json
import subprocess
from pathlib import Path

import numpy as np
import pandas as pd
import scanpy as sc
from scipy.sparse import csr_matrix

WS = Path("I:/飞书agent/bio_workspace")
DS = "ststatssmoke"
rng = np.random.default_rng(42)

xs, ys = np.meshgrid(np.arange(10), np.arange(10))
coords = np.column_stack([xs.ravel(), ys.ravel()]).astype(float)
n = coords.shape[0]
left = (coords[:, 0] < 5).astype(np.float32)
X = rng.poisson(2, (n, 6)).astype(np.float32)
X[:, 0] += left * 8  # G_grad 空间梯度
adata = sc.AnnData(csr_matrix(X))
adata.var_names = ["G_grad", "G_rand", "G3", "G4", "G5", "G6"]
adata.obs["spatial_domain"] = pd.Categorical(
    np.where(left > 0, "A", "B"))
adata.obsm["spatial"] = coords
sc.pp.normalize_total(adata)
sc.pp.log1p(adata)
sc.pp.highly_variable_genes(adata, n_top_genes=6)

ds_dir = WS / DS
(ds_dir / "stats_autocorr").mkdir(parents=True, exist_ok=True)
adata.write_h5ad(ds_dir / "processed.h5ad")

IMG = "feishu-research-agent/bio:st-cpu-latest"
BASE = ["docker", "run", "--rm", "--network", "none",
        "-v", f"{str(WS).replace(chr(92), '/')}:/ws",
        IMG]


def run_stats(**kw):
    payload = json.dumps({"dataset_id": DS, **kw})
    r = subprocess.run(
        BASE + ["python", "/opt/st_tools/stats.py"],
        input=payload, capture_output=True, text=True, timeout=1200)
    if r.returncode != 0:
        raise SystemExit(f"{kw} rc={r.returncode}\n"
                         f"{r.stdout[-400:]}\n{r.stderr[-600:]}")
    return json.loads(r.stdout.strip().splitlines()[-1])


o1 = run_stats(analysis="autocorr", mode="moran")
assert o1["ok"] and o1["top_gene"] == "G_grad", o1
o2 = run_stats(analysis="cooccurrence")
assert o2["ok"] and o2["n_clusters"] == 2, o2
o3 = run_stats(analysis="nhood_enrichment", n_perms=99)
assert o3["ok"] and o3["top_zscore"] > 0, o3
for f in ("stats_autocorr/autocorr_moran.csv", "stats_autocorr/autocorr_G_grad.png",
          "stats_cooccurrence/cooccurrence.csv", "stats_cooccurrence/cooccurrence.png",
          "stats_nhood_enrichment/nhood_zscore.csv",
          "stats_nhood_enrichment/nhood_enrichment.png"):
    assert (ds_dir / f).exists(), f
print("SMOKE OK", o1["top_gene"], round(o1["top_stat"], 3),
      o2["n_clusters"], o3["top_pair"], round(o3["top_zscore"], 2))
```

注意：镜像 CMD 约定需先 `docker run ... IMG python -c "import common"` 级别核对 st_tools 在容器内的路径（/opt/st_tools）与 stdin 传参方式——domains.py 模式为 stdin JSON，subprocess input 传 payload 即可；若 BioRunner 实际用 `python /opt/st_tools/stats.py` + stdin，以上直接可用。

Run: `.venv\Scripts\python.exe scripts\_smoke_st_stats.py`
Expected: `SMOKE OK G_grad <I> 2 <pair> <z>`；Moran's I 把注入的 G_grad 排第一

- [ ] **Step 3: 断网断言**

BASE 已带 `--network none`——Step 2 通过即断网自包含验证通过。

- [ ] **Step 4: Commit**

```bash
git add scripts/_smoke_st_stats.py
git commit -m "test(st): st_stats 容器冒烟（合成网格三分析，断网）"
```

---

### Task 5: 全量回归 + 推送 + CI + 文档收尾

**Files:**
- Modify: `docs/ROADMAP.md`（Batch 表格加 Phase 45 行）
- Modify: `测试总结+2026-09-09T01-55-00.md`（追加十一）

- [ ] **Step 1: 全量门禁**

Run: `.venv\Scripts\python.exe -m pytest tests -q --basetemp=I:\飞书agent\.pytest_tmp`
Expected: 全绿
Run: `.venv\Scripts\ruff.exe check sandbox/st_tools orchestrator tests scripts/_smoke_st_stats.py`
Run: `.venv\Scripts\mypy.exe orchestrator/tools/builtin/l3_spatial.py`
Expected: 干净

- [ ] **Step 2: ROADMAP 加行**（st_deconvolve 行之后）

```markdown
| 2026-09-11 | Batch 45 空间统计三分析（st_stats：Moran/Geary 自相关 + 共现 + 邻域富集，单工具三分析） | ✅ 完成 |
```

- [ ] **Step 3: 测试总结追加十一**（实现要点 + 冒烟数字 + 后续建议）

- [ ] **Step 4: 提交推送 + CI 确认**

```bash
git add -A; git commit -m "docs: Phase 45 st_stats ROADMAP/测试总结收尾"; git push
.venv\Scripts\python.exe scripts\_ci_log.py  # 轮询至 success
```

---

## Self-Review 记录

- Spec 覆盖：§2 参数→Task 1/2 注册 + Task 3 main() 解析全覆盖；§3 产物→Task 3 三 _do_* + Task 4 冒烟文件断言；§4 注册/错误码→Task 2 + Task 3 fail 分支；§5 测试→Task 1（注册）+ Task 4（容器冒烟替代 spec"宿主冒烟"——宿主 venv 无 squidpy，与 st_domains 无宿主 smoke 先例一致）；§5 标题映射→Task 2 Step 4。
- 占位符扫描：无 TBD；所有 API 经容器探针/common.py 源码核实（探针输出见会话记录）。
- 类型一致性：emit 键 `pngs`/`stats_csv`/`cooccurrence_csv`/`zscore_csv`/`count_csv`——`pngs` 在宿主四键收图口径内；`*_csv` 键值以 `.csv` 结尾被 collect_sections 自动收表。
- 与设计偏差（已合理化）：邻域图"复用优先、缺则兜底"优于 spec"每次重建"（与 st_process 契约一致，domains.py 同样复用）；autocorr 四张单图替代 1×4 拼接（squidpy spatial_scatter 多子图需 ax 参数行为未探针确认，单图 return_ax=True 是 T7 已验证路径）。
