# sc_cnv（CNV 推断与恶性判定，双后端）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 落地 spec `docs/superpowers/specs/2026-09-10-scnv-design.md`——新增 sc_cnv 工具（25 号 sc_*），双后端（infercnvpy 默认 / cnvturbo HMM i6）从同一 counts 矩阵推断全基因组 CNV，输出恶性判定 + 亚克隆，产物写回 processed.h5ad 并落图/csv。

**Architecture:** 容器内脚本 `sandbox/sc_tools/cnv.py` 走 stdin-JSON/stdout-JSON 协议（common.py）；counts 取 filtered/raw 回退链、标签/UMAP 取 processed.h5ad 按 obs_names 对齐（doublet.py 先例）；统一后处理（per-cell score = 与参考中心基因级偏离均值；infercnvpy 路阈值+CNV 簇多数投票 / cnvturbo 路 HMM 细胞级判定；恶性子集 leiden 亚克隆）；染色体热图自绘（figS3B 式，双后端同款渲染）。基因坐标 TSV 构建期烘进镜像。

**Tech Stack:** infercnvpy 0.6.1、cnvturbo 0.3.0（PyPI 均已确认存在）、Ensembl GRCh38 GTF、scanpy leiden(igraph)、bio:cpu-latest 镜像、pytest（fake runner 单测 + 真容器集成测试）。

**环境注意（Windows PowerShell）：**
- 镜像构建命令：`docker build -t feishu-research-agent/bio:cpu-latest sandbox -f sandbox/bio.Dockerfile`（在仓库根执行）
- pytest 一律带 `--basetemp=$env:TEMP\pytest_basetemp_<名>`（2026-09-10 basetemp 纪律：临时目录只给 pytest）
- mypy 双平台门禁：`& .venv\Scripts\python.exe -m mypy` 与 `& .venv\Scripts\python.exe -m mypy --platform linux`，均须 0 错（sandbox 包在 strict 范围内）
- ruff：`& .venv\Scripts\python.exe -m ruff check .`（line-length 110）

**与 spec 的一处渲染层偏差（已定案）**：spec §3.6 括号里提到用两库各自的 chromosome_heatmap 接口；实施改为**统一自绘**（figS3B 式，cnvturbo skill 实战也推荐自绘）——交付物不变（同款热图），换来双后端渲染一致 + 规避两库绘图 API 差异风险。

---

### Task 1: fetch_gene_pos.py + Dockerfile 层 + 镜像构建 + 双后端 API 探针

**Files:**
- Create: `sandbox/fetch_gene_pos.py`
- Modify: `sandbox/bio.Dockerfile`（Phase 37 R 层之后、useradd 层之前插两层）
- 探针脚本写到 `$env:TEMP\probe_cnv.py`（一次性，不进仓库）

- [ ] **Step 1: 写 sandbox/fetch_gene_pos.py**

```python
"""构建期脚本：Ensembl GRCh38 GTF.gz → 基因坐标 TSV（bio 镜像层内运行）。

下载 Homo_sapiens.GRCh38.114.gtf.gz，抽 feature=="gene" 行的
gene_name / chromosome / start / end（chr1-22/X/Y），按基因符号去重
（同名保留首个，PAR_Y 重复随去重消解），基因组序排序后写
/opt/cnv/gene_pos_grch38.tsv（~2MB，运行期断网可用）。
下载失败非零退出 → docker build 报错重试（fetch_gene_sets.py 同语义）。
"""
from __future__ import annotations

import gzip
import io
import os
import re
import sys
import urllib.request

GTF_URL = ("https://ftp.ensembl.org/pub/release-114/gtf/homo_sapiens/"
           "Homo_sapiens.GRCh38.114.gtf.gz")
OUT_DIR = "/opt/cnv"
OUT_PATH = os.path.join(OUT_DIR, "gene_pos_grch38.tsv")
KEEP = {str(i) for i in range(1, 23)} | {"X", "Y"}
CHROM_ORDER = [str(i) for i in range(1, 23)] + ["X", "Y"]
GENE_NAME = re.compile(r'gene_name "([^"]+)"')


def main() -> None:
    """下载 GTF、抽 gene 级坐标、去重排序写 TSV。"""
    os.makedirs(OUT_DIR, exist_ok=True)
    print(f"downloading {GTF_URL}", file=sys.stderr)
    req = urllib.request.Request(GTF_URL, headers={"User-Agent": "bio-build"})
    with urllib.request.urlopen(req, timeout=600) as resp:
        raw = io.BytesIO(resp.read())
    seen: set[str] = set()
    rows: list[tuple[str, str, int, int]] = []
    with gzip.open(raw, "rt") as fh:
        for line in fh:
            if "\tgene\t" not in line:
                continue
            f = line.split("\t")
            if f[0] not in KEEP:
                continue
            m = GENE_NAME.search(f[8])
            if m is None:
                continue
            name = m.group(1)
            if not name or name.startswith("ENSG") or name in seen:
                continue
            seen.add(name)
            rows.append((name, f[0], int(f[3]), int(f[4])))
    rank = {c: i for i, c in enumerate(CHROM_ORDER)}
    rows.sort(key=lambda r: (rank[r[1]], r[2]))
    with open(OUT_PATH, "w", encoding="utf-8") as out:
        out.write("gene_name\tchromosome\tstart\tend\n")
        for name, chrom, start, end in rows:
            out.write(f"{name}\tchr{chrom}\t{start}\t{end}\n")
    print(f"wrote {len(rows)} genes to {OUT_PATH}", file=sys.stderr)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Dockerfile 加层**

在 `sandbox/bio.Dockerfile` 的 Phase 37 R 层（`&& rm -rf /var/lib/apt/lists/*` 之后）与 `RUN useradd ...` 层之间插入：

```dockerfile
# B1 CNV 推断：infercnvpy（成熟默认）+ cnvturbo（对齐 R inferCNV HMM i6）
# 双后端。纯 CPU 工具；若装包报原生编译缺失，按 bbknn 层先例同层临时
# 装 g++ 编译后 purge。两层分离：改坐标脚本不重装 pip。
RUN pip install --no-cache-dir \
    -i https://pypi.tuna.tsinghua.edu.cn/simple infercnvpy cnvturbo \
    && python -c "import infercnvpy, cnvturbo; print('cnv deps ok')"
COPY fetch_gene_pos.py /tmp/fetch_gene_pos.py
RUN python /tmp/fetch_gene_pos.py && rm /tmp/fetch_gene_pos.py
```

- [ ] **Step 3: 构建镜像（层缓存命中到 Phase 37，新层主要是两包安装 + GTF 下载）**

```powershell
docker build -t feishu-research-agent/bio:cpu-latest sandbox -f sandbox/bio.Dockerfile
```

预期：末尾输出 `cnv deps ok` 与 `wrote 6xxxx genes to /opt/cnv/gene_pos_grch38.tsv`，构建成功。若 Ensembl 下载超时，直接重跑（fetch_gene_sets 同语义）。

- [ ] **Step 4: 写探针脚本并运行（spec §3.3 风险点在此退役）**

写 `$env:TEMP\probe_cnv.py`（内容如下），然后管道进容器执行：

```powershell
Get-Content $env:TEMP\probe_cnv.py -Raw | docker run --rm -i feishu-research-agent/bio:cpu-latest python -
```

探针内容：

```python
"""双后端 API 冒烟：验证 var 三列注入路线 + 关键函数签名 + 列对齐。"""
import numpy as np
import pandas as pd
import anndata as ad
import infercnvpy as cnv
from cnvturbo import tl as ct_tl

pos = pd.read_csv("/opt/cnv/gene_pos_grch38.tsv", sep="\t")
print("tsv rows:", len(pos))
rng = np.random.default_rng(0)
sel = pd.concat([pos[pos["chromosome"] == f"chr{c}"].head(40)
                 for c in (1, 7, 10)])
genes = sel["gene_name"].tolist()
X = rng.poisson(2.0, (60, len(genes))).astype(np.float32)
adata = ad.AnnData(
    X=X,
    obs=pd.DataFrame({"ct": ["T cells"] * 30 + ["B cells"] * 30},
                     index=[f"c{i}" for i in range(60)]),
    var=pd.DataFrame(index=genes))
adata.var["chromosome"] = sel["chromosome"].to_numpy()
adata.var["start"] = sel["start"].to_numpy()
adata.var["end"] = sel["end"].to_numpy()

cnv.tl.infercnv(adata, reference_key="ct",
                reference_cat=["T cells", "B cells"], window_size=250)
print("infercnvpy X_cnv:", adata.obsm["X_cnv"].shape,
      "var aligned:", adata.obsm["X_cnv"].shape[1] == adata.n_vars)

adata.layers["counts"] = adata.X.copy()
ct_tl.infercnv_r_compat(
    adata, raw_layer="counts", reference_key="ct",
    reference_cat=["T cells", "B cells"], window_size=101,
    min_mean_expr_cutoff=0.1, exclude_chromsomes=("chrX", "chrY"),
    apply_2x_transform=True, n_jobs=1, key_added="cnv")
n_xy = adata.var["chromosome"].isin(["chrX", "chrY"]).sum()
print("cnvturbo X_cnv:", adata.obsm["X_cnv"].shape,
      "xy-excluded aligned:",
      adata.obsm["X_cnv"].shape[1] == adata.n_vars - int(n_xy))

p = ct_tl.compute_hspike_emission_params(
    adata, raw_layer="counts", reference_key="ct",
    reference_cat=["T cells", "B cells"], min_mean_expr_cutoff=0.1,
    n_sim_cells=50, n_genes_per_chr=100, output_space="copy_ratio",
    return_sd_trend=True)
print("hspike keys:", sorted(p.keys()) if isinstance(p, dict) else type(p))

ct_tl.hmm_call_subclusters(
    adata, use_rep="cnv", reference_key="ct",
    reference_cat=["T cells", "B cells"],
    precomputed_emit_means=p["emit_means"],
    precomputed_emit_stds=p["emit_stds"],
    precomputed_sd_intercepts=p["sd_intercepts"],
    precomputed_sd_slopes=p["sd_slopes"],
    leiden_resolution="auto", cluster_by_groups=True,
    min_segment_length=5, min_segments_for_tumor=1,
    key_added="cnv_call", n_jobs=1)
print("cnv_call values:", adata.obs["cnv_call"].value_counts().to_dict())
print("PROBE OK")
```

预期末行 `PROBE OK`。

- [ ] **Step 5: 记录探针实测结论（决策规则）**

把以下事实记入本文件末尾"探针实测记录"节（Task 2 cnv.py 的 API 调用以实测为准）：
- `tl.infercnv` 是否接受 var 三列注入（预期是）、X_cnv 列是否与 var 对齐
- `infercnv_r_compat` 的 `raw_layer`/`exclude_chromsomes` 拼写（skill 原文即此拼写）、X_cnv 是否剔除 chrX/Y 列
- `compute_hspike_emission_params` 是否以 adata 为首参、返回 dict 的键名
- `hmm_call_subclusters` 的 `use_rep` 值（"cnv" 还是 "X_cnv"）与 obs 列取值（"Tumor"/"Normal"）

若任一签名与 Task 2 代码不符 → 以实测修正 Task 2 对应调用行，**行为契约（stdin 参数/obs 列/产物路径）不变**。若 cnvturbo 完全不接受 var 注入 → 回退方案：镜像层改为烘 GTF.gz 全文件并改用其 from_gtf。

- [ ] **Step 6: Commit**

```powershell
git add sandbox/fetch_gene_pos.py sandbox/bio.Dockerfile
git commit -m "B1 CNV：bio 镜像加 infercnvpy/cnvturbo 双后端 + GRCh38 坐标烘焙（探针冒烟通过）"
```

---

### Task 2: cnv.py 主体实现

**Files:**
- Create: `sandbox/sc_tools/cnv.py`

- [ ] **Step 1: 写 sandbox/sc_tools/cnv.py（完整实现）**

```python
"""sc_cnv：拷贝数变异推断与恶性判定（B1，双后端 infercnvpy/cnvturbo）。

stdin: {"dataset_id": ..., "method": "infercnvpy"|"cnvturbo",
        "celltype_col": "leiden",          # 参考细胞来源列（注释列亦可）
        "ref_groups": ["T cells", ...],    # 显式参考；缺省内置清单匹配
        "resolution": 1.0}                 # 亚克隆 leiden 分辨率
数据流（doublet.py 先例）：counts 走 filtered/raw 回退链（全基因矩阵），
标签/UMAP 走 processed.h5ad 按 obs_names 交集对齐；两后端同源起步
（infercnvpy 侧自 normalize+log1p，cnvturbo 侧吃原始 counts）保证交叉
验证可比。结果 reindex 写回 processed.h5ad：cnv_score / is_malignant /
（仅 cnvturbo）cnv_call / cnv_subclone。R 兼容参数（window=101、
cutoff=0.1、2x transform、排除 chrX/Y）内部定死不暴露。
产物落 /ws/{ds}/cnv/：figS3B 式染色体热图（双后端统一自绘）、
score/subclone UMAP、注释×恶性计数 csv、亚克隆×染色体 csv。
"""
from __future__ import annotations

from typing import Any

import matplotlib.colors as mcolors
import matplotlib.pyplot as plt
import numpy as np
import pandas as pd
from common import WS_ROOT, emit, fail, load_adata, read_args, run

GENE_POS_TSV = "/opt/cnv/gene_pos_grch38.tsv"
CHROM_ORDER = [f"chr{i}" for i in range(1, 23)] + ["chrX", "chrY"]
# 人源常见非恶性参考（大小写不敏感子串匹配，"T cell" 命中 "CD4 T cells"）。
# Epithelial 故意不在默认清单（可为恶性来源），靠 ref_groups 显式加。
DEFAULT_REF_PATTERNS = [
    "T cell", "B cell", "NK", "Macrophage", "Monocyte", "Dendritic",
    "Neutrophil", "Fibroblast", "Endothelial", "Pericyte",
    "Smooth muscle", "Erythrocyte",
]


class NoReferenceError(ValueError):
    """默认清单与注释取值零匹配（不静默降级为无参考模式）。"""


def _match_references(values: list[str],
                      ref_groups: list[str] | None) -> list[str]:
    """解析参考类型：显式 ref_groups 直接用；缺省内置清单子串匹配。"""
    if ref_groups:
        missing = [g for g in ref_groups if g not in values]
        if missing:
            raise ValueError(
                f"ref_groups not in column values: {missing}; "
                f"available: {values}")
        return list(ref_groups)
    matched = [v for v in values if any(
        p.lower() in v.lower() for p in DEFAULT_REF_PATTERNS)]
    if not matched:
        raise NoReferenceError(
            "no annotation value matches the default non-malignant list "
            "(T/B/NK/Macrophage/Monocyte/Dendritic/Neutrophil/Fibroblast/"
            "Endothelial/Pericyte/Smooth muscle/Erythrocyte); pass "
            f'ref_groups explicitly, e.g. ["T cells", "Fibroblast"]; '
            f"available values: {values}")
    return matched


def _attach_gene_pos(adata: Any) -> Any:
    """按 TSV 定位并基因组序排序基因，返回带 var 三列的新 AnnData。

    upper 归一匹配、同名歧义丢弃（宁可少配不错配）；定位 <1000 报错。
    """
    pos = pd.read_csv(GENE_POS_TSV, sep="\t")
    upper_to_row: dict[str, pd.Series] = {}
    ambiguous: set[str] = set()
    for _, row in pos.iterrows():
        key = str(row["gene_name"]).upper()
        if key in upper_to_row:
            ambiguous.add(key)
        else:
            upper_to_row[key] = row
    for key in ambiguous:
        upper_to_row.pop(key, None)
    keep: list[str] = []
    chroms: list[str] = []
    starts: list[int] = []
    for g in adata.var_names:
        row = upper_to_row.get(str(g).upper())
        if row is None:
            continue
        keep.append(str(g))
        chroms.append(str(row["chromosome"]))
        starts.append(int(row["start"]))
    if len(keep) < 1000:
        raise ValueError(
            f"too few genes positioned ({len(keep)} < 1000); check gene "
            "naming (symbol vs ensembl) and species (human GRCh38)")
    sub = adata[:, keep].copy()
    sub.var["chromosome"] = pd.Categorical(chroms, categories=CHROM_ORDER)
    sub.var["start"] = np.asarray(starts, dtype=np.int64)
    sub.var["end"] = pos.set_index("gene_name")["end"][
        [str(g) for g in sub.var_names]].to_numpy()
    rank = {c: i for i, c in enumerate(CHROM_ORDER)}
    order = sorted(range(sub.n_vars), key=lambda i: (
        rank[str(sub.var["chromosome"].iloc[i])],
        int(sub.var["start"].iloc[i])))
    sub = sub[:, order].copy()
    return sub


def _run_infercnvpy(cnv_ad: Any, celltype_col: str,
                    ref_cats: list[str]) -> Any:
    """infercnvpy 后端：normalize+log1p（其文档惯例）→ tl.infercnv。"""
    import infercnvpy as cnv
    import scanpy as sc

    sc.pp.normalize_total(cnv_ad, target_sum=1e4)
    sc.pp.log1p(cnv_ad)
    cnv.tl.infercnv(
        cnv_ad,
        reference_key=celltype_col,
        reference_cat=ref_cats,
        window_size=250,
    )
    return cnv_ad.obsm["X_cnv"]


def _run_cnvturbo(cnv_ad: Any, celltype_col: str,
                  ref_cats: list[str]) -> tuple[Any, np.ndarray]:
    """cnvturbo 后端：R 兼容主推断 + hspike 校准 + HMM i6 恶性判定。

    counts 塞进 layers["counts"]（raw_layer 语义）；返回 (X_cnv,
    is_malignant)，is_malignant = HMM 判定 Tumor。
    """
    from cnvturbo import tl as ct_tl

    cnv_ad.layers["counts"] = cnv_ad.X.copy()
    ct_tl.infercnv_r_compat(
        cnv_ad,
        raw_layer="counts",
        reference_key=celltype_col,
        reference_cat=ref_cats,
        window_size=101,
        min_mean_expr_cutoff=0.1,
        exclude_chromsomes=("chrX", "chrY"),
        apply_2x_transform=True,
        n_jobs=2,
        key_added="cnv",
    )
    emit_p = ct_tl.compute_hspike_emission_params(
        cnv_ad,
        raw_layer="counts",
        reference_key=celltype_col,
        reference_cat=ref_cats,
        min_mean_expr_cutoff=0.1,
        n_sim_cells=100,
        n_genes_per_chr=400,
        output_space="copy_ratio",
        return_sd_trend=True,
    )
    ct_tl.hmm_call_subclusters(
        cnv_ad,
        use_rep="cnv",
        reference_key=celltype_col,
        reference_cat=ref_cats,
        precomputed_emit_means=emit_p["emit_means"],
        precomputed_emit_stds=emit_p["emit_stds"],
        precomputed_sd_intercepts=emit_p["sd_intercepts"],
        precomputed_sd_slopes=emit_p["sd_slopes"],
        leiden_resolution="auto",
        cluster_by_groups=True,
        min_segment_length=5,
        min_segments_for_tumor=1,
        key_added="cnv_call",
        n_jobs=2,
    )
    calls = cnv_ad.obs["cnv_call"].astype(str).to_numpy()
    return cnv_ad.obsm["X_cnv"], calls == "Tumor"


def _cell_scores(x_cnv: np.ndarray, ref_mask: np.ndarray) -> np.ndarray:
    """每细胞 CNV 负荷：与参考细胞中心基因级偏离的均值。"""
    ref_mean = np.asarray(x_cnv[ref_mask].mean(axis=0)).ravel()
    return np.abs(x_cnv - ref_mean).mean(axis=1)


def _leiden_on_cnv(x_cnv: np.ndarray, resolution: float) -> np.ndarray:
    """X_cnv 矩阵上 PCA→邻居→leiden（与 sc_process 同款 igraph 参数）。"""
    import anndata as ad
    import scanpy as sc

    tmp = ad.AnnData(X=np.asarray(x_cnv, dtype=np.float32))
    n_comps = max(2, min(30, tmp.n_vars - 1, tmp.n_obs - 1))
    sc.tl.pca(tmp, n_comps=n_comps, svd_solver="arpack")
    sc.pp.neighbors(tmp, n_neighbors=min(15, tmp.n_obs - 1))
    sc.tl.leiden(tmp, resolution=resolution, flavor="igraph",
                 n_iterations=2, directed=False)
    return tmp.obs["leiden"].astype(str).to_numpy()


def _umap_score_png(umap: np.ndarray, scores: np.ndarray,
                    png_path: Any) -> None:
    """UMAP 连续着色（cnv_score）。"""
    fig, ax = plt.subplots(figsize=(6.5, 5))
    sc_ = ax.scatter(umap[:, 0], umap[:, 1], s=5, c=scores,
                     cmap="viridis", linewidths=0)
    fig.colorbar(sc_, ax=ax, shrink=0.8, label="cnv_score")
    ax.set_xticks([])
    ax.set_yticks([])
    ax.set_title("CNV score", fontsize=10)
    fig.tight_layout()
    fig.savefig(png_path, dpi=150, bbox_inches="tight")
    plt.close(fig)


def _umap_subclone_png(umap: np.ndarray, subclone: np.ndarray,
                       png_path: Any) -> None:
    """UMAP 分类着色（cnv_subclone，tab20 循环）。"""
    cats = sorted(set(str(s) for s in subclone))
    cmap = plt.get_cmap("tab20")
    fig, ax = plt.subplots(figsize=(7, 5.5))
    for i, c in enumerate(cats):
        m = np.asarray([str(s) == c for s in subclone])
        ax.scatter(umap[m, 0], umap[m, 1], s=5, color=cmap(i % 20),
                   label=f"{c} ({int(m.sum())})", linewidths=0)
    ax.legend(fontsize=7, loc="center left", bbox_to_anchor=(1.0, 0.5),
              markerscale=2)
    ax.set_xticks([])
    ax.set_yticks([])
    ax.set_title("CNV subclone", fontsize=10)
    fig.tight_layout()
    fig.savefig(png_path, dpi=150, bbox_inches="tight")
    plt.close(fig)


def _chromosome_heatmap(x_cnv: np.ndarray, chrom_col: np.ndarray,
                        ref_mask: np.ndarray, is_mal: np.ndarray,
                        subclone: np.ndarray, png_path: Any,
                        method: str) -> None:
    """figS3B 式染色体热图：上=参考细胞，下=恶性细胞按亚克隆分组。

    双后端统一渲染（色标中心=参考细胞中位数，±1 范围 RdBu_r）。
    """
    rng = np.random.default_rng(0)
    idx_ref = np.where(ref_mask)[0]
    if len(idx_ref) > 800:
        idx_ref = rng.choice(idx_ref, 800, replace=False)
    idx_mal = np.where(is_mal)[0]
    sub_of_mal = np.asarray(subclone)[is_mal]
    rows: list[int] = []
    for c in sorted(set(sub_of_mal.tolist())):
        m = idx_mal[sub_of_mal == c]
        if len(m) > 600:
            m = rng.choice(m, 600, replace=False)
        rows.extend(int(i) for i in m)
    M = x_cnv[list(idx_ref) + rows]
    center = float(np.median(x_cnv[ref_mask]))
    fig, ax = plt.subplots(figsize=(10, 8))
    im = ax.imshow(M, aspect="auto", cmap="RdBu_r", interpolation="nearest",
                   norm=mcolors.TwoSlopeNorm(vcenter=center,
                                             vmin=center - 1.0,
                                             vmax=center + 1.0))
    present = [c for c in CHROM_ORDER if (chrom_col == c).any()]
    bounds = np.cumsum([0] + [int((chrom_col == c).sum()) for c in present])
    for b in bounds[1:-1]:
        ax.axvline(b - 0.5, color="black", lw=0.4)
    ax.set_xticks((bounds[:-1] + bounds[1:]) / 2)
    ax.set_xticklabels(present, fontsize=7)
    ax.set_yticks([])
    ax.axhline(len(idx_ref) - 0.5, color="black", lw=1.2)
    fig.colorbar(im, ax=ax, shrink=0.6, label="CNV signal")
    ax.set_title(
        f"CNV chromosome heatmap ({method}); "
        f"top: reference (n={len(idx_ref)}), bottom: malignant (n={len(rows)})",
        fontsize=10)
    fig.tight_layout()
    fig.savefig(png_path, dpi=150, bbox_inches="tight")
    plt.close(fig)


def main() -> None:
    """主流程：对齐 → 双后端之一推断 → 打分/判定/亚克隆 → 写回产物。"""
    args = read_args()
    method = str(args.get("method", "infercnvpy")).strip().lower()
    if method not in ("infercnvpy", "cnvturbo"):
        raise ValueError(
            f"method must be infercnvpy or cnvturbo, got {method!r}")
    celltype_col = str(args.get("celltype_col", "leiden")).strip()
    ref_groups = args.get("ref_groups") or None
    resolution = float(args.get("resolution", 1.0))

    counts_ad = load_adata({"dataset_id": args["dataset_id"], "file": "any"})
    adata = load_adata({"dataset_id": args["dataset_id"],
                        "file": "processed"})
    if celltype_col not in adata.obs:
        raise ValueError(
            f"celltype column {celltype_col!r} not in obs; available: "
            f"{sorted(str(c) for c in adata.obs.columns)}")
    if "X_umap" not in adata.obsm:
        raise ValueError("processed.h5ad missing X_umap; re-run sc_process")
    common_cells = adata.obs_names.intersection(counts_ad.obs_names)
    if len(common_cells) < 200:
        raise ValueError(
            f"too few cells shared between counts and processed: "
            f"{len(common_cells)} (<200)")

    values = sorted(set(adata.obs[celltype_col].astype(str)))
    try:
        ref_cats = _match_references(values, ref_groups)
    except NoReferenceError as e:
        fail("SC_CNV_NO_REFERENCE", str(e))
        return

    # 双后端同源起步：同一 counts 子集 + 标签对齐（doublet.py 先例）
    cnv_ad = counts_ad[common_cells].copy()
    cnv_ad.obs[celltype_col] = (
        adata.obs.loc[common_cells, celltype_col].astype(str).to_numpy())
    n_ref = int(cnv_ad.obs[celltype_col].isin(ref_cats).sum())
    if n_ref < 50:
        raise ValueError(f"too few reference cells ({n_ref} < 50); "
                         "check ref choice")

    genes_total = int(cnv_ad.n_vars)
    cnv_ad = _attach_gene_pos(cnv_ad)
    n_positioned = int(cnv_ad.n_vars)

    if method == "infercnvpy":
        x_cnv = np.asarray(_run_infercnvpy(cnv_ad, celltype_col, ref_cats),
                           dtype=np.float64)
        turbo_calls = None
    else:
        x_raw, turbo_calls = _run_cnvturbo(cnv_ad, celltype_col, ref_cats)
        x_cnv = np.asarray(x_raw, dtype=np.float64)

    # 染色体列与 X_cnv 列对齐（cnvturbo 主推断剔除 chrX/Y 列）
    chrom_all = cnv_ad.var["chromosome"].astype(str).to_numpy()
    if method == "cnvturbo":
        chrom_col = chrom_all[~np.isin(chrom_all, ("chrX", "chrY"))]
    else:
        chrom_col = chrom_all
    if x_cnv.shape[1] != len(chrom_col):
        raise ValueError(
            f"X_cnv columns ({x_cnv.shape[1]}) != expected genes "
            f"({len(chrom_col)}); backend var tracking changed")

    ref_mask = cnv_ad.obs[celltype_col].isin(ref_cats).to_numpy(dtype=bool)
    scores = _cell_scores(x_cnv, ref_mask)
    ref_scores = scores[ref_mask]
    threshold = float(ref_scores.mean() + 3.0 * ref_scores.std())

    note = ""
    if turbo_calls is not None:
        is_mal = turbo_calls
        note = "cnvturbo HMM i6 细胞级判定"
    else:
        # infercnvpy：细胞级阈值 → CNV 簇多数投票平滑（簇级判定更稳）
        clusters = _leiden_on_cnv(x_cnv, 0.5)
        raw_flag = scores > threshold
        is_mal = np.zeros(len(scores), dtype=bool)
        for c in set(clusters):
            m = clusters == c
            is_mal[m] = raw_flag[m].mean() > 0.5
        note = "infercnvpy 阈值 + CNV 簇多数投票（阈值=参考分 mean+3sd）"

    subclone = np.full(len(scores), "non-malignant", dtype=object)
    n_mal = int(is_mal.sum())
    if n_mal >= 50:
        mal_leiden = _leiden_on_cnv(x_cnv[is_mal], resolution)
        sizes = pd.Series(mal_leiden).value_counts()
        remap = {c: f"C{i + 1}" for i, c in enumerate(sizes.index)}
        subclone[is_mal] = [remap[c] for c in mal_leiden]
        note += f"；亚克隆 {len(sizes)} 个"
    elif n_mal > 0:
        subclone[is_mal] = "C1"
        note += "；恶性细胞 <50 退化为单克隆 C1"

    out_dir = WS_ROOT / args["dataset_id"] / "cnv"
    out_dir.mkdir(parents=True, exist_ok=True)

    ct_series = cnv_ad.obs[celltype_col].astype(str)
    summary = pd.crosstab(ct_series,
                          pd.Series(is_mal, index=ct_series.index))
    summary_csv = out_dir / "cnv_celltype_summary.csv"
    summary.to_csv(summary_csv)
    malignant_by_ct = {str(k): int(v) for k, v in
                       ct_series[np.asarray(is_mal)]
                       .value_counts().head(10).items()}

    ref_mean = np.asarray(x_cnv[ref_mask].mean(axis=0)).ravel()
    dev = x_cnv - ref_mean
    sub_s = pd.Series(subclone)
    sub_labels = [c for c in sub_s.value_counts().index
                  if c != "non-malignant"]
    chr_rows: dict[str, dict[str, float]] = {}
    for label in sub_labels:
        m = (sub_s == label).to_numpy()
        chr_rows[label] = {
            c: round(float(dev[m][:, chrom_col == c].mean()), 4)
            for c in CHROM_ORDER if (chrom_col == c).any()}
    chr_csv = out_dir / "cnv_subclone_by_chromosome.csv"
    pd.DataFrame(chr_rows).T.to_csv(chr_csv, index_label="subclone")

    heatmap_png = out_dir / "cnv_chromosome_heatmap.png"
    _chromosome_heatmap(x_cnv, chrom_col, ref_mask, is_mal, subclone,
                        heatmap_png, method)
    umap = adata.obsm["X_umap"]
    umap_s = pd.Series(scores, index=cnv_ad.obs_names).reindex(
        adata.obs_names).to_numpy(dtype=float)
    sub_s_full = pd.Series(subclone, index=cnv_ad.obs_names).reindex(
        adata.obs_names).astype(str).to_numpy()
    score_png = out_dir / "cnv_score_umap.png"
    _umap_score_png(np.asarray(umap), umap_s, score_png)
    sub_png = out_dir / "cnv_subclone_umap.png"
    _umap_subclone_png(np.asarray(umap), sub_s_full, sub_png)

    adata.obs["cnv_score"] = pd.Series(
        scores, index=cnv_ad.obs_names).reindex(adata.obs_names)
    adata.obs["is_malignant"] = pd.Series(
        is_mal, index=cnv_ad.obs_names).reindex(adata.obs_names)
    adata.obs["cnv_subclone"] = pd.Series(
        subclone, index=cnv_ad.obs_names).reindex(adata.obs_names)
    if turbo_calls is not None:
        adata.obs["cnv_call"] = pd.Series(
            cnv_ad.obs["cnv_call"].astype(str).to_numpy(),
            index=cnv_ad.obs_names).reindex(adata.obs_names)
    h5ad_path = WS_ROOT / args["dataset_id"] / "processed.h5ad"
    adata.write(h5ad_path)
    emit({
        "ok": True,
        "dataset_ref": args["dataset_id"],
        "method": method,
        "matched_references": ref_cats,
        "annotation_values": values,
        "n_reference_cells": n_ref,
        "n_cells": int(len(common_cells)),
        "genes": {"total": genes_total, "positioned": n_positioned},
        "n_malignant": n_mal,
        "malignant_ratio": round(n_mal / max(1, len(common_cells)), 4),
        "threshold": round(threshold, 4),
        "n_subclones": len(sub_labels),
        "subclone_sizes": {c: int((subclone == c).sum())
                           for c in sub_labels},
        "malignant_by_celltype": malignant_by_ct,
        "chromosome_heatmap_png": str(heatmap_png),
        "score_umap_png": str(score_png),
        "subclone_umap_png": str(sub_png),
        "celltype_summary_csv": str(summary_csv),
        "subclone_by_chromosome_csv": str(chr_csv),
        "saved": str(h5ad_path),
        "note": note + "；is_malignant/cnv_subclone 已写回 processed.h5ad，"
                       "sc_plot/sc_de 可作分组列",
    })


if __name__ == "__main__":
    run(main)
```

**注意**：若 Task 1 探针实测发现签名差异（如 `exclude_chromsomes` 拼写、`use_rep` 取值、hspike 返回键名），按探针记录修正上述对应调用行，契约不变。

- [ ] **Step 2: 门禁**

```powershell
& .venv\Scripts\python.exe -m ruff check sandbox/sc_tools/cnv.py sandbox/fetch_gene_pos.py
& .venv\Scripts\python.exe -m mypy
& .venv\Scripts\python.exe -m mypy --platform linux
```

预期：ruff 0 违规；mypy 双平台 0 错（cnv.py 属 sandbox 包 strict 范围——若报 no-any-return 之类，按 sandbox 既有惯例以显式注解/cast 收敛，不加 type: ignore）。

- [ ] **Step 3: Commit**

```powershell
git add sandbox/sc_tools/cnv.py
git commit -m "B1 CNV：sc_tools/cnv.py 双后端推断+恶性判定+亚克隆统一后处理"
```

---

### Task 3: 注册 sc_cnv + 单测（TDD）

**Files:**
- Modify: `orchestrator/tools/builtin/l3_singlecell.py`（sc_knockout handler 之后加 handler；文件末尾 ToolSpec 列表 sc_knockout 之后加注册）
- Modify: `tests/unit/test_l3_singlecell.py`（文件末尾追加 3 用例）

- [ ] **Step 1: 写失败测试（追加到 tests/unit/test_l3_singlecell.py 末尾）**

```python
def test_register_sc_cnv(tmp_path):
    """B1 CNV：sc_cnv 注册、planner 可见、L1_compute。"""
    reg, _ = _registry(tmp_path)
    names = [t.name for t in reg.list(planner_visible=True)]
    assert "sc_cnv" in names
    assert reg.get("sc_cnv").risk_level == "L1_compute"
    assert reg.get("sc_cnv").timeout_sec == 3600


def test_sc_cnv_forwards_params(tmp_path):
    """B1 CNV：参数透传进容器 payload，timeout 与 ToolSpec 一致。"""
    reg, runner = _registry(tmp_path)
    runner.run.return_value = {
        "ok": True, "dataset_ref": "d", "n_malignant": 5}

    out = reg.get("sc_cnv").handler(
        dataset_ref="d", method="cnvturbo",
        celltype_col="celltypist_label",
        ref_groups=["T cells"], resolution=0.8)

    script, payload = runner.run.call_args[0]
    assert script == "cnv"
    assert payload == {"dataset_id": "d", "method": "cnvturbo",
                       "celltype_col": "celltypist_label",
                       "ref_groups": ["T cells"], "resolution": 0.8}
    assert runner.run.call_args[1]["timeout_sec"] == 3600
    assert "ok" not in out
    assert out["n_malignant"] == 5


def test_sc_cnv_default_params(tmp_path):
    """B1 CNV：缺省 method=infercnvpy / celltype_col=leiden / ref_groups=None。"""
    reg, runner = _registry(tmp_path)
    runner.run.return_value = {"ok": True, "dataset_ref": "d"}

    reg.get("sc_cnv").handler(dataset_ref="d")

    _, payload = runner.run.call_args[0]
    assert payload == {"dataset_id": "d", "method": "infercnvpy",
                       "celltype_col": "leiden", "ref_groups": None,
                       "resolution": 1.0}
```

- [ ] **Step 2: 跑测确认失败**

```powershell
& .venv\Scripts\python.exe -m pytest tests/unit/test_l3_singlecell.py -v --basetemp=$env:TEMP\pytest_basetemp_scnv_reg
```

预期：3 个新用例 FAIL（`sc_cnv` not in names / handler 不存在）。

- [ ] **Step 3: 注册实现**

在 `orchestrator/tools/builtin/l3_singlecell.py` 的 `sc_knockout` handler 函数之后加：

```python
    def sc_cnv(*, dataset_ref: str, method: str = "infercnvpy",
               celltype_col: str = "leiden",
               ref_groups: list[str] | None = None,
               resolution: float = 1.0) -> dict[str, Any]:
        """CNV 推断与恶性判定（B1）：双后端 → 亚克隆。"""
        try:
            out = runner.run("cnv", {
                "dataset_id": dataset_ref, "method": method,
                "celltype_col": celltype_col, "ref_groups": ref_groups,
                "resolution": resolution,
            }, timeout_sec=3600)
        except BioRunError as e:
            return _err(e)
        out.pop("ok", None)
        return out
```

在文件末尾 `sc_knockout` 的 ToolSpec 注册之后加：

```python
    registry.register(ToolSpec(
        name="sc_cnv",
        description=(
            "CNV 推断与恶性判定（B1，inferCNV 式有参考模式）：以免疫/"
            "基质等非恶性细胞为基线推断全基因组拷贝数变异，输出每细胞"
            " cnv_score、恶性判定 is_malignant 与恶性亚克隆 cnv_subclone"
            "（写回 processed.h5ad），附染色体热图与注释类型×恶性计数表。"
            "method 默认 infercnvpy（成熟参考实现），可选 cnvturbo（对齐"
            " R inferCNV HMM i6，可交叉验证）。参考细胞默认从 "
            "celltype_col 按内置非恶性清单子串匹配（T/B/NK/Macrophage/"
            "Monocyte/Dendritic/Neutrophil/Fibroblast/Endothelial/"
            "Pericyte/Smooth muscle/Erythrocyte），也可 ref_groups 显式"
            "指定（如含正常上皮时传 ['T cells', 'Epithelial']）；零匹配"
            "报错（不静默降级）。需先 sc_process；人源 GRCh38 基因符号。"
            "结果列可作 sc_plot/sc_de 分组。"
        ),
        parameters={
            "type": "object",
            "properties": {
                "dataset_ref": {"type": "string",
                                "description": "sc_process 输出的 dataset_ref"},
                "method": {"type": "string", "default": "infercnvpy",
                           "enum": ["infercnvpy", "cnvturbo"]},
                "celltype_col": {"type": "string", "default": "leiden",
                                 "description": "参考细胞来源列"
                                                "（leiden 或注释列）"},
                "ref_groups": {
                    "type": "array", "items": {"type": "string"},
                    "description": "显式参考细胞类型列表（覆盖默认清单）"},
                "resolution": {"type": "number", "default": 1.0,
                               "description": "恶性亚克隆 leiden 分辨率"},
            },
            "required": ["dataset_ref"],
        },
        risk_level="L1_compute",
        handler=sc_cnv,
        timeout_sec=3600,
    ))
```

同时把模块 docstring 首行 "23 个 L1_compute 工具" 的口径核对为最新计数（23→24 早已漂移，按实际不改口径也行——只加工具不动 docstring 数字若它已过时；如改，改为 "25 个"）。

- [ ] **Step 4: 跑测通过**

```powershell
& .venv\Scripts\python.exe -m pytest tests/unit/test_l3_singlecell.py -v --basetemp=$env:TEMP\pytest_basetemp_scnv_reg
```

预期：全部 PASS（原有 + 3 新增）。

- [ ] **Step 5: 门禁 + Commit**

```powershell
& .venv\Scripts\python.exe -m ruff check .
& .venv\Scripts\python.exe -m mypy
& .venv\Scripts\python.exe -m mypy --platform linux
git add orchestrator/tools/builtin/l3_singlecell.py tests/unit/test_l3_singlecell.py
git commit -m "B1 CNV：sc_cnv 注册（第 25 个 sc_* 工具）+ 单测 3 用例"
```

---

### Task 4: 重建镜像 + 真容器集成测试

**Files:**
- Create: `tests/integration/test_bio_cnv.py`

- [ ] **Step 1: 重建镜像（sc_tools 层重打，秒级）**

```powershell
docker build -t feishu-research-agent/bio:cpu-latest sandbox -f sandbox/bio.Dockerfile
```

- [ ] **Step 2: 写 tests/integration/test_bio_cnv.py**

```python
"""B1 sc_cnv 真容器集成测试（需 Docker + bio:cpu-latest，缺则自动 skip）。

镜像构建：
    docker build -t feishu-research-agent/bio:cpu-latest sandbox -f sandbox/bio.Dockerfile
合成数据在容器内构造（宿主 venv 无 scanpy）：免疫参考群（T/B cells）+
人工 CNV 信号"恶性"群（chr7 基因×1.8 gain、chr10 ×0.6 loss）→
sc_cnv 双后端 → 断言恶性判定集中在造的恶性群、obs 写回与产物齐全。
"""
import json
import shutil
import subprocess
from pathlib import Path

import pytest

_IMAGE = "feishu-research-agent/bio:cpu-latest"


def _docker_ready() -> bool:
    """docker 可用且 bio 镜像已构建。"""
    if shutil.which("docker") is None:
        return False
    try:
        subprocess.run(["docker", "info"], capture_output=True, timeout=10)
    except Exception:
        return False
    inspect = subprocess.run(
        ["docker", "image", "inspect", _IMAGE],
        capture_output=True, timeout=10,
    )
    return inspect.returncode == 0


pytestmark = pytest.mark.skipif(
    not _docker_ready(), reason="docker 不可用或 bio 镜像未构建"
)

_BUILDER = r'''
import numpy as np
import pandas as pd
import anndata as ad

rng = np.random.default_rng(42)
pos = pd.read_csv("/opt/cnv/gene_pos_grch38.tsv", sep="\t")
sel = pd.concat([pos[pos["chromosome"] == f"chr{c}"].head(220)
                 for c in (1, 2, 3, 7, 10)]).drop_duplicates("gene_name")
genes = sel["gene_name"].tolist()
n_g = len(genes)
n_t, n_b, n_m = 120, 120, 150
base = rng.poisson(2.0, (n_t + n_b, n_g)).astype(np.float32)
mal = rng.poisson(2.0, (n_m, n_g)).astype(np.float32)
gain = sel["chromosome"].eq("chr7").to_numpy()
loss = sel["chromosome"].eq("chr10").to_numpy()
mal[:, gain] = rng.poisson(3.6, (n_m, int(gain.sum()))).astype(np.float32)
mal[:, loss] = rng.poisson(0.72, (n_m, int(loss.sum()))).astype(np.float32)
counts = np.vstack([base, mal])
barcodes = [f"c{i}" for i in range(counts.shape[0])]
ct = ["T cells"] * n_t + ["B cells"] * n_b + ["Epithelial tumor"] * n_m
adata = ad.AnnData(
    X=counts,
    obs=pd.DataFrame({"celltype": ct}, index=barcodes),
    var=pd.DataFrame(index=pd.Index(genes)))
adata.var_names_make_unique()
adata.write_h5ad("/ws/itest/filtered.h5ad")
proc = ad.AnnData(
    X=np.zeros((counts.shape[0], 10), dtype=np.float32),
    obs=pd.DataFrame({"celltype": ct}, index=barcodes),
    var=pd.DataFrame(index=[f"g{i}" for i in range(10)]))
proc.obsm["X_umap"] = rng.normal(size=(counts.shape[0], 2))
proc.write_h5ad("/ws/itest/processed.h5ad")
print("built", counts.shape)
'''


@pytest.fixture(scope="module")
def bio_ws(tmp_path_factory):
    """容器内构造合成数据集 itest，返回宿主侧 workspace 路径。"""
    ws = tmp_path_factory.mktemp("biows")
    (ws / "itest").mkdir()
    bdir = tmp_path_factory.mktemp("builder")
    (bdir / "build_data.py").write_text(_BUILDER, encoding="utf-8")
    out = subprocess.run(
        ["docker", "run", "--rm",
         "-v", f"{ws}:/ws", "-v", f"{bdir}:/builder",
         _IMAGE, "python", "/builder/build_data.py"],
        capture_output=True, timeout=600)
    assert out.returncode == 0, out.stderr.decode()[-2000:]
    return ws


def _run_cnv(ws: Path, payload: dict, timeout: int = 1800) -> dict:
    """stdin JSON 喂 cnv.py，解析 stdout 末行 JSON。"""
    raw = json.dumps(payload).encode()
    out = subprocess.run(
        ["docker", "run", "--rm", "-i", "-v", f"{ws}:/ws",
         _IMAGE, "python", "/opt/sc_tools/cnv.py"],
        input=raw, capture_output=True, timeout=timeout)
    assert out.returncode == 0, out.stderr.decode()[-2000:]
    return json.loads(out.stdout.decode().splitlines()[-1])


@pytest.mark.parametrize("method", ["infercnvpy", "cnvturbo"])
def test_sc_cnv_detects_synthetic_malignant(bio_ws, method):
    """合成 CNV 信号（chr7 gain/chr10 loss）双后端判定集中于恶性群。"""
    res = _run_cnv(bio_ws, {"dataset_id": "itest", "method": method,
                            "celltype_col": "celltype"})
    assert res["ok"] is True
    assert set(res["matched_references"]) >= {"T cells", "B cells"}
    assert res["genes"]["positioned"] >= 1000
    epi_mal = res["malignant_by_celltype"].get("Epithelial tumor", 0)
    assert epi_mal > 0
    assert epi_mal / res["n_malignant"] >= 0.8  # 精度：≥80% 落在造的恶性群
    for key in ("chromosome_heatmap_png", "score_umap_png",
                "subclone_umap_png", "celltype_summary_csv",
                "subclone_by_chromosome_csv"):
        host = Path(res[key].replace("/ws", str(bio_ws)))
        assert host.exists(), f"missing {key}: {host}"
    # obs 写回（容器内回读验证）
    check = subprocess.run(
        ["docker", "run", "--rm", "-v", f"{bio_ws}:/ws", _IMAGE,
         "python", "-c",
         "import anndata as ad; a = ad.read_h5ad('/ws/itest/processed.h5ad')"
         "; assert 'cnv_score' in a.obs and 'is_malignant' in a.obs and "
         "'cnv_subclone' in a.obs; print('obs-ok')"],
        capture_output=True, timeout=300)
    assert b"obs-ok" in check.stdout, check.stderr.decode()[-2000:]


def test_sc_cnv_ref_groups_miss(bio_ws):
    """ref_groups 值不存在 → 明确报错列可用值（planner 自纠）。"""
    res = _run_cnv(bio_ws, {"dataset_id": "itest",
                            "celltype_col": "celltype",
                            "ref_groups": ["NotExists"]}, timeout=600)
    assert res["ok"] is False
    assert "ref_groups not in column values" in res["error_message"]
    assert "Epithelial tumor" in res["error_message"]  # 错误里列可用值
```

- [ ] **Step 3: 跑集成测试（Docker Desktop 须在线）**

```powershell
& .venv\Scripts\python.exe -m pytest tests/integration/test_bio_cnv.py -v --basetemp=$env:TEMP\pytest_basetemp_bio
```

预期：3 用例 PASS（两后端参数化 + ref_groups 错误路）。单后端跑 1-5 分钟量级。

- [ ] **Step 4: 若断言失败的处置规则**

- 判定精度 <0.8：先看 `cnv_celltype_summary.csv` 与 threshold——信号造弱了就调 `_BUILDER` 的 gain/loss 强度（如 3.6→4.0）；不许改 cnv.py 判定逻辑来"凑绿"
- `X_cnv columns != expected genes` 对齐错：按 Task 1 探针记录修 chrom_col 推导
- cnvturbo 签名类报错：按探针记录修调用行

- [ ] **Step 5: Commit**

```powershell
git add tests/integration/test_bio_cnv.py
git commit -m "B1 CNV：真容器集成测试（合成 CNV 双后端判定吻合 + 错误自纠）"
```

---

### Task 5: 文档同步 + 全量回归 + 推送 CI

**Files:**
- Modify: `docs/平台功能说明书.md`（工具表 sc_knockout 行后插 1 行）
- Modify: `docs/使用说明书.md`（步骤表 + 触发块）
- Modify: `docs/ROADMAP.md`（变更记录表末尾 +1 行）
- Modify: `i:\飞书agent\测试总结+2026-09-09T01-55-00.md`（追加六）
- Modify: `docs/superpowers/specs/2026-09-10-scnv-design.md`（状态行）

- [ ] **Step 1: 平台功能说明书**——`sc_knockout` 行（约 194 行）之后、`sc_plot` 行之前插：

```markdown
| `sc_cnv` | CNV 推断与恶性判定（双后端 infercnvpy 默认 / cnvturbo HMM i6；有参考模式，免疫/基质默认清单或 ref_groups 显式，零匹配报错不降级；counts 走 filtered 链；写回 cnv_score/is_malignant[/cnv_call]/cnv_subclone） | 染色体热图 + score/subclone UMAP png + 注释×恶性 csv + 亚克隆×染色体 csv |
```

同时核对该节标题/导语里的工具计数（"24 个"→"25 个"若存在）。

- [ ] **Step 2: 使用说明书**——① 步骤表 `调控网络 sc_scenic` 行（约 117 行）后插：

```markdown
| CNV 恶性判定 sc_cnv | 每细胞 cnv_score + 恶性标记 + **染色体 CNV 热图** + 亚克隆分组 |
```

② `### 场景 E`（空间转录组）之前、多组学段落之后插触发块：

```markdown
CNV 恶性判定（sc_process/sc_annotate 之后）：

```
哪些细胞是恶性的？帮我做 CNV 推断分亚克隆
```

（触发 sc_cnv（inferCNV 式有参考模式，双后端：默认 infercnvpy，可选
cnvturbo——对齐 R inferCNV HMM i6，可交叉验证）：以免疫/基质等非恶性
细胞为基线（默认清单子串匹配，也可 ref_groups 显式指定；数据含正常
上皮时建议显式传），推断全基因组 CNV → 每细胞 cnv_score +
is_malignant + 恶性亚克隆 cnv_subclone 写回 processed.h5ad，附染色体
热图、注释类型×恶性计数表与亚克隆×染色体信号表。人源数据（GRCh38
基因符号）。）
```

- [ ] **Step 3: ROADMAP**——变更记录表末尾（345 行后）加（CI 号推送后回填）：

```markdown
| 2026-09-10 | **B1 CNV 推断落地：sc_cnv 双后端工具**（spec docs/superpowers/specs/2026-09-10-scnv-design.md，commit <hash>）：infercnvpy（成熟默认）+ cnvturbo（对齐 R inferCNV HMM i6）同源起步——counts 走 filtered 链、双后端从同一矩阵出发保交叉验证可比；有参考模式（内置非恶性清单子串匹配 + ref_groups 显式覆盖，零匹配 SC_CNV_NO_REFERENCE 不静默降级）；恶性判定 infercnvpy 路参考分 mean+3sd+CNV 簇多数投票 / cnvturbo 路 HMM 细胞级；GRCh38 坐标 TSV 构建期烘焙（fetch_gene_pos.py，Ensembl GTF→~2MB，断网可用）；产物含 figS3B 式染色体热图/score/subclone UMAP/注释×恶性 csv/亚克隆×染色体 csv（D 报告汇编素材）；TDD 注册单测 3 + 真容器集成测试（合成 chr7 gain/chr10 loss 双后端判定吻合 ≥80% 精度）；CI <id> 绿 |
```

- [ ] **Step 4: 测试总结追加六**——记录：任务链、探针实测结论（Task 1 Step 5 的记录搬入）、集成测试实际数字（通过数/耗时）、全量回归数字、遇到的问题与修法、后续建议（如：真实肿瘤数据集人工核对、D 报告汇编消费 cnv_subclone_by_chromosome.csv、小鼠坐标扩展位）。

- [ ] **Step 5: spec 状态行**——`状态：设计已获用户批准（对话内分节设计稿 "可以，继续"）` 追加 `；已实施（见 ROADMAP 2026-09-10 行与测试总结追加六）`。

- [ ] **Step 6: 全量门禁与回归（Docker Desktop 在线，pg 容器健康）**

```powershell
& .venv\Scripts\python.exe -m ruff check .
& .venv\Scripts\python.exe -m mypy
& .venv\Scripts\python.exe -m mypy --platform linux
& .venv\Scripts\python.exe -m pytest --basetemp=$env:TEMP\pytest_basetemp_full -q
```

预期：ruff 0 违规；mypy 双平台 0 错；回归 **1200+3+2=1205 passed 级别**（1200 全数 + 3 注册单测 + 3 集成测试；skip 仅在无 Docker 环境出现）。

- [ ] **Step 7: 提交推送核 CI**

```powershell
git add docs/平台功能说明书.md docs/使用说明书.md docs/ROADMAP.md docs/superpowers/specs/2026-09-10-scnv-design.md "测试总结+2026-09-09T01-55-00.md"
git commit -m "B1 CNV：文档同步（说明书/ROADMAP/spec 状态/测试总结）"
git push
```

`gh run list --limit 1`（或网页）核 CI 绿；把 CI run id 回填 ROADMAP 步骤 3 的 `<id>` 与测试总结（`git commit --amend` 不用——追加一个小修正提交或直接编辑后随下次提交；选：先推送核绿，再补一行修正提交含 CI id）。

---

## 探针实测记录（2026-09-10）

镜像 `feishu-research-agent/bio:cpu-latest`（infercnvpy 0.6.1 / cnvturbo 0.3.0）内三轮管道探针实测（合成 60 细胞 × chr1/7/10[/X] 各 40 基因，`var` 注入 chromosome/start/end 三列）。TSV 烘焙实测 41092 基因（计划预估 6xxxx 偏高，不影响使用）。

1. **cnvturbo / infercnvpy 均接受 var 三列注入**，无需 from_gtf，回退方案（烘 GTF.gz 全文件）**不启用**。✅ 与计划假设一致。
2. **infercnvpy `tl.infercnv` 的 X_cnv 是窗口级列，不与 var 对齐**（本例 (60,120)→X_cnv (60,3)，window_size=250 时每染色体 1 窗）——❌ 与计划"列与 var 对齐"假设不一致。实测事实：
   - `adata.uns["cnv"]["chr_pos"]` 为 `{染色体名: 该染色体在 X_cnv 中的起始列号}` 字典（如 `{'chr1': 0, 'chr7': 1, 'chr10': 2}`），可据此做窗口→染色体映射；
   - `calculate_gene_values=True` + `inplace=False` 时返回三元组 `(chr_pos: dict, X_cnv: csr_matrix, gene_values: ndarray)`，第三元素为基因级矩阵 (60,120)，**与 var 逐列对齐**——Task 2 infercnvpy 路若需基因级对齐应改走此返回路径；
   - infercnvpy 签名含 `exclude_chromosomes`（正确拼写，默认已排 chrX/chrY）、`step=10`、`key_added="cnv"`。
3. **cnvturbo `infercnv_r_compat` 的参数名是 `exclude_chromosomes`（o 拼写正常），不是计划里的 `exclude_chromsomes`**——❌ 需修正。默认排除 ('chrM','chrMT','MT','M')；显式传 `("chrX","chrY")` 实测**确实剔除列**（含 40 个 chrX 基因时 X_cnv (60,120)→(60,80)）。`adata.uns["cnv"]` 含 `kept_var_names`（保列基因清单，长 80 与 X_cnv 列数一致）、`chr_pos`、`is_gene_space=True` 等——**Task 2 推导列对齐应优先用 `uns["cnv"]["kept_var_names"]`**，比手工剔 chrX/Y 更稳。X_cnv 在未剔除染色体上为基因级、与 var 对齐（✅ 这部分假设一致）。
4. **`compute_hspike_emission_params` 以 adata 为首参 ✅，但返回 4 元组而非 dict**——❌ 需修正：`(emit_means, emit_stds, sd_intercepts, sd_slopes)`（`return_sd_trend=True` 时；各元素形状 (6,)，对应 HMM i6 六状态）。Task 2 的 `emit_p["emit_means"]` 等键名访问必须改为位置解包 `means, stds, intercepts, slopes = emit_p`。
5. **`hmm_call_subclusters`**：
   - `use_rep="cnv"` ✅ 有效（默认值即 `"cnv"`，指 `obsm["X_cnv"]` 经 `infercnv_r_compat(key_added="cnv")` 写入的那套）；
   - precomputed 参数名实为 **`precomputed_emit_sd_intercepts` / `precomputed_emit_sd_slopes`**（计划写的 `precomputed_sd_intercepts` / `precomputed_sd_slopes` 缺 `emit_`，❌ 需修正）；
   - `key_added="cnv_call"` 生效，obs 产出 `cnv_call` / `cnv_call_score` / `cnv_call_expr_deviation` / `cnv_call_subcluster` 四列；`cnv_call` 取值域含 `"Normal"`/`"Tumor"`（本探针合成数据无 CNV 信号，实测全 `"Normal"`）✅ 与计划假设一致。

**Task 2 需修正的调用行汇总**（行为契约不变）：
- `_run_cnvturbo` 内 `infercnv_r_compat(...)`：`exclude_chromsomes=` → `exclude_chromosomes=`；
- `_run_cnvturbo` 内 hspike 返回：`emit_p["emit_means"]` 等四处 → 位置解包四元组；
- `_run_cnvturbo` 内 `hmm_call_subclusters(...)`：`precomputed_sd_intercepts=` → `precomputed_emit_sd_intercepts=`、`precomputed_sd_slopes=` → `precomputed_emit_sd_slopes=`；
- infercnvpy 路列对齐：`x_cnv` 不能假设与 var 对齐——改为 `calculate_gene_values=True, inplace=False` 取第三返回元素（基因级矩阵），或用 `uns["cnv"]["chr_pos"]` 做窗口级染色体映射；cnvturbo 路改用 `uns["cnv"]["kept_var_names"]` 推导 `chrom_col`，替代手工剔 chrX/Y 的假设（`main()` 中 `chrom_col` 推导与对齐断言同步修正）。
