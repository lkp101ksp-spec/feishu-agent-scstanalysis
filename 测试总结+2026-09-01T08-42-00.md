# 测试总结 2026-09-01 08:42（真机轮次更新至 19:00）

## Phase 20：单细胞转录组分析（bio 容器 sc_* 工具链）

### 本轮测试范围

1. **单测（新增 2 个文件，29 用例）**
   - `tests/unit/test_bio_runner.py`（15 用例）：路径白名单（允许/白名单外/空路径/../ 逃逸）、
     dataset_id 幂等键（同路径同大小稳定、大小变化换 id）、容器执行错误链
     （成功 JSON 解析 / 非零退出 / 超时 / 非 JSON 输出 / 脚本 fail() error_code 透传）、
     docker 命令含 `--network none` + `--cpus`/`--memory` 限额。
   - `tests/unit/test_l3_singlecell.py`（6 用例）：5 个 sc_* 工具注册（全部 L1_compute）、
     sc_load 挂载与 ok 字段剥离、SC_PATH_FORBIDDEN 错误输出、sc_qc 参数透传、
     SC_TIMEOUT 透传、sc_plot genes maxItems=6。

2. **集成测试（`tests/integration/test_research_runner.py` 新增 8 用例）**
   - sc_* 节点成功后 umap.png 上传 + 图片消息发送（/ws 容器路径 → 主机 bio_workspace_root 映射）。
   - 未配置 bio_workspace_root 时跳过发图不报错（非 sc 部署兼容）。
   - umap_png/dotplot_png/pngs 三类字段全收集；单图上传失败不中断后续发图。
   - 非 sc_* 工具输出同名字段不发送。
   - 含 sc_* 节点的 plan wall-clock 放大到 research_sc_timeout_sec；纯检索任务维持原超时（回归基线）。

3. **真机端到端冒烟（bio 容器，tiny 10x 数据 80 细胞 × 120 基因）**
   - 链路：load → qc → process（5 clusters）→ markers（wilcoxon）→ plot（violin）。
   - 产物：raw.h5ad / filtered.h5ad / processed.h5ad / umap.png / dotplot.png /
     GENE001_violin.png 全部生成且非空。
   - 白名单校验、容器资源限额（--cpus 2 --memory 8g）、网络隔离（--network none）生效。

4. **全量回归**：`pytest tests` → **767 passed, 8 warnings**（Pydantic V2 deprecation 已知项）。

### 本轮发现并修复的问题

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | bio 脚本 stdin 参数收不到（KeyError: 'path'） | `docker run` 缺 `-i`，此前误判为 PowerShell 编码问题 | bio_runner.py 命令加 `-i` |
| 2 | anndata 无 `read_10x_mtx` | 镜像内新版 anndata 移除该 API | load.py 改用 `scanpy.read_10x_mtx` |
| 3 | 未压缩 10x 目录读不了（只找 matrix.mtx.gz） | scanpy 只认 .gz | load.py 自动临时 gzip 到 /tmp 再读（不改只读 /data） |
| 4 | KeyError: 'mt' | qc_vars 引用 var 列但未先赋值 | load.py 补 `adata.var["mt"] = is_mt` |
| 5 | research_runner NameError: Path | Path 只在方法内局部导入，静态方法引用不到 | 模块顶部统一导入 |
| 6 | pytest 默认临时目录无权限 | `C:\Users\...\Temp\pytest-of-Administrator` 访问被拒 | 测试加 `--basetemp=.pytest_tmp`（项目内） |

### 结论

Phase 20 全链路（工具注册 → 容器执行 → 图片回传 IM → 超时放大）测试通过，
真机冒烟通过，全量回归无破坏。

---

## 真机测试记录（2026-09-01 09:11–14:00，ws_client 长连接 + 飞书 IM 实测）

### 测试环境

- 服务：`python -m gateway.ws_client`（ws 长连接直连飞书）
- 配置：`.env` 新增 `BIO_DATA_ROOTS=I:\飞书agent\bio_test_data`、
  `BIO_WORKSPACE_ROOT=I:\飞书agent\bio_workspace`
- 测试数据：程序生成的 tiny 10x mtx（200 细胞 × 300 基因，3 群结构
  80/80/40，群特异基因 MARKER_A/B/C × 20 + 4 个 MT 基因 + 背景基因）

### 测试轮次与发现

| 轮次 | 任务 | 结果 | 发现 → 修复 |
|------|------|------|------------|
| 1 | /research 全链路（默认参数） | n1 成功 n2 失败 | planner 正确规划 4 节点链；QC 默认 min_genes=600 对低基因数数据全滤光 → **qc.py 失败消息附带数据实际分布（genes/cell median/p90/max）指导调参** |
| 2 | 同上（min_genes=50 等适配参数） | 4 节点全 success | dataset_ref 幂等生效；产物全落盘；但 n3/n4 核心输出被摘要截断 → **outputs_digest 改每节点限 4 行/总 16 行，跳过图片路径字段**；图片上传被拒（缺 im:resource 权限）→ 用户开通 |
| 3 | 同上（权限开通后） | success + 摘要完整 | n_clusters=3、cluster_sizes 80/80/40、markers 全部 MARKER_A/B/C 系（与数据设计完全一致）；图片上传成功但取回字段写错（image_id → **image_key**，im_adapter 修复） |
| 4 | 同上（image_key 修复后） | **IM 收到 UMAP + dotplot 图片** | 图片回传链路验证通过 |
| 5 | 图片写入绑定文档（v1 实现） | 写入失败 1770001 | v1 错误：创建块传 token（必须空对象）+ 上传 parent_node 用了 doc_id（必须 block_id） |
| 6 | 图片写入绑定文档（三步法重写） | **文档出现真实图片块** | 按官方 FAQ 三步流程重写：① 建空 image block → ② drive 上传（parent_node=block_id）→ ③ PATCH replace_image；单图失败跳过不阻塞文本写回 |

### 验证结论

1. **全链路**：/research → planner 4 节点 DAG → bio 容器串行执行 → IM 文本摘要 +
   图片消息 → 绑定文档写入（文本 + 表格 + 真实图片块）全部真机通过。
2. **分析正确性**：toy 数据 3 群被精确还原（80/80/40），marker 基因每簇 top10
   全部命中预设的群特异基因（score 11.97）。
3. **健壮性**：QC 过滤 / 权限缺失 / 上传失败 / 图片插入失败均有清晰错误码或
   降级路径，文本写回不因图片问题阻塞。
4. **全量回归**：776 passed（含本轮新增 docx 三步插图、ImageBlock path 模式、
   摘要防饿死等 9 个新用例）。

### 本轮沉淀的飞书 API 事实（踩坑记录）

- IM 图片：`im/v1/images` 上传返回 **image_key**（不是 image_id）；需 `im:resource` 权限。
- docx 图片三步法：创建时 `image` 必须空对象（带 token → 1770001）；
  素材上传 `parent_node` = image block_id（不是 doc_id）；最后 PATCH
  `replace_image`；需 `drive:file:upload` 权限。

### 后续建议

1. **真实数据验证**：用真实 10x 数据集（如 PBMC 3k）复测，关注 QC 默认参数
   适配性与 sc_process 耗时（>2000 细胞时 PCA/UMAP 显著变慢）。
2. **write_doc 节点图片**：当前自动写回注入图片，模型规划的 write_doc 节点
   不注入；后续可让 write_doc 支持 image 块参数（path 白名单校验）。
3. **workspace 清理策略**：bio_workspace 按 dataset_id 累积 h5ad/png，
   长期运行需要过期清理（可挂 kernel idle sweeper 同款机制）。
4. **GPU 镜像**：大规模数据（>10 万细胞）建议构建 `bio:gpu-latest`
   （rapids-singlecell），BioRunner 已支持镜像参数化切换。
5. **空间转录组扩展**：复用 bio 容器架构（新增 st_* 工具 + 镜像装
   squidpy/spatial-tools）。

---

## 真机批量验收（2026-09-01 16:58–18:30，P15/P17/P18/P19 + write_doc 修复）

### 测试环境

- 服务：ws_client 单实例（清理了双实例：.venv + 系统 Python312 两个长连接
  会导致飞书事件随机分发，测试前必查）
- 绑定文档：wiki 链接 `/bind-doc https://.../wiki/HlTGwdTO4i8VGLkYUnDcgJAxnXb`
  （自动解析为真实 docx id）

### 测试结果

| 项 | 场景 | 结果 | 证据 |
|---|------|------|------|
| P17 | write_doc 节点审批 → 同意 | ✅ | `node_l2 → success`，文档写入成功（18:34） |
| P17 | write_doc 节点审批 → 跳过 | ✅ | `node_l2 → cancelled`（18:22），文档未写入 |
| P15 | 单聊整体写回卡 → 同意 | ✅ | `card_confirm → success`（TP53 任务），文档写入 |
| P15 | 重复点击同卡 | ✅ | toast「该卡片已处理过」；日志 `dup on finished row: status=success → already_handled` |
| P18 | 评论 `/ask 问答` | ✅ | `/ask 这个文档讲了什么？` → 机器人回复 BRCA1 文档摘要（answered=t） |
| P18 | 普通评论不误答 | ✅ | 「这个文档讲了什么？」（无 /ask）、「哈哈」均 answered=f |
| P18 | 删除评论对账 | ✅ | 18:25:02 同步后本地评论表无被删行 |
| P18 | 评论分页拉全量 | ✅ | 单次同步拉到 8-28 旧评论（跨 4 天，page_token 循环生效） |
| P19 | 标签推荐 5 项行为 | ✅ | `scripts/verify_p19_tag_recommend.py` 连真实 PG 验证（事务回滚不落库） |
| P15 | 群聊非发起者 forbidden | ⏸ 顺延 | 需测试群 + 第二账号配合 |
| P15 | 群聊发起者正常写入 | ⏸ 顺延 | 同上 |

### 本轮发现并修复的问题（write_doc 节点路径欠账集中暴露）

| # | 问题 | 根因 | 修复 |
|---|------|------|------|
| 1 | write_doc 节点 `TOOL_EXEC_FAILED: 'DocAdapter' object has no attribute 'append_blocks'` | 8-30 docx 重构改名 render_blocks 时只修了 runner 自动写回路径，**工具节点路径漏改**（P17 无真机验收欠账的直接代价） | l2_side_effect.py 改调 `render_blocks` |
| 2 | planner 传入 blocks 是 repr 风格字符串（`"[{'type': 'text', ...}]"`），非 Block 对象 | planner 节点 inputs 统一 str() 强转 + scheduler 引用替换不还原类型 | serializer 新增 `parse_blocks` 宽松解析（JSON 串 / repr 串 / list[dict] / 单 dict → Block 列表） |
| 3 | 审批卡重复点击返回 `decided`（应 `already_handled`） | broker.wait() 取走决策后条目即清，二次 decide 重新生效 | broker 加 `_consumed` 已消费标记（终态幂等）+ gateway 查 doc_writes.status 非 pending 直接拦截（持久化兜底，双保险） |
| 4 | 错误 doc_id 绑定成功（nzb/nkb 一字之差），写入时才 1770002 失败 | bind_doc_service.bind() 只做格式正则校验，不查文档存在性 | 待改进项：bind 时调 docx get 校验存在性（记入后续建议） |
| 5 | write_doc 工具描述过于简陋（"追加块到飞书 doc"），planner 只能盲猜 blocks 结构 | 描述没给 blocks 形态示例 | 工具描述补 blocks 形态与上游引用示例（'n3.summary'） |

### 验证结论

1. **P17 全通过**：节点级 L2 审批 approve/deny 双路径真机验证（deny → cancelled，
   下游无写动作）。
2. **P18 全通过**：/ask 问答 / 普通评论不误答 / 删除对账 / 分页拉全量。
3. **P19 全通过**：共现优先、热度兜底、limit 截断、排除无交集、404 五项行为。
4. **P15 单聊通过**：写回审批 + toast 三态 + 幂等；群聊 2 场景顺延（需第二账号）。
5. **全量回归**：785 passed（+9：broker 终态幂等 1、gateway 持久化兜底 1、
   parse_blocks 6、write_doc handler 2 中的 1 个计入 builtin）。

### 后续建议（新增）

1. **bind-doc 存在性校验**：bind() 时调 `docx get` 验证 doc_id 真实存在，
   拒绝一字之差的坏 ID 在绑定环节（本次 nzb/nkb 踩坑）。
2. **群聊场景补测**：P15 场景 3/4（非发起者 forbidden / 发起者群聊写入），
   需要测试群 + 第二账号。
3. **双 ws_client 防复发**：本次又出现双实例（.venv + 系统 Python），建议
   ws_client 启动时写 pidfile + 检测存量连接互斥。
