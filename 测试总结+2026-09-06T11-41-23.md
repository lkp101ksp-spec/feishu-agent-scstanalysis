# 测试总结 2026-09-06T11-41-23 — Phase 37 前置探针（muon 兼容性 / bio 镜像基底 / R 包源可达性）

## 任务
Phase 37 前置探针：① 宿主 venv 安装 muon 并跑迷你 WNN 端到端；② 确认 bio 镜像基础镜像的
Debian/R 版本；③ 探测 P3M/CRAN 可达性与 scTenifoldKnk 在架情况。未改动任何项目代码；
探针脚本为 `code_workspace/probe_muon.py`。

## 1. muon 安装（清华源）
- 装上 **muon 0.1.9 + mudata 0.4.1**，连带新装仅 protobuf 7.36.1。
- 核心包前后对比：pandas 2.3.3 / numpy 2.5.2 / scanpy 1.12.4 / anndata 0.13.3.post0 —— **均未变动**。

## 2. 迷你 WNN 端到端（200 细胞，RNA 60 基因 poisson 3 簇 + ADT 8 蛋白簇相关）
全流程（RNA normalize+log1p+scale+PCA20；ADT CLR+PCA7；sc.pp.neighbors×2 →
mu.pp.neighbors → mu.tl.umap → mu.tl.leiden）**全绿，全部断言通过**：
- `X_umap` 在 `mdata.obsm`；`leiden` 在 `mdata.obs`（3 簇）。
- **mod_weight 实际位置：`mdata.obs["rna:mod_weight"]` 与 `mdata.obs["adt:mod_weight"]` 两列**
  （非单一矩阵，不在 obsm），每细胞两列之和 = 1.000000。
- **leiden vs 注入簇 ARI = 1.0000**（>0.8 达标）。

### 踩坑记录（重要）
1. **`mu.pp.neighbors(mdata)` 用默认参数在本探针规模下必崩（0xC0000374 堆腐化）**：
   默认 `n_multineighbors=200` ≥ n_cells=200 → pynndescent 返回含 **-1** 的索引
   （实测 neg_count=200）→ muon 不校验直接建 csr_array → scipy `_binopt`/`_add_sparse`
   堆腐化崩进程（faulthandler 栈：muon/_core/preproc.py:543 `neighbordistances += graph`）。
   绕法：**显式传 `n_multineighbors`（本探针用 30）**。真实数据（数千细胞）默认 200 无此问题，
   但 sc 工具实现建议 `n_multineighbors=min(200, n_obs-1)` 防御。
2. **新版 muon 要求各模态先跑 `sc.pp.neighbors`**，否则
   `ValueError: Did not find .uns["neighbors"] for modality "rna"`。
3. ADT PCA n_comps=8 在 8 特征下 arpack 非法（`ValueError: n_components=8 must be strictly
   less than min(n_samples, n_features)=8`），探针用 7。
4. 良性警告：pynndescent "Failed to correctly find n_neighbors"（仅默认参数崩溃路径）；
   muon/mudata 内部 `__version__` FutureWarning ×3；umap-learn 的 Tensorflow ImportWarning。

## 3. bio 镜像基底
- [bio.Dockerfile](sandbox/bio.Dockerfile) `FROM python:3.12-slim`。
- **实测 `python:3.12-slim` 现已是 Debian 13 (trixie)**（非 bookworm！）：
  `PRETTY_NAME="Debian GNU/Linux 13 (trixie)"`；`apt-cache policy r-base-core` 候选 **4.5.0-3**（trixie/main）。
- 本地 `feishu-research-agent/bio:cpu-latest` 实测同为 **trixie**。
- 推论：Phase 37 若装 R，apt 直装即 R 4.5.0；P3M URL 应改用 `__linux__/trixie`（bookworm URL 与基底不符）。

## 4. R 包源可达性
| 项 | 结果 |
|---|---|
| P3M bookworm src `PACKAGES.gz` | **HTTP 200**（`Server: Posit Package Manager v2026.08.0`），含 scTenifoldKnk 1.1 |
| P3M trixie src `PACKAGES.gz` | **HTTP 200**，含 scTenifoldKnk 1.1（与 trixie 基底匹配，推荐） |
| 清华 CRAN scTenifoldKnk 页面 | 在架，citation meta 标注 **version 1.1** |
| scTenifoldKnk CRAN 版本 | **1.1**（P3M src 与清华一致）；`NeedsCompilation: no`（纯 R 包）；硬依赖 scTenifoldNet(>=1.4)、enrichR、igraph 等 |
| P3M bookworm 二进制 | **未知**：`/bin/contrib/<R ver>/PACKAGES.gz` 对 bookworm/jammy/noble × R4.2–4.8 全 404（说明该直链非有效探测路径）；带 R UA 请求 src/contrib 索引仍返回源码索引（无 `Built:` 字段）。宿主 curl 无法定论，需容器内真 R 安装验证。scTenifoldKnk 为纯 R 包，无二进制也可快速源码安装 |

## 结论
- **muon 0.1.9 与现有栈（pandas 2.3.3/numpy 2.5.2/scanpy 1.12.4/anndata 0.13.3）兼容，WNN 全链路可用**，
  但工具实现需注意：先 per-modality `sc.pp.neighbors`、显式 `n_multineighbors`、mod_weight 读取 `obs["<mod>:mod_weight"]`。
- bio 基底已是 **Debian 13 trixie + apt R 4.5.0**，P3M 用 trixie 仓库；scTenifoldKnk 1.1 在架且纯 R。

## 后续建议
1. Phase 37 实现 `sc_wnn`（muon）时封装：`sc.pp.neighbors` per modality → `mu.pp.neighbors(n_multineighbors=min(200,n_obs-1))` → umap/leiden；权重从 `obs["<mod>:mod_weight"]` 读。
2. bio 镜像如需 R：Dockerfile 加 `apt-get install -y r-base-core`（4.5.0）+ P3M trixie 源装 scTenifoldKnk；
   其依赖 scTenifoldNet/enrichR 同库在架（安装时一并解析）。
3. 容器内复验一次 P3M 是否对 trixie R4.5 UA 提供二进制（影响安装耗时预期）。
4. bio.Dockerfile 注释中的 bookworm 假设（若有）应随 trixie 基底更新。

---

# Phase 37 实施节（2026-09-06）— sc_wnn WNN 多组学 + sc_knockout 虚拟敲除

## 交付
- `sandbox/sc_tools/wnn.py` + `sandbox/sc_tools/knockout.py` + `sandbox/r_tools/knk.R`
- `orchestrator/tools/builtin/l3_singlecell.py`：sc_wnn / sc_knockout 两 handler + 两 ToolSpec
  （L1_compute，timeout 1200/3600）；docstring Phase 列表 + `21 个` → `23 个`
- `sandbox/bio.Dockerfile`：muon 层（清华源）+ R 层（apt r-base 4.5.0 + P3M trixie 装
  scTenifoldKnk，同层 purge r-base-dev/g++）+ `COPY r_tools/ /opt/r_tools/`
- `tests/unit/test_l3_singlecell.py`：+6 用例（注册/超时、同根单挂、异根双挂、schema
  required、knockout 参数转发、R 错误透传）
- sc_* 工具数 **21 → 23**

## 实施期修复（四处，均最小改动并同步 sandbox 脚本）
1. **knk.R 参数名对齐 CRAN 1.1 实测签名**：`qc_mtThreshold` → `qc_maxMTratio`、
   `qc_minLSize` → `qc_minLibSize`（容器内 `args(scTenifoldKnk)` 实证，旧名为旧版本签名）。
2. **wnn.py 删冗余 import**（ruff 告警清零）。
3. **wnn.py `_read` 容器绝对路径容忍分支**：异根双挂时 handler 传 `/data_adt/<rel>`，
   `_read` 开头加 `if rel.startswith("/"): return ad.read_h5ad(Path(rel))`
   （白名单校验已在 handler 层 resolve_data_path 把关，计划 Task 3 预判的最小修复落实）。
4. **sc_wnn handler 删 ruff F841 冗余 `mounts` 预赋值**（同根/异根分支已各自显式传 mounts）。

## 冒烟结果
- **wnn 宿主冒烟一次通过**（`code_workspace/smoke_phase37.py`，240 细胞，RNA 60 基因 3 簇
  + ADT 8 蛋白簇相关）：leiden vs 注入簇 **ARI=1.000**（>0.8 达标）；wnn_weight 两列每细胞
  和=1；**ADT 权重 0.682 > RNA 0.318**，符合假数据信噪比设计（ADT 信号更干净）；新数据集
  processed.h5ad/raw.h5ad 生成，raw/X_umap/leiden/权重列齐备。
- **knockout 断网容器冒烟**（400 细胞 × 320 基因，SPI1 驱动 9 靶基因模块，
  n_genes=300/n_net=5/n_cells=300）：**86 秒**跑通（远 < 30 分钟预期）；diffRegulation
  **top-20 回收注入靶基因 7/9**（≥3 达标），**SPI1 自身 Z 值第一**（算法自洽——敲除
  基因本身差异最大）；产物 diffRegulation.csv + knockout_volcano.png 落
  smoke_ws37ko/ko37/knockout/SPI1/。

## 镜像与回归
- 镜像 **bff5e3bf62b2**；断网验证：`py ok 0.1.9` + `R ok 1.1`。
- R 层构建仅 **67.9s**——P3M trixie 二进制全命中（探针遗留问题 3 闭环：UA 设置后
  trixie R4.5 有二进制，无源码编译等待）。
- 单测 **53 passed**（47+6）；全量回归 **1051 passed**（57.39s），0 failed。

## GHIST 暂缓说明
toolsv1 的 GHIST 实现为 **PyTorch UNet3+ 从零训练框架**：需 Xenium 级输入 5 类文件 +
核分割预处理链，无预训练权重、建议 24GB 显存——属独立赛道投入，非本批顺手可做。
已记 ROADMAP 远期池：「待 GPU + Xenium 数据齐备后立项」。

## 后续建议 / 待办
1. **统一测试轮真机验收**（用户决策延后）：Phase 31-37 累计 **18 个工具**待逐个真机测
   （31 sc_enrichment / 32 score/metabolism/pseudotime / 33 de/subcluster/integrate/cellfreq /
   34 cellchat/milo/deconv / 35 annotate/meta/doublet/cellcycle / 36 scenic / 37 wnn/knockout）。
2. wnn 真机用真实 CITE-seq 数据验收时关注共享细胞数（<50 报错）与模态权重合理性。
3. knockout 真机注意 gko 须在 HVG 集合内（报错会列近似匹配）；大 n_net/n_genes 下耗时
   线性增长，先小参试跑再放大。

---

# 统一测试轮 · 镜像重建验证（2026-09-06）— 小鼠两库 + sc_tools 改动

## 任务
`sandbox/fetch_gene_sets.py` 新增小鼠两库（KEGG_2019_Mouse / WikiPathways_2019_Mouse），
sc_tools 多脚本改动（common/cellcycle/metabolism/enrichment），重建 bio 镜像并断网验证。
纯 docker 任务，未写代码。

## 构建
- `docker build -t feishu-research-agent/bio:cpu-latest sandbox -f sandbox/bio.Dockerfile`
  **exit 0 成功**。gene_sets 预取层联网 Enrichr 重跑正常；sc_tools/r_tools COPY 层全重建。
- 镜像 ID：**74d59b4036fa**（sha256:74d59b4036fad146125b9955a2d8ffe921908f1fe079bbc42af5db3c6c18d78d）。

## 断网验证（--network none，均 exit 0）
a. **基因库齐全**：`['go_bp.json', 'hallmark.json', 'kegg.json', 'kegg_mouse.json',
   'wikipathways_mouse.json']` 5 库齐备；`kegg_mouse terms: 303`。
   - 注意偏差：样本基因为 **全大写**（`ABCA2/ABCC4/TAP1` 等），非预期的 `Mki67` 式
     小鼠大小写。核查确认：fetch_gene_sets.py **无大小写归一逻辑**（grep upper/lower/
     case 无匹配），即 Enrichr 小鼠库原始返回即为大写符号。此与 sc_tools 设计自洽
     ——`common.upper_gene_map` 正是为此做小鼠符号→大写库基因的映射（见 b）。
b. **模块导入 + 助手可用**：`common.upper_gene_map(['Mki67','Mcm5','Foo'],
   ['MKI67','MCM5','BAR'])` → **`['Mki67', 'Mcm5']`**（大小写不敏感匹配 + 保留输入
   原始拼写 + 剔除无匹配项，行为全对）；`cellcycle, metabolism, enrichment, wnn,
   knockout, scenic, annotate, doublet, cellchat, milo, deconv, meta` **全部导入成功**，
   输出 `all imports ok`。
- 两次运行仅有 sitecustomize 的 np.object/np.str FutureWarning（良性，既有现象）。

## 结论
- 镜像重建成功，5 基因库（含小鼠两库）离线可用，sc_tools 12 模块与新助手函数全部就绪。
- 无报错。

## 后续建议
1. enrichment 真机跑小鼠数据时以 `upper_gene_map` 匹配结果为实际覆盖率为准
   （小鼠库符号为大写，直接 exact-match 会全空，必须走该助手）。
2. 进入 Phase 31-37 共 18 个工具的真机验收时，统一使用本镜像 74d59b4036fa。

# 小鼠库大写符号适配修复（2026-09-06 后续小修）

## 背景
镜像重建验证已确认 Enrichr 小鼠库（KEGG_2019_Mouse / WikiPathways_2019_Mouse）
基因符号为全大写（ABCA2 式），而小鼠数据 var 名是 Mki67 式，大小写敏感交集全落空。

## 改动（sandbox/sc_tools，未重建镜像，待验收驱动前统一重建）
1. **metabolism.py**：通路基因交集处分支——`species="mouse"` 时
   `matched = upper_gene_map(sorted(raw_vars), genes)`（import 自 common），
   `species="human"` 保持精确匹配不变；emit 无变动。
2. **enrichment.py**：ORA（gp.enrich gene_list）与 GSEA（prerank rnk）两处输入
   基因列表，在所选库别名以 `_mouse` 结尾时先做 `.upper()` 再传入；人源库不动。

## 验证
- **ruff**：两文件 `All checks passed!`（初次 I001 import 排序告警，改单行 import 后清零）。
- **宿主功能自测**（交集逻辑，未跑全 metabolism）：
  `raw_vars=['Abca2','Mki67']`, `genes=['ABCA2','MKI67']` →
  mouse 分支 `upper_gene_map` 返回 **`['Abca2','Mki67']`**（原始 var 名，符合预期）；
  human 精确匹配分支返回 `[]`（验证大小写敏感旧行为确实全落空）。
- **单元回归**：`pytest tests/unit/test_l3_singlecell.py` → **55 passed**。
- **全量回归**：`pytest tests -q --basetemp=.pytest_tmp` → **1053 passed**
  （基线 1051 + 此前物种适配 2 个，无回归）。

## 后续建议
1. 本修复属 sc_tools COPY 层，真机验收前需随下一轮 bio 镜像统一重建后生效。
2. 真机跑小鼠 metabolism/enrichment 时，建议顺带核对 `n_pathways_scored` 与
   ORA 命中数非空，作为大写对齐端到端生效的旁证。

---

# 统一测试轮缺陷修复（2026-09-06）— l3_singlecell handler timeout_sec 全量补齐

## 背景
统一测试轮真机验收发现：仅 scenic/wnn/knockout 的 handler 把超时传给
`runner.run(timeout_sec=...)`，其余 20 个 handler 静默回退 BioRunner 默认 900s，
与各 ToolSpec 声明值（600/1200/1800）不符，导致 cellchat（59k 细胞 >900s）
与 milo 被误杀。

## 改动（仅 orchestrator/tools/builtin/l3_singlecell.py）
- 21 处 `runner.run` 调用补齐显式 `timeout_sec`（sc_meta merge_csv/其他两分支均补）：
  sc_load/qc/plot/cellfreq/meta×2/cellcycle=600；markers/pseudotime/de/deconv/doublet=1200；
  process/enrichment/score_genes/metabolism/subcluster/integrate/cellchat/milo/annotate=1800。
  补齐后与 23 个 ToolSpec.timeout_sec 逐一核对一致；scenic/wnn/knockout 原有传值未动。
- `register_l3_singlecell` docstring 增约定："新增工具时 handler 的 runner.run
  timeout_sec 必须与 ToolSpec.timeout_sec 一致（缺失会静默回退默认 900s）"；
  顺手把过时描述 "6 工具" 更正为 "23 工具"。

## 验证
- **ruff**：`All checks passed!`（零告警）。
- **单测**：`pytest tests/unit/test_l3_singlecell.py -q --basetemp=.pytest_tmp` →
  **55 passed**（既有断言均为选择性键断言，无 kwargs 全等比较，测试文件无需改动）。
- **全量回归**：`pytest tests -q --basetemp=.pytest_tmp` → **1053 passed, 0 failed**
  （与基线一致）。

## 后续建议
1. 本修复为 orchestrator 层（宿主代码），不依赖镜像重建，验收脚本
   `code_workspace/acceptance_round.py` 可直接用新代码重跑 cellchat/milo 等失败步骤。
2. 建议后续加一条注册期断言（如 registry 层校验 handler 调用超时与 spec 一致）
   或在测试中对每个 sc_* handler 断言 `runner.run.call_args.kwargs["timeout_sec"]
   == spec.timeout_sec`，从机制上防止再犯。

---

# 耗时工具抽样上限参数（2026-09-06）— sc_cellchat / sc_milo

## 改动
1. **sandbox/sc_tools/cellchat.py**：新增 `max_cells_per_group`（int，默认 0=全量）。
   >0 时在过小细胞类型剔除后、liana 调用前按 celltype_col 分层抽样
   （每组 `min(参数值, 组大小)`，固定 `random_state=42`，无 seed 参数）。
   emit 增 `n_cells_used` 与 `subsampled`（bool，按实际细胞数是否减少判定），
   抽样时追加 `note`（注明分层口径与 总数→抽样数）。
2. **sandbox/sc_tools/milo.py**：新增 `max_cells_per_sample`（int，默认 0=全量）。
   >0 时在样本/分组校验后、KNN 前按 **sample_col** 分层抽样（保样本复制结构，
   milo GLM 统计必需；不按 group_col），固定 `random_state=42`。
   emit 增 `n_cells_used`/`subsampled`；`method_note` 在抽样时追加抽样说明。
3. **orchestrator/tools/builtin/l3_singlecell.py**：两 handler 增对应参数
   （默认 0）并转发 payload；两 schema properties 同步（default 0 + 中文
   description）。docstring 与工具计数未动；timeout 未动。
4. **code_workspace/acceptance_round.py**：步骤 11/23（sc_cellchat 小鼠/人源）
   显式 `max_cells_per_group=100`；步骤 12/25（sc_milo 小鼠/人源）显式
   `max_cells_per_sample=100`（测试轮驱动抽样加速；产品默认仍为全量）。
5. **tests/unit/test_l3_singlecell.py**：尾部物种适配节后新增 2 用例
   （转发 100 到 payload + schema 默认 0 断言）。

## 与规格偏差
- 规格称「cellchat/milo 的 handler timeout 已是 3600」，实际 sc_cellchat handler
  的 `runner.run(timeout_sec=1800)`（上一轮 timeout 补齐时与当时 ToolSpec 一致的
  1800；ToolSpec 现为 3600）。按指示未动，是否对齐到 3600 待用户决策。

## 验证
- **单测**：`pytest tests/unit/test_l3_singlecell.py -q --basetemp=.pytest_tmp`
  → **57 passed**（55+2）。
- **ruff**：5 个改动文件 `All checks passed!`（零告警）。
- **全量回归**：`pytest tests -q --basetemp=.pytest_tmp` → **1055 passed**,
  0 failed（基线 1053 + 2）。日志中 `RuntimeError: docker down` /
  `kernel idle sweep failed` 为 gateway ws_client 后台线程 mock 注入的既有噪声，
  非本次改动引入（pytest 退出码 0）。

## 后续建议
1. 本改动含 sc_tools COPY 层（cellchat.py/milo.py），真机验收前需随 bio 镜像
   统一重建后生效（用户统一 docker build）。
2. 重跑统一测试轮 11/12/23/25 步骤验证抽样口径：emit 应见 `subsampled=true`、
   `n_cells_used` ≈ 组数×100 / 样本数×100，cellchat note / milo method_note 含
   抽样说明。
3. 若对齐 handler timeout 至 3600，顺带在单测断言
   `runner.run.call_args.kwargs["timeout_sec"] == spec.timeout_sec`（沿用上节
   建议 2 的机制化防线）。

---

# 验收轮执行记录（2026-09-06 下午，final2→final5）

## 轮次概览
- final2（--from 11）：cellchat 小鼠 251.2s PASS（475511 pairs，抽样 100/组）；
  milo 19.5s PASS（62 nhoods，抽样 100/样本）；annotate 40.3s PASS（n_labels=3）；
  doublet FAIL（scrublet stdout 污染 JSON 契约）；cellcycle PASS（case_mapped=true，
  小鼠大小写适配生效）。
- final3（--from 14）：doublet 修复后 PASS（251.7s）；scenic 小鼠 FAIL（pyarrow
  schema 重名/缺失）；knockout 小鼠 FAIL（parallelly 300% 硬限制）；wnn PASS；
  deconv FAIL（bulk34.csv 与 tiny 基因名不配）；人源 cellcycle/metabolism PASS；
  enrichment 人源 FAIL（group="" 未回退默认簇）。
- final4（--steps 16,17,19,21,23..30）：knockout 小鼠 PASS（367.5s，top1=Spi1 自体
  命中）；deconv PASS（bulk_tiny.csv 伪 bulk）；enrichment 人源 PASS；cellchat
  人源 PASS；annotate 人源 PASS（celltypist 13 labels）；de 人源 PASS；
  doublet 人源 PASS；integrate 人源 PASS；scenic 两物种仍 FAIL（ctxcore）；
  milo 人源 FAIL（每组仅 1 样本，统计不可用）；knockout 人源 FAIL（SPI1 不在
  top1000 HVG）。

## 修复清单（本轮）
1. **doublet.py**：scrublet 进度打印污染 stdout → `contextlib.redirect_stdout`
   守卫包裹全部 scrublet 调用。
2. **scenic.py**：(a) 同名基因去重（10x 矩阵 var 重名 → pyarrow schema 冲突）；
   (b) ctxcore 0.2.0 增量 prefetch 缓存 bug（difference 未排除已加载列 →
   append_column 重名 → KeyError 'Field X exists 2 times in schema'）→
   `_prune` 内 monkeypatch `_prefetch_as_pyarrow_table`，置空 df_cached 强制
   全量重读。锁版本不可行：ctxcore 0.1.1 sdist 在 Py3.12 拉老 numpy 源码编译失败。
3. **knk.R**：容器内 detectCores() 见宿主 24 核 vs cgroups 4 核 → parallelly
   300% 硬限制报错；`options(parallelly.maxWorkers.localhost=...)` 覆盖放行。
4. **enrichment.py**：`args.get("group", groups[0])` 因 handler 恒传 "" 失效 →
   改 `or groups[0]` 空串回退默认首簇。
5. **测试数据**：新增 bio_test_data/bulk_tiny.csv（tiny 伪 bulk，190 基因×3 样本，
   按 leiden 聚类求和）；人源 processed.h5ad 增 obs `sample2` 伪重复列
   （sample×index%3，每组 3 重复，供 milo GLM）。
6. **驱动**：新增 --steps 参数（逗号分隔步骤号，优先级最高）；scenic 16/28
   max_cells=100+n_workers=2；knockout 17/29 n_genes=1000/2000+n_net=3；
   deconv 换 bulk_tiny.csv；milo 人源 sample_col=sample2。

## 已知数据限制
- 人源 milo：真实样本每组仅 1 个（A/B），GLM 统计上不可用；验收用 sample2 伪
  重复仅验证代码路径，生物学结论无效。正式分析需每组 ≥2 生物学重复。

## 待办
- final5 重验 16/25/28/29（scenic 修复后首验）。

## scenic ctx 假死根因定位（2026-09-06 晚，final5-7 调试）

### 现象
scenic 步骤在驱动（BioRunner，--cpus 4 --memory 16g）下稳定假死：RSS ~8-10GB、
CPU 3%、70+ 线程全 futex 等待，直至 3600s 超时；同镜像手动跑（无资源限制）36 分钟
正常完成（180 regulons）。

### 排除过程
1. 误判 1：ctxcore 0.2.0 增量 prefetch 缓存 bug（KeyError Field X exists 2 times）——
   真实存在，monkeypatch 修复后 KeyError 消失但假死依旧。
2. 误判 2：挂载盘随机列读 I/O 慢——改顺序整表读后读秒级完成，假死依旧。
3. A/B 隔离：仅加 --memory 16g 即复现假死；不加内存限制（即使有 --cpus 4）正常。
4. 根因：distributed worker 内存管理按 cgroup 限额算 spill 阈值（0.6×16=9.6GB），
   并集缓存表把 RSS 顶到阈值 → worker 陷入 spill/pause 死循环假死。
   修复：ctx 的线程 LocalCluster 加 `memory_limit=0`（关闭内存管理）；
   并集预取加 threading.Lock 防双线程并发全读大表（OOM 风险）。

### 佐证
- 修复后生产限额下小鼠 scenic 全链 PASS：grn 246s + ctx 1844s + aucell 3s，
  180 regulons，RSS 稳定 6.4GB（此前 9.6GB 顶阈值）。
- 全量回归 1055 passed 无破坏。

### 工具链教训
- **同文件并行 SearchReplace 会丢第一处编辑**（双写竞争），已发生 4 次
  （acceptance_round 小鼠 scenic 参数、--steps 参数、knockout 小鼠参数、
  scenic.py _log 定义）。规则：同文件多处修改必须串行执行。
- 容器假死诊断路径：docker stats（CPU/MEM）→ ps aux（子进程有无）→
  /proc/pid/task/*/wchan（线程阻塞点）→ stderr 埋点日志（stdout 是 JSON 契约）。

## 统一验收 final8-9（2026-09-07 凌晨）

### final8 结果（镜像 bec76e6246f4，--steps 16,25,28,29）
- [16] scenic 小鼠 FAIL（2183s）SC_OUTPUT_INVALID：计算全程成功（180 regulons，JSON 已发），
  但 pyscenic df2regulons 往 stdout 打印 "Create regulons from a dataframe..." 破坏契约。
- [25] milo 人源 FAIL（11s）：驱动步骤25 sample2 编辑被同文件并行 SearchReplace 丢失
  （累计第4次），sample_col 仍是 sample → A=1,B=1。
- [28] scenic 人源注定同 [16] 因失败 → 提前终止 final8，省约 45 分钟无效计算。

### 修复
1. scenic.py：df2regulons 调用加 contextlib.redirect_stdout 守卫（同 doublet 修复模式）。
2. acceptance_round.py 步骤25：sample_col 改回 sample2（本次串行编辑，已确认生效）。
3. 镜像重建 d7cc8983723c（仅 COPY 层变动，秒级）。

### final9 进行中（--steps 16,25,28,29）
- [16] 小鼠 scenic：产物 6 件全落盘（23:47，约38分钟），等契约判定。
- [25] milo 人源：23:47→23:51 约4分钟跑完进入下一步（无早期崩溃）。
- [28] 人源 scenic 运行中；[29] knockout 人源待跑（约6分钟）。
- 注：驱动 stdout 块缓冲（<8KB 不刷盘），日志需进程退出才可见；
  中途进度靠产物时间戳 + docker ps 观测。

### 当前验收总账：26/30 PASS，剩余 16/25/28/29 待 final9 判定。

### final9 结果（2026-09-07 00:50，镜像 d7cc8983723c，--steps 16,25,28,29）
- [16] sc_scenic 小鼠 PASS（2240.7s）180 regulons —— stdout 守卫生效，契约干净。
- [25] sc_milo 人源 PASS（11.7s）71 nhoods —— sample2 伪重复（3/组）生效。
- [28] sc_scenic 人源 PASS（2470.3s）205 regulons。
- [29] sc_knockout 人源 PASS（752.5s）SPI1 top1 自命中（n_genes=2000 生效）。

## 统一验收收官：30/30 PASS（100%，18 工具 × 双物种 + 基座链路）
- 验收报告：code_workspace/acceptance_report.md（驱动自动落盘）。
- 遗留观察：scenic 双物种各约 37-41 分钟（max_cells=100 下 ctx 阶段由
  基因/模块数主导而非细胞数，属 pyscenic 固有开销）；生产全量分析会更久，
  属预期。回归后接 ut-6 真 LLM 抽查、ut-7 飞书端抽查、ut-8 commit。
- 全量回归（.venv）：`pytest tests -q --basetemp=.pytest_tmp` → **1055 passed, 0 failed**（57.3s）。
  注意：裸 `pytest` 会走系统 Python（缺 lark_oapi，17 收集错误），必须用 .venv。

## ut-6 真 LLM 规划抽查（2026-09-07 01:2x，scripts/diag_llm_spotcheck.py）

### 机制确认（重要架构事实）
- 普通自然语言消息走 process() 无工具闲聊路径（无 tools schema），模型会
  幻觉 <function_calls> 裸文本——不是 bug，是入口设计。
- sc_* 工具只有两个生产入口：/research（Planner DAG，schema 内联 prompt）
  和 /code（chat_with_tools 原生 function calling）。
- /research 是 daemon 线程异步执行：进程退出即杀任务线程。抽查脚本必须
  驻留并按 message_id 轮询 tasks 表终态（_TERMINAL_STATUSES）。

### 抽查结果 3/3 全成功（真 LLM + 真 Docker 沙箱 + 真 IM 回复）
1. /research 对 f1e89bf88edc 做双联体检测 → success，sc_doublet：
   300 细胞、3 双联体、rate=0.01。
2. /research 对 b7a44c75464f 做细胞周期评分 → success，sc_cellcycle：
   s_genes=42 / g2m_genes=53 / case_mapped=false（人源大写符号直配）。
3. /research 对 50165a559c91 按 leiden 做 Hallmark 富集 → success，
   sc_enrichment：n_ora_genes=300、gene_sets=[hallmark]、group=0
   （空串默认回退修复在真 LLM 链路同步生效）。

### 后续建议
- 产品决策点：是否让普通自然语言也进 planner（当前必须 /research 前缀，
  用户不知道就当成闲聊）。可加入口级意图预判或把默认对话链切到
  chat_with_tools。
- diag_llm_spotcheck.py 可复用为常规划查工具（argparse 多指令）。

## ut-7 飞书端抽查（2026-09-07 凌晨，用户真机实测）

### 发现的两个生产问题与修复
1. **/model 全拒**：.env 缺 FEISHU_ADMIN_OPEN_IDS（ROADMAP 早已标注
   "真机验收待配置 admin 名单"）。修复：写入用户 open_id
   ou_19923f2cacdb13f7b998187f115c1903（从 sessions 表真实消息反查）。
2. **SC_PATH_FORBIDDEN 规划歧义**：LLM 把既有数据集引用 f1e89bf88edc 当
   文件路径规划 sc_load（n1 failed，后续 n2-n4 级联 skipped）。双保险修复：
   - sc_load handler 直通：path 若是 workspace 下已存在的数据集目录名
     （纯名字无分隔符），直接返回该 dataset_ref，不重复加载；
     getattr 防御 mock 无 workspace_root。
   - sc_load 工具描述补提示：既有 ref 直接给下游，不要重复 sc_load。
   - 新增单测 test_sc_load_passthrough_existing_dataset_ref；
     l3 单测 58 passed。

### 验证
- 管理员名单进程内解析正确。
- 同指令 "/research 对 f1e89bf88edc 做双联体检测" 复测 success
  （300 细胞/3 双联体）。
- ws_client 已重启（pid=81204，--force 接管），新 .env+代码生效。
- 待用户复测：/model 卡片 + 任意 /research 指令。

### 教训
- 真机抽查的价值：LLM 规划非确定性，同一指令可能走出不同 DAG；
  工具 handler 对"模型常见误用"做宽容直通比只改 prompt 更稳。

### /model 卡片标题渲染 bug（ut-7 截图发现，已修复）
- 现象：卡片标题渲染出 Python dict repr（{'title': {'tag': ...}}）。
- 根因：IMAdapter.send_card 假定 header 是字符串标题，对 dict 形态做
  str() 后塞进 title.content。/model 与 /code 审批卡共 3 处调用方传的是
  完整 header dict——即 /code 审批卡在生产上也有同款 bug。
- 修复：send_card 归一化——header 为 dict 则透传，字符串才包 title；
  config 字段也透传。新增 test_im_send_card_sdk_dict_header_passthrough。
- 回归：adapters+l3 共 85 passed。ws_client 已重启生效（pid=82284）。
- 候选池加模型方法（config/llm.yaml 注释已有）：providers 段加条目
  （name + 三个 env 变量名）→ .env 加对应变量 → 重启，按钮自动生成；
  缺 env 的条目启动时自动跳过。

## /model 双卡片交互改造（2026-09-07 上午，用户需求）

### 需求
卡 1 只做主/备槽位入口；卡 2 列出候选池全部模型，点击即切换。

### 实现（回调响应卡原地换卡面，P2CardActionTriggerResponse.card）
- model_switch_service.status_card：按钮改为「切换主模型/切换备模型」
  （action=model_pick, slot）；新增 picker_card(slot)：候选池全量按钮
  （name · model，当前在任 ✓+禁用）+「← 返回状态」(model_back)；
  action 组按 4 个/组分段（飞书上限）。
- gateway/app.py：新增 model_pick/model_back 回调分支（admin 校验在
  service 内）；model_switch 成功时附最新状态卡。
- gateway/ws_client.py：card_result_to_response 处理 model_pick/
  model_back（CallBackCard type=raw 原地替换卡面，无 toast）；
  model_switched 保留 toast 并附状态卡刷回卡 1。
- 关键认知：回调响应卡更新 ≠ IMAdapter.update_card（后者仍无），
  双卡片交互在一张卡上原地完成，不刷屏。

### 测试
- model_switch_service 25 + ws_client 新增 2 + renew_card_callback 集成
  4 全过；全量回归 **1064 passed**。

### /model 点按钮报「切换不可用」（ut-7 复测发现，已修复）
- 根因：build_runtime 里 ModelSwitchService 构造在 create_app 之后，
  只挂了 orch.model_switch_service（/model 出卡正常），而卡片回调走
  process_card_payload → app.state.ctx，ctx.model_switch_service 恒为
  None → model_pick/model_switch 分支均返回 model_switch_unavailable。
  集成测试直接往 create_app 传 service，没覆盖装配缺口。
- 修复：runtime.py 构造后补挂 app.state.ctx.model_switch_service；
  test_build_runtime_assembles_real_graph 增双通道断言（orch+ctx）。
- 回归 test_ws_client 25 passed；ws_client 重启生效（pid=76032）。
- 教训：服务有「出卡路径」和「回调路径」两个挂载点时，装配测试必须
  两个都断言。

## ut-7 收官 + ut-8（2026-09-07 上午）

### 用户复测确认
- /model 双卡片流程端到端通过：卡 1 状态+槽位入口 → 点「切换主/备模型」
  原地换候选池卡 → 点模型 toast+换回状态卡 →「← 返回状态」正常。
- 用户原话：「现在显示正常了」。

### ut-8 收尾
- ROADMAP 变更记录：补 Phase 37 行 + 统一测试轮收官行；Phase 30 行
  「真机验收待配置 admin 名单」更新为已通过。
- .gitignore 补统一测试轮运行时产物（cisTarget_databases/
  motifAnnotations/code_workspace/.pytest_tmp/.pytest_basetemp）。
- commit：统一测试轮全部修复 + ut-6 抽查脚本 + 文档同步。

## 统一测试轮总账（ut-1 ~ ut-8 全绿）
- ut-5 验收：18 工具 × 双物种 30/30 PASS（100%）。
- ut-6 真 LLM 规划抽查：3/3 success。
- ut-7 飞书端抽查：4 项生产问题全修复并复测通过
  （admin 名单 / sc_load 直通 / 卡片标题渲染 / 双卡片+双挂载）。
- 回归基线：1055 → 1064 passed。
- 后续建议：① 产品决策点——普通自然语言是否进 planner（当前必须
  /research 或 /code 前缀）；② 服务装配有多个挂载点时，装配测试
  逐点断言；③ toolsv1/ 参考仓库未入库（迁移源，本地保留）。

### 用户复测确认（补记）
- /research 对数据集 f1e89bf88edc 做双联体检测 → 正常（sc_load 既有
  dataset_ref 直通修复在真 LLM 链路端到端生效，用户原话「这个测试也正常」）。
- 至此 ut-7 两项复测（/model 双卡片 + /research 双联体）全部用户确认通过。

## Phase 38：自然语言意图预判 + 确认转研究（2026-09-07 中午）

### 背景与决策
- ut-6 遗留产品决策点：普通自然语言消息走无工具闲聊路径（模型幻觉
  function_calls 裸文本）。用户选定方案「意图预判 + 自动转研究（确认闸）」。
- toolsv1/ 参考仓库按用户决策移出归档至 I:\archive\toolsv1（23 工具全部
  迁移验收完毕，参考源使命完成；代码零运行时依赖，仅文档注释提及）。

### 实现
- orchestrator/intent_gate.py IntentGateService：LLM 轻量分类
  （system prompt 列举研究意图特征：数据集 hex 引用/sc_*/st_* 工具名/
  分析名词；严格 JSON 输出，复用 planner._extract_json_object 容错解析）→
  疑似研究意图发确认卡（指令预览 + 确认执行/忽略按钮，owner 内嵌 value）。
- 确认闸：点「确认执行」才经网关 research_intent 回调分支转
  ResearchRunner.handle（text 前补 /research 前缀，等同用户发指令）；
  点「忽略」或超时（TTL 1800s）不启动任何任务——误判零成本。
- 内存 pending store + 幂等（首击固化，重复点击 already_handled）+ owner
  内嵌比对（forbidden），均对齐 code_approval 模式；不落库。
- 单挂载点设计：只挂 orch.intent_gate，回调经 ctx.orchestrator 同源取
  （ut-7 双挂载事故的根上规避）；装配测试断言 orch 挂载 + ctx.orchestrator
  同一性。
- 分类/发卡任何异常一律回退闲聊路径（绝不阻断正常聊天）；/ 开头未知
  指令跳过分类不耗 LLM 调用。
- 卡片交互复用 ut-7 回调响应卡机制：批准/忽略后原地换面去按钮。
- settings：INTENT_GATE_ENABLED（默认开）/ INTENT_GATE_TTL_SEC=1800。
- 审计：点击走公共段（target_id 链补 intent_id）+ 批准追加
  research_intent_approved 专项审计（detail 截断 200 字符）。

### 测试
- 单测 test_intent_gate.py 13 例（offer 6 + decide 7：含 TTL 过期/发卡失败
  清理/forbidden/幂等）。
- 集成 test_intent_gate_callback.py 5 例（批准转 runner 全链/忽略/forbidden/
  未装配 unavailable/审计闭环）。
- test_message_flow.py +3 例（命中短路闲聊/未命中落闲聊/未装配旧行为）。
- test_ws_client.py +4 例 toast 映射 + 装配断言。
- 全量回归：**1088 passed**（1064 + 24）。
- 事故记录：settings dataclass 字段首次编辑丢失（loader kwargs 在而字段
  不在，TypeError 暴露）——与 Phase 36 settings 写竞争事故同类，编辑后
  必须 grep 回读确认落盘。

### 部署
- ws_client 重启 pid=79300（HEAD=5a886e2）。
- 待真机验证：私聊发自然语言分析指令（不带 /research）→ 确认卡 →
  点确认执行 → 任务受理；点忽略 → 无事发生；闲聊消息 → 正常回复。

### Phase 38 增补：research/code 双路由（2026-09-07 下午）
- 用户指出意图闸只转 /research，未区分代码任务——分类器升级三路由
  （research/code/chat），确认卡三按钮：确认执行（分类路径）/ 改用另一
  路径（research↔code 一键纠偏，防分类错向）/ 忽略。
- 网关 research_intent 分支按 result.route 分发 ResearchRunner 或
  CodingRunner；回调 route 非法值回退分类路由（不放任前缀注入）；
  toast/换面卡/审计 detail_json 均带路由；审计 action 更名 intent_approved。
- 测试：单测 +4（code 卡面/未知路由保守/纠偏改道/非法 route 回退），
  集成 +1（code 路由进 coding_runner 且 research_runner 未动）；
  目标集 80 passed，全量回归 **1093 passed**。
- 事故记录（第二次）：SearchReplace 报成功但落盘丢失
  （_attach_gate 默认参数旧值残留，测试红暴露）——与 settings 字段丢失
  同类，重大编辑后 grep 回读磁盘已成强制纪律。
- 部署：ws_client 重启 pid=82712。

### Phase 38 真机验收（用户确认，2026-09-07）
- 场景 1：自然语言生信指令（不带前缀）→ 「研究任务意图」确认卡正常弹出 ✅
- 场景 2：自然语言代码指令 → 「代码任务意图」确认卡正常弹出 ✅
- 场景 3：纠偏按钮「改用另一路径」→ 换路受理正常 ✅
- 用户原话「这三个都正常可用」——Phase 38 真机验收通过，全部收官。

### Phase 39：/code 过程反馈 v2 进度卡（2026-09-07）
- 背景：v1 为节流文本进度消息（刷聊天流）；v2 改为单张进度卡原地刷新，
  依托飞书主动更新 API（PATCH /open-apis/im/v1/messages/:message_id，
  无需用户点击，区别于回调换面）。
- 实现：
  - feishu_adapter/im_adapter.py：新增 update_card(message_id, card)
    （SDK-only PATCH；CLI 通道抛 NotImplementedError）；抽 _card_json
    静态助手供 send_card/update_card 共用。
  - orchestrator/coding/coding_runner.py：新增 _ProgressCard——
    每 3 个工具事件且最小间隔 2s 节流原地刷新；最近 5 条事件滚动窗；
    关键事件（tools_disabled/compressed）立即刷新；更新失败熔断
    （_broken）不再重试；finish/finish_error 终态定格卡
    （完成/结束/异常三种卡面）。_make_reporter 工厂：start() 拿不到
    message_id 或发卡异常 → 回退 v1 _ProgressReporter（接口对齐，
    v1 增补 no-op finish/finish_error）。
- 测试：TestProgressCard 10 例 + TestMakeReporter 5 例 +
  test_adapters_sdk update_card 3 例；test_coding_e2e 五处卡片计数断言
  按 action 过滤修复（进度卡无 actions 元素，auto_approve 线程需跳过，
  否则 KeyError 杀线程 → 审批不触发 → APPROVAL_DENIED 假红）。
- 事故记录（第三次同类）：SearchReplace 同文件批量编辑部分落盘丢失
  （import 行、run_sync 接线、e2e 三处断言），逐条重放 + grep 回读
  磁盘后全绿——纪律固化：同一文件每次消息只发一处编辑，编辑后必 grep。
- 结果：目标集 59 passed，全量回归 **1112 passed**（净增 19）。
- 部署：ws_client 重启 pid=37208；待真机验收 /code 进度卡。

#### 后续建议
- 真机发一个多工具 /code 任务，观察进度卡原地刷新与终态定格。
- 若飞书 PATCH 限流敏感，可调大 min_interval（当前 2s）。
- 同样模式可复用到 /research 长任务（Docker 跑批阶段进度）。

### Phase 39 真机验收（用户确认，2026-09-07）
- 输入：/code 写 gen.py 生成 100 行 CSV → analyze.py 统计写 report.txt → 读回
- 结果：6 steps · 6 工具全 ✓ · 耗时 40s，终态卡「代码任务完成 ✅」
  渲染正确（步数/耗时/结果摘要齐全），正文 [coding 完成] 回复照常
- 用户贴出输出确认 —— Phase 39 真机验收通过，全部收官。
- 备注：进行中卡片原地刷新过程未特意截图留证，后续长任务可再观察；
  输出可见一处既有现象（终态卡内摘要与正文回复内容重复），为
  _render_result 既有行为，非 v2 引入。

### Phase 39 复跑补充确认（2026-09-07）
- 用户复跑同一 /code 任务：进度卡原地刷新实时盯屏确认无问题，验收完全闭环。
- 附带验证：工作区脚手架复用正确（不重写已有 gen.py/analyze.py）；
  LLM 误用 run_cmd（"python && gen.py" 单串）2✗ 后自愈改数组形式；
  L2 审批卡（run_cmd×2）与进度卡并存互不干扰；终态 7✓/2✗ 计数准确。
- 经验沉淀：cmd 数组每元素独立参数，shell 连接符（&&）不可用——
  已在 SKILL/系统提示层可考虑加固（后续可选）。

### run_cmd 工具描述加固（2026-09-07）
- 动因：Phase 39 复跑真机暴露 LLM 把 "python && gen.py" 整串塞进 cmd
  单元素（2✗ 后自愈）——从源头防呆。
- 改动：code_tools.SCHEMA 中 run_cmd description 明确 cmd 为独立参数数组
  （附正确示例 ["python","gen.py"]）、不支持 shell 连接符（&&、||、|、>、;），
  需串联请拆多次调用或 python -c 内联；cmd 参数描述加反例警示。
- 测试：+1 回归用例（schema 描述含警示要点）；全量回归 **1113 passed**。
- 部署：ws_client 重启 pid=79228（schema 静态声明随进程加载，须重启生效）。

### run_cmd 加固真机复测（用户确认，2026-09-07）
- 前置：/code clear 清空工作区（顺带确认：/clear 非注册命令会落闲聊，
  正确入口为 /code clear——已在会话中告知用户，未知斜杠命令提示列为
  可选改进项）。
- 全新工作区重发同一 /code 任务：6 steps · 工具 **6✓/0✗** · 31s，
  全程零失败——对比加固前 7✓/2✗（&& 整串误用自愈），run_cmd schema
  描述加固真机生效（直接 ["python","gen.py"] 数组形式，无自愈弯路）。
- 进度卡原地刷新与终态定格复验通过。run_cmd 加固项收官。

### Phase 40：未知斜杠命令兜底提示（2026-09-07）
- 动因：真机 /clear 落入闲聊 LLM（回复"我没有清除功能"）——未注册 / 命令
  静默落闲聊，用户困惑。
- 实现：app.py 新增 1.72 兜底分支（/model 之后、群门控之前）——已知命令
  （市场 12 条 + /research /code /model /bind-doc /bind-doc-renew）此前已
  全部路由，走到这里的 / 开头消息即未注册命令：回「[未知命令] xxx + 可用
  命令清单 + 自然语言入口提示」，status=unknown_command。
- 测试：+4 用例（/clear 兜底提示不落 LLM 不建 task / 带参命令只回显命令词 /
  群聊未知命令同样提示 / 已注册命令不触发兜底回归保护）；目标集 33 passed，
  全量回归 **1117 passed**。
- 部署：ws_client 重启 pid=4840；待真机复验（发 /clear 应得提示而非闲聊）。

### Phase 40 真机验收（用户确认，2026-09-07）
- /clear 与 /restart 均正确返回「[未知命令] + 可用命令清单 + 自然语言
  入口提示」，不再落闲聊 LLM——Phase 40 真机验收通过，收官。

### Phase 41：/research 进度卡复用（2026-09-07）
- 用户决策：进度卡模式从 /code 复用到 /research（scenic 40 分钟级长任务
  最需要可视进度）。
- 设计：Scheduler 无事件钩子 → 观察线程模式（每 2s 拍节点快照喂卡），
  与 coding _ProgressCard 同构（节流/熔断/禁用回退）。
- 实现（research_runner.py）：
  - _ResearchProgressCard：start「规划中」卡（handle 受理即发）→
    plan_done 转「执行中 x/y」立即刷新 → tick 终态数变化立即刷新否则
    8s 节流 → finish「研究任务完成」/ finish_error「异常终止」终态定格；
    PATCH 失败熔断；未 start/无 message_id 全 no-op（行为同旧版）。
  - _watch_nodes 观察线程 + _snapshot_nodes 快照助手（分类计数 ✓/✗/⊘/–
    + 运行中节点含已耗时 + 最近完成序列，控制流展开动态 total）。
  - 接线：plan_failed/超时/异常三出口均 finish_error 定格；watcher 在
    finally 中 stop+join。
- 测试：单测 14 例（生命周期/节流/熔断/卡面/快照/耗时格式化）+ 集成
  3 例（全流程/规划失败定格/禁用回退）；既有 5 处断言按「末元素含
  actions」过滤审批卡（_wait_card_sent/_approval_cards 助手）。
- 全量回归 **1134 passed**；ws_client 重启 pid=45608；待真机验收。

#### 后续建议
- 真机发一个多节点 /research 任务观察「规划中→执行中→完成」三态切换。
- 节流参数 min_interval=8s（比 /code 的 2s 保守，研究任务更长）；
  若飞书限流无压力可调小。

### Phase 42：数据画像注入规划提示（方案 B，2026-09-07）
- 动因（真机）：/research 任务 planner 套 sc_qc 默认 min_genes=600，
  数据集 f1e89bf88edc genes/cell 中位数仅 66 → SC_QC_OVERFILTERED
  全过滤，n1 failed 致 n2/n3 级联 skipped。
- 方案 B 实现（orchestrator/tools/bio/dataset_profile.py 新增）：
  - extract_dataset_refs：任务文本提取 12 位 hex ref（去重保序，≤3 个）；
  - profile_dataset：h5py 直读 workspace/<ref>/（raw→filtered→processed
    读取链），dense array 与 CSR group 双编码支持，>5000 行抽样、分块
    2000 行；统计 n_cells/n_genes/genes_per_cell(median/p90/max)/mt_pct
    （var['mt'] 布尔列优先，否则 MT-/mt- 前缀派生）；任何异常 → None；
  - build_profile_context：画像文本段 + 阈值指引（min_genes ≤ median/2、
    max_mt_pct 参考 p95、严禁套默认值）。
- 注入：research_runner._execute 在 session_context 追加画像段
  （try/except 包裹，失败仅告警不影响规划主链路）。
- 真数据冒烟：f1e89bf88edc 提取 median=66 p90=73 max=83，与真机
  SC_QC_OVERFILTERED 错误消息完全吻合。
- 测试：单测 14（ref 提取/dense/CSR/mt 列优先/读取链/四类容错/上下文
  构建）+ 集成 2（注入含画像 / 纯文本零影响回归）；两处断言算错自纠
  （median 取中值、p95 线性插值 56.25）。
- 全量回归 **1150 passed**；ws_client 重启 pid=51372；待真机验收。

#### 后续建议
- 真机重发「/research 对数据集 f1e89bf88edc 做质控再做双联体检测」，
  观察 planner 是否按画像选 min_genes≈30。
- 若 LLM 仍忽略画像指引，考虑把画像同时注入 user prompt 前部。
- CSC 编码 h5ad 暂不支持画像（罕见，缺失可接受）。

### Phase 42 真机验收（用户确认，2026-09-07）
- 用户重发「/research 对数据集 f1e89bf88edc 做质控，然后做双联体检测」
  （不带显式阈值）：n1 质控 / n2 聚类 / n3 双联体 **全 success**。
- 关键证据：n1 n_cells_after=300（零过滤，阈值选对）、n2 3 簇各 100、
  n3 双联体 3 个（1%）——planner 按数据画像自主选参，SC_QC_OVERFILTERED
  根治确认。Phase 41+42 真机双收官。

#### 后续建议
- 大真实数据集（万级细胞）再验一次画像注入（抽样路径 5000 行）。
- /code 的 bioqc 工具默认阈值同样偏严，可视后续真机反馈决定是否画像化。

### CI 接入（2026-09-07）
- 方案：本地 CI（git pre-push 钩子 → scripts/check.ps1 质量门）+ 预置云端
  GitHub Actions 工作流（用户选定「本地 CI + 预置云端配置」）。
- 新增：`.githooks/pre-push`（sh，SKIP_PRE_PUSH=1 紧急跳过）+
  `git config core.hooksPath .githooks`（钩子版本化）+ `.gitattributes`
  （钩子锁 LF 防 autocrlf 转 CRLF）+ `.github/workflows/ci.yml`
  （ubuntu + py3.12 + postgres:16-alpine 映射 5433，与 PG_TEST_URL 默认值
  匹配可跑真库层；docker 测试无 kernel 镜像自动 skip；依赖走
  `pip install -e .[dev]`——lock 文件含本地绝对路径 `-e i:\飞书agent`
  自引用，仅本机环境复现用）。
- 副产物一（依赖补齐）：Phase 42 dataset_profile.py 引 h5py/numpy 但
  pyproject/lock 均未声明（venv 碰巧有故能跑；import 走 try/except 属
  静默功能缺失风险）→ 补 `h5py>=3.10.0`/`numpy>=1.26.0` +
  lock 钉版 `h5py==3.16.0`/`numpy==2.5.2`。
- 副产物二（存量 lint 清零）：`ruff check .` 查出 26 个存量违规
  （Phase 10 质量门后各 Phase 未跑门禁劣化，含 Phase 42 残留未用 h5py
  导入、Phase 39 E501 超长）→ `--fix` 22 + 手动 4（2×E501 断行/noqa、
  2×E741 变量改名 l→lig/fc）+ load.py 无效 noqa（SIM115 不在 select
  且中文紧贴致指令失效）转纯注释。全部 import 排序/未用导入删除/
  局部改名，零行为变化。
- 副产物三（check.ps1 修复）：pytest 加 `--basetemp=仓库内 .pytest_tmp`
  （默认 Temp\pytest-of-Administrator 遇 WinError 5 权限拒绝）；
  SearchReplace 编辑丢 UTF-8 BOM 致 PS 5.1 按 ANSI 误读中文注释报
  Unexpected token——已补回 BOM（存量 ps1 均带 BOM，后续编辑 ps1
  必须保 BOM，教训入档）。
- 验证：ruff check . 全绿；钩子完整路径 `sh .githooks/pre-push` exit=0
  （1144 passed + 6 deselected，71s，[PASS] 质量门全部通过）；
  SKIP_PRE_PUSH=1 跳过路径 exit=0；全量回归 1150 passed 零回归。
- ws_client 无需重启（纯 CI 配置 + 零行为 lint 修复，无运行时代码变更）。

#### 后续建议
- 配置远程仓库（GitHub/Gitee）后 push 即触发本地钩子 + 云端工作流双门禁。
- pre-push 全量约 70s，赶时间用 `SKIP_PRE_PUSH=1 git push`。
- 若钩子在 git GUI 客户端不生效，检查该客户端是否读 core.hooksPath。

### /model 池扩充 Kimi（2026-09-07）
- 需求：/model 候选池加 Kimi coding API（用户提供 url + key）。
- 按 Phase 30 预留扩展流程：llm.yaml providers 加 `name: kimi` 条目
  （`LLM_PROVIDER_KIMI_*` 三 env）+ .env 写真值 + .env.example 示例
  从 moonshot.cn 旧端点更新为 `api.kimi.com/coding/v1`。
- 模型名确认：`GET /models` 实测四款（kimi-for-coding /
  kimi-for-coding-highspeed / k3 / k3-256k）；初测 kimi-for-coding 连通，
  **用户最终选定 k3（1M 上下文）**，连通性复测 200 出 pong。
- 连通性：POST chat/completions **200 返回 pong**；`reasoning_content`
  独立字段不污染 content；项目 payload 仅 model+messages 极简原生兼容，
  strip_think 无害。教训：Windows PowerShell `curl -d` JSON 引号转义坑
  （引号被剥离致 invalid_request），API 验证一律走 Python httpx。
- config_check OPTIONAL 不登记 PROVIDER 键位（遵循 Phase 30 决策：
  池任意扩充不逐一登记）。
- 验证：settings 解析三 provider 正确（kimi 条目 url/model/key 前缀核对）；
  全量 **1150 passed** 零回归；ws_client 重启 pid=47568 HEAD=daaa486
  （k3 生效）；待真机 /model 卡片验证。

#### 后续建议
- 真机 /model 卡片确认 kimi 候选出现并可切换（切换后闲聊一句验证
  kimi 实际出话，再切回 primary）。
- kimi-for-coding（256k）/ kimi-for-coding-highspeed（高速版）可视需要追加条目。

### fallback 模型切换 glm-4.7 → glm-5.3（2026-09-07）
- 用户指定更换；连通性实测 **200 出 pong**（GLM coding 端点，极简 payload）。
- 改动仅 `.env` 一处 `LLM_FALLBACK_MODEL`（注释同步去掉"额度已用尽"过时描述）；
  settings 解析确认 `('fallback', 'glm-5.3')`；ws_client 重启 pid=52204。

### /model 池真机验收（用户确认，2026-09-07）
- 用户确认真机 /model 卡片与切换**无问题**：三候选（MiniMax-M3 /
  glm-5.3 / kimi-k3）显示与切换链路正常。Kimi 入池 + glm-5.3 切换双收官。

### Phase 43：/code 数据画像注入（2026-09-07）
- 背景：Phase 42 只治了 /research 规划侧；/code 直连 sc_qc 默认
  min_genes=600 同样偏严（调研确认 bioqc 仅为 skills 知识面文档无工具，
  /code 实际调 builtin sc_qc，schema 默认值 600/3/20）。
- 实现（对齐 Phase 42 模式，复用 build_profile_context）：
  coding_runner.__init__ 存 `bio_workspace_root`（空 = 功能关闭，向后兼容）；
  run_sync system 拼装后追加画像段（try/except 包裹，失败仅告警不影响主链路）。
- TDD 3 用例：ref 命中注入（含 median=2 统计核对）/ 纯文本零注入回归 /
  bio_root 不存在静默跳过主链路正常。先红后绿。
- 全量回归 **1153 passed**；ruff 绿；ws_client 重启 pid=81152
  （工作区含未提交 Phase 43，commit 后 HEAD 前进）；待真机验收。

#### 后续建议
- 真机验证：/code 对数据集 f1e89bf88edc 做质控（不带显式阈值），
  观察 LLM 是否按画像选 min_genes≈30。

### Phase 44：场景级 provider 静态双绑（A1，2026-09-07）
- 需求（任务 3）：/code、/research 各自独立绑定 provider，池子价值最大化；
  用户选定 A1（.env 静态双绑，零迁移零 UI 改动）。
- 架构调研结论：全链路单 LLMRouter 实例 + reconfigure 原地换属性 = 全局生效，
  分场景必须建第二实例；Planner 的 role 路由仅 audit 标签（Phase 5 预留未实现）；
  code_model 只能同 provider 换模型名，跨 provider 不可行。
- 实现：新模块 orchestrator/scene_router.py `build_scene_router`（命中池 →
  独立 LLMRouter：primary=池条目 / fallback=出厂 fallback / max_retries 透传；
  空或 miss → None 告警回退全局）+ settings 新字段 CODE_PROVIDER /
  RESEARCH_PROVIDER + Orchestrator(research_llm=)（planner 与
  orch.research_llm 走场景，orch.llm 保持全局管闲聊/意图闸）+
  research_runner._research_llm helper（Scheduler condition/repair 双接）+
  runtime 装配（CodingRunner + SkillDiagnoser 用 llm_code or llm）。
- 部署（用户选定分工）：llm.yaml 新增 kimi-coding 条目（与 kimi 共享
  url/key env，仅 model_env 不同 = kimi-for-coding）+ .env 加
  LLM_PROVIDER_KIMI_CODING_MODEL / CODE_PROVIDER=kimi-coding /
  RESEARCH_PROVIDER=kimi。
- TDD 9 用例（先红后绿；OrchestratorWiring 初版断言 planner 失败——
  planner 在 settings+doc_adapter 重分支内，改断言 research_llm 属性，
  planner 接线由集成回归覆盖）：空 name 回退 / 命中建 router 含 fallback
  与 retries / miss 告警 / env 加载 / Orchestrator 双接 / helper 三态回退。
- 全量回归 **1162 passed**；ruff 绿；ws_client pid=73668，启动日志确认
  `scene llm bound: code=kimi-coding research=kimi`；待真机验收。

#### 后续建议
- 真机验证：/code 写脚本（终态卡 tokens 与 kimi-for-coding 风格输出）+
  /research 短任务各跑一次；如需确认实际出话模型，可问"你是谁/你的模型名"。
- /model 全局热切换现在只管闲聊/意图闸等场景；如需场景级可视化切换，
  升级 A2（llm_active 加列 + 迁移 + 卡片场景维度）。

### /code 真机验收 + Phase 44 逐调用日志补强（2026-09-07）
- 用户贴出 /code 真机输出（任务2验收）：对数据集 f1e89bf88edc 质控，
  `[coding 完成] 步数 2 · 工具 1✓/0✗ · ~519 tokens`，QC 参数
  min_genes=20 / min_cells=3 / max_mt_pct=5，过滤前后 300×190 剔除 0，
  摘要引用"median=66、线粒体比例 p95≈1.4%"。
- **Phase 43 验收判定：通过（硬证据）**——median=66 与 mt-p95≈1.4% 仅存在于
  注入的画像段（本轮无 OVERFILTERED 错误可泄露统计），且 min_genes=20
  符合画像指引 ≤median/2（=33），非默认 600。
- Phase 44 验收缺口自查：LLMRouter 全程零调用日志/审计，无法从日志实锤
  出话模型=kimi-for-coding → 补强：_call_once 与 _call_with_tools_once
  各加 `logger.info("llm call: model=%s host=%s", ...)`。
- 副产物修复：test_scene_router::TestSettingsEnvLoad 在真实 .env 含
  CODE_PROVIDER 时失败（load_env_file setdefault 回注）——monkeypatch
  load_env_file 为 no-op 隔离；先红（全量 1 failed）后绿。
- 全量回归 **1162 passed**；ruff 绿。

#### 后续建议
- 用户再跑一次 /code（任意小任务），随后查 logs\ws_client.err.log 出现
  `llm call: model=kimi-for-coding host=https://api.kimi.com/coding/v1`
  即 Phase 44 真机验收闭环；/research 跑一次应见 model=k3。

### Phase 44 真机验收闭环 + /code 文件落盘验证（2026-09-07）
- 用户连跑两条真机：
  1. `/code 对数据集 f1e89bf88edc 做质控，把过滤前后的细胞数写进 qc_report.txt`
     → 步数 3 · 工具 2✓/0✗ · ~729 tokens；min_genes=30/max_mt_pct=10
     （仍画像指引 ≤median/2，非默认 600，Phase 43 二次佐证）；
     qc_report.txt 落盘 code_workspace\code_016355f0\，内容核对无误
     （前后 300/300 细胞、190/190 基因、参数与画像统计齐全）——
     /code 文件输出能力顺带验证通过。
  2. `/research`（归一化+聚类，2 节点）→ 双节点 success，
     n1 质控 300→300、n2 聚类 n_clusters=3（100/100/100）。
- **Phase 44 硬证据**：logs\ws_client.err.log 逐调用日志——
  /code 时段 4 次 `llm call: model=kimi-for-coding host=https://api.kimi.com/coding/v1`；
  /research 时段 2 次 `llm call: model=k3 host=同上`。同端点不同模型名，
  场景双绑路由成立；无 MiniMax-M3 混入（/code、/research 全程未碰全局 router）。
- **验收判定：Phase 44 真机验收通过，任务 2、任务 3 全部闭环。**

#### 后续建议
- ~~任务 1（git 远程 → CI 双门禁）仍待用户建 GitHub 空仓库贴 URL。~~ 已完成（见下节）。
- 可选：A2 升级（场景级 /model 可视化切换）；token 用量观测
  （终态卡已显 ~tokens，可落库做按场景统计）。

### 任务 1 闭环：git 远程 + CI 双门禁激活（2026-09-07）
- 用户授权浏览器协作建仓库：内置浏览器登录 GitHub（lkp101ksp-spec）→
  建 Private 空仓库 `lkp101ksp-spec/feishu-agent`（无 README/gitignore 初始化）。
- 推送前安全自查：.env 在 .gitignore、git grep 全库无真实密钥、
  .env.example 全占位符——通过。
- 排障三连（真实环境经验，记入沉淀）：
  1. GCM 浏览器流登错号（kunpengliao1）→ erase 缓存 + 改走
     device code 流，用内置浏览器（已是正确账号）完成授权；
  2. push 报 port 443 连不上——本机系统代理 127.0.0.1:17890 浏览器生效
     但 git 不走；全局 .gitconfig 被沙箱拦，改仓库本地
     `http.https://github.com/.proxy` 生效；
  3. device 流 token push 报 404 Repository not found（scope/缓存成疑），
     改走内置浏览器直接建 classic PAT（repo+workflow，30 天）→
     `git credential-manager store` 持久化 → push 成功。
- 首次 push 被本地 pre-push 质量门拦下一个临时调试脚本的 E501——
  本地门禁实证有效（删临时文件后通过）。
- push 成功（master -> master，track 建立）；GitHub Actions CI 首跑
  自动触发（commit 7c49a1e，In progress）。
- **验收判定：任务 1 完成。双门禁 = 本地 pre-push（已实证拦截）+
  云端 Actions（首跑中）。**

#### 后续建议
- 观察 CI 首跑结果（pg 服务容器 5433、docker 层自动 skip 为预期）；
  红了对日志修，绿了整个任务盘清零。
- PAT 存于 Windows 凭据管理器，30 天后到期需换；也可随时在
  GitHub settings/tokens 删除重建。

### 2026-09-08 CI 转绿排障（run1→run7）

- 段错误（exit 139）：research_runner 后台线程回复在 mark_success 前，
  fixture dispose 与线程写 sqlite 竞态。修复=拆棚前 join 本测试新起线程。
- 收集崩溃（exit 2）：ctypes.WinDLL 跨平台守卫。
- 环境差异 10 连：conftest 哑值兜底（仅无 .env 生效）/ engine 池参数
  方言分流 / 盘符路径测试限 win32 / resolve_safe 跨平台绝对路径拒绝 /
  curl 替换 Windows 专属 shell / scheduler 驱动协程 done 标志消竞态
  （_all_terminal 瞬时窗口：FAILED 亦终态，repair 弹回前 drive 误退）。
- **结果：run 34147592219 SUCCESS（默认层 1162 + pg 真库层双绿）。**
- 经验：CI 日志诊断脚本 scripts/_ci_log.py（段错误上下文/FAILED 摘要/
  按测试名抓 traceback）；ci.yml pytest 保持 -v（崩溃无摘要时可定位）。

#### 后续建议
- PAT 约 2026-10-08 到期需换（GitHub settings/tokens 重建后
  git credential-manager store 更新即可）。
- 建议 revoke 错号 kunpengliao1 的 GCM OAuth grant
  （github.com/settings/applications）。
- 本机 Temp\pytest-of-Administrator 目录 ACL 损坏（WinError 5），
  建议人工删除；期间 pytest 一律加 --basetemp=.pytest_tmp。

### 2026-09-08 CI 依赖锁定（64c8549，run 34173415492 绿）

- 背景：ci.yml 原 pip install -e .[dev] 每次装最新版依赖，上游发大版本
  会无预警红 CI（CI 排障期间识别的隐患，版本漂移面）。
- 做法：requirements-lock.txt 剔除 -e 自引用行，本机/CI 共用一份锁；
  CI 安装 = pip install -r requirements-lock.txt + pip install -e . --no-deps；
  setup-python 缓存键指向锁文件。旧 61 行陈旧锁刷新为 161 钉版。
- 验证：本地 pip install --dry-run exit 0；CI ubuntu 全新安装 + 双层测试绿。
- 经验：venv 无平台专属包故无需 sys_platform 标记；未来引入 pywin32 类
  需手动补标记（已写进锁文件头注释）。

#### 后续建议
- 依赖变更（pyproject 增删）后必须同步刷新锁文件，否则 CI 与本机漂移。
