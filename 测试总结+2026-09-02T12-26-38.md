# 测试总结+2026-09-02T12-26-38（Phase 21 批②：st_domains + st_commot）

## 本次测试范围

Phase 21 批② 8 任务（Subagent-Driven 执行）：st 镜像追加 commot、
process raw 快照、domains.py（banksy-lite）、commot.py（COMMOT+CellChat）、
工具注册（TDD）、tiny visium 追加 LR 基因、容器链 7 步冒烟、真机验收。

实施计划：`docs/superpowers/plans/2026-09-01-phase21-st-batch2.md`

## 任务执行记录

| # | 内容 | commit | 结果 |
|---|---|---|---|
| T1 | 镜像加 commot + 重建 + API 探测 | 88b0cf5 | commot 0.0.3 装入；CellChat human 1939 对；CXCL12-CXCR4/VEGFA-KDR/CSF1-CSF1R 三对确认存在 |
| T2 | process.py 落 raw 快照 | 6e4e887 | raw: True (196,184)，X min -1.41；markers 无回归 |
| T3 | domains.py（banksy-lite） | 2559134 | banksy/leiden 两轮 n_domains=3（70/70/56）ARI=1.0 |
| T4 | commot.py（错误路径先行） | 4822a44 | 旧数据 ST_COMMOT_EMPTY rc=1 正确 |
| T5 | 注册 2 工具（TDD）+ fail 模式统一 | 5b14102 | 红→绿，16 passed（含批①无回归） |
| T6 | tiny visium 加 3 对 LR 基因 | c1376f9 | 190 基因；LR 域特异表达 6.1-6.6 vs 背景 0.3 |
| T7 | 冒烟扩展 7 步 | 585bb62 | SMOKE PASS：commot n_lr=3 恰为造入三对，top_path=VEGF，15/15 产物非空 |
| T8 | 全量回归 + 真机验收 | 本 commit | 802 passed；真机 n1-n6 全 success |

## 容器冒烟最终输出

```
SMOKE PASS (n_domains=3, ari=1.0, top_path=VEGF, n_lr=3)
top_lr_pairs: VEGFA-KDR (376.54) / CXCL12-CXCR4 (337.13) / CSF1-CSF1R (182.07)
图质量：sender 13.5% / receiver 13.7% 非白像素（对照 dotplot 8.0%）——非空白
```

## 真机 /research 验收（2026-09-02）

```
/research 对 tiny_visium …banksy 细分空间域并与 leiden 对比，再做人类配体受体空间通讯分析

- n1 st_load:    success（dataset_ref=5452ddf1a510，196×190，mt mean 1.76%）
- n2 st_qc:      success（196→196）
- n3 st_process: success（n_domains=3，{0:70, 1:70, 2:56}）
- n4 st_markers: success（3 groups；新现象：CXCL12 进入 D1 markers——LR 基因
                  特异表达被 markers 捡到，符合数据构造，非缺陷）
- n5 st_domains: success（dataset_ref 贯穿）
- n6 st_commot:  success
写回：审批超时（10 分钟无人处理），正常降级跳过（非缺陷，复测时可点卡片验证）
```

## 与计划的差异（子代理实测发现，均已修复落地）

1. **T1 numpy 2.x 兼容**：commot 0.0.3 的 `_usot.py` 用已删除的 `np.Inf`
   （本次解析装 numpy 2.4.6）——Dockerfile 装 pip 后 `sed s/np\.Inf\b/np.inf/g`
   修补（仅一处），未选降级 numpy（会牵连 pandas/xarray 大批降级）。
2. **T1 LR 库无表头**：`ligand_receptor_database` 返回 DataFrame 列名
   `'0'/'1'/'2'/'3'`（计划探测命令的 `df['ligand']` 会 KeyError）；脚本走
   `filter_lr_database` 不受影响。
3. **T4 脚本名与包同名自引用（最隐蔽）**：`python /opt/st_tools/commot.py`
   时 `sys.path[0]` 是脚本目录，`import commot` 解析到脚本自身——导入前
   剔除脚本目录；另 0.0.3 `__init__` 不暴露子模块需显式 `import commot.pp/tl/pl`。
4. **T4/T7 fail 契约**：`fail()` 后补 `raise SystemExit(1)`（计划代码只
   return → rc=0；BioRunner 有 rc=0 兜底但统一为批①契约）。domains.py
   同步统一（T5）。
5. **T7 COMMOT 0.0.3 实际 API 与文档差异（3 处）**：
   - sender/receiver 边际和存 **obsm**（列带 `s-`/`r-` 前缀 + 通路级列 +
     `total-total` 聚合键），非计划假设的 uns；top_pathway 仅在通路级列选；
   - `_plotting.py` 对 str 型 obs_names 的 `.loc` 严格匹配在 pandas 2.2
     必炸——plot 前 sum DataFrame index 重置为 int 位置索引，写盘前恢复；
   - top_lr_pairs 过滤 `total-total` 聚合键（非 LR 对）。
6. **T6 计划代码 bug**：`lig_cols.append((len(genes), ld))` 把全局基因索引
   当 lr_mat（仅 6 列）列索引用 → IndexError；改用 lr_mat 内部局部列号。
7. **T5 工具注册测试**：`reg.list_tools()` 不存在（实为 `reg.list()`）；
   既有 `test_st_registers_five_tools` 同步更新为 seven。

## 后续建议

1. **批③（st_deconvolve）**：cell2location + torch CPU（预计 +3GB 镜像层）、
   双参考来源（sc 产物 dataset_ref / 白名单 h5ad 路径）、独立超时
   `st_deconvolve_timeout_sec`（默认 3600s）——spec §2.4 已定。
2. 写回审批超时属正常降级；下次真机测试时可点卡片补验批②图片写文档
   （IM 发图已验证过）。
3. ws_client 双实例已三次复发（本次又清一对）——pidfile 防复发建议提级处理。
4. commot.py 的 obsm 键（`commot-cellchat-sum-sender` 系列）依赖 0.0.3
   实现，升版 commot 时需回归冒烟。
5. planners repr 串问题（genes/blocks）长期方案仍在 ROADMAP。

---

# Phase 21 批③ Task 1 追记（st 镜像 + cell2location API 探测，13:24-13:40）

实施计划：`docs/superpowers/plans/2026-09-02-phase21-st-batch3.md`

## 构建结果

| 项 | 结果 |
|---|---|
| torch | **回退路径**：2.13.0+cu130（清华 PyPI 索引版，find-links 无更高 cp312 CPU 版，pip 选索引最高版）→ 拉全套 CUDA13 依赖（cudnn/cublas/nccl/triton 等约 5GB，无 GPU 容器内 `cuda False`，功能不受影响） |
| cell2location | **0.1.5** 依赖解析一次成功（scvi-tools 1.5.0.post1 / pyro-ppl 1.9.1 / lightning 2.6.5 / torchmetrics 1.9.0），无需 --no-deps；import 验证通过（仅无害 SyntaxWarning） |
| 镜像大小 | 2.13GB → **7.17GB**（+5.04GB，全部为 CUDA 依赖膨胀） |
| np.Inf sed | 用 find 递归形式（sh 无 globstar，计划 glob `**` 仅匹配一层），import 兜底未触发 |

## API 探测结论（Task 2 脚本定稿依据，0.1.5 与官方教程差异大）

1. **train 设备参数**：`train(max_epochs=, batch_size=, accelerator="cpu")` **实测通过**（RegressionModel/Cell2location 的 train 均为 `**kwargs` 透传 lightning；无显式 use_gpu/accelerator 形参）。`export_posterior(sample_kwargs={"accelerator": "cpu"})` 同样通过。
2. **setup_anndata**：`(adata, layer, batch_key=None, labels_key=None, categorical_covariate_keys, continuous_covariate_keys, **kwargs)`——batch_key=None/labels_key 支持确认。
3. **filter_genes**：`from cell2location.utils.filtering import filter_genes` 可导入 ✓。
4. **ref 产物结构（⚠ 与教程不符）**：export 后 `ref.var` **无** `means_est_inf_{c}` 列（计划代码会 KeyError）；实际写入 `ref.varm['means_per_cluster_mu_fg']`（形状 n_vars×n_factors，列名 `means_per_cluster_mu_fg_{factor}`）。
5. **cell_state_df 形状（⚠）**：`Cell2location.__init__` 校验 `adata.var_names == cell_state_df.index`——期望 **(n_vars, n_factors) 不转置**（旧教程 `.T` 写法 0.1.5 必炸 ValueError）。正确：`cs = ref.varm['means_per_cluster_mu_fg'].copy(); cs.columns = factor_names`。
6. **空间产物 obsm**：`q05_cell_abundance_w_sf` 存在（n_spots×n_types），但列名带前缀 `q05cell_abundance_w_sf_{factor}`（无下划线分隔）——Task 2 写 obs 前应 rename 成纯 factor 名。
7. `uns['mod']` keys 含 `factor_names` ✓（实测 `['A','B','C']`）；`post_sample_means` 为 dict 非 DataFrame。

## 计划外处理

- sed 由计划 glob 改 find（任务描述预授权的备选）。
- 端到端补测（ref 训练→export→Cell2location 训练→export）超出计划 Step 3 范围，但一次性暴露了 #4/#5/#6 三个 Task 2 必炸点，建议后续批次保留此探测深度。
- 镜像 7.17GB 偏大：如需瘦身可改 `torch==X.Y.Z+cpu` 固定版本从 find-links 装（tuna pytorch-wheels/cpu 有历史 CPU 版），非本任务必需。

---

# Phase 21 批③ Task 2 追记（deconvolve.py 脚本 + 错误路径验证，13:37-13:55）

## 执行结果

| 项 | 结果 |
|---|---|
| 脚本 | `sandbox/st_tools/deconvolve.py` 落地（双参考 _load_ref / MT 剔除 / filter_genes / 基因交集 sane check / 每类型空间图 / deconv.h5ad） |
| 三处必炸点修正 | 全部按 Task 1 探测结论落实（详见下）+ 源码级二次核实 |
| 镜像重建 | 仅 COPY 层重做（pip 层全 CACHED），sha256:fee35cf2 |
| 错误路径验证 | stdin `{"dataset_id":"smoke_st","sc_ref_dataset":"deadbeefdead"}` → **ST_REF_INVALID rc=1** ✓（stdout：`{"ok": false, "error_code": "ST_REF_INVALID", "error_message": "sc ref deadbeefdead has no processed.h5ad; run sc_process first..."}`；前置 sp 读取 + ensure_spatial 均先通过） |

## 三处修正落实情况（源码 grep 二次核实，均与 Task 1 结论一致）

1. **签名矩阵**：`_reference_model.py:258` 实证 `adata.varm[f"{k}_per_cluster_mu_fg"] = sample_df.loc[adata.var.index, :]`，`add_to_varm` 默认含 "means" → 键 `means_per_cluster_mu_fg`；其 DataFrame 列名为 `means_per_cluster_mu_fg_{factor}`（sample2df_vars 前缀规则），故脚本用 `cs.columns = ref.uns["mod"]["factor_names"]` 覆盖（`_export2adata` 实证写入该 uns 键）。
2. **cell_state_df 不转置**：varm 赋值经 `.loc[adata.var.index, :]` 保证 index == var_names，天然通过 `Cell2location.__init__` 校验；直接 `copy()` 不转置。
3. **丰度列前缀**：`_cell2location_model.py:377` 实证 obsm 键 `q05_cell_abundance_w_sf`、列名 `q05cell_abundance_w_sf_{factor}`（q05 后无下划线）——脚本 `.replace("q05cell_abundance_w_sf_", "")` 精确匹配。
4. **accelerator="cpu"**：0.1.5 train 顶层签名为 `**kwargs` 透传（顶层 signature 看不到该形参，属正常；Task 1 已实测透传通过），train 与 sample_kwargs 均按计划保留 accelerator 写法。

## 验证边界与后续建议

1. **正向训练路径未跑**（按任务划分留 Task 4/5 tiny_scrna 数据就绪后冒烟）——三处键名已源码级核实，但 `filter_genes` 布尔返回、`setup_anndata(labels_key=)` 全链仍需正向冒烟兜底。
2. import 链已在容器内验证（deconvolve.py py_compile + cell2location/squidpy/matplotlib import 全通过）。
3. 建议 Task 5 冒烟时留意：tiny 数据（300 细胞/196 spots）下 `batch_size=2500 > n_obs` 的 DataLoader 行为（官方教程同参数，预期无碍，若报错降 batch_size）。

---

# Phase 21 批③ Task 3-6 记录（注册/数据/冒烟/真机，14:00-14:40）

## 任务执行记录

| # | 内容 | commit | 结果 |
|---|---|---|---|
| T3 | settings 超时 + 注册 st_deconvolve（TDD） | 039e908 | 17+612 passed；双来源分流（12hex/路径）+ 闭包绑定超时注入 |
| T4 | make_tiny_scrna.py 参考生成器 | e76b032 | 300×190；190 基因与 visium 逐位一致；方向性 27 倍分离 |
| T5 | 冒烟扩展 deconvolve（场景 B） | 22faa5d | SMOKE PASS 8 步；deconvolve 356s（超时 3600s 占 10%）；修 2 bug |
| T6 | 全量回归 + 真机场景 A | 本节 | 803 passed；真机 n1-n7 全 success |

## T5 冒烟关键数据

```
SMOKE PASS (n_domains=3, ari=1.0, top_path=VEGF, n_lr=3, n_types=3)
deconvolve: 356s（max_epochs=2000），n_cell_types=3（CT_A/B/C），
mean_abundance {CT_A: 2.93, CT_B: 2.98, CT_C: 2.41}，17 产物全非空，
deconv 图非白像素 77%+
```

T5 修正（deconvolve.py，容器 stderr 实测）：
1. `_strip_mt`：`Index.str.startswith()` 返回 ndarray，原 `.values` 属性不存在 → np.asarray 直用
2. `filter_genes` 0.1.5 返回**基因名 Index** 非 bool mask → `keep.intersection(sp.var_names)` 求交

## 真机 /research 场景 A 验收（2026-09-02，两轮）

### 第 1 轮：n5 sc_qc 失败（数据规模错配，非缺陷）

planner 按真实 scRNA 惯例选 `min_genes=600`，tiny 参考每细胞中位仅 66 基因
→ SC_QC_OVERFILTERED 全滤光。**错误链真机表现完美**：消息含
median=66/p90=73/max=83 分布与 "retry with min_genes below the median"
调参指导；n6-n8 依赖级联 skipped 与写回降级提示均正确。

### 第 2 轮（文案指定 min_genes=20）：n1-n7 全 success ✅

```
- n1 st_load / n2 st_qc / n3 st_process：dataset_ref=5452ddf1a510（与批②
  同 id，目录聚合幂等复用 workspace）
- n4 sc_load：f1e89bf88edc（300×190）
- n5 sc_qc：300→300（min_genes=20 生效）
- n6 sc_process：leiden 聚类成功（3 群）
- n7 st_deconvolve：场景 A 串联——planner 把 sc_process 的 dataset_ref 传入
  sc_ref（12hex 分流 → workspace 拼接 counts+leiden）
写回：已写入文档（经卡片确认）——批②超时未验的文档插图链路本次补验 ✓
```

## Phase 21 全量收官

三批 21 个任务全部完成：
- 批① 5 工具（load/qc/process/markers/plot）+ tiny visium + 双镜像架构
- 批② st_domains（banksy-lite）+ st_commot（COMMOT+CellChat）
- 批③ st_deconvolve（cell2location 双参考 + 独立超时 3600s）

sc_*/st_* 共 13 个 L1_compute 工具；全量单测 803 passed；
三批真机 /research 全链路验收通过（含幂等、错误链、级联降级、图片回传、文档写回）。

## 后续建议

1. **tiny 数据参数指导**：tiny 规模任务的文案宜显式提示 qc 参数（本轮
   min_genes=600 教训）；长期可在 sc_qc/st_qc 的 ToolSpec description 加
   "小规模测试数据请降低 min_genes（参考失败消息中的 median）"。
2. **镜像瘦身**（可选）：st 镜像 7.17GB（torch CUDA 回退所致）——固定
   `torch==X.Y.Z+cpu` 从 tuna pytorch-wheels find-links 装可减 ~5GB。
3. **遗留**（跨批）：ws_client 双实例 pidfile（已四次复发）、planner repr
   串长期方案、bind-doc 存在性校验、P15 群聊补测。
4. cell2location 0.1.5 与官方教程有三处 API 差异（varm 键/不转置/前缀），
   升版时需重跑冒烟（smoke_st_chain.py 8 步即回归防线）。

---

# Phase 22 Task 3 追记（lifespan + ConfigDict + qc description，14:47-15:05）

实施计划：`docs/superpowers/plans/2026-09-02-phase22-ops-hardening.md`（Task 3）

## 改动与回归

| 项 | 结果 |
|---|---|
| lifespan | gateway/app.py：`@app.on_event("startup")`（auto_sync_worker.start_async）→ `@asynccontextmanager _lifespan` 挂 `FastAPI(lifespan=)`，`asynccontextmanager` import 提区；行为等价（Phase 9 ADR-0024 逻辑不变），on_event 块删除 |
| ConfigDict | tool_registry.py ToolSpec：原内嵌 `class Config` 仅 1 字段 `arbitrary_types_allowed=True`，等价搬移至 `model_config = ConfigDict(arbitrary_types_allowed=True)`，无字段丢失 |
| qc description | sc_qc/st_qc description 各追加小数据 min_genes 调参指导（genes/cell 分布 median/p90/max + 按调低重试；只追加不改既有文案）——落地批③后续建议 #1 的长期方案 |
| 全量回归（前） | 811 passed, **8 warnings**（本项目 5：PydanticDeprecatedSince20×1 + on_event×4；另 fastapi testclient×1 + lark SDK×2） |
| 全量回归（后） | 811 passed, **3 warnings**（本项目 5 条全部消失；余 3 条均为第三方：fastapi testclient StarletteDeprecationWarning×1 + lark SDK×2，非本项目代码不在清理范围） |
| commit | 1822673（4 files, +21/-10） |

## 过程教训

- **并行编辑同一文件存在写覆盖竞态**：本次对 app.py（3 处）与 tool_registry.py
  （2 处）并行发 SearchReplace，各自丢失部分改动且工具不报错——app.py 丢了
  import 与 _lifespan 定义（on_event 已删，auto_sync 启动逻辑一度缺失）、
  tool_registry.py 丢了 ConfigDict import（23 个 collection error 才暴露）。
  靠全量回归 + `git diff` 逐文件核验后串行补齐。**同文件多编辑必须串行。**

## 后续建议

1. 剩余 3 条 warnings 属第三方：fastapi testclient 的 StarletteDeprecationWarning
   待 fastapi/starlette 升级自然消失；lark SDK 2 条（utcfromtimestamp /
   get_event_loop）不在本项目清理范围。
2. Phase 22 余项：Task 4 st 镜像 torch CPU 瘦身（7.17GB→~2.5GB）+ 冒烟、
   Task 5 ROADMAP 对齐 + 双实例真机验证（本测试总结文件归 Task 5 收尾统一
   commit）。

---

# Phase 22 收官（T1/T2/T4/T5，18:20-18:50）

实施计划：`docs/superpowers/plans/2026-09-02-phase22-ops-hardening.md`

## T1 ws_client pidfile 单实例守卫（commit 2c4b61b）

- gateway/ws_client.py：`.ws_client.pid` + ctypes OpenProcess 探活
  （SYNCHRONIZE|QUERY_LIMITED；GetLastError==87 判死，其余按活着保守）；
  活实例拒绝退出（exit 1）；stale pidfile 接管；`--force` TerminateProcess
  杀旧接管（0.5s 轮询上限 5s）；atexit 清理仅删本进程 pid（不误删接管者）。
- tests/unit/test_ws_client_pidfile.py 5 用例 TDD（monkeypatch _PIDFILE/_pid_alive
  隔离）；28 passed。

## T2 bind-doc 存在性探活（commit 3b7ff70）

- bind_doc_service.py bind()：`_DOC_ID_RE` 校验后、session.bind_doc 前，
  doc_adapter 可用即 `list_root_children(doc_id)` 探活，失败抛
  BindDocInvalidError（含"请检查 doc_id 或链接是否正确"）。
- 3 用例 TDD；12+35 passed。闭环 P15 真机 nzb/nkb 一字之差 1770002 踩坑。

## T4 st 镜像 torch CPU 瘦身（commit 4901cb3）

- 清华 tuna pytorch-wheels/cpu 整站 404（已下线，两次实测）；改用 SJTUG 同构
  镜像（上游同为 download.pytorch.org/whl），固定 `torch==2.14.0+cpu`
  （cp312 x86_64 最高版；cell2location>=1.9 / lightning<4.0,>=2.1 / scvi-tools
  无上界均兼容）；CUDA 静默回退分支删除——构建失败即暴露。
- 镜像 **7.17GB → 3.19GB**；容器内 `2.14.0+cpu / cuda=False / cell2location 0.1.5`。
- 冒烟 8 步 SMOKE PASS，deconvolve **309s**（基线 356s，-13%），17 产物全 OK
  （n_domains=3, ari=1.0, top_path=VEGF, n_lr=3, n_types=3）。

## T5 全量回归 + 真机双实例验证

- 全量回归：**811 passed, 3 warnings**（55.57s；3 条均为第三方，见 T3 追记）。
- 真机验证（18:38-18:40）：
  1. 清场时发现又一对旧实例（135264 + 42200，14:18:35 同刻，pidfile 前代码）
     —— 第六次复发，全部被 kill。
  2. 新代码实例 #1 启动：pidfile 写入 127456，Lark connected。
  3. **第二实例拒绝**：秒退 exit 1，日志 `ws_client already running
     (pid=127456); use --force to replace` ✓
  4. **--force 接管**：WARNING `terminating old ws_client (pid=127456)` →
     新实例（pidfile→115100）Lark reconnect 正常 ✓
  5. 最终单实例运行中。
- **重要认知修正**：进程列表里的"成对 python"是 venv shim 父子对
  （.venv\Scripts\python.exe 启动器派生 C:\Python312\python.exe 实进程，
  后者才写 pidfile/连 Lark），并非两个逻辑实例；历史复发的本质仍是
  "重启时旧实例未清"（多对共存），pidfile 守卫对此有效。

## 后续建议

1. 测试/运维脚本里重启 ws_client 时仍建议先按 CommandLine 过滤清场（防御
   shim 父进程滞留）；pidfile 已是第一道防线。
2. tuna pytorch-wheels 已 404，后续其他镜像若引用该源需同步换 SJTUG 或官方。
3. torch 升版时重跑 smoke_st_chain.py 8 步（deconvolve 耗时基线 309-356s）。
4. 遗留不变：bio_workspace 磁盘治理、planner repr 串长期方案、P15 群聊
   2 场景补测、Phase 16 沙箱网络白名单/产物回收。

---

# Phase 23 bio_workspace 磁盘治理（19:40-20:10）

实施计划：`docs/superpowers/plans/2026-09-02-phase23-bio-workspace-gc.md`
spec：`docs/superpowers/specs/2026-09-02-bio-workspace-gc-design.md`
执行方式：Subagent-Driven（T1-T4 子代理，T5 父代理），4 commit 全部零偏离。

## 任务与 commit

| 任务 | commit | 结果 |
|---|---|---|
| T1 sweep 核心（TTL+LRU+宽限期+12hex 候选） | a954598 | 8 用例 TDD，8 passed |
| T2 BioRunner .last_access 打点（12hex 校验防穿越） | 6c8a1b3 | 5 用例，21 passed（含既有 16） |
| T3 settings 五配置项（env 全可覆盖） | f651b5e | 2 用例，10 passed |
| T4 ws_client bio-workspace-gc sweeper 线程 | 474fb18 | 4 用例，4+5 passed |

## T5 验证

- 全量回归：**830 passed, 3 warnings**（811+19 新增；3 条第三方 warning 不变）
- 冒烟 8 步：**SMOKE PASS**（load 5s / qc 4s / process 24s / markers 13s /
  plot 6s / domains 18s / commot 19s / deconvolve 339s；17 产物 OK；
  ari=1.0, top_path=VEGF, n_lr=3, n_types=3）——打点未影响 bio 链路
- 真机 GC 验证：
  1. 造假超期数据集 `deadbeef0000`（mtime 回拨 8 天）
  2. `BIO_WORKSPACE_GC_INTERVAL_SEC=60` + `--force` 重启：sweeper 启动日志
     正确；60s 后 `deleted deadbeef0000 (freed 3 bytes)` + 汇总日志 ✓
  3. 4 个真实数据集 + smoke_st 全部无损 ✓
  4. 去 env 正常重启：`(interval=3600s, ttl=604800s, cap=10GB)`，
     Lark connected，单实例运行
- `--force` 接管链路（Phase 22 T1）顺带二次真机验证通过

## 过程教训

- **smoke_st_chain.py 的 visium 参数必须绝对路径**（docstring 已写明，
  本轮误传相对路径导致 docker -v 挂载失败 rc=1 且 stderr 为空——docker
  对相对挂载路径的报错走 stderr 但脚本只打印了截断尾部；按绝对路径
  重跑即过）。
- smoke 脚本 WS 固定为 bio_workspace/smoke_st（自带隔离），与主 workspace
  数据集互不干扰；GC 的 12hex 候选过滤天然跳过 smoke_st。

## 后续建议

1. GC 默认阈值（7d/10GB/2h/3600s）按当前 tiny 数据规模设定；接入真实
   GB 级数据后如触发频繁误删预期内数据，调 BIO_WORKSPACE_TTL_SEC /
   BIO_WORKSPACE_CAP_GB。
2. 被 GC 删除的数据集重新 sc_load/st_load 即可重建（幂等键不变秒回），
   若用户反馈"dataset not found"困惑，可在该错误消息中提示"可能已被
   GC 清理，请重新 load"。
3. 遗留不变：planner repr 串长期方案、P15 群聊 2 场景补测、
   Phase 16 沙箱网络白名单/产物回收。

---

# Phase 24 planner repr 串执行层纠正（20:30-21:10）

实施计划：`docs/superpowers/plans/2026-09-02-phase24-planner-repr-coercion.md`
spec：`docs/superpowers/specs/2026-09-02-planner-repr-coercion-design.md`
执行方式：Subagent-Driven（T1/T2 子代理零偏离，T3 父代理）。

## 任务与 commit

| 任务 | commit | 结果 |
|---|---|---|
| T1 param_coerce 纯函数（JSON/repr 双段还原，类型不匹配保留） | 2917a2f | 9 用例 TDD，9 passed |
| T2 execute() 集成 + warning 日志 | 6347e56 | 红绿严格（断言差逐字符合预期），13 passed |
| T3 planner prompt 源头减量 + 回归 + 重启 | 本 commit | 840 passed, 3 warnings |

## 设计要点

- 根因：planner（LLM）把 array/object 参数二次编码成 repr 字符串
  （"genes": "[\'A\',\'B\']"），plan JSON 整体合法查不出，流进 handler 才炸；
  既有点修 4 处同模式复制（serializer/parse_gene_list/_parse_literal/
  kernel_manager），新工具加参就得再贴。
- 方案 A：执行层 choke point 按 ToolSpec.parameters 声明类型纠正
  （array/object + 非空 str → json.loads → ast.literal_eval，类型须匹配）；
  失败/不匹配原样保留不掩盖真错误；既有点修幂等共存。
- 可观测：纠正成功记 warning `tool X param coerced from repr-string: [...]`，
  作为 LLM 复发率长期观测点。
- prompt 减量：planner 输出格式段追加"数组/对象参数必须输出真 JSON"。

## 验证

- 全量回归 **840 passed**（830+10），warnings 不变（3 条第三方）
- ws_client `--force` 重启生效：全 scanner + GC sweeper + Lark connected
- 纠正是被动兜底，LLM 输出正常时零行为变化——不安排专项真机任务，
  后续 /research 真机轮若日志出现 coercion warning 即证明链路生效
  （且任务不再因 repr 串失败）。

## 后续建议

1. 观测 1-2 周 coercion warning 频率：若降为 0 说明 prompt 指引有效；
   若高频出现可考虑上游 planner 结构化输出（远期池）。
2. 既有点修（parse_gene_list 等）暂不清理（幂等共存零成本）；下次动
   对应文件时可顺手收编到 coerce_params 口径。
3. 遗留不变：P15 群聊 2 场景补测、Phase 16 沙箱网络白名单/产物回收。

---

# 勘误：Phase 16"余项"实为决策不做（2026-09-02 21:20）

此前多轮测试总结的"遗留/后续建议"把"Phase 16 沙箱网络白名单、
workspace 产物回收"列为待办——经复查 Phase 16 spec §1 现状核实表，
这两项是 2026-09-01 评估后**明确决策不做**的非目标（spec §3.3）：

- 网络白名单：沙箱默认 `docker_network_mode="none"` 全断网，比白名单
  更严格；需要网络的研究走宿主侧工具。引入白名单=引入放行面。
- workspace 产物回收：workspace 是容器内 tmpfs（512MB），容器 stop 即
  rm -f，无主机挂载、无残留路径。

ROADMAP 变更记录已同步修正措辞。今后"遗留"清单不再含此两项；
真正剩余的历史遗留只有：P15 群聊 2 场景补测（需人工）。
