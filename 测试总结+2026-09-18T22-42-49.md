# 测试总结（2026-09-18 22:42）——Phase 68：st_niche_scan 全 niche NicheNet 批量扫描工具化

## 验证一览

| # | 场景 | 结果 | 关键数字 |
|---|------|------|----------|
| 1 | 容器脚本 TDD 单测 | 全绿 | p68-2 落地：枚举/预算自管/逐 niche 容错/汇总断言 |
| 2 | L3 契约测试（test_l3_dispatch_contract） | 2 passed | timeout 表守护认可 3600（附录 A 3600 行 +st_niche_scan）；参数合成走 spec |
| 3 | 工具清单守护（test_l3_spatial） | 14 passed | st_* 17 工具（Phase 68 +st_niche_scan；函数名 fourteen 历史遗留只改清单不改名） |
| 4 | 合成冒烟 _smoke_st_niche_scan.py（5 轮） | SMOKE OK | 20×20 三区网格 F/R/S：R 区 A2M self-hit top10 真值锚自证、matrix=(30,3)、slug 产物、enum-empty/bad-groupby/no-spatial 三拒收全过 |
| 5 | 真机 oscc 对拍（probe_oscc_nichescan_tool.py） | **全绿** | 5/5 niche top_ligands aupr **逐值一致** + n_sender/n_receiver/n_geneset_used/n_ligands_tested/ring_counts 全对齐；三件汇总产物（nichenet_allniche_matrix.csv 等）落盘 |
| 6 | 门禁 ruff check / mypy（默认+linux） / pytest 全量 | 全绿 | ruff All checks passed；mypy 双平台 Success（修复 7 错后）；**pytest 1341 passed**（153.93s，基线维持零回归） |

## 真机对拍生物学口径（工具版 = 探针版逐位复现）

- SCC=CDH1/IGFL2/ADM、LP Stroma=CXCL1/ADM/COL4A5、LN Stroma=TIMP2/CD82/COL17A1、Mucosa=NECTIN1/CD59/ATP1B1、Glandular=IL6/ADM/CSF3——与 Phase 67 探针结论逐位一致，工具化零语义漂移。

## 关键修复与教训沉淀

1. **fail() 终止语义三分法**（本阶段最重要发现）：sandbox/sc_tools 的 `common.py fail()` 只 emit JSON **不终止进程**；main 内用法=fail+return；子函数内=fail+`raise SystemExit(1)`（load.py L21/48 先例，裸 return 会让 main 解包 None 抛 TypeError 产生第二个 JSON——冒烟第 3 轮"双 JSON"事故根因）；`run()` 包装器显式重抛 SystemExit。
2. **NicheNet aupr 机理与 geneset 纯度**：配体活性来自"先验靶在 receiver geneset 中富集"而非"配体在 sender 表达高"（后者只影响 min_expr 门控）；geneset 混入噪声基因会稀释 aupr 区分度（全体 0.64+，hub 配体 PDPN 领跑）——真值锚自证要求 geneset 与锚靶等宽（100% 纯）。
3. **mypy 抓出签名注解真 bug**：`_load_and_validate` 签名声明 5 元组、实现返回 4 元组（docstring 旧口径残留）——运行时解包按实际 4 个成功所以对拍能跑通，属"注解漂移无害但门禁必抓"；同批修复裸 `dict` 泛型 ×4、json.loads 返回 Any ×1。
4. **对拍首轮唯一 diff = results 缺 ring_counts 字段**：探针 meta 有、工具版 stdout 没放——补一行对齐后二轮全绿；对拍脚本按字段逐项比对让"字段缺失"以 None mismatch 形式显式暴露，比整 dict 比对可诊断性强。
5. **ruff format 新版 hug-brackets**：会对既有注册块大改（`))`→`)`换行`)`）——项目实际门禁是 ruff check 非 format，既有文件保持旧风格、新文件单独 format。
6. 冒烟矩阵 (27,3)→(30,3)：ligands 采样防 var_names 重复（`[g for g in hl if g != "A2M"][:59] + ["A2M"]`），AnnData 唯一性 UserWarning 消除。

## 后续建议

- st_niche_scan 已进 ci.yml bio-image-smoke 冒烟链；下次 manual run 可顺带验收 CI 侧（本机已 5 轮全绿）。
- 矩阵行数随 niche 数增长（本例 30 配体×5 niche），热图暂未超标注阈值；未来大切片 niche>10 时考虑按列 z-score 或 cap 行数（Phase 67 建议顺延）。
- 预算自管口径：单 niche 上限 1800s、收尾余量 180s、TOOL_TIMEOUT 3600s——超预算 niche 记 TIME_BUDGET 进 failures 不中断；若真实数据 niche 数显著增多（>15），考虑 timeout 分档或并发。
- oscc 继续作为 st 侧真机基准；st_niche_scan 与探针版逐值等价已坐实，后续 st_nichenet 改动可用对拍脚本一键回归。
