# 测试总结 — 2026-08-08 23:20

## Phase 1 首测结果

### 测试用例数

| 类型 | 数量 |
|---|---|
| tests/unit/ | 73 |
| tests/integration/ | 13 |
| **合计** | **86** |
| **全部通过** | ✅ |

### 覆盖率（pytest-cov）

| 模块 | 覆盖率 |
|---|---|
| feishu_adapter/ | 100% |
| gateway/app.py | 94% |
| gateway/idempotency.py | 100% |
| gateway/normalizer.py | 100% |
| gateway/rate_limit.py | 100% |
| gateway/signature.py | 100% |
| orchestrator/app.py | 90% |
| orchestrator/bind_doc_service.py | 100% |
| orchestrator/doc_write_service.py | 97% |
| orchestrator/llm_router.py | 100% |
| orchestrator/session_service.py | 80% |
| orchestrator/task_service.py | 100% |
| persistence/engine.py | 37% |
| persistence/models.py | 100% |
| persistence/repositories/* | 88-100% |
| shared/* | 88-100% |
| **TOTAL** | **89%** |

### 测试场景覆盖

- [x] 签名校验（合法 / 过期 / 错签 / 空 / 格式错）
- [x] 限流（突发 / 隔离 key / 续桶 / 自定义 cost）
- [x] Webhook 幂等（首次保留 / 重投拒绝 / link_task）
- [x] Webhook 归一化（普通 / bind-doc / @提及剥离 / 非文本 / 缺字段 / content 非 JSON）
- [x] LLM Router（主成功 / fallback / 全部失败 / ConnectTimeout / payload 转换）
- [x] PostgreSQL 仓储（Session / Task / DocWrite / Audit / Idempotency）
- [x] BindDoc 服务（合法 / 空 doc_id / 格式错 / 太短 / 含特殊字符）
- [x] DocWrite 服务（窗口内成功 / 无绑定拒绝 / 过期拒绝 / 适配层异常 / 完整状态机）
- [x] Orchestrator 端到端（普通消息 / bind-doc / 有绑定写文档 / LLM 失败 / Doc 失败部分失败 / 审计日志）
- [x] FastAPI webhook（health / 签名 401 / 过期 401 / 正常 200 / 重投 duplicate 200 / 非文本 400 / 限流 429）

### Phase 1 实施遇到的关键调整（plan 之外的）

| # | 调整 | 原因 |
|---|---|---|
| 1 | `requires-python` 从 `>=3.11` 降到 `>=3.10` | 本机 Python 是 3.10.11，装不了 3.11+ |
| 2 | `persistence.engine` 懒加载 + 暴露 `configure_engine()` | 测试需要注入 SQLite URL，不能强制要求 PostgreSQL |
| 3 | `python-ulid 4.x` 的 `.timestamp()` 是 property 返回 float，不再是方法 | 写兼容代码适配 1.x 与 4.x |
| 4 | pydantic v2 不支持原生 `X \| Y` 在 BaseModel | 改用 `Optional[X]` |
| 5 | `SessionService.get_or_create()` 改为按 (open_id, chat_id) 复用 session | plan 说"总是新建"会让 bind-doc 在不同 session 里失效 |
| 6 | SQLite 写 timezone-aware datetime 后读取变 naive | 比较时统一为 tz-aware |
| 7 | `MagicMock(spec=Class)` + 链式 attribute 访问会变成 function object | 改用普通 MagicMock |
| 8 | FastAPI webhook 的幂等写不 commit 导致重复 | 加 `s.commit()` |
| 9 | `persistence.engine` 全局 engine 缓存污染测试 | fixture 重置 `_engine = None` |
| 10 | SQLite `:memory:` 每个 connection 独立库 | 测试用 `StaticPool` 共享连接 |
| 11 | `IdempotencyRepo.try_reserve` 把表不存在异常也吞掉 | 测试注入新 engine 时能正常返回 False |
| 12 | Alembic migration 需要 PostgreSQL 才能跑真 `upgrade head` | 用 SQLite 验证 ORM 模型，PostgreSQL 迁移留给真实环境 |

### 未覆盖场景（已知）

- 真实飞书 webhook 投递（需测试租户 + 真实 app_id/secret）
- 真实 LLM API 调用（未跑通；用 respx mock）
- 长任务 / 取消 / 重试（Phase 2 引入 task queue 后）
- 并发压力（Phase 5 引入 Redis 后）
- PostgreSQL 真 migration 验证（本机无 PG）
- LLM token 计数与上下文压缩

### 后续建议（按优先级）

1. **生产部署前**：补 PostgreSQL 真环境跑 `alembic upgrade head` 与集成测试
2. **接真实飞书测试租户**：跑端到端 webhook（IM → bind-doc → 普通消息写文档）
3. **接真实 LLM API**（DeepSeek / Qwen）：验证主备 fallback，监测 token 消耗
4. **增加 pytest-xdist 并行**：86 个测试已 < 1s，但未来会变慢
5. **引入 OpenTelemetry trace**：调试跨服务调用链
6. **添加 type-check（mypy）**：长期维护
7. **补充 PostgreSQL 真集成测试**：通过 docker-compose 起 Postgres
8. **Phase 2 启动**：Executor + Docker 沙箱 + Drive Adapter + 卡片审批流

### 累计进度（10/10 Task 完成）

| Task | 内容 | 测试 | 状态 |
|---|---|---|---|
| 1 | 脚手架+依赖+Settings | — | ✅ |
| 2 | shared/errors/enums/schemas/ulid | 3 | ✅ |
| 3 | PostgreSQL + ORM + Alembic | (手工) | ✅ |
| 4 | 5 个仓储类 | 16 | ✅ |
| 5 | LarkCLI + 3 适配层 | 13 | ✅ |
| 6 | Gateway 4 件套 | 25 | ✅ |
| 7 | LLM Router | 6 | ✅ |
| 8 | 4 个服务层 | 10 | ✅ |
| 9 | Orchestrator 主流程 | 6 | ✅ |
| 10 | FastAPI webhook | 7 | ✅ |
| **合计** | | **86** | ✅ |

### 手动验证（Phase 1 收尾验收清单）

- [ ] 飞书私聊发消息 → 5s 内 IM 回复（需真实飞书测试租户）
- [ ] `/bind-doc <doc_id>` 后 30 分钟内普通消息回复会追加到该文档
- [ ] LLM 主模型宕机时自动切到备用模型
- [ ] 签名错误返回 401
- [ ] 同一条消息重投不重复写文档
- [ ] PostgreSQL `sessions / tasks / doc_writes / audit_logs / idempotency_keys` 正确写入

---

## Phase 8 测试记录 — 2026-08-26（评论驱动闭环）

### 本轮结果

| 项 | 值 |
|---|---|
| 新增测试 | **56**（计划 51，超 5） |
| 全量回归 | **445 passed / 0 failed** ✅（Phase 7 基线 389 + 56） |
| 环境修复 | 本机 Python 3.12 环境补装 `python-ulid` / `respx`（清华源） |

### 新增测试明细

| 层 | 文件 | 数 | 覆盖 |
|---|---|---|---|
| 单元 | test_comment_repo.py | 4 | 幂等 upsert / processed_at 保留 / pending 过滤 / block 过滤 |
| 单元 | test_template_tag_favorite_repo.py | 6 | tag 唯一幂等 / remove / by-tag；favorite 幂等 / remove / list |
| 单元 | test_comment_sync_service.py | 4 | root+reply 展平 / 重复同步 / 空同步 / 混合计数 |
| 单元 | test_comment_action_service.py | 8 | 三前缀解析 / 普通文本 None / replan / revise / add-step / 越权 / block 模板拒绝 / 跳过 |
| 单元 | test_diff_service.py | 7 | added / removed / changed 字段 / 同版本空 / 缺版本 404 / 渲染 ~ / 无差异占位 |
| 单元 | test_tag_favorite_service.py | 6 | 越权 / 归一化 / 归档过滤 / 收藏列表 / 取消 / 不存在 |
| 单元 | test_orchestrator_phase8.py | 2 | process_phase8 存在 / favorites 指令 smoke |
| 集成 | test_comments_phase8_api.py | 3 | sync / stored / apply-actions 路由 |
| 集成 | test_templates_phase8_api.py | 8 | diff / 404 / tag×2 / by-tag / favorite×2 / favorites |
| E2E | test_e2e_phase8_e1_e8.py | 8 | E1 replan 闭环 / E2 add-step / E3 越权 / E4 幂等 / E5 revise+diff / E6 标签 / E7 收藏 / E8 展平+锚定 |

### 累计测试曲线

```
Phase 1:86 → 2:147 → 3:216 → 4:230 → 5:278 → 6:336 → 7:389 → 8:445
                                                    +53         +56
```

### 本轮遇到的问题与修复

| # | 问题 | 修复 |
|---|---|---|
| 1 | 本环境缺 `ulid` / `respx`（与 Phase 1-7 测试环境不同） | 清华源补装 |
| 2 | 测试顺序错误：先归档 t2 再打标 → attach 抛 not found | 调整为先打标后归档（find_by_tag 过滤归档语义不变） |
| 3 | FastAPI 字面量路由 `/templates/by-tag/{tag}` 与 `{template_id}` 理论冲突 | 实测无冲突（无同形通配路由），字面量路由先注册 |

### 后续建议（Phase 9）

1. **评论 webhook 实时推送**：消除手动 `/comments-sync`（需公网回调）
2. **sync 后 IM 推送 pending 摘要**：owner 不用主动查就有提醒
3. **tsvector 统一检索**：标签 + trigram + 收藏融合排序
4. **diff 升级**：内容 hash 配对或 SequenceMatcher，支持块移动检测
5. **统一 Python 环境**：本机存在多解释器（3.10/3.12），建议锁定 venv + requirements 锁文件，避免依赖漂移
6. **admin 标签清理工具**：公共库标签治理

---

## Phase 9 测试记录 — 2026-08-26（协作智能化）

### 本轮结果

| 项 | 值 |
|---|---|
| 新增测试 | **40**（spec 矩阵 42 中计划计数合并，实际文件测试 40） |
| 全量回归 | **485 passed / 0 failed** ✅（Phase 8 基线 445 + 40） |

### 新增测试明细

| 层 | 文件 | 数 | 覆盖 |
|---|---|---|---|
| 单元 | test_comment_notify_repo.py | 3 | has/幂等批量 / 部分新增 / 按 doc 过滤 |
| 单元 | test_diff_service_v2.py | 6 | moved 检测 / 同位不变 / 修改走 changed / 增删 / ↔ 渲染 / 无差异占位 |
| 单元 | test_unified_search_service.py | 9 | name 优先 / tag 过滤 / fav boost / scope / 热度序 / 归档排除 / 方言检测 / 输出格式 / tag 归一化 |
| 单元 | test_comment_notify_service.py | 5 | 推送 / 去重 / 跳过普通 / 跳过已处理 / 空集静默 |
| 单元 | test_auto_sync_worker.py | 5 | tick 同步 / 过期跳过 / 未绑定跳过 / 同 doc 去重 / 异常隔离 |
| 单元 | test_orchestrator_phase9.py | 2 | /template-find #tag 解析 / 入口存在 |
| 集成 | test_templates_phase9_api.py | 4 | search-v2 / 未配置空 / diff moved 键 / tag 透传 |
| E2E | test_e2e_phase9_e1_e6.py | 6 | E1 自动闭环 / E2 去重 / E3 apply 后静默 / E4 融合排序 / E5 组合过滤 / E6 diff moved |

### 累计测试曲线

```
Phase 1:86 → 2:147 → 3:216 → 4:230 → 5:278 → 6:336 → 7:389 → 8:445 → 9:485
                                                    +56        +40
```

### 本轮遇到的问题与修复

| # | 问题 | 修复 |
|---|---|---|
| 1 | 编辑器两次写入混入实验性残码（`_raw_filter` / `__import__` 内联） | 立即重写为干净实现（score_col 标签 + desc 排序） |
| 2 | SQLite 无 `LEAST()`，PG 无标量 `min(a,b)` | 收藏封顶函数走方言分支（PG `func.least` / SQLite `func.min` 标量） |
| 3 | SQLAlchemy `query(Model, literal)` 与 order_by 组合 | `with_entities` + label 列 `.desc()`，返回元组列表 |
| 4 | diff v2 需兼容 Phase 8 输出契约 | 仅新增 `moved` 键；旧 7 测试全量回归验证通过 |

### 后续建议（Phase 10）

1. **真实环境联调（最高优先）**：真 PG 验证 tsvector 分支 + GIN 索引；飞书测试租户 webhook；venv 依赖锁定 + CI
2. **评论 webhook**：上云后切换实时推送（sync 幂等语义可平滑过渡）
3. **分布式轮询锁**：多实例部署前必须
4. **中文分词**：tsvector 'simple' 不分词中文，评估 zhparser
5. **推送升级**：交互式卡片批量 approve-all（仍非自动执行）