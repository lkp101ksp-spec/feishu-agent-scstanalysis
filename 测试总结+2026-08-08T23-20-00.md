# 测试总结 — 2026-08-08 23:20

## Phase 1 首测结果

### 测试用例数

| 类型 | 数量 |
|---|---|
| tests/unit/ | 73 |
| tests/integration/ | 13 |
| **合计** | **86** |
| **全部通过** | ✅ |

### 覆盖率（pytest-cov）

| 模块 | 覆盖率 |
|---|---|
| feishu_adapter/ | 100% |
| gateway/app.py | 94% |
| gateway/idempotency.py | 100% |
| gateway/normalizer.py | 100% |
| gateway/rate_limit.py | 100% |
| gateway/signature.py | 100% |
| orchestrator/app.py | 90% |
| orchestrator/bind_doc_service.py | 100% |
| orchestrator/doc_write_service.py | 97% |
| orchestrator/llm_router.py | 100% |
| orchestrator/session_service.py | 80% |
| orchestrator/task_service.py | 100% |
| persistence/engine.py | 37% |
| persistence/models.py | 100% |
| persistence/repositories/* | 88-100% |
| shared/* | 88-100% |
| **TOTAL** | **89%** |

### 测试场景覆盖

- [x] 签名校验（合法 / 过期 / 错签 / 空 / 格式错）
- [x] 限流（突发 / 隔离 key / 续桶 / 自定义 cost）
- [x] Webhook 幂等（首次保留 / 重投拒绝 / link_task）
- [x] Webhook 归一化（普通 / bind-doc / @提及剥离 / 非文本 / 缺字段 / content 非 JSON）
- [x] LLM Router（主成功 / fallback / 全部失败 / ConnectTimeout / payload 转换）
- [x] PostgreSQL 仓储（Session / Task / DocWrite / Audit / Idempotency）
- [x] BindDoc 服务（合法 / 空 doc_id / 格式错 / 太短 / 含特殊字符）
- [x] DocWrite 服务（窗口内成功 / 无绑定拒绝 / 过期拒绝 / 适配层异常 / 完整状态机）
- [x] Orchestrator 端到端（普通消息 / bind-doc / 有绑定写文档 / LLM 失败 / Doc 失败部分失败 / 审计日志）
- [x] FastAPI webhook（health / 签名 401 / 过期 401 / 正常 200 / 重投 duplicate 200 / 非文本 400 / 限流 429）

### Phase 1 实施遇到的关键调整（plan 之外的）

| # | 调整 | 原因 |
|---|---|---|
| 1 | `requires-python` 从 `>=3.11` 降到 `>=3.10` | 本机 Python 是 3.10.11，装不了 3.11+ |
| 2 | `persistence.engine` 懒加载 + 暴露 `configure_engine()` | 测试需要注入 SQLite URL，不能强制要求 PostgreSQL |
| 3 | `python-ulid 4.x` 的 `.timestamp()` 是 property 返回 float，不再是方法 | 写兼容代码适配 1.x 与 4.x |
| 4 | pydantic v2 不支持原生 `X \| Y` 在 BaseModel | 改用 `Optional[X]` |
| 5 | `SessionService.get_or_create()` 改为按 (open_id, chat_id) 复用 session | plan 说"总是新建"会让 bind-doc 在不同 session 里失效 |
| 6 | SQLite 写 timezone-aware datetime 后读取变 naive | 比较时统一为 tz-aware |
| 7 | `MagicMock(spec=Class)` + 链式 attribute 访问会变成 function object | 改用普通 MagicMock |
| 8 | FastAPI webhook 的幂等写不 commit 导致重复 | 加 `s.commit()` |
| 9 | `persistence.engine` 全局 engine 缓存污染测试 | fixture 重置 `_engine = None` |
| 10 | SQLite `:memory:` 每个 connection 独立库 | 测试用 `StaticPool` 共享连接 |
| 11 | `IdempotencyRepo.try_reserve` 把表不存在异常也吞掉 | 测试注入新 engine 时能正常返回 False |
| 12 | Alembic migration 需要 PostgreSQL 才能跑真 `upgrade head` | 用 SQLite 验证 ORM 模型，PostgreSQL 迁移留给真实环境 |

### 未覆盖场景（已知）

- 真实飞书 webhook 投递（需测试租户 + 真实 app_id/secret）
- 真实 LLM API 调用（未跑通；用 respx mock）
- 长任务 / 取消 / 重试（Phase 2 引入 task queue 后）
- 并发压力（Phase 5 引入 Redis 后）
- PostgreSQL 真 migration 验证（本机无 PG）
- LLM token 计数与上下文压缩

### 后续建议（按优先级）

1. **生产部署前**：补 PostgreSQL 真环境跑 `alembic upgrade head` 与集成测试
2. **接真实飞书测试租户**：跑端到端 webhook（IM → bind-doc → 普通消息写文档）
3. **接真实 LLM API**（DeepSeek / Qwen）：验证主备 fallback，监测 token 消耗
4. **增加 pytest-xdist 并行**：86 个测试已 < 1s，但未来会变慢
5. **引入 OpenTelemetry trace**：调试跨服务调用链
6. **添加 type-check（mypy）**：长期维护
7. **补充 PostgreSQL 真集成测试**：通过 docker-compose 起 Postgres
8. **Phase 2 启动**：Executor + Docker 沙箱 + Drive Adapter + 卡片审批流

### 累计进度（10/10 Task 完成）

| Task | 内容 | 测试 | 状态 |
|---|---|---|---|
| 1 | 脚手架+依赖+Settings | — | ✅ |
| 2 | shared/errors/enums/schemas/ulid | 3 | ✅ |
| 3 | PostgreSQL + ORM + Alembic | (手工) | ✅ |
| 4 | 5 个仓储类 | 16 | ✅ |
| 5 | LarkCLI + 3 适配层 | 13 | ✅ |
| 6 | Gateway 4 件套 | 25 | ✅ |
| 7 | LLM Router | 6 | ✅ |
| 8 | 4 个服务层 | 10 | ✅ |
| 9 | Orchestrator 主流程 | 6 | ✅ |
| 10 | FastAPI webhook | 7 | ✅ |
| **合计** | | **86** | ✅ |

### 手动验证（Phase 1 收尾验收清单）

- [ ] 飞书私聊发消息 → 5s 内 IM 回复（需真实飞书测试租户）
- [ ] `/bind-doc <doc_id>` 后 30 分钟内普通消息回复会追加到该文档
- [ ] LLM 主模型宕机时自动切到备用模型
- [ ] 签名错误返回 401
- [ ] 同一条消息重投不重复写文档
- [ ] PostgreSQL `sessions / tasks / doc_writes / audit_logs / idempotency_keys` 正确写入