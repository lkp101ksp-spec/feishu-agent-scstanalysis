# 测试总结 — 2026-08-08 23:20

## 测试记录 — 2026-09-01 00:25（fasta 真实长度 + utcnow 清理，真机验证通过）

### 改动一：records.length 由 fasta 回填真实序列长度

- 根因：esummary docsum 端点对 protein 库不返回 length（此前全 0，是假数据）
- 修复：[blast_ncbi.py](file:///i:/飞书agent/orchestrator/tools/bio/blast_ncbi.py) 新增 `_fetch_lengths`（efetch fasta 批量取，按 '>' 分条、跳 header、去空白计数），docsum 后回填；fasta 失败降级保留 0 不炸检索
- 单测：+2（`test_handle_fills_real_lengths_from_fasta` / `test_handle_fasta_failure_degrades_to_zero`）
- **真机验证**：BRCA1 human 50 条记录，全长蛋白 **1863 aa**（真实值），isoform 651-717aa 符合实际 ✅

### 改动二：datetime.utcnow() 全量清理

- 22 个文件替换 `datetime.now(UTC)`（`dt.` 前缀用 `dt.UTC`）
- 漏网修复：kernel_manager 的 `default_factory=datetime.utcnow`（无括号形式，正则未覆盖，测试暴露后手工修）
- models.py `_utcnow()` 本就正确（`now(timezone.utc)`），保留
- 弃用警告 **296 → 8**（剩余为 FastAPI on_event 等无关项）

### 结果

| 项 | 数值 |
|---|---|
| 全量用例 | **684 passed**（+2） |
| 弃用警告 | 296 → 8 |
| 真机长度验证 | BRCA1 1863aa 等真实值 |

### 后续建议

1. 剩余 8 条警告为 FastAPI `on_event` 弃用（按先前结论不单独处理）
2. fasta 长度回填使每次检索多一次 efetch 调用，受 rate limiter 约束无风险；如后续做批量检索再评估

---

## 测试记录 — 2026-08-31 23:40（真机测试 A 通过：防刷屏 + NCBI 修复验证）

### 结果

测试 A（`/research 在 NCBI 蛋白库搜索 BRCA1 human，整理所有记录的标题和长度`）真机**全链路 success**：

| 验证点 | 结果 |
|---|---|
| NCBI 大响应解析修复（n1 blast_search, 20 条） | ✅ success（修复前同场景直接 TOOL_EXEC_FAILED） |
| 防刷屏（n1.ids / n1.records / n2.result） | ✅ 均显示「共 20 项（防刷屏略）；首条: …」 |
| 多节点链路（n2 run_python 整理 records） | ✅ stdout 格式化输出 20 条标题列表 |
| card_confirm 写回文档 | ✅ 已写入文档（经卡片确认） |

### 已知数据源特性（非 bug）

- records 的 `length` 全为 0：NCBI esummary docsum 端点对 protein 库不返回 length 字段，工具默认 0。如需真实长度需另调 efetch fasta，当前不做。

### Phase 14 真机验证状态

- [x] 测试 A：防刷屏 + blast_search 修复（本轮通过）
- [x] 测试 B1：`ZQZQZQZQ` 空命中 → 模型生成单节点计划直接返回空结果，无失败，success 收场（自愈未触发，行为正确）
- [x] 测试 B2：accession 字段 → 模型防御性映射到 `id` 字段（uid 即 accession 号），无 KeyError，success 收场（自愈未触发）

**结论**：连续两次真机测试表明 planner 模型生成 run_python 代码防御性足够强（空命中不硬取下标、字段缺失会映射替代），失败路径被自然规避。节点自愈重试作为兜底机制未在真机触发，但已有两条单测覆盖（fake LLM 修复 code 后重跑成功 / 重试上限与错误过滤），功能 correctness 由单测保证。

---

## 测试记录 — 2026-08-31 23:25（真机测试 A 失败 → 修复 NCBI 大响应解析）

### 现象

测试 A（`/research 在 NCBI 蛋白库搜索 BRCA1 human，整理所有记录的标题和长度`）真机失败：

```
n1 TOOL_EXEC_FAILED: Extra data: line 1 column 99177 (char 99176)
```

### 根因（梯度复现确认）

- NCBI efetch（esummary 后端）大响应按 ~82KB 分段，**多个完整 JSON 文档无分隔符直接拼接**
- `resp.json()` 只解析第一段即抛 `Extra data`；max_hits=5/20/50（63KB 内）正常，100 条（145KB）必炸
- 真机失败时模型生成了较大 max_hits（首段 JSON 恰好 99176 chars），复现阈值完全吻合

### 修复

- [blast_ncbi.py](file:///i:/飞书agent/orchestrator/tools/bio/blast_ncbi.py)：新增 `_load_json_stream`（`raw_decode` 逐段解析），`_efetch` 多段合并 records，`_esearch` 防御性同改
- 回归测试 `test_handle_efetch_concatenated_json_merges_records`（两段拼接 JSON → 合并 2 条 records）
- 真机验证：`max_hits=100` 返回 100 条完整 records ✅

### 附带发现与处理

- 系统中残留**两个** ws_client 进程（venv + 系统 Python），会重复消费事件；已全部停止并重启为单进程（23:23 已连接）

### 结果

| 项 | 数值 |
|---|---|
| 全量用例 | **682 passed**（+1 新回归） |
| 真机 max_hits=100 | 100 条 records 解析成功 |

### 后续建议

1. **重跑测试 A**（原文案不变）：预期 blast_search 成功，records 触发防刷屏「共 N 项」格式
2. 诊断脚本保留：[scripts/diag_ncbi_resp.py](file:///i:/飞书agent/scripts/diag_ncbi_resp.py)（梯度复现 NCBI 响应）
3. ws_client 重启后需重新 bind-doc 才能写回文档（旧绑定过期后 renew 卡片会重新发）

---

## 测试记录 — 2026-08-31（Phase 14 收官全量回归）

### 结果

| 类型 | 数量 |
|---|---|
| 全量用例 | **681 passed** |
| 失败/错误 | 0 |
| 耗时 | 55.32s |

### 本轮验证的新功能（Phase 14）

- [x] 节点自愈重试（`test_node_repair_recovers_run_python_failure` / `test_node_repair_limit_and_error_filter`）：run_python 代码级失败由 LLM 修复 code 后重入队，含重试上限与错误类型过滤
- [x] 规则短路三段引用（`test_rule_evaluate_direct`）：`n2.result.percentage` 等多段引用直接数值比较，不调 LLM
- [x] 关键输出防刷屏（`test_outputs_digest_list_anti_spam`）：多项 list 输出只展示「N 项 + 首条摘要」
- [x] ast_guard 语法错误附行列位置 + code 头部
- [x] card_confirm 审批写回归（approve/deny/timeout/无 broker 回退/bind_scope 回归共 5 条）
- [x] DocWriteService 冷导入优化（TYPE_CHECKING 斩断 lark SDK 依赖，~10s → ~0.5s）

### 已知噪音（不阻塞）

- 296 条 `datetime.utcnow()` DeprecationWarning（Python 3.12+）：量大但无害，可后续统一迁移到 `datetime.now(UTC)`
- FastAPI `on_event("startup")` 弃用警告：可后续迁移 lifespan

### 后续建议

1. 真机验证节点自愈重试：构造一个必然失败再修复的 /research 任务，观察 ws 日志中「自愈重试 1/1」记录
2. 真机验证防刷屏效果：跑一个产生大 list 输出的任务，确认 IM 消息为「共 N 项（防刷屏略）」格式
3. 统一清理 `datetime.utcnow()` 弃用警告（建议单独一轮，涉及 ~10 个文件）
4. ws_client 已重启生效；若后续改动 scheduler，需再次重启

---



## Phase 1 首测结果

### 测试用例数

| 类型 | 数量 |
|---|---|
| tests/unit/ | 73 |
| tests/integration/ | 13 |
| **合计** | **86** |
| **全部通过** | ✅ |

### 覆盖率（pytest-cov）

| 模块 | 覆盖率 |
|---|---|
| feishu_adapter/ | 100% |
| gateway/app.py | 94% |
| gateway/idempotency.py | 100% |
| gateway/normalizer.py | 100% |
| gateway/rate_limit.py | 100% |
| gateway/signature.py | 100% |
| orchestrator/app.py | 90% |
| orchestrator/bind_doc_service.py | 100% |
| orchestrator/doc_write_service.py | 97% |
| orchestrator/llm_router.py | 100% |
| orchestrator/session_service.py | 80% |
| orchestrator/task_service.py | 100% |
| persistence/engine.py | 37% |
| persistence/models.py | 100% |
| persistence/repositories/* | 88-100% |
| shared/* | 88-100% |
| **TOTAL** | **89%** |

### 测试场景覆盖

- [x] 签名校验（合法 / 过期 / 错签 / 空 / 格式错）
- [x] 限流（突发 / 隔离 key / 续桶 / 自定义 cost）
- [x] Webhook 幂等（首次保留 / 重投拒绝 / link_task）
- [x] Webhook 归一化（普通 / bind-doc / @提及剥离 / 非文本 / 缺字段 / content 非 JSON）
- [x] LLM Router（主成功 / fallback / 全部失败 / ConnectTimeout / payload 转换）
- [x] PostgreSQL 仓储（Session / Task / DocWrite / Audit / Idempotency）
- [x] BindDoc 服务（合法 / 空 doc_id / 格式错 / 太短 / 含特殊字符）
- [x] DocWrite 服务（窗口内成功 / 无绑定拒绝 / 过期拒绝 / 适配层异常 / 完整状态机）
- [x] Orchestrator 端到端（普通消息 / bind-doc / 有绑定写文档 / LLM 失败 / Doc 失败部分失败 / 审计日志）
- [x] FastAPI webhook（health / 签名 401 / 过期 401 / 正常 200 / 重投 duplicate 200 / 非文本 400 / 限流 429）

### Phase 1 实施遇到的关键调整（plan 之外的）

| # | 调整 | 原因 |
|---|---|---|
| 1 | `requires-python` 从 `>=3.11` 降到 `>=3.10` | 本机 Python 是 3.10.11，装不了 3.11+ |
| 2 | `persistence.engine` 懒加载 + 暴露 `configure_engine()` | 测试需要注入 SQLite URL，不能强制要求 PostgreSQL |
| 3 | `python-ulid 4.x` 的 `.timestamp()` 是 property 返回 float，不再是方法 | 写兼容代码适配 1.x 与 4.x |
| 4 | pydantic v2 不支持原生 `X \| Y` 在 BaseModel | 改用 `Optional[X]` |
| 5 | `SessionService.get_or_create()` 改为按 (open_id, chat_id) 复用 session | plan 说"总是新建"会让 bind-doc 在不同 session 里失效 |
| 6 | SQLite 写 timezone-aware datetime 后读取变 naive | 比较时统一为 tz-aware |
| 7 | `MagicMock(spec=Class)` + 链式 attribute 访问会变成 function object | 改用普通 MagicMock |
| 8 | FastAPI webhook 的幂等写不 commit 导致重复 | 加 `s.commit()` |
| 9 | `persistence.engine` 全局 engine 缓存污染测试 | fixture 重置 `_engine = None` |
| 10 | SQLite `:memory:` 每个 connection 独立库 | 测试用 `StaticPool` 共享连接 |
| 11 | `IdempotencyRepo.try_reserve` 把表不存在异常也吞掉 | 测试注入新 engine 时能正常返回 False |
| 12 | Alembic migration 需要 PostgreSQL 才能跑真 `upgrade head` | 用 SQLite 验证 ORM 模型，PostgreSQL 迁移留给真实环境 |

### 未覆盖场景（已知）

- 真实飞书 webhook 投递（需测试租户 + 真实 app_id/secret）
- 真实 LLM API 调用（未跑通；用 respx mock）
- 长任务 / 取消 / 重试（Phase 2 引入 task queue 后）
- 并发压力（Phase 5 引入 Redis 后）
- PostgreSQL 真 migration 验证（本机无 PG）
- LLM token 计数与上下文压缩

### 后续建议（按优先级）

1. **生产部署前**：补 PostgreSQL 真环境跑 `alembic upgrade head` 与集成测试
2. **接真实飞书测试租户**：跑端到端 webhook（IM → bind-doc → 普通消息写文档）
3. **接真实 LLM API**（DeepSeek / Qwen）：验证主备 fallback，监测 token 消耗
4. **增加 pytest-xdist 并行**：86 个测试已 < 1s，但未来会变慢
5. **引入 OpenTelemetry trace**：调试跨服务调用链
6. **添加 type-check（mypy）**：长期维护
7. **补充 PostgreSQL 真集成测试**：通过 docker-compose 起 Postgres
8. **Phase 2 启动**：Executor + Docker 沙箱 + Drive Adapter + 卡片审批流

### 累计进度（10/10 Task 完成）

| Task | 内容 | 测试 | 状态 |
|---|---|---|---|
| 1 | 脚手架+依赖+Settings | — | ✅ |
| 2 | shared/errors/enums/schemas/ulid | 3 | ✅ |
| 3 | PostgreSQL + ORM + Alembic | (手工) | ✅ |
| 4 | 5 个仓储类 | 16 | ✅ |
| 5 | LarkCLI + 3 适配层 | 13 | ✅ |
| 6 | Gateway 4 件套 | 25 | ✅ |
| 7 | LLM Router | 6 | ✅ |
| 8 | 4 个服务层 | 10 | ✅ |
| 9 | Orchestrator 主流程 | 6 | ✅ |
| 10 | FastAPI webhook | 7 | ✅ |
| **合计** | | **86** | ✅ |

### 手动验证（Phase 1 收尾验收清单）

- [ ] 飞书私聊发消息 → 5s 内 IM 回复（需真实飞书测试租户）
- [ ] `/bind-doc <doc_id>` 后 30 分钟内普通消息回复会追加到该文档
- [ ] LLM 主模型宕机时自动切到备用模型
- [ ] 签名错误返回 401
- [ ] 同一条消息重投不重复写文档
- [ ] PostgreSQL `sessions / tasks / doc_writes / audit_logs / idempotency_keys` 正确写入

---

## Phase 8 测试记录 — 2026-08-26（评论驱动闭环）

### 本轮结果

| 项 | 值 |
|---|---|
| 新增测试 | **56**（计划 51，超 5） |
| 全量回归 | **445 passed / 0 failed** ✅（Phase 7 基线 389 + 56） |
| 环境修复 | 本机 Python 3.12 环境补装 `python-ulid` / `respx`（清华源） |

### 新增测试明细

| 层 | 文件 | 数 | 覆盖 |
|---|---|---|---|
| 单元 | test_comment_repo.py | 4 | 幂等 upsert / processed_at 保留 / pending 过滤 / block 过滤 |
| 单元 | test_template_tag_favorite_repo.py | 6 | tag 唯一幂等 / remove / by-tag；favorite 幂等 / remove / list |
| 单元 | test_comment_sync_service.py | 4 | root+reply 展平 / 重复同步 / 空同步 / 混合计数 |
| 单元 | test_comment_action_service.py | 8 | 三前缀解析 / 普通文本 None / replan / revise / add-step / 越权 / block 模板拒绝 / 跳过 |
| 单元 | test_diff_service.py | 7 | added / removed / changed 字段 / 同版本空 / 缺版本 404 / 渲染 ~ / 无差异占位 |
| 单元 | test_tag_favorite_service.py | 6 | 越权 / 归一化 / 归档过滤 / 收藏列表 / 取消 / 不存在 |
| 单元 | test_orchestrator_phase8.py | 2 | process_phase8 存在 / favorites 指令 smoke |
| 集成 | test_comments_phase8_api.py | 3 | sync / stored / apply-actions 路由 |
| 集成 | test_templates_phase8_api.py | 8 | diff / 404 / tag×2 / by-tag / favorite×2 / favorites |
| E2E | test_e2e_phase8_e1_e8.py | 8 | E1 replan 闭环 / E2 add-step / E3 越权 / E4 幂等 / E5 revise+diff / E6 标签 / E7 收藏 / E8 展平+锚定 |

### 累计测试曲线

```
Phase 1:86 → 2:147 → 3:216 → 4:230 → 5:278 → 6:336 → 7:389 → 8:445
                                                    +53         +56
```

### 本轮遇到的问题与修复

| # | 问题 | 修复 |
|---|---|---|
| 1 | 本环境缺 `ulid` / `respx`（与 Phase 1-7 测试环境不同） | 清华源补装 |
| 2 | 测试顺序错误：先归档 t2 再打标 → attach 抛 not found | 调整为先打标后归档（find_by_tag 过滤归档语义不变） |
| 3 | FastAPI 字面量路由 `/templates/by-tag/{tag}` 与 `{template_id}` 理论冲突 | 实测无冲突（无同形通配路由），字面量路由先注册 |

### 后续建议（Phase 9）

1. **评论 webhook 实时推送**：消除手动 `/comments-sync`（需公网回调）
2. **sync 后 IM 推送 pending 摘要**：owner 不用主动查就有提醒
3. **tsvector 统一检索**：标签 + trigram + 收藏融合排序
4. **diff 升级**：内容 hash 配对或 SequenceMatcher，支持块移动检测
5. **统一 Python 环境**：本机存在多解释器（3.10/3.12），建议锁定 venv + requirements 锁文件，避免依赖漂移
6. **admin 标签清理工具**：公共库标签治理

---

## Phase 9 测试记录 — 2026-08-26（协作智能化）

### 本轮结果

| 项 | 值 |
|---|---|
| 新增测试 | **40**（spec 矩阵 42 中计划计数合并，实际文件测试 40） |
| 全量回归 | **485 passed / 0 failed** ✅（Phase 8 基线 445 + 40） |

### 新增测试明细

| 层 | 文件 | 数 | 覆盖 |
|---|---|---|---|
| 单元 | test_comment_notify_repo.py | 3 | has/幂等批量 / 部分新增 / 按 doc 过滤 |
| 单元 | test_diff_service_v2.py | 6 | moved 检测 / 同位不变 / 修改走 changed / 增删 / ↔ 渲染 / 无差异占位 |
| 单元 | test_unified_search_service.py | 9 | name 优先 / tag 过滤 / fav boost / scope / 热度序 / 归档排除 / 方言检测 / 输出格式 / tag 归一化 |
| 单元 | test_comment_notify_service.py | 5 | 推送 / 去重 / 跳过普通 / 跳过已处理 / 空集静默 |
| 单元 | test_auto_sync_worker.py | 5 | tick 同步 / 过期跳过 / 未绑定跳过 / 同 doc 去重 / 异常隔离 |
| 单元 | test_orchestrator_phase9.py | 2 | /template-find #tag 解析 / 入口存在 |
| 集成 | test_templates_phase9_api.py | 4 | search-v2 / 未配置空 / diff moved 键 / tag 透传 |
| E2E | test_e2e_phase9_e1_e6.py | 6 | E1 自动闭环 / E2 去重 / E3 apply 后静默 / E4 融合排序 / E5 组合过滤 / E6 diff moved |

### 累计测试曲线

```
Phase 1:86 → 2:147 → 3:216 → 4:230 → 5:278 → 6:336 → 7:389 → 8:445 → 9:485
                                                    +56        +40
```

### 本轮遇到的问题与修复

| # | 问题 | 修复 |
|---|---|---|
| 1 | 编辑器两次写入混入实验性残码（`_raw_filter` / `__import__` 内联） | 立即重写为干净实现（score_col 标签 + desc 排序） |
| 2 | SQLite 无 `LEAST()`，PG 无标量 `min(a,b)` | 收藏封顶函数走方言分支（PG `func.least` / SQLite `func.min` 标量） |
| 3 | SQLAlchemy `query(Model, literal)` 与 order_by 组合 | `with_entities` + label 列 `.desc()`，返回元组列表 |
| 4 | diff v2 需兼容 Phase 8 输出契约 | 仅新增 `moved` 键；旧 7 测试全量回归验证通过 |

### 后续建议（Phase 10）

1. **真实环境联调（最高优先）**：真 PG 验证 tsvector 分支 + GIN 索引；飞书测试租户 webhook；venv 依赖锁定 + CI
2. **评论 webhook**：上云后切换实时推送（sync 幂等语义可平滑过渡）
3. **分布式轮询锁**：多实例部署前必须
4. **中文分词**：tsvector 'simple' 不分词中文，评估 zhparser
5. **推送升级**：交互式卡片批量 approve-all（仍非自动执行）
---

## Phase 10 测试记录（2026-08-27）

**范围**：联调工程化清欠账轮——依赖锁定 / 真 PG 验证（迁移 0002 + tsvector/GIN）/ 本机质量门（ruff+cov+check.ps1）/ 飞书就绪包（config_check + 联调指南）。

### 测试结果

| 项 | 结果 |
|---|---|
| 新增测试 | **14**（pg 层 6 + config_check 单测 8） |
| 默认层回归 | **493 passed / 0 failed** ✅（485 + 8，`-m "not pg"`） |
| pg 层（真库） | **6 passed / 0 failed** ✅（docker compose PG16@5433，`-m pg`） |
| ruff check | **0 error** ✅（line-length=110，E701 豁免，tests 目录放宽） |
| 覆盖率 | **85%**（3813 stmts / 576 miss，check.ps1 --cov） |
| check.ps1 全链路 | **[PASS]** ✅（ruff → format 提示 → pytest+cov → -Pg 可选） |

### 新增测试明细

| 层 | 文件 | 数 | 覆盖 |
|---|---|---|---|
| pg | test_pg_migrations.py | 2 | 0002 建全 17 表且与 ORM 集合相等 / downgrade 回 0001 基线可重放 |
| pg | test_pg_search_v2.py | 4 | tsvector 真分支 name>desc 档位 / GIN EXPLAIN bitmap 命中 / E4 融合排序真库复现 / 中文 simple 现状记录 |
| 单元 | test_config_check.py | 8 | 全缺 error / 全齐 0 error / 坏 yaml warn / DB 前缀 error / 可选缺失仅 warn / probe 关不联网 / CLI exit 2&0 / 不可达 warn |

### 累计测试曲线

```
Phase 1:86 → 2:147 → 3:216 → 4:230 → 5:278 → 6:336 → 7:389 → 8:445 → 9:485 → 10:493(+pg 6)
                                                    +56        +40               +8 真库层6
```

### 本轮遇到的问题与修复

| # | 问题 | 修复 |
|---|---|---|
| 1 | conftest 里 `pytestmark = pytest.mark.pg` 不作用于测试项（6 deselected） | mark 移入两个测试模块模块级声明 |
| 2 | `postgresql://` 让 SQLAlchemy 找未安装的 psycopg2 | conftest/模板统一 `postgresql+psycopg://`；libpq 探测时剥离方言段 |
| 3 | Windows 下 `Config("alembic.ini")` 按 cp936 读中文注释 → UnicodeDecodeError | 测试程序化构造 cfg；env.py 需要的 `DATABASE_URL`+飞书/LLM 九变量由夹具 monkeypatch 注入 |
| 4 | 裸 `ts_rank` 对 name/desc 命中同分 → 排序随机（t_desc 压过 t_name） | `case(name 命中 2.0 / 全文命中 1.0) + ts_rank 微分`，与 SQLite 分支口径对齐；WHERE 保持 GIN 索引表达式 |
| 5 | EXPLAIN `.scalar()` 只取首行（Bitmap Heap Scan）漏 Index Scan 行；空表必走 seq scan | fetchall 拼接 + 小写比较 + `SET enable_seqscan=off` 强制暴露索引 |
| 6 | pytest `tmp_path` 夹具在本机 PermissionError（系统 Temp 受限） | config_check 单测改 `tempfile.TemporaryDirectory(dir=ROOT)` |
| 7 | ruff format 会重排存量 201 文件（违背"不改行为"约束） | check.ps1 将 format 降为 informational，pre-commit 仅挂 ruff check（偏差记 spec §14.2-5） |

### 后续建议（Phase 11+）

1. **真飞书租户联调执行**：按 `docs/联调指南.md` 8 步冒烟清单逐项跑（本轮交付的是就绪包，真租户验证尚未发生）
2. **ruff format 专门轮**：一次性全量 format + 全量回归（消除 201 文件欠账）
3. **中文分词**：tsvector `simple` 不切中文（pg 层第 4 测试已固化现状），评估 zhparser/pg_jieba
4. **评论 webhook 实时化**：替代轮询（Phase 9 worker 语义可平滑切换）
5. **CI**：GitHub Actions 跑 check.ps1 等价命令（ruff + 493 默认层），pg 层用 services: postgres

---

## Phase 10 补充：LLM 真实连通性测试（2026-08-27）

**背景**：冒烟清单步骤 3 前半——不依赖飞书租户，用真实 API Key 验证主备 LLM 路由。

### 测试结果

| 项 | 结果 |
|---|---|
| GLM coding plan（`open.bigmodel.cn/api/coding/paas/v4` + `glm-4.7`） | ✅ 200，回复干净（reasoning 在独立字段） |
| GLM 标准端点（`/api/paas/v4`） | ❌ 429 余额不足——**印证 coding plan key 仅 coding 端点生效** |
| MiniMax coding plan（`api.minimaxi.com/v1` + `MiniMax-M3`） | ✅ 200，但 **content 内联 `<think>…</think>`** |
| 项目 `LLMRouter` 主路径（GLM） | ✅ 专业回复，无 think 标签 |
| 项目 `LLMRouter` fallback 路径（primary 指坏端口） | ✅ 自动切 MiniMax，think 已剥离 |
| `config_check --probe --docker` | ✅ 0 errors；双 LLM 在线；Docker 25.0.3 在线 |
| 回归 | **495 passed**（493 + 2 新增 strip_think 测试） |

### 实测发现与修复（2 处）

| # | 发现 | 修复 |
|---|---|---|
| 1 | MiniMax-M3 将推理过程内联在 content（`<think>` 标签），原样返回会污染 IM 回复/文档写入 | `llm_router.strip_think()` 统一剥离（主备路径都过），+2 单测 |
| 2 | `.env` 无加载机制（`load_settings` 只读 `os.environ`，uvicorn 不会自动读 .env） | `settings.load_env_file()` 纯标准库注入（setdefault 语义：显式环境变量优先，不影响测试 monkeypatch） |

`.env` 已写入真实凭据（git 已忽略）；飞书三件套暂为占位，待租户联调替换。

### 后续建议

1. 下一步即租户侧 checklist（联调指南 §2）+ 内网穿透，随后按 §5 冒烟步骤 1/2/4/6 逐项过
2. `glm-4.7` 每次回复消耗 ~450 reasoning tokens，正式跑批前评估成本或用 `thinking: {"type": "disabled"}` 类参数（coding plan 是否支持待验证）

---

## Phase 10 补充：飞书长连接接入（ADR-0031，2026-08-27）

**背景**：用户不采用内网穿透；改用官方 `lark-oapi` SDK WebSocket 长连接接收事件（IM 消息 + 卡片回调），并顺带补齐了缺失的生产组装入口。

### 测试结果

| 项 | 结果 |
|---|---|
| 新增测试 | **7**（ws 适配 4 + runtime 组装 2 + 入口存在 1） |
| 默认层回归 | **502 passed** ✅（495 + 7） |
| pg 层 | **6 passed** ✅ |
| ruff check | **0 error** ✅ |

### 交付物

| 文件 | 说明 |
|---|---|
| gateway/ws_client.py | 长连接进程（`python -m gateway.ws_client`）：SDK model→webhook 兼容 payload 适配层 + dispatcher；IM/卡片事件异常吃掉保连接 |
| gateway/runtime.py | **生产组装入口**（Phase 1 以来首次）：真实 LarkCLI adapters + LLMRouter + repos/services 全家桶 → Orchestrator（Phase 5-9 双通道注入）→ create_app；`build_app()` 供 uvicorn --factory |
| gateway/app.py 重构 | 抽 `run_im_pipeline` / `process_card_payload` 公共管线，webhook 与 ws 零逻辑分叉 |
| ADR-0031 / 指南 §3-4 / .env.example | 长连接为主推方案；可选组 FEISHU_API_* 四变量 |

### 本轮发现与决策

| # | 项 | 说明 |
|---|---|---|
| 1 | lark-oapi 1.7.3 model 结构 | `P2ImMessageReceiveV1(dict)` 可直接构造（测试用）；卡片回调 model 在 `lark_oapi.event.callback.model`（非 api.card） |
| 2 | 卡片回调 ws 化 | `register_p2_card_action_trigger`；卡片按钮 value 约定为平铺字段载体（action/session_id/approval_id），operator open_id 自动合入（value 优先） |
| 3 | 评论全家桶需 API 直连凭据 | FEISHU_API_BASE_URL/TOKEN 不设则优雅缺席（API not configured），IM 主链路不受影响 |
| 4 | BindDocService 构造签名 | 实为 (session_service, audit_repo, ttl, session_repo=, im_adapter=, renew_threshold_sec=)，runtime 组装按实际签名修正 |

### 后续建议

1. 真租户联调：事件订阅选「长连接」，跑 `python -m gateway.ws_client`，按指南 §5 冒烟
2. lark-cli 出站调用仍为 Phase 1 现状——若租户环境无 lark-cli 二进制，IM 回复/doc 写将失败，届时评估切 lark-oapi API 客户端（另开一轮）
3. ws 进程单 Session 模式（低并发联调可接受），多实例部署需 Session 生命周期改造 + ADR-0024 分布式锁

---

## Phase 10 真租户联调记录（2026-08-27 ~ 08-28）

### 已打通子系统（冒烟清单 §5）

| # | 子系统 | 结果 | 关键修复 |
|---|---|---|---|
| 1 | 长连接收事件 | ✅ 单聊 + 群@ | 租户侧补 `im:message.group_at_msg` 权限 + 发布版本 |
| 2 | LLM 路由 | ✅ MiniMax-M3 主 / GLM-4.7 备（主备互换，GLM 额度尽） | — |
| 3 | /bind-doc | ✅ 直链 docx + wiki 链接两种 | wiki token ≠ docx id：normalizer 加 `wiki:` 前缀 + get_node 解析（需 `wiki:wiki:readonly`） |
| 4 | 云文档写入 | ✅ | SDK `client.request` 返回 BaseResponse：JSON 体从 `resp.raw.content` 读 |
| 5 | 锚点定位写入 | ✅ 会话级（`/bind-doc <链接> @章节`）+ 消息级（`#写到 章节 \| 内容`） | 见下方两个 commit；写入插到锚点块后、同锚点续写跟随 |
| 6 | PG 全文（真库） | ✅ 6 passed | — |
| 7 | 续期链路 | 🔧 已接线，待用户点卡片验证 | 扫描器从未在生产启动（本轮接线）；`/bind-doc-renew` 接入 process() |

### 架构级 bug（本轮发现并修复）

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 1 | sessions/tasks 不落库（绑定"成功"但查库为空） | repo 层只 flush 不 commit，生产 runtime 长持有 session 无人 commit | `app.state.main_session` + 管线成功 commit / 异常 rollback（4d81243） |
| 2 | 会话级锚点静默丢失 | `_handle_bind` 未把 anchor 传给 `bind()` | 补传参 + 集成回归测试（304f043） |
| 3 | 同机两个 ws_client 进程（.venv + 系统 Python）双连接 | 手动起进程未清干净 | 清理并固定单实例起停流程 |

### 关键 commit

- `8ae7d5e` wiki 链接支持 / `ec55187` BaseResponse 修复 / `4d81243` 主 session commit
- `bbe2b26` 会话级锚点（sessions.bind_anchor，迁移 0003）
- `0b66ab0` 消息级锚点 `#写到`（doc_writes.anchor_text，迁移 0004）
- `304f043` 锚点传参 bug 回归
- `e76665e` 续期链路接入生产（扫描线程 + 去重 + /bind-doc-renew + 环境变量）

### 当前测试规模

**559 passed**（默认层）+ **6 passed**（pg 层）+ ruff 0 error ✅

### 续期冒烟已完成 ✅（2026-08-28 12:35 收尾）

验证过程与结果：
- 用户重新绑定后收到续期卡片，但点按钮弹「应用不存在」→ **根因：应用未在开发者后台订阅「卡片回传交互」回调**（事件订阅 ≠ 回调订阅，是两个标签页）。修复路径：开发者后台 → 事件与回调 → 回调配置 → 订阅方式长连接 → 添加「卡片回传交互」→ 创建版本并发布
- 配置发布后回调立即打通：日志 `ws card handled: {'ok': True, 'new_expires': ...}`（3 次点击全部续期成功，bind_expires_at 后移）
- 发现点击无反馈体验缺口 → `on_card` 返回 `P2CardActionTriggerResponse` Toast（成功「已续期至 HH:MM」北京时间 / 失败带原因），+3 测试（commit `21826f5`）
- 短 TTL 冒烟期「点完又来一张一样的卡片」是预期现象：TTL(180s) < 阈值(240s)，续期后仍临期，扫描器对新 expires 再发卡；恢复正常配置后不再发生
- 收尾：`.env` 已恢复 TTL=1800 / 阈值=300 / 扫描=60s，ws_client 已重启确认（interval=60s, threshold=300s）
- 期间再次发现同机双 ws_client 进程（.venv + C:\Python312），已清理并固定单实例起停流程

### 幂等重投验证 ✅（2026-08-28 12:50）

飞书后台事件日志不提供手动重推按钮（仅推送失败时自动重试），改用脚本模拟重推：
- 从 `idempotency_keys` 表取最近一条已处理消息的 `message_id`（`om_x100b663b6c8a30b4b328c2cae2a345c`）
- 构造新 `event_id` 的 payload（模拟飞书用新事件 ID 重推同一条消息），调 `run_im_pipeline`
- 结果返回 `{"status": "duplicate"}`，短路未进入业务处理 ✅
- 幂等 key = `app_id:chat_id:message_id`（不依赖 event_id），飞书无论用哪个 event_id 重推同消息都会被 `try_reserve` 短路
- `idempotency_keys` 表共 10 条记录，覆盖 2 天联调消息
- 诊断脚本：`scripts/diag_idempotency.py`

### 模板/评论指令接入生产 IM 路由 ✅（2026-08-28 20:50 用户真机验证通过）

产品决策：**IM 为主 + REST 保留**（科研用户生活在聊天框，REST 留给运维自动化）。实施：
- 抽取 `_try_market_commands` 统一路由器（12 条指令单份维护），生产 `process()` 1.6 分支与 phase5-9 共用，净减 ~55 行重复代码
- 指令清单：模板 9 条（/template-list、-rollback、-share、-submit-public、-fork、-diff、-tag、-favorite(s)、-find）+ 评论 3 条（/comments、/comments-sync、/comment-apply）
- 服务此前已在生产 runtime 组装齐全（orch 属性注入），本次仅补路由缺口
- 评论三件套依赖 `FEISHU_API_BASE_URL/TOKEN` 环境变量（.env 未配 → 回复「未配置」提示，不阻塞）
- 测试：+5 集成测试（路由 template-list/template-find/tag、未配置提示、普通消息不误伤），**564 passed** + ruff 0 error，commit `25e4954`
- 用户真机验证：/template-list、/template-find、普通消息均正常

### 后续建议

1. ~~续期卡片验证通过后：恢复 .env TTL=1800~~ ✅ 已完成（2026-08-28）
2. ~~模板/评论指令接入决策与实施~~ ✅ 已完成（2026-08-28，IM 为主 + REST 保留，commit `25e4954`）
3. ~~幂等重投验证~~ ✅ 已完成（2026-08-28，脚本模拟重推确认 duplicate 短路）
4. LLM 写入内容目前只有 AI 回复；如需「用户原话 + AI 回复」一起落文档再议（用户已确认暂不需要）
5. ~~联调指南建议补一条排障：点卡片按钮弹「应用不存在」~~ ✅ 已补入 §6 排障
6. ~~评论三件套激活：提供开放平台直连凭据~~ ✅ Phase 11 已 SDK 化零凭据（ADR-0032），无需任何凭据即全功能
7. 可选增强：飞书后台注册机器人指令菜单（输入 `/` 自动补全 12 条指令，提升发现性）；IM 回复升级为卡片（长列表更美观）

---

## Phase 11 评论闭环补完（2026-08-28）

### 实施与自动化测试

- 9 任务全部完成：CommentClient SDK 化 / 版本号返回 / 回执挂钩 / CommentEventService+bot_info / runtime 零凭据恒组装 / ws 事件注册+轮询守护线程 / diag_comments.py / ADR-0032~0034 / 全量回归
- commits：`5760fa9`（runtime）、`25b4645`（ws）、`e693f5b`（diag）、`6140d63`（ADR/指南）、`87690fb`（ruff 清理）、`5b441b3`（真机结构修复）
- **全量 578 passed + ruff 0 error**；无 DB 迁移

### 真机发现与修复（本轮核心价值）

1. **element.type=text_run**：官方真机评论正文片段 type 是 `text_run` 而非直觉的 `text`，旧解析全量正文为空 → 通知不触发。commit `5b441b3` 修复并兼容两种
2. **POST replies 响应 reply 在 data 顶层**（无 reply 包裹），reply_id 取值适配
3. **bot info 响应无 data 包裹**（bot 直接在顶层），真机 bot open_id = `ou_a2d9e4b8a0a6afd498a26a239c4d093e`
4. **ws 模式轮询从未运转**：uvicorn startup 钩子在 ws_client 进程不触发，改守护线程（对齐 renew scanner 模式）
5. **双 ws_client 进程抢事件再现**：venv 与系统 Python 各起一个，事件被路由到无输出进程（零日志+无回复）。清理后单实例恢复；惯例「先清旧再单实例」
6. **群消息默认不可达**：机器人在群内只收 @消息（平台权限行为），私聊全功能正常；需群聊则申请「获取群组中所有消息」权限

### 真机闭环验证 ✅（2026-08-28 23:45）

| 步骤 | 结果 |
|---|---|
| 文档发评论 `/revise tpl_test …` | ✅ 轮询同步 5 条，正文正确 |
| IM 待处理通知 | ✅ notified=1（手动 tick 等价轮询单轮） |
| `/comment-apply` | ✅ applied 1 / skipped 4，tpl_test 升 v1 |
| **回执写回** | ✅ 评论区出现「已按此评论完成修改：模板 tpl_test 已更新至版本 1。」 |
| processed_at 打标 | ✅ |

### 后续建议

1. 群聊使用需申请「获取群组中所有消息」权限（或群内 @机器人）
2. ws_client 起停建议固化脚本（清旧+单实例+健康确认），避免再踩双进程坑

### 事件通道真机验证 ✅（2026-08-29 00:23，Phase 11 收官）

后台订阅 `drive.notice.comment_add_v1` + 发布版本后，又发现并修复两个真机问题：

7. **事件字段在 notice_meta 内**：真机事件 file_token/notice_type/from_user_id 嵌在 `notice_meta` 里（comment_id/reply_id 在顶层），旧顶层假设致 file_token 为空 → `ignored_unbound`。commit `444e72b` 修复映射并附原始事件 INFO 日志
8. **独立 session 无 commit 数据丢失**：repo 只 flush、主链路靠管线收尾提交，而 event_session/poll_session 无人提交——事件触发的同步数据全部滞留未提交事务丢失（现象：handled 但库里查不到）。commit `310cc5f`：CommentEventService / CommentAutoSyncWorker 各自负责成功 commit / 异常 rollback，runtime 注入 session

| 验证项 | 结果 |
|---|---|
| 事件接收（后台订阅生效后） | ✅ 00:07:32 首个事件到达 |
| 字段映射修复后 handled | ✅ 500ms 内处理完 |
| 数据落库 | ✅ 「再试试」即时入库；bug 期间丢失的 3 条评论自动补齐 |
| 指令评论秒级 IM 通知（事件通道） | ✅ `/revise` 评论 ≤5s 私聊收到通知 |

**Phase 11 全链路（事件为主 + 轮询兜底 + 回执写回）全部真机验证通过，闭环完成。**

### 群消息权限开通 + 群聊门控真机验证 ✅（2026-08-29 22:07）

处理 spec §14.4 遗留项 1。开通前预判刷屏风险：`process()` 原对非指令消息一律 LLM 回复，群权限开通后每条闲聊都会刷屏——**先加门控再开权限**：

- commit `b49e219`：`IncomingMessage` 加 `chat_type`（ws/webhook 双入口透传）；`process()` 指令路由后插入门控——group 且非指令（非 `/` 开头、非 `#写到`）→ `skipped: group_non_command` 静默忽略；私聊行为不变
- 后台开通 `im:message.group_msg`（敏感权限）+ 创建版本发布；584 单测全过 + ruff clean 后重启 ws_client（又清了一次双进程）

| 验证项 | 结果 |
|---|---|
| 群闲聊静默忽略（门控防刷屏） | ✅ 22:07:10 闲聊 → `skipped: group_non_command`，无回复 |
| 群内指令正常处理 | ✅ 22:07:26 `/template-list` → `template_listed: ['tpl_test']`，群内收到回复 |
| 私聊回归 | ✅ 行为不变（p2p 不走门控，584 单测含回归用例） |

**群聊正式可用：指令照常响应、闲聊门控防刷屏；群消息权限遗留项关闭。**

### 全量评论提醒开关真机验证 ✅（2026-08-29 23:15）

处理 spec §14.4 遗留项 3（commit `e550697`，587 单测全过）：

- `CommentNotifyService` 加 `notify_all` 参数：True 时所有新评论都推（标题 `[评论提醒]`，纯闲聊不附 apply 提示）；False 保持只推指令评论。runtime 三处注入，`COMMENT_NOTIFY_ALL=1/true` 环境变量控制，默认关
- 排障插曲：首轮评论未收到提醒 → 日志显示 `ignored_unbound`（绑定 TTL 30 分钟已过期，非开关问题），重新 `/bind-doc` 后通过

| 验证项 | 结果 |
|---|---|
| 普通评论（无指令）全量推送 | ✅ 23:15:28 事件 → 23:15:29 `notified: 12`（1.2s），私聊收到 `[评论提醒]` |
| 历史未通知评论补推 | ✅ 11 条历史 pending 普通评论一并推送（去重日志此前只记指令评论，预期行为） |

**spec §14.4 三项遗留全部关闭（群消息权限 ✅ / 双进程坑已记档 ✅ / 全量提醒开关 ✅）。当前部署 `COMMENT_NOTIFY_ALL=1` 已开启。**

## Phase 12 科研执行引擎激活（2026-08-30）

### 实施内容（spec：2026-08-29-phase12-design）

激活休眠的 Phase 2-4 引擎（Planner→DAG→Scheduler→Executor），六板块：① `/research` 显式指令路由 ② 引擎生产初始化（init 条件放宽 + adapter None 守卫）③ Planner 工具 schema 内联（LLMRouter 伪 function calling 根治）④ L1 真实现（summarize/classify 接真 LLM，stub 对 Planner 隐藏）⑤ ResearchRunner 后台执行 + 受理即回 ⑥ 配置文档（`LLM_TIMEOUT_SEC`/`RESEARCH_TASK_TIMEOUT_SEC`/`MAX_CONCURRENT_NODES`）。

### 真机六轮迭代修复（本轮核心价值，全在 2026-08-30）

引擎从未接生产，首次真机即连环暴露 LLM 输出与代码假设的落差：

1. **ReadTimeout + 围栏 JSON**：DAG 生成实测 ~94s（30s 硬编码必超时）；输出常带 markdown 围栏。→ 超时改 120s 可配 + 容错解析 + 重试附错误反馈（`5efeb70`）
2. **kind 缺失**：纯 tool 节点模型常省略。→ 缺省 tool + entry 自动推导（`9ded34e`）
3. **inputs int**：max_words=300 炸 `dict[str,str]` 契约。→ str() 强转（`245d4dd`）
4. **调不存在的 API**：read_doc→`read_blocks`、写回→`append_blocks` 均不存在（真实为 `get_block_tree`/`render_blocks`）——引擎代码从未真机跑过的必然结果；且模型无从得知绑定 doc_id。→ 真实 API + session_context 注入 + 失败可见性（节点失败日志 + IM「失败详情」）（`eaa610c`）
5. **`blocks` 键撞名 + risk_level 误判**：read_doc 输出 `blocks` 被 ToolHandler 当富文本块误解析炸掉；ExecutionTask.risk_level 默认 L1 导致 L0 节点白等 30s docker 超时。→ 键改 `block_tree`/`text` + 解析降级 + risk 以注册表为准 + kernel 探测失败一次记忆（`e3a6eb2`）
6. **L2 误规划 + 字段猜错 + 库名错误**：模型见 L2 工具名就规划又被 approval 拒（TOOL_DENIED）；把 blast 输出 records 猜成 summary；`nr` 是 BLAST 库名非 Entrez 库致 esearch 静默 0 命中。→ L2 整体剔除 + prompt 链式示例 + `_resolve_inputs` 别名兜底 + nr→protein 映射（`3c38375`/`ef182b0`/`8344d98`）

### 真机四场景验收 ✅

| 场景 | 结果 |
|---|---|
| ① 私聊 `/research` 绑定文档总结 | ✅ success：read_doc→summarize×2，结果写入绑定文档 |
| ② `/research` BLAST BRCA1 | ✅ success：NCBI 真实 1257 命中，records 展示，摘要准确 |
| ③ 群聊 `/research` | ✅ success：门控放行指令，模型自选 gene 库 |
| ④ 闲聊回归 | ✅ 私聊正常对话；群内非指令按设计 skipped |

自动化 **616 passed + ruff clean**（Phase 11 结束时 587，+29）。

### 后续建议

1. Phase 13（T2）：kernel 镜像构建 + run_python/run_blast 真实沙箱执行——本轮已把「无沙箱降级」路径打稳
2. Phase 13（T3）：branch/while/for 控制流真机验证
3. L2 审批流正式链路（当前研究任务由 Runner 自动写回替代审批）
4. 新工具注册时描述必须声明输出字段（`输出 xxx=<含义>，下游用 <node_id>.xxx 引用`）——本轮验证模型严格跟随该约定
5. ws_client 起停惯例已稳定：StopCommand 清旧 → 单实例后台启动 → CheckCommandStatus 确认 connected

## Phase 13 T2 沙箱执行激活（2026-08-30）

### 实施内容（spec：2026-08-30-phase13-t2-sandbox-design）

run_python 从 stub 变为容器内真实执行，四板块（`5120650`/`68ecaf8`/`47a6517`）：

1. **kernel 镜像**：`sandbox/kernel.Dockerfile`（python:3.11-slim + numpy 2.4.6/pandas/matplotlib，清华 pip 源）+ `run_user.py` harness 固化进镜像（stdout 捕获 + `###RESULT###` sentinel + 末顶层表达式 repr）
2. **exec 通道**：`KernelPool.exec_code`——代码经 stdin `cat > /tmp/r_x.py` 零转义写入 tmpfs → `python /opt/run_user.py` → sentinel 切分 stdout/result；超时→release 重建容器（防残留进程）
3. **run_python 接线**：真实现替换 stub（kernel_pool 注入 + `SANDBOX_TIMEOUT`/`SANDBOX_UNAVAILABLE` 优雅错误码）；app 装配顺序调整（sandbox 先于工具注册）；Planner 可见 + 描述声明输出字段
4. **配置外化**：`DOCKER_*`/`KERNEL_*` 入 settings 环境变量；.env.example + 联调指南 6d 冒烟行

### 本轮核心排障：集成测试卡死 8 分钟（Windows subprocess 管道挂死）

**定位过程**：镜像构建成功后 4 用例集成测试卡死（容器 Up、pytest 无进展）→ 手动 PowerShell 管道正常 → diag 脚本验证 `input=` 写码/执行通道正常 → 唯 `while True` 超时用例必挂 → 最小复现确认 `subprocess.run(timeout=3)` 15s 不返回。

**根因**：`subprocess.run` 超时仅 `kill()` docker.exe 本体；Windows 下 docker.exe 派生子进程仍持有 stdio 管道句柄，`communicate()` 等 EOF 永不到达。容器仍 Up 佐证 release 从未执行到。

**修复**：`sandbox.py` 默认 subprocess 换 `_run_subprocess`（Popen + 超时后 `taskkill /F /T` 杀整棵进程树再回收输出，POSIX 走 `proc.kill()`）。修复后超时用例 5s 准时返回，重建→复用链路正常。

**顺带适配**：numpy 2.x 标量 repr 为 `np.int64(10)`（1.x 为 `10`），断言兼容两种。

### 本轮核心排障②：真机 result=None 假 success（scheduler 引用误判）

**现象**：真机第一轮 `/research` n1 success 但 `[n1.result] None`；plan 日志显示模型代码末行明明有 dict 表达式。

**定位**：容器 `/tmp/r_xxx.py` 为 **0 字节**（现场证据）→ 写码 stdin 为空 → 逐层回溯：diag 证伪 GBK 编码/`_run_subprocess` stdin/ASTGuard 三假设 → 最终锁定 [scheduler.py](i:/飞书agent/orchestrator/planner/scheduler.py) `_resolve_inputs`。

**根因链条**：code 含 `.`（`{float(v):.6e}`）→ 整段被拆成 `<node>.<field>` 引用 → `.` 前非已知节点 → code 置 None → docker exec 不带 `-i` stdin 空 → 0 字节文件执行 rc=0 → **假 success**。

**修复**（`4858d12`）：① 引用判定要求 `.` 前是计划内 node_id（否则字面值透传，+回归测试）；② `exec_code` 空 code 返回 `INVALID_INPUT` 显式失败；③ `stop` kill 后必 rm（清 6 个残留容器）。

### 真机验收 ✅（两轮）

| 轮 | 结果 |
|---|---|
| 1 | ❌ result=None 假 success（暴露引用误判 bug，见上） |
| 2 | ✅ stdout 完整输出 `2^100 = 1267650600228229401496703205376`，位数 31（len/log10 双验证） |

注：轮 2 `[n1.result] None` 为协议正常语义（模型该轮全用 print、末行无表达式），stdout 承载完整答案——两种代码风格（末行表达式 / 全 print）均 success。

### 验收总览

- 真容器集成测试 **4/4 全绿（13.67s）**：roundtrip（2**100）/ numpy 可用 / 1/0→PY_RUNTIME_ERROR / while True→SANDBOX_TIMEOUT→容器重建→复用
- 全量 **635 passed + ruff clean**（Phase 12 结束 629，+2 单测 +4 集成真跑）
- 真机 run_python 沙箱执行打通（spec §9 真机验收 ✅）

### 后续建议

1. T3：branch/while/for 控制流真机验证（引擎已支持，未真机跑过）
2. run_blast 仍为 stub（隐藏）——后续轮次接 NCBI URL API 或 qblast 真实现
3. 沙箱安全可在真机追加验证：`os.system` 被 ASTGuard 拒、死循环被 60s 超时兜底
4. `kernel_exec_timeout_sec` 默认 60s，重计算任务（大矩阵/仿真）可在 .env 调大
5. 经验沉淀：Windows 调 docker CLI 一律走 `_run_subprocess`（新代码勿直接 `subprocess.run(timeout=)`）；`<node>.<field>` 引用语法对含点字符串天然歧义，新工具的字符串参数（尤其代码类）都会踩

---

## Phase 13 T3 控制流执行激活（2026-08-30）

### 实施内容（commit `efa229a`）

| 板块 | 变更 |
|---|---|
| Scheduler 解释器 | 主循环按 kind 分流：tool/llm 走 executor，**branch/for/while 在调度循环内同步解释**（副本展开追加进 plan.nodes 由轮询循环自然调度，不接 PlanRuntime） |
| 副本机制 | node_id 前缀（branch `{bid}_t{n}_`/`_f{n}_`、for `{fid}_i{n}_`、while `{wid}_r{n}_`）+ 依赖重写（指向控制流节点→继承其 depends_on；子树内互链→加前缀；外部依赖保留）；**控制流字段必须随副本传递**（branch 内嵌 while/for 合法） |
| while 重入 | `_while_state[node_id]={round,body_ids,ref_map}`；`_ready_nodes` 门控（上轮 body 全终态才可再判定）；**do-while 语义**（首轮无上游数据免判定先执行一轮）；第 2 轮起判定 prompt 中 body 原始 id 重写为当前轮副本 id |
| LLM 判定器 | `_ask_bool`（中英文措辞容错，**负向词优先**防「不成立」被「成立」抢先命中）；`condition_llm` 未注入/异常/不可解析 → 节点 FAILED（LLM_FAILED） |
| 引用两级语义 | 整值引用（`"n1.text"`）→ 原始值；内嵌引用（code 里 `len(n1.text)`）→ repr(值) Python 字面量，**引号内形态只换内核**（`'n1.text'`→保留外层引号，防 `''xxx''` 双层引号语法错） |
| planner | max_retries 1→2 + 失败留痕（raw head 400 字符 warning）；prompt 补 for `{item}`/branch/while 完整示例 |
| 装配 | research_runner 传 `condition_llm=orch.llm`；run_python description 补内嵌引用说明 |

迭代修复：`2df63af` planner 留痕+重试 / `af6acbd` 判定上下文分字段+长度元数据 / `767cccf` 内嵌引用替换 / `20aa859` 副本控制流字段 / `bb18638` while 判定 prompt 引用重写+留痕 / `b55f69c` while 首轮免判定。

### 本轮核心排障：真机六轮迭代（每轮定位→修复→复验）

| 轮 | 现象 | 根因 | 修复 |
|---|---|---|---|
| 1 | 规划失败 `Expecting ',' delimiter`（重试也没救） | planner 失败不留原始响应，排障盲区 | 失败留痕 raw head + max_retries 2（`2df63af`） |
| 2 | `docx list blocks failed code=1770001` | 绑定过期→模型编 `BOUND_DOC_ID_PLACEHOLDER` | 用户重新 /bind-doc（未改码，记入建议） |
| 3 | b1 判定错走 false_branch（文档明明超 500 字） | 上下文整体 JSON 截断，block_tree 巨字段淹没 text | 分字段格式化 + `text<共N字符>` 长度元数据（`af6acbd`） |
| 4 | n2 `len('n1.text')` 算字面量长度 7 | 模型期望 code 内引用生效，整值替换覆盖不到 | 内嵌引用替换；单测先暴露双层引号→引号内只换内核（`767cccf`） |
| 5 | branch 内嵌 while 副本 `LLM_FAILED: 缺条件 prompt` | `_copy_subtree` 没复制控制流字段 | 副本完整传递全部控制流字段（`20aa859`） |
| 6a | while 19 轮全 true 到 LOOP_MAX_ITER（该停没停） | 判定 prompt 写 body 原始 id `n_step.result`，上下文是副本名 `w1_r0_s1.result`，名字错位 LLM 失据 | ref_map + 判定 prompt 引用重写 + judge INFO 留痕（`bb18638`） |
| 6b | `[w1.rounds] 0` 一轮没跑 | w1 无上游首轮上下文空，LLM 无据判 false（「直到…为止」类任务天然 do-while） | 首轮无上游数据免判定先执行一轮（`b55f69c`） |

**开发期自坑**：① 单测 `asyncio.run() cannot be called from a running event loop`——pytest-asyncio auto 模式下改 `async def _run`；② `_ask_bool` 断言「不成立」误判 true——负向词族优先；③ 内嵌替换 `''xxx''` 双层引号——单测先暴露。

### 真机验收 ✅（三场景）

| 场景 | plan 结构 | 结果 |
|---|---|---|
| ① for：BRCA1 blast 记录逐条中文摘要 | n1(blast) → f1 → f1_i0_s1~f1_i4_s1 | ✅ f1.iterations=5，5 副本逐条摘要，`{item}` 注入生效 |
| ② branch：文档超 500 字→true 分支摘要写回 | n1(docx list) → b1 → b1_tn2(摘要+写回) | ✅ b1.chosen=true_branch，完整摘要落文档（第 3/4 轮才修通） |
| ③ while：反复生成随机数直到 >0.99 | w1 → w1_r0_r1~w1_r10_r1（每轮 run_python 出随机数） | ✅ 11 轮逐轮执行、逐轮判定、正常收敛停止（do-while 首轮 + 10 轮判定） |

**模型行为洞察**：简单任务（「分别计算 2/3/4 的 10 次方」「反复生成随机数」）模型会单 run_python 平铺合理完成——需「项数运行时可知」「逐步执行、禁止单次代码写循环」措辞诱导才生成控制流节点。

### 验收总览

- 新增 [test_scheduler_control_flow.py](i:/飞书agent/tests/unit/test_scheduler_control_flow.py) 16 用例 + test_scheduler 内嵌引用用例
- 全量 **651 passed + ruff clean**（Phase 13 T2 结束 635，+16 单测）
- 真机三场景全绿（for ✅ / branch ✅ / while ✅）

### 后续建议

1. 文档绑定过期应主动提示重新绑定（现靠模型发现编占位符才暴露，本轮场景②第 2 轮即此坑）
2. while 每轮 LLM 判定 ~2s 轮转开销，长循环任务可考虑判定节流或规则短路（如条件是纯数值比较可走 Python 判定）
3. 嵌套控制流跨子树引用失配：branch 子树内节点引用另一子树节点仍按外部依赖处理，跨子树引用会失配（已知局限，当前 prompt 不会生成此类结构）
4. 控制流嵌套深度无显式上限，while body 内再嵌 while 理论可无限展开，深嵌套值得加守护
5. 经验沉淀：控制流排障三件套——plan JSON INFO 日志、planner raw head、judge cond+result+context_head 留痕，均为真机排障盲区驱动补上

---

## Phase 14 L2 写回审批流（2026-08-31）

### 实施内容

| 板块 | 变更 |
|---|---|
| ApprovalBroker（新文件） | doc_write_id → threading.Event 跨线程决策传递：research 线程 `wait` 阻塞 / ws 回调线程 `decide` 写入；**决策先到暂存**（发卡后秒点竞态）；首次决策固化（重复点击幂等拒绝）；wait 取走即清理 |
| Settings | `research_writeback_approval`（card_confirm 默认 / bind_scope 兼容开关）+ `research_approval_timeout_sec`（600），env 可覆盖 |
| DocWriteService | `create_confirm_pending`（bind 校验 + pending 落库）/ `complete_confirmed`（approve→approved→writing→success/failed；deny/timeout→cancelled）/ `cancel_stale_pending`（启动清扫孤儿 pending）；bind 校验抽 `_valid_bound_doc` 复用 |
| 卡片回调 | gateway `process_card_payload` 加 research_writeback 分支（audit 落 operator+decision；target_id 回退 doc_write_id）；AppContext/create_app 透传 broker |
| research_runner | 双模式接线：card_confirm 走 `_writeback_with_confirm`（**DocWriteService 用本线程 session 构造，不跨线程共享主 Session**）；bind_scope / broker 缺失降级直写（回归基线不变） |
| runtime | broker 单例挂 orch + ctx；启动清扫遗留 card_confirm pending |

### 真机验收 ✅（三场景 + 1 项真机发现修复）

| 场景 | 结果 |
|---|---|
| ① 同意写入 | ✅ 卡片（预览+按钮）→ 同意 → 文档末尾写入 → IM「已写入文档（经卡片确认）」→ doc_writes card_confirm/success |
| ② 跳过 | ✅ 不写文档 → IM「已按您的选择跳过写回」→ doc_writes cancelled |
| ③ 超时 | ✅ 不写文档 → IM「审批超时…已跳过写回」→ doc_writes cancelled |

**真机发现并修复：文档看不到研究结果**——用户反馈 IM 有 `[n1.result] 1048576` 但文档只有节点状态表。根因：`render_plan_summary_blocks` 只渲染状态，关键输出只在 IM。修：加 `outputs` 参数，文档新增「关键输出」小节（heading3+列表），复验 3^15=14348907 正确落文档。

### 本轮排障实录：「文档没出现结果」假阴性

**现象**：场景① IM 报「已写入文档（经卡片确认）」，用户称文档没内容。

**定位三板斧**（闭环证据链）：
1. **拉 DB**：doc_writes 最新记录 card_confirm/success（但 anchor_block_id 空——render_blocks 返回 None，属已知局限）
2. **直连 API 拉文档块**：文档 288 个根块，末尾确有新写入三块（Plan 执行结果/Nodes:1/表格）——**写入成功了，在文档末尾，用户没滚到底**
3. **ws 日志交叉验证**：`research plan` 显示该任务 1 节点（run_python code="2**20"）与 Nodes:1 吻合；`approval decided` 的 doc_write_id 与 DB 记录一致

**教训**：「IM 说成功但用户说没看到」先别怀疑代码——写入位置（文档末尾追加）与用户视线（看局部）可能错位；证据链（DB 状态 + API 实拉内容 + 日志 plan/决策）比猜测快。

### 验收总览

- 新增测试：test_approval_broker（6）+ test_doc_write_service（+7）+ test_renew_card_callback（+3）+ test_research_runner（+5 含 bind_scope 回归）
- 全量 **672 passed + ruff clean**（Phase 13 T3 结束 651，+21）
- 真机三场景全绿（同意 ✅ / 跳过 ✅ / 超时 ✅）

### 后续建议

1. `render_blocks` 让 `_sdk_render_blocks` 返回末块 id → 补齐 anchor_block_id（锚点续写跟随才能定位到研究写回）
2. 写回内容可携带任务标识（任务描述摘要/时间戳）——多任务写回同文档时用户难区分是哪次（本轮用户即提出）
3. 卡片预览为纯文本（表格转逗号分隔），富表格预览待做；重复点击无用户可见 toast
4. 节点级 L2 审批（write_doc 开放给 planner + Scheduler 挂起等外部事件）仍为后续架构级议题
5. 群聊场景 operator 非 session owner 不拦截（当前记审计），需产品决策是否强校验

---

## 体验优化轮（Phase 14 后续，2026-08-31 晚，commit `6fdad23`）

Phase 14 真机反馈的四项小优化一轮清掉（无独立 spec，改动均小）：

| # | 项 | 实现 | 验证 |
|---|---|---|---|
| ① | 写回带任务标识 | `render_plan_summary_blocks` 加 `task_label`（描述摘要 30 字 + `· MM-DD HH:MM`） | 真机 ✅ 文档标题「Plan 执行结果（success）— 用 run_python 计算 7 的 12 次方… · 08-31 21:05」 |
| ② | anchor_block_id 补齐 | `_sdk_render_blocks`/`render_blocks` 返回末块 id → `complete_confirmed` 落库 | 真机 ✅ DB 记录 anchor=doxcn7irxz…（此前恒空） |
| ③ | 绑定过期主动提示 | 受理时查 stale bind → IM 提示重新 /bind-doc + session_context「严禁编造 doc_id」 | 单测路径覆盖（真机需绑定自然过期触发） |
| ④ | 判定规则短路 | `_rule_evaluate`：单一 `ref op number` 且上下文值可数值化 → Python 比较不调 LLM（省 ~2s/轮）；多条件/非数值/无值回退 LLM；正则容忍「是否/已/还」引导词 | 单测 ✅（+2 用例，含全程 0 次 LLM 的 while 收敛） |

**顺带修正**：`_body_context` 行名从 `{bid}: {field}: v` 改为 `{bid}.{field}: v`——与判定 prompt 引用（重写后副本 id）完全对齐，规则短路与 LLM 判定都能按名取值（首轮免判定测试断言相应更新为 0 次 LLM）。

### 验收

- 全量 **674 passed + ruff clean**（Phase 14 结束 672，+2）
- 真机：任务标识/关键输出/anchor 三项直接验证 ✅

### 后续建议

1. 规则短路命中率取决于模型是否生成「ref op number」式 prompt——可观察真机 while 任务的 `rule-judge` vs `judge` 日志比例，必要时在 planner prompt 里引导数值条件写法
2. 写回预览卡片仍为纯文本，富表格预览待做（沿用 Phase 14 遗留）

---

## run_blast 清理 + Phase 12/13/14 综合演练（2026-08-31 深夜）

### run_blast 死代码清理（commit `dd534c7`）

调研 NCBI API 细节时发现：**blast_search（L3）早已真实实现**（eutils esearch→efetch，Phase 12 真机验证过），`run_blast` 只是 L1 里从未接沙箱、对 Planner 不可见的纯死代码；FASTA 序列比对（qblast Put/Get）经确认当前用不到，一并放弃。

- 删 [l1_compute.py](i:/飞书agent/orchestrator/tools/builtin/l1_compute.py) 的 run_blast 注册块 + `_run_blast` 函数（docstring 注明去向）
- 测试同步：`test_l1_registers_three` + planner 可见性测试改为「全部真实工具可见」
- 全量 674 passed + ruff clean

### 综合演练设计

一条 `/research` 串联三阶段全部能力：**blast_search（真实 NCBI）→ run_python（真沙箱）→ branch 判定 → 审批卡片写回**（含优化轮的任务标识/关键输出/anchor）。

### 三轮真机迭代（连环挖出两个跨层注入 bug）

| 轮 | 现象 | 根因 | 修复 |
|---|---|---|---|
| 1 | `b1_tn3 PY_RUNTIME_ERROR: TypeError: string indices must be integers` | `exec_code` 的 result 恒为末表达式 **repr 字符串**，下游节点 `data = n2.result` 注入的是带引号字符串字面量，`data["percentage"]` 炸 | 宿主侧 `_revive_result`：`ast.literal_eval` 还原原生 JSON 安全类型（dict/list/数字/bool/None；tuple→list，set/对象 repr 跳过），无需重建沙箱镜像（`203cf91`） |
| 2 | `b1_tt1 TOOL_BLOCKED: code has syntax error; cannot validate safety` | 模型生成 f-string 代码 `f"...{n2.result['percentage']}%"`，注入 dict repr 后 `{` 紧跟 `{` 被 f-string 当转义大括号 → 语法错误 → ast_guard 解析失败整节点被拦 | [scheduler.py](i:/飞书agent/orchestrator/planner/scheduler.py) `_sub` 整段替换时对 `{`/`[`/`(` 开头的容器 repr **加括号包裹**（`({…})['k']` 在 f-string 内合法）（`2672bfc`） |
| 3 | ✅ 全绿 | — | — |

**第三轮验收结果**：n1 blast_search（5 条 BRCA1 真实 NCBI 记录，total_count 67407）→ n2 run_python（占比 0.01%）→ b1 判定 true_branch → b1_tn3 输出「样本充足：取回 5 / 67407，占比 0.01%」→ 卡片确认写入文档。四层能力（真实检索/沙箱计算/控制流/审批写回）首次端到端闭环。

### 验收总览

- 全量 **677 passed + ruff clean**（优化轮结束 674，+3：`_revive_result` 边界 / dict 注入守护含 f-string 场景 AST+求值断言 / 真容器契约更新）
- 三个 commit：`dd534c7`（清理）/ `203cf91`（result 原生类型）/ `2672bfc`（f-string 括号包裹）

### 后续建议

1. **规则短路对三段引用不命中**：本轮 branch 条件为自然语言（「n2.result 中 retrieved 字段…是否 >= 5」），`_RULE_CMP_RE` 只认两段 `ref op number` → 走 LLM 判定（结果正确但多 ~2s）。可扩展支持 `a.b.c` 形式从上下文行取 dict 字段
2. **注入仍是文本替换**：单引号 f-string 内嵌单引号下标在 Python<3.12（当前容器镜像 3.11-slim）会语法错误——本轮 planner 恰好生成双引号 f-string 幸免；根治方案是「代码头部预绑定变量」式注入（`_n2_result = {...}` 前置 + 引用改写标识符），属架构级改动
3. **ast_guard 报错可读性**：注入产生的语法错误整节点 TOOL_BLOCKED，错误信息只有 stderr 尾部——可在失败详情附「注入前原始 code」辅助排障
4. **commit message 在 PowerShell 中含单/双引号会被拆参**——多 `-m` 时避免内嵌引号（本轮两次返工）
5. **关键输出刷屏**：`n1.records` 全量 JSON 5 条即近 800 字截断——可考虑 list 类输出只展示「N 条 + 首条摘要」
