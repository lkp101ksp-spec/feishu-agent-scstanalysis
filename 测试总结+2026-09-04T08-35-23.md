# 测试总结+2026-09-04T08-35-23（Phase 26 T6：settings 七字段 + code_approval 回调 + /code 路由）

## 本次测试范围

Phase 26 T6：按计划 `docs/superpowers/plans/2026-09-03-phase26-t6-wiring.md`
严格实现三处修改 + 两个测试文件末尾追加用例（Step7 commit 按用户要求跳过，
未执行 git commit）。

涉及文件：

- 修改 `config/settings.py`：Settings dataclass 末尾追加 Phase 26 七字段
  （code_workspace_root / code_skills_dir / code_max_steps / code_token_budget /
  code_timeout_sec / code_model / code_registry_tools），`load_settings()` 同步
  追加 env 读取
- 修改 `gateway/app.py`：`process_card_payload` 在 research_writeback 分支后、
  函数末尾前新增 `code_approval` 分支（broker 未配置拒收 / owner 内嵌比对
  forbidden / decide 幂等 already_handled）
- 修改 `orchestrator/app.py`：`process()` 在 /research 分支后、群聊门控（1.7）
  前新增 1.68 /code 分支（coding_runner 未配置回错误提示）
- 追加 `tests/integration/test_renew_card_callback.py`：4 用例（decide 到达 broker /
  owner 不匹配 forbidden / deny 透传 / 重复点击幂等）
- 追加 `tests/integration/test_message_flow.py`：3 用例（/code 路由到 runner /
  runner 缺失 coding_unavailable / 非行首前缀不匹配）

测试命令：`.\.venv\Scripts\python.exe -m pytest tests/integration/test_renew_card_callback.py tests/integration/test_message_flow.py -v --basetemp=.pytest_basetemp_t6`

## 执行记录

| 步骤 | 内容 | 结果 |
|---|---|---|
| Step1 | settings 七字段 + env 读取 | 完成 |
| Step2 | gateway code_approval 分支 | 完成 |
| Step3 | orchestrator /code 路由 | 完成 |
| Step4 | 卡片回调测试 4 用例追加 | 完成 |
| Step5 | /code 路由测试 3 用例追加 | 完成 |
| Step6 | pytest 两文件全量跑 | **40 passed, 3 warnings in 11.78s**（既有 33 用例无回归 + 新增 7 用例全绿） |

与计划无差异；3 条 warning 均为第三方库（starlette testclient / lark_oapi）
弃用提示，与本次改动无关。

## 覆盖点

- settings：七字段默认值 + env 覆盖路径（CODE_* 环境变量）
- code_approval：owner 发卡内嵌比对（不落库、不查 doc_writes）、broker 决策写入
  与 deny 透传、重复点击幂等（首决策不被覆盖）
- /code 路由：行首精确匹配（`/code` 与 `/code ` 前缀）、runner 缺失优雅降级、
  普通文本不误触

## 后续建议

1. T7 CodingRunner 装配后，`orch` fixture 若自带 `coding_runner` 属性，
   `test_code_command_without_runner_replies_error` 的 `del` 分支已兼容，无需改动。
2. 真机联调时验证飞书卡片 value 需内嵌 `owner` / `code_approval_id` / `decision`
   三字段（发卡侧由 T7 的 CodingRunner 负责拼装）。
3. `.pytest_basetemp_t6` 已删除；总览 T6 已勾选 ✅，可进入 T7。
