# 测试总结+2026-09-04T08-35-09（Phase 26 T3：CodeTools 六原语）

## 本次测试范围

Phase 26 T3：code 域固定六原语（read_file/write_file/edit_file/list_dir/search_files/run_cmd）
+ 统一 dispatch，按计划 `docs/superpowers/plans/2026-09-03-phase26-t3-code-tools.md`
严格走 TDD（Step5 commit 按用户要求跳过，未执行 git commit）。

涉及文件：

- 新建 `orchestrator/coding/code_tools.py`（CodeTools：SCHEMA + dispatch + 六原语）
- 新建 `tests/unit/test_coding_code_tools.py`（16 用例）
- 修改 `orchestrator/coding/workspace.py`（1 行，见下"与计划差异"）

测试命令：`.\.venv\Scripts\python.exe -m pytest tests/unit/test_coding_code_tools.py -v --basetemp=.pytest_basetemp_t3`

## 执行记录

| 步骤 | 内容 | 结果 |
|---|---|---|
| Step1 | 写失败测试 | 完成 |
| Step2 | 首次跑 pytest | 如期 collection FAIL：`ModuleNotFoundError: orchestrator.coding.code_tools` |
| Step3 | 实现 code_tools.py | 完成 |
| Step4a | 第二跑 | 15 passed / 1 failed（test_allow_python_executes，见下） |
| Step4b | 修正后复跑（含 T2 回归） | **34 passed in 0.75s**（T3 16 + T2 18，全绿） |

## 与计划的差异（1 处，修在 T2 前置文件 workspace.py）

**CommandPolicy 白名单未归一化可执行名**：测试用 `sys.executable`（全路径
`...\.venv\Scripts\python.exe`）触发 allow 分支，而 T2 实现的
`verdict` 用 `cmd[0].lower()` 原样比对，全路径不命中白名单 → 落入
need_approval → 无 approve_fn 被 APPROVAL_DENIED。计划末尾注意事项明确
"python 前缀白名单对其同样生效"，故将 [workspace.py](file:///i:/飞书agent/orchestrator/coding/workspace.py#L45)
改为 `Path(cmd[0]).stem.lower()`：同时兼容裸名（`python`）、带扩展名
（`python.exe`）与全路径三种形态。T2 原 18 用例复跑全过，无回归。

## 覆盖点

- 文件五原语：写读回环、缺失文件 NOT_FOUND、offset/limit 行窗口、
  `../` 逃逸 PATH_FORBIDDEN、edit 唯一替换 / NOT_FOUND / NOT_UNIQUE、
  list_dir 条目、search_files 正则 + glob 过滤
- run_cmd 三态：allow 直跑（sys.executable）、block 拒（shutdown）、
  need_approval 无 approve_fn 拒（APPROVAL_DENIED）、approve_fn=True 放行执行
- dispatch：arguments 兼容 JSON 字符串、未知工具 "unknown tool"、
  SCHEMA 恰好覆盖六原语

## 后续建议

1. T6/T7 拼装时注意：CodeTools.dispatch 自兼容 JSON 字符串与 dict，与 T5
   AgentLoop 的"先 json.loads 再传 dict"契约不冲突（幂等）。
2. `Path(cmd[0]).stem` 归一化仅按"名字"放行，无法防 PATH 劫持/同名恶意
   二进制；如需更强保证，后续可对 allow 命令做绝对路径校验（shutil.which 比对）。
3. 未 commit 的工作树含 T3 全部产物 + workspace.py 的 1 行修正，执行总览
   勾选/提交前请先 `git status` 确认无其他混入改动。
4. `.pytest_basetemp_t3` 已清理。
