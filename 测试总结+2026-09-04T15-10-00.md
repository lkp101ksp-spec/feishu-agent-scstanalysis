# 测试总结 + 2026-09-04T15:10

## Phase 27 T1+T3：SkillDiagnoser 核心 + 写回逻辑

### 测试范围
- 新增：`tests/unit/test_coding_skill_diagnoser.py`（9 用例）
- 回归：`test_coding_skill_loader.py` / `test_coding_agent_loop.py` / `test_coding_runner.py`

### 结果
| 测试文件 | 结果 |
|---|---|
| test_coding_skill_diagnoser.py | 9 passed (0.64s) |
| test_coding_skill_loader + agent_loop + coding_runner | 32 passed (0.73s) |

### 覆盖点
- diagnose 无 skill 调用短路（LLM 不被调用）
- diagnose 合法 JSON / 带 ```json 围栏 / 非 JSON（parse error）三种 LLM 返回
- apply SKILL.md：追加"## 改进记录（date）"段落 + .bak 备份内容一致
- apply tools.yaml：字段更新（timeout_sec 60→600）+ .bak 备份
- apply 异常路径：skill 目录不存在 / 目标文件不存在 / patch 为空 → 错误 dict 不抛异常

### 后续建议
1. T2 集成时在 CodingRunner 失败分支接 SkillDiagnoser.diagnose → 人工审批 → apply。
2. 当前 LoopResult.tool_events 仅含 {step,name,ok}，无 observation 细节；若诊断质量不足，
   建议 Phase 28 在 AgentLoop 事件中追加失败观察摘要（截断 500 字符）。
3. tools.yaml patch 目前支持 "{tool: {field: val}}" 与 "{field: val}"（作用于首个工具）两种格式，
   更复杂的结构变更（如 parameters 增删）建议后续做 schema 校验。

---

# Phase 27 T2+T4：skill 改进审批卡集成 + 集成测试

## 测试范围

Phase 27 T2（coding_runner 诊断发卡 + gateway `skill_improve` 回调 +
build_runtime 装配 SkillDiagnoser）与 T4（集成测试）。

涉及文件：

- 修改 `orchestrator/coding/coding_runner.py`：`__init__` 追加 `diagnoser=None`
  （向后兼容）；run_sync 末尾调 `_maybe_diagnose_skill`（status!=final 时诊断
  发卡，全程 try 静默）；新增 `_skill_improve_card` / `_cap_suggestion`
  （字段裁剪防按钮 value 超限）
- 修改 `gateway/app.py`：`code_approval` 分支后新增 `skill_improve` 分支
  （owner 内嵌比对 forbidden → broker.decide 幂等 → approve 时经
  `ctx.orchestrator.coding_runner.diagnoser` 执行 apply，结果经响应 JSON 返回）
- 修改 `gateway/runtime.py`：CodingRunner 装配注入
  `SkillDiagnoser(llm=llm, skills_dir=settings.code_skills_dir)`
- 追加 `tests/integration/test_coding_e2e.py`：3 用例（失败全链诊断发卡+批准
  写回 / final 跳过诊断 / 诊断异常不影响主流程）
- 追加 `tests/integration/test_renew_card_callback.py`：6 用例（owner 不匹配 /
  批准写回 + .bak / 拒绝不写回 / apply 失败回报 / 重复点击幂等 /
  diagnoser 未配置）

## 执行记录

| 步骤 | 内容 | 结果 |
|---|---|---|
| T2+T4 目标测试 | diagnoser 单测 + coding e2e + 卡片回调 | **36 passed in 2.37s**（新增 9 用例全过） |
| 回归 tests/unit | 全量单测 | **737 passed**（零回归） |
| 回归 gateway/coding 集成 | e2e + 卡片回调 + webhook_app + message_flow | **63 passed**（零回归） |
| Phase 27 全量回归 | 全仓 tests | **948 passed**（930 基线 + 新增 18：T1 单测 9 + e2e 3 + gateway 6） |

## 发现的问题与决策

1. **Phase 26 既有怪癖（未改）**：skill 子进程失败（非零退出 → SCRIPT_ERROR）
   经 ToolHandler 包装后变成 `{"ok": true, "result": "<ToolResult repr>"}`——
   handler 返回的 ToolResult 非 dict 被包进 outputs，error_code 丢失，
   AgentLoop 视为成功，连败禁用不触发。按「不改 Phase 26 行为」约束未动，
   建议后续 Phase 修 ToolHandler：handler 返回 ToolResult 时直接透传而非包 dict。
2. 失败轨迹改由「LLM 连续传非法 JSON 参数调 skill 工具」（BAD_ARGUMENTS_JSON，
   真实 ok=False）构造：3 连败 → 工具禁用 → 第 4 轮仍调 → no_tools，
   无需审批线程，测试更简且贴合「参数描述不清」诊断场景。
3. broker 用于 skill_improve 纯作首击固化/幂等（无人 wait），`_decisions`
   条目不消费不清理，量极小可接受。

## 后续建议

1. 真机验收：私聊构造一次 skill 失败任务（如让模型反复错调 demo skill），
   确认收到「/code skill 改进审批」卡 → 点「批准写回」→ SKILL.md 追加
   改进记录 + 生成 .bak；点「忽略」不写回；换账号点卡 forbidden。
2. 修复上述 ToolHandler ToolResult 透传问题后，补一条「子进程真失败 →
   连败禁用 → 诊断发卡」的 e2e 用例。
3. 卡片无 update 能力，apply 结果仅以 toast 返回；v2 可考虑 IMAdapter
   update_card 原地更新卡片状态。

---

# Phase 27 补充：ToolHandler ToolResult 透传修复（2026-09-04，TDD）

## 背景

T2+T4 发现的 Phase 26 既有怪癖：skill 子进程失败（SCRIPT_ERROR）经
ToolHandler dict 包装变成 `{"ok": true, "result": "<ToolResult repr>"}`，
error_code 丢失，AgentLoop 误判成功、连败禁用不触发——真机上 skill 崩溃
无法进入诊断链。本轮修复使「子进程真失败 → 连败禁用 → 诊断发卡」链路贯通。

## 修复内容

- `orchestrator/tools/tool_handler.py`：handler 返回 ToolResult 时直接透传
  （保留 error_code/error_message/outputs，仅注入 AST notices），不再走
  dict 包装分支
- 影响面核查：全项目仅 skill_loader 的 handler 返回 ToolResult（research
  链路 builtin handlers 全返回 dict，行为不变）；skill_loader 成功路径
  error_code="" 为 falsy，`_toolresult_to_dict` 判定不受影响

## TDD 执行记录

| 步骤 | 内容 | 结果 |
|---|---|---|
| RED | 新建 `tests/unit/test_tool_handler_toolresult.py` 2 用例 | 按预期失败（error_code=None / outputs 包 repr，正是 bug 本身） |
| GREEN | tool_handler.py 透传分支（+7 行） | 目标 2 用例 + 相关 42 用例（blocks/ACL/builtin/coding_runner/coding_e2e）全绿 |
| 补 e2e | `test_e2e_crashed_skill_subprocess_triggers_diagnose`：合法参数调必崩 skill → SCRIPT_ERROR 观察 → 连败禁用 → no_tools → 诊断卡 → 批准写回 | 1.10s 通过（首轮 APPROVAL_DENIED 暴露 skill 工具 L2 需审批，补持续批准线程后过） |
| 全量回归 | 全仓 tests | **951 passed**（948 + 新增 3），零回归 |

## 关键断言（修复价值证明）

新 e2e 用例中断言首次失败观察含 `SCRIPT_ERROR`——修复前该观察为
`{"ok": true, "result": "ToolResult(...)"}`，AgentLoop 计为成功。

## 后续建议

1. 真机验收：现在可以用「让 skill 真崩」（如 bio_preprocess 传不存在路径）
   构造真实失败场景触发诊断卡，无需再依赖参数错误。
2. skill_loader 成功路径 error_code="" 建议后续统一为 None（cosmetic，
   当前 falsy 判定无行为差异）。

---

# Phase 27 真机验收（2026-09-04 晚，ws_client 真连飞书）

## 验收方法

新建验收桩 skill `skills/qc_stat/`（脚本无论参数如何都 stderr 报
"argument parsing failed" + exit 2），私聊 bot 发 /code 任务并要求
"只用 qc_stat_run、遇错调参重试"，构造真实连败场景。验收后桩已删除。

## 验收结果 5/5

| 项 | 结果 |
|---|---|
| 连败禁用 → 诊断卡触发 | ✅（修复后，见下） |
| 批准写回 | ✅ tools.yaml timeout_sec 60→600，.bak 生成，日志记录 patched |
| 重复点击幂等 | ✅ already_handled，不重复写入 |
| 「忽略」分支 | ✅ 文件不再变化 |
| 成功任务回归 | ✅ final 正常完成（hello.txt 13B），不发诊断卡 |

## 真机发现并修复的设计缺陷（TDD，commit 见本节末）

**现象**：第一次验收时模型连败 3 次后按引导文字总结（final 收尾），
诊断卡未触发——旧触发条件 `status != "final"` 把这条最常见路径漏掉
（e2e 里 no_tools 路径需要模型禁用后仍坚持调用，真实模型不会这样做）。

**修复**：
- `agent_loop.py`：LoopResult 新增 `tools_disabled: bool` 字段，
  5 个返回点统一携带连败禁用标记
- `coding_runner.py`：触发条件改为 `status == "final" and not
  result.tools_disabled` 才跳过——final 但发生过连败禁用（任务实质失败）
  仍诊断；"先失败后成功"的正常 final（无禁用）不误伤

**TDD 记录**：RED 3 用例预期失败（LoopResult 无属性 / 卡 0 张）→
GREEN 后 45 用例全绿 → 全量回归 **954 passed**（951 + 新增 3），零回归。

## 发现的后续优化点（记入 ROADMAP，未修）

1. 诊断 LLM 会幻觉 tools.yaml schema 外字段（max_retries/retry_on/
   param_variants），apply 原样合并写入——loader 容忍不报错但字段永不生效，
   属"假改进"。后续：诊断 prompt 约束只输出 schema 支持字段
   （timeout_sec/description/parameters/command），或 apply 做白名单过滤。
2. 诊断对轨迹理解有偏差（把连败禁用机制终止描述为"agent 直接终止"）。
   Phase 28 补失败观察摘要后可改善。
3. ws_client 观测：前台 job 启动时 stdout 管道勿加 `Select-Object -First N`
   （截断后日志丢失）；已改为 `*> logs\ws_client_console.log` 全量落盘。
   另注意 Lark 长连偶有 ping_timeout 断连，SDK 自动重连正常（18:05 断连后
   任务照常完成）。

---

# Phase 28：失败观察摘要 + Working Memory（2026-09-04，TDD）

## 前置小修：诊断写回白名单（commit 875b280）

真机验收发现诊断 LLM 幻觉 tools.yaml schema 外字段（max_retries 等）
原样写入永不生效。修复（TDD 3 用例）：
- `skill_diagnoser.py`：`TOOLS_YAML_FIELDS` 白名单，`_merge_tool_patch`
  过滤 schema 外字段（全外字段拒绝写回不生成 .bak）；prompt 明确列出
  支持字段并引导重试类建议改写 SKILL.md
- 回归 957 全过

## Phase 28 T1+T2 实施

| 任务 | 交付 | 用例 |
|---|---|---|
| T1 失败观察摘要 | agent_loop：失败事件追加 `error` 摘要（error_code+error_message 拼 接，截断 500）；diagnoser `_condense_events` 送入 prompt | 3+2 |
| T2 Working Memory | agent_loop：`MEMORY_HEADER` 滚动失败记忆（紧随 system 的 user 消息，窗口 5 行，原地替换/压缩后自动重插），模型每轮可见"已试路径"防重复试错 | 3 |

## TDD 执行记录

- RED：T1 2 用例 KeyError: 'error' 预期失败；diagnoser prompt 用例首跑
  IndexError 是测试自身取参错误（chat 只收一个列表参数）修正后 RED；
  T2 滚动窗口用例先走 run() 全链——连败禁用 3 次即停到不了 6+ 次失败，
  改为直接单测 `_refresh_memory_message`（教训：全链测试设计要先核对
  机制约束）
- GREEN：相关 56 用例全绿 → 全量回归 **965 passed**（957+8），零回归

## 教训：并行编辑同一文件被覆盖

实施小修时把 3 个 SearchReplace 并行发到 skill_diagnoser.py，后写覆盖
先写（edit1/edit3 丢失、edit2 侥幸存活），表现为"工具报成功但内容缺失"
+ 测试持续失败且 pycache 清理无效。定位：Select-String 确认磁盘真实
内容。规则：**同一文件的多个编辑必须串行**。

## 后续建议

1. 真机轻量验收（可选）：重造 qc_stat 桩失败 → 诊断卡 issue 应引用真实
   stderr 文本而非泛泛猜测；Working Memory 生效时模型第 2 次失败起应
   避免重复完全相同的调用
2. Working Memory 目前只记失败；如需记成功路径摘要（跨步复用结果），
   等 Phase 28 真机反馈再决定，避免过度设计
