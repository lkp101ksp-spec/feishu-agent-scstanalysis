# 测试总结 2026-09-09T01:55 — 长会话记忆 feature 全链

## 范围

长会话记忆（私聊多轮记忆 + 全量历史带压缩/冻结 + /clear 手动重置 + 方案甲服务层接入）。
spec：`docs/superpowers/specs/2026-09-08-long-session-memory-design.md`；
计划：`docs/superpowers/plans/2026-09-08-long-session-memory.md`（8 Task TDD）。

## 逐 Task 测试记录

| Task | 内容 | 测试 | 结果 |
|---|---|---|---|
| 1 | MessageRow + 迁移 + MessageRepo | tests/unit/test_message_repo.py ×3（真 sqlite：建表/升序隔离/replace_all） | 绿；pg 迁移 upgrade 0006 执行成功 |
| 2 | ContextCompressor.summarize_only | test_context_compressor.py +2（调 LLM/空表不调用） | 先红后绿 |
| 3 | ChatMemory 编排 | test_chat_memory.py ×6（透传/压缩回写/冻结全链路/摘要失败降级/DB 失败降级/append+clear） | 先红后绿 |
| 4 | Orchestrator 接线 | tests/integration/test_chat_memory_flow.py ×4（注入+落库/freeze 换新会话/未装配回归护栏//clear） | 先红（3）后绿 |
| 5 | runtime 装配 + 全量回归 | 全量 pytest | **1181 passed**（基线 1165 + 16） |
| 6 | CI | run 34247750820 | completed/success |
| 7 | 真机验收 | 见下节 | 两轮：第一轮暴露 3 bug，第二轮全过 |

## 真机验收（2026-09-09 凌晨，临时阈值 budget=200/compress=0.3/freeze=0.5）

### 第一轮：暴露 3 个真 bug（全部修复）

1. **阈值 env 未接线**：.env 设了 CONTEXT_TOKEN_BUDGET 等三项，但 `load_settings()` 从未读取这四个
   `context_*` 字段（dataclass 字段存在 ≠ env 已接线），实际跑默认值 200k/0.8/0.95，永不触发。
   → settings.py 补 4 行 env 读取（沿用既有风格），修复后实测 `budget=200 compress=0.3 freeze=0.5`。
2. **audit_id 缺失 TypeError**：`freeze_session` 与 `maybe_compress` 两处 `audit_repo.write()` 少传必填
   `audit_id`。/clear 真机直接崩（日志 traceback 实证）。Phase 3.1 归档代码从未真机跑过，且测试全部
   用 MagicMock/None 绕开审计件——**同一路径第三次契约漂移被 mock 掩盖**。
   → 两处补 `audit_id=new_ulid()`；新增 2 个**真 AuditRepo** 回归测试（red 时精确复现真机 TypeError）。
3. **session_freezes 不落库**：runtime 装配 `SessionService` 时未传 `freeze_repo`（ freeze 从未接线过）。
   → runtime.py 补传 `SessionFreezeRepo(session)`。

### 第二轮：全部符合预期

- 历史注入：第 2 条追问"我刚才说我叫什么"，机器人答出"天明" ✓
- 压缩+冻结：超阈值后收到"[系统] 上下文已满，已开启新会话（历史摘要已继承）"，本条仍正常回复 ✓
- 摘要继承：新会话追问"我之前说我在做什么课题"，答出"单细胞课题"（摘要种子行生效）✓
- /clear：收到"[成功] 已开启新会话，历史已清空"，`status=cleared` ✓
- pg 硬证据：sessions 四代冻结链（3 archived + 1 active，origin_session_id 逐代指回）；
  messages 表各会话 `[已压缩]` system 种子行就位 ✓
- 验收后还原：.env 三项临时阈值删除，ws_client 重启恢复默认（pid 51836）✓

## 问题与教训

1. **SearchReplace 假成功本轮再发 4 处**（test_message_flow 第一处 /reboot、test_session_service_phase3
   import、context_compressor import、runtime import），全部 Grep 回读兜底。纪律不变：改完必验证。
2. **"字段存在 ≠ env 已接线"**：settings 是手写 dataclass + 显式 os.environ 读取，新增可配置字段时
   必须同时接 load_settings，并在计划阶段核实（不能只查字段定义）。
3. **mock 边界的代价**：freeze_session 一路被 upsert 契约漂移、naive datetime、audit_id 三个真 bug
   连撞三次，全部是"测试用 MagicMock/None 绕开真件"所致。对策已落地：关键路径回归测试一律换真件
   （真 SessionRepo/AuditRepo + sqlite StaticPool）。
4. **迁移编号冲突**：计划侦察时 alembic head 信息过时（实际链已到 0005），0003_messages 与既有
   0003_sessions_bind_anchor 撞号产生多头——以 `alembic heads` 现场为准重编号 0006。

## 追加：服务层 __init__ 类型注解（2026-09-09 晚，commit de982b6）

- 动机：audit_id 缺参 TypeError 逃过 CI 的根因是三个服务 `__init__` 参数未注解（=Any，mypy 放行）。
- 改动：SessionService / ContextCompressor / ChatMemory 的 `__init__` 参数全部显式注解
  （SessionRepo / SessionFreezeRepo / AuditRepo / LLMRouter / MessageRepo / IMAdapter 等），3 文件 +20/-7。
- 门禁：mypy 双平台（175 files）+ ruff 绿；全量回归 **1181 passed, 8 warnings in 53.04s**。
- 事故：回归中途 pytest 挂死于 6%——Docker Desktop 停运致 pg 不可达、某测试 TCP 超时挂死；
  同时 ws_client（pid 51836）随之死亡。处置：拉起 Docker（pg 自动恢复 healthy）、重启 ws_client（pid 36692）。
  教训：全量回归前先 `docker ps` 确认 pg 在线；ws_client 不会自愈，Docker 恢复后必须人工确认/重启。
- 推送插曲：代理客户端（127.0.0.1:17890）一度停运，push 报连接失败但实际已推上
  （远端先到 de982b6，二次推送"拒绝"实为已同步）——以 `git ls-remote` 为准核实。
- CI：run 34343017503（de982b6）completed/success；40dc9aa 的 CI 34260774441 亦确认 success。

## 追加二：运维加固轮 2（2026-09-09 晚，ws_client 自愈/守护 + pytest 防挂死）

事故驱动：当天下午 Docker Desktop 停运 → ① 全量回归 pytest 挂死 40 分钟才发现；
② ws_client 静默死亡，机器人离线无人知晓。

### 改动

1. **ws_client 文件日志**：`logs/ws_client.log`（RotatingFileHandler 5MB×3）——隐藏窗口启动时
   stderr 全丢，此前进程死亡原因无从排查，这是所有运维加固的地基。
2. **DBHealthMonitor**（`gateway/health_monitor.py`）：SELECT 1 探活状态机，健康↔故障跃迁时给
   `FEISHU_ADMIN_OPEN_IDS` 发飞书告警，故障期指数退避 30s→300s 封顶。
   **关键设计：probe 走 psycopg `connect_timeout=5` 短超时直连，不用共享 engine**——
   真机复现 docker stop 后 `engine.connect()`（pool_pre_ping 借池内死连接）**无限挂起**，
   监控线程一旦被拖死就等于没有监控。回归测试 `test_probe_pg_url_fails_fast_on_dead_port` 钉契约。
3. **ws_guardian**（`scripts/ws_guardian.py`）：pidfile 探活（复用 ws_client `_pid_alive`），
   死亡拉起 + 30s 崩溃循环冷却 + guardian 自身单实例守卫（`.ws_guardian.pid`）。
   登录自启：Startup 目录 VBS（`FeishuAgentWsGuardian.vbs`）——schtasks 需 UAC 被否决，
   VBS 免权限等效；卸载=删文件。
4. **pytest-timeout 2.4.0**：pyproject 全局 `timeout = 300`（单测试硬上限）——pg 标记层本有
   3s 探活 skip，挂死的是未标记但经 .env DATABASE_URL 碰真库的测试，全局超时是兜底。
   锁文件已按 ADR-0028 刷新（纯 Python 包，无平台标记负担）。

### 真机验证（逐项过）

- pg 停机：监控 5~10s 内探活失败 → DOWN 告警发出；退避 30s→60s→120s 探测不轰炸 ✓
- pg 恢复：下一轮 tick RECOVERED 告警 ✓
- 杀 ws_client：guardian 首轮 tick 检测到死亡 → 拉起新进程 → 12s 完成启动并连上 ws ✓
- guardian 双开防护：单实例守卫 + 2 条 monkeypatch 单测 ✓

### 副产物发现（遗留）

- **wedge 点定位**：`ModelSwitchService` 启动时 DB 恢复 active 模型调用无超时，
  pg 黑端口时阻塞 130s（异常最终被吞"keep yaml default"，进程不死但启动被拖住）。
  同类"启动期 DB 调用无超时"可能还有，建议后续统一给 psycopg 层加 connect_timeout。
- SearchReplace 假成功再发 2 处（ws_client import、health_monitor sqlalchemy import），
  mypy/ruff 当场抓获，纪律再次验证：改完必跑门禁。

### 测试

TDD 新增 15 用例（7 monitor 状态机 + 8 guardian 含单实例），全量回归 **1196 passed**
（基线 1181 + 15）。

## 追加三：启动期 DB 超时 + 方案 B persistence strict（2026-09-09 深夜）

用户点单依次执行：① 启动期 DB 调用统一加超时（wedge 类根治）；② 方案 B（persistence mypy strict）。

### 任务 A：engine 层 pg connect_timeout=10（commit 60328a8）

- 根因复述：pg 容器停止时 psycopg 无显式超时，纯靠 OS TCP 重传约 130s 才报错——
  ModelSwitchService 启动恢复 active 模型因此被拖住两分钟（"keep yaml default" 吞异常）。
- 改法：engine.py 抽取 `_engine_kwargs()` 作 pg/sqlite 方言参数**唯一出口**，pg 统一带
  `connect_args={"connect_timeout": 10}`；`configure_engine`（测试/运行时替换路径）同出口，
  杜绝"主路径有超时、替换路径没有"的分叉。
- TDD：tests/unit/test_engine.py ×3（pg 带超时/sqlite 不带 connect_args/configure_engine 同契约），
  手法 `patch.object(engine_mod, "create_engine")` + `patch("config.settings.load_settings")`
  （有效因 engine.py 为函数内 import）。
- 回归 **1199 passed**（1196 + 3）；CI run 34354932815 completed/success。

### 任务 B：方案 B persistence strict（commit 6447517）

- 摸底 `mypy --strict persistence`：25 错（22 在 persistence 内，3 个 config/settings.py 属
  strict CLI 全量扫的 import 牵连，per-module overrides 不波及、不修）。
- 修复 22→0：models.py 9 处 JSON 列泛型化（`Mapped[dict[str, Any]]` / `| None` / `list[Any]`），
  7 个 repo 签名同步补泛型；engine.py session_factory / _SessionLocal / _engine_kwargs 补注解。
- **mypy 2.3.1 新坑**：`[[tool.mypy.overrides]]` 内 `strict = true` 会**泄漏为全局 strict**
  （176 文件 0→554 错实证）——改为显式展开 10 个 per-module flags（disallow_any_generics 等）。
  `warn_redundant_casts` 是全局 flag 放 overrides 会被警告拒绝，剔出。
- 探针实验验证门禁非摆设：临时注入裸 `x: dict` 函数，被 no-untyped-def + type-arg 双规则
  精确抓获；用备份恢复（**不用 git checkout**——会连带丢弃未提交修复，被安全闸拦下一课）。
- 门禁：mypy 双平台 176 文件 0 错、ruff 绿、回归 **1199 passed**；CI 见 git log 最新 run。

### 教训沉淀

1. SearchReplace 假成功本轮大爆发（批量 14 处约半数未落盘；验证正则 `Mapped\[dict\]` 漏匹配
   `Mapped[dict | None]` 形态造成误判）——纪律升级：**单个替换 + 立即 Grep 验证**，
   验证正则必须覆盖目标全部形态。
2. mypy overrides 的 `strict = true` 不可信（至少 2.3.1），分层推进一律显式 flags。
3. 探针实验是验证"门禁真在守门"的廉价手段，值得列为 strict 扩容的标准动作。

## 追加四：方案 C gateway/orchestrator 层 strict（2026-09-10 凌晨，commit fcef71c）

persistence 之后两大包跟进 strict，存量 486 错（CLI 全量口径）/ 实际两包本体 433 错清零。

### 打法

6 并行子代理按子包分批（独立 mypy cache-dir 防竞争）：gateway 61 / coding 70 / templates 67 /
tools 83 / planner+executor+runtime 43 / orchestrator 根部 109。错误类型：type-arg 267 +
no-untyped-def 170 + no-any-return 43 + no-untyped-call 6（全为包内互调，注解补齐后自然消除，
不跨 feishu_adapter）。零 type: ignore；鸭子类型注入点（llm/im/adapters）标显式 Any 防不实标注；
cast 仅类型层面恒等。pyproject overrides 扩为 `[persistence.*, gateway.*, orchestrator.*]`。

### 排障两则

1. **cast 撞 TYPE_CHECKING 运行时 NameError**：子代理把 ApprovalBroker 挪进 TYPE_CHECKING 却在
   运行时代码 `cast(ApprovalBroker, broker)`——cast 首参会求值名字，8 个审批集成测试当场抓获
   （"approval card not sent in time"）。修法：`cast("ApprovalBroker", broker)` 字符串形式。
   教训：**TYPE_CHECKING 导入的名字绝不能在运行时表达式出现**，mypy 查不出这类，只有回归能兜底。
2. **201 errors 虚惊**：.pytest_tmp 混入 mypy_cache_g*/strict_errors.txt 致 pytest basetemp 清理
   大面积 fixture error——自身环境污染非代码问题，清场后重跑全绿。教训：basetemp 目录只给 pytest 用。

### 门禁与回归

mypy 双平台 176 文件 0 错、ruff 绿（修 1 处 E501 折行 + 2 处 import 排序 --fix）、
全量回归 **1199 passed**（77.57s）。ws_client 注解改动后按守则重启：杀旧进程（37356）→
guardian 首轮自动拉起（25808）→ ws 长连接上线，自愈链路顺手再实战验证一次。

### 副产物（子代理揪出的存量隐患）

- **plan_runtime.py 5 处 audit_repo.write 不传必填 audit_id**：接真 AuditRepo 必 TypeError
  （与 freeze_session 同型契约漂移）。生产未接线 PlanRuntime 故从未炸过，测试全 FakeAuditRepo 掩盖。
  子代理以本地 `_AuditSink` Protocol 如实标注现状未改行为。待用户决策：补 new_ulid() 或给
  audit_id 默认值。
- gateway 组遗留 `create_app` 13 个服务参数沿用 `object | None`（AppContext 字段升级时再收紧）。

## 追加五：全库 strict 收官 + audit_id 修复 + 重启事件复盘（2026-09-10 上午，commit 8230e47）

### audit_id 契约漂移修复（用户拍板：调用处补 new_ulid()）

TDD：先写契约钉子测试覆盖全部 5 审计路径（含 dynamic_append_failed 失败路径——首版漏了
只触发 4/5，测试红得正确），断言每条日志带 26 位 ULID 且彼此独立；随后 `_AuditSink`
Protocol 对齐真 `AuditRepo.write` 签名（audit_id 必填），5 处调用补 `audit_id=new_ulid()`。
契约必填不弱化，"每条审计有独立标识"约束保住。

### 第三批 strict：五包 115 错清零，全库 8 包 strict 达成

摸底实为 115 错/27 文件（此前 ~50 口径只算了一部分）：sandbox 68（生信工具，第三方 untyped
重灾区）、feishu_adapter 41、config 3、shared 3、skills 0（白捡）。2 并行子代理：
- sandbox 手法：dask/scanpy 等第三方 untyped 调用 10 处用 **callable 别名 cast**
  （`cast("Callable[..., Client]", Client)`，TYPE_CHECKING 导入类型 + 字符串首参，
  运行时延迟导入不动零行为变更）；adata 统一 Any 系刻意选择（score.py:54 无防护
  `adata.raw.var_names`，精确标注 AnnData 会炸 union-attr 且补防护违反零行为变更）。
- feishu_adapter 手法：38 处裸 dict 泛型化 + 10 处局部变量注解收窄，**零 cast**。
- pyproject：全部 8 包 strict 后 flags 从 per-module overrides **升为全局**，根除
  mypy 2.3.1 "overrides strict 泄漏"坑（已无泄漏与否的问题）。
- 子代理再报工具坑：**同一文件并行多个 SearchReplace 会互相覆盖落盘**（假成功 diff）。
  对策：同文件编辑串行/整文件 Write + 落盘复验。此坑连续两批出现，列为子代理纪律。

### 门禁与回归

mypy 双平台 176 文件 0 错、ruff 绿；回归 1190 passed + 10 skipped——skip 全为环境性：
**机器 08:08 被 Windows Update 重启**，Docker 未起 pg 测试自动跳。Docker 拉起后补跑
pg 6 项 + sandbox Docker 4 项全过，**1200/1200 全数验证**。CI 34424015904 绿。

### 重启事件复盘（VBS 自启失效 → .lnk 修复）

重启把 guardian+ws_client+Docker 清光，**08:09 登录后 Startup VBS 没有执行**
（guardian 日志零新记录、pidfile 停在昨晚）。排查链：I: 为本地 NTFS 盘排除挂载时序 →
StartupApproved 注册表里 Ollama/智谱的 .lnk 均有条目启用、唯 .vbs 无条目——
**新版 Win11 对 Startup 文件夹裸 VBScript 不执行**（VBScript 弃用路线）。且该 VBS
设立以来从未经历过真实登录验证（设立当晚无重启），属"未验证的自愈承诺"。
修复：Startup 换 `FeishuAgentWsGuardian.lnk`（指向 venv pythonw + 守护脚本），
手动 Invoke-Item 验证全链路——09:08:40 单实例守卫正确拒绝第二实例（pid=35796），
链路通。**待下次真实重启做终验**（观察 guardian 日志有无新 started 行）。
另：Docker Desktop 未设自启（用户可开 Settings→General→Start Docker Desktop when
you sign in），本次手动拉起。
运维知识：venv pythonw 是 redirector 壳，每个逻辑进程=壳+真身 2 个 OS 进程，
看进程表别误判双开。

## 追加六：B1 CNV 推断 sc_cnv 双后端落地（2026-09-10，spec/plans/2026-09-10-scnv）

### 任务链

| Task | 内容 | 测试 | 结果 |
|---|---|---|---|
| 1 | fetch_gene_pos.py + bio.Dockerfile 两层（infercnvpy==0.6.1/cnvturbo==0.3.0 钉版 + GRCh38 坐标 TSV 烘焙） | 镜像重建 + 三轮容器探针 | 绿；TSV 实测 **41092 基因** |
| 2 | sandbox/sc_tools/cnv.py（495 行，双后端推断+恶性判定+亚克隆统一后处理） | ruff + mypy 双平台 | 0 违规 0 错 |
| 3 | l3_singlecell.py 注册 sc_cnv（第 25 个 sc_* 工具，L1_compute/timeout 3600） | TDD 3 用例先红后绿 | 单测 **61 passed** |
| 4 | 镜像重打 + tests/integration/test_bio_cnv.py | 真容器 3 用例 | **3 passed，54.23s** |
| 5 | 五文档同步 + 全量回归 + 推送 | ruff/mypy 双平台/全量 pytest | 见下 |

### 探针实测结论（计划 Task 1 Step 5 记录搬入）

镜像内三轮管道探针（合成 60 细胞 × chr1/7/10[/X] 各 40 基因，var 注入三列）：
1. 双后端均接受 var 三列注入，from_gtf 回退方案不启用 ✅；
2. **infercnvpy `tl.infercnv` 的 X_cnv 是窗口级列不与 var 对齐** ❌（计划假设不符）——改走
   `calculate_gene_values=True, inplace=False` 三元组返回的第三元素（基因级矩阵逐列对齐 var）；
   其 `exclude_chromosomes` 默认已排 chrX/chrY；
3. cnvturbo `infercnv_r_compat` 参数名是 `exclude_chromosomes`（o 拼写正常，计划写错）；
   X_cnv 剔除显式排除染色体的列，**列对齐改用 `uns["cnv"]["kept_var_names"]` 推导**；
4. `compute_hspike_emission_params` 返回 **4 元组**（非 dict）：位置解包
   `(emit_means, emit_stds, sd_intercepts, sd_slopes)`，各 (6,) 对应 HMM i6 六状态；
5. `hmm_call_subclusters`：precomputed 参数名实为 `precomputed_emit_sd_intercepts/sd_slopes`
   （计划缺 `emit_`）；obs 产出 cnv_call 取值域 `"Normal"/"Tumor"` ✅。
四处签名差异全部按探针实测修正 Task 2 调用行，行为契约不变。

### 集成测试实际数字（tests/integration/test_bio_cnv.py，真容器）

- **3 passed in 54.23s**：infercnvpy/cnvturbo 双后端参数化 + ref_groups 错误自纠路；
- 合成数据：免疫参考群（T/B cells 各 120）+ 恶性群（Epithelial tumor 150，chr7 ×1.8 gain /
  chr10 ×0.6 loss，1100 基因）；断言恶性判定 ≥80% 落在造的恶性群——**实测 150/150=1.0，
  双后端精度满分**；
- obs 写回（cnv_score/is_malignant/cnv_subclone，cnvturbo 路另有 cnv_call）容器内回读验证通过；
- 产物五件（染色体热图/score UMAP/subclone UMAP/注释×恶性 csv/亚克隆×染色体 csv）宿主侧
  逐一存在断言；
- 细节：infercnvpy 路在合成数据上 threshold 实测 0.0（参考群为人造同质群，mean+3sd≈0），
  恶性判定仍全部正确——合成数据特性，真实数据阈值会正常浮起，非 bug。

### 全量回归（Task 5）

- `ruff check .` → All checks passed!
- `mypy` / `mypy --platform linux` → 各 Success: no issues found in 178 source files
- 全量 pytest → **1206 passed, 8 warnings in 130.90s**（0 skip：Docker/pg 在线，
  test_bio_cnv 3 用例复跑一致通过，镜像状态无漂移）
- 提交推送：5 commit（镜像层/cnv.py/注册单测/集成测试/文档同步，e5fbd10 收官），
  pre-push 质量门通过；**CI run 34477627304 绿**（ROADMAP 行 hash/CI id 已回填）

### 遇到的问题与修法

1. 探针先行退役了 spec §3.3 全部签名风险（上述 4 处差异），避免 Task 2 写完再在集成测试返工；
2. infercnvpy 窗口级 X_cnv 若按计划假设直接当基因级用，列对齐断言必炸——探针记录直接给出
   修正路径（三元组返回/kept_var_names），一次改对；
3. 全量回归日志尾部有 `RuntimeError: docker down` traceback——系 ws_client idle_sweep 测试的
   mock side_effect 后台线程噪声（"kernel idle sweep failed" 为测试内预期日志），exit 0、
   1206 passed，非回归失败。

## 追加七：D 分析报告自动汇编落地（2026-09-10，spec/plans/2026-09-10-report）

### 任务链

| Task | 内容 | 测试 | 结果 |
|---|---|---|---|
| 1 | settings.report_folder_token + DocAdapter.create_document/grant_doc_view（独立 BaseRequest） | tests/unit/test_doc_adapter_create_grant.py ×7（假 SDK 通道） | 先红后绿，7 passed |
| 2 | orchestrator/report/section_digest.py（泛化产物收集 + csv 摘要截断） | tests/unit/test_report_digest.py ×8 | 先红后绿，8 passed |
| 3 | report_builder.py + __init__.py（LLM 逐节解读 + md 底稿 + 四态交付编排） | tests/unit/test_report_builder.py ×11 | 先红后绿，11 passed |
| 4 | research_runner 收尾钩子（条件触发 + 故障隔离） | tests/integration/test_research_runner.py 追加 ×3 | 先红（2）后绿；该文件全量 **39 passed** |
| 5 | 五文档同步 + 全量回归 + 推送 CI | ruff/mypy 双平台/全量 pytest | 见下 |

### 覆盖要点

- 单测合计 26 例（7+8+11）：建文档/授权封装的假 SDK 通道契约、产物自动分类（图片四键/csv/关键数字）、
  未知工具名回退、csv 头部摘要与中部截断、LLM 全部成功/单节失败降级/无 LLM 全模板/总评失败省略、
  md 相对路径与落盘位置、to_blocks 块序、maybe_build_report 四态（sent/skipped/degraded/md_only）。
- 钩子集成 3 用例：sc 流程自动建文档发链接+授权提问人+md 落盘、汇编抛错故障隔离主流程照常完成、
  非 sc 流程零报告动作。测试不依赖真 LLM（SimpleNamespace 无 llm 属性 → 全走模板降级句）。
- 全量回归（Task 5 Step 2）：`ruff check .` All checks passed!；`mypy` / `mypy --platform linux`
  各 Success: no issues found in 181 source files；全量 pytest **1235 passed, 8 warnings in 133.26s**
  （日志尾部 RuntimeError: docker down 为 ws_client idle_sweep 测试 mock 后台线程噪声，同追加六记录，exit 0 非回归失败）。

### 后续建议

- **真机验证建文档/授权链路**：单测均为假 SDK 通道，`create_document`/`grant_doc_view` 的真实
  API 行为（folder_token 权限、drive:drive 权限域、docx 链接可打开）需真机跑一条 sc 流程验证。
- **报告质量人工评审**：LLM 逐节解读的生物学准确性、字数控制（150-300 字）与"禁止编造数字"
  约束的实际遵守情况，首份真机报告出来后人工过一遍。
- **md 底稿 GC 注意点**：底稿落 `bio_workspace/{ds}/report/`，随数据集 TTL 7 天被 sweeper 清理；
  云文档是唯一长期留存形态，md 仅作短期底稿/降级兜底，需长期留存须自行转存。

## 后续建议

- **sc_cnv 真实肿瘤数据集人工核对**（追加六）：合成数据双后端精度 1.0 仅为烟雾测试；
  首个真实肿瘤样本跑完后人工作对一次（判定比例/亚克隆数是否符合病理预期、infercnvpy 路
  阈值是否浮起正常、参考选择是否合理）。
- D 报告汇编落地时直接消费 `cnv_subclone_by_chromosome.csv` 与染色体热图（素材已就位）。
- 小鼠坐标扩展位：`fetch_gene_pos.py` 换 GRCm39 GTF 烘第二份 TSV + cnv.py 加 species 参数
  （当前内部定死人源 GRCh38，ToolSpec 描述已写明）。
- 远期：token 计数换真 tokenizer（当前 len÷4 粗估对中文偏松）；摘要质量评估（LLM 自评或抽样人工）。
- **群聊记忆长期搁置（2026-09-10 用户定案）**：相当长时间内不做；tokenizer 相应维持远期
  观察不排期（其主要转正理由即群聊）。残余风险记录：私聊超长会话仍有账面低估且无 API
  超限反应式兜底（llm_router 仅主备模型切换，超限 400 走 LLMCallError 任务失败）；
  触发条件=日志出现 context 类 400，届时二选一：中文系数档（len×0.75 一类，几行）或
  超限紧急压缩重试一次。token_counter 已是注入点（context_compressor 构造参数），换实现不动架构。
- ~~启动期 DB 调用统一加超时~~ ✅ 已完成（追加三任务 A，engine 层 connect_timeout=10）。
- 默认阈值已定案（2026-09-09 用户拍板）：保持 200k/0.8/0.95 默认，不调 30k。理由：压缩/冻结路径
  已有真件回归测试 + 两轮真机验收背书，不再需要"低阈值换覆盖"；低阈值反而带来摘要 LLM 调用成本、
  时延与长程细节有损丢失。链路保鲜靠 CI 定期跑 `-m pg` 集成测试更干净。
- 待用户手工：删除 Temp\pytest-of-Administrator 坏目录；GCM PAT 约 2026-10-08 到期需续；
  可选开启 Docker Desktop 登录自启。
- ~~plan_runtime audit_id 契约漂移修复~~ ✅ 已完成（追加五，调用处补 new_ulid() 方案）。
- ~~全库 mypy strict~~ ✅ 已完成（方案 B/C + 追加五第三批，8 包全 strict、flags 升全局）。
- VBS→.lnk 自启替换待下次真实重启终验（见追加五复盘）。
- ws_client 运行守则——Docker/pg 异常后主动检查进程存活，必要时重启（有 guardian 兜底仍建议确认）。

## 追加八：B1+D 真机验收（2026-09-11，humantest/b7a44c75464f 真实肿瘤数据）

### 验收对象与数据

- 数据集：bio_test_data/humantest（HPV45+ 人源肿瘤，含上皮/免疫/基质），
  20533→19149 细胞（qc min_genes=1000），29 簇，celltypist 14 类标签。
- 环境准备：Docker Desktop 手动拉起（早 8:18 ws_client 实例崩溃后 guardian 未拉起，
  且存在两个 guardian 进程——单实例守卫疑似失效，见后续建议）；手动启动 ws_client 后在线。

### 流程 A 首跑失败与修复（commit 52f3b84，CI 34563897618 绿）

1. **OOM → BrokenProcessPool**：n4 sc_cnv 容器 --memory 16g 限额内，20533×31884
   float64 稠密矩阵多份拷贝 OOM，joblib worker 被杀。修复：基因表达预过滤
   （R inferCNV cutoff=0.1 语义，31878→11591 基因）+ 全链路 float32 + infercnvpy
   n_jobs=2。复测峰值 6.6GiB/16GiB（41%）。
2. **NaN 列 → PCA 拒绝**：修复 OOM 后首跑暴露 infercnv 输出带 NaN 列（基因过少
   染色体/窗口边界），加防御性列剔除 + chrom_col 对齐 + note 记录（复跑未再出现）。
3. **pngs 聚合键缺失**：cnv.py emit 的图键（chromosome_heatmap_png 等）不在宿主
   收图四键（umap_png/dotplot_png/spatial_png/pngs）内 → IM 发图与 D 报告都收不到
   CNV 图。修复：emit 补 pngs 聚合键（合成集成测试同步加断言）。
4. 教训：同一文件并行 SearchReplace 会互相覆盖（本次 NaN 块与 float32 补丁先后
   被覆盖丢失，靠门禁/回读拦截）；改同文件必须串行。

### 流程 A 复跑（infercnvpy，全链路 success）

- CNV 三图到 IM ✓、报告云文档 5 图全插图 ✓、链接可打开/图可显示/授权生效（用户确认）✓
- 恶性 6625/19149=34.6%，上皮 6405/10235（62.6%），免疫/基质误报 <2% ✓
- 报告数字抽查全对（阈值 0.0099、参考 8472、16 亚克隆、chr6/chrY 数值）——LLM 未编数字 ✓
- 产物备份 cnv_infercnvpy_backup/（2csv+3png+每细胞判定表）

### 流程 B（cnvturbo，单节点 success）+ 双后端交叉验证

- cnvturbo：10677 恶性（55.8%）= 全上皮 10235 + 全浆细胞 399 + 全 pDC 43；18 亚克隆。
- **主轴一致**：两后端恶性主体均锁定上皮、免疫/基质均判正常；**chr6 缺失作为主干/
  早期事件被两后端独立复现**（-0.08~-0.09 vs -0.09~-0.116）；chr17 扩增共同提及。
- **边界分歧**：细胞级一致率 76.6%、Jaccard 0.589；cnvturbo HMM（cluster_by_groups+
  min_segments_for_tumor=1）偏松，全群判恶性；infercnvpy 阈值法偏保守（62.6% 上皮）。
  生物学上 DTC 样本全上皮恶性并不荒谬；浆细胞/pDC 全判恶性存疑，倾向 cnvturbo 过判。

### 后续建议

- **guardian 运维排查（高优先）**：早 8:18 ws_client 崩溃后 guardian 未拉起；且
  C:\Python312 与 .venv 两个 guardian 并存（单实例守卫疑似未生效）。验收后专项排查。
- cnvturbo 口径评估：是否将 min_segments_for_tumor 调 2+ 或改细胞级判定权重，
  待更多真实样本积累后定（当前对齐 R inferCNV HMM i6 默认）。
- D 报告小瑕疵：① SECTION_TITLES 缺 sc_qc/sc_load 中文映射（标题回退为工具名）；
  ② LLM 解读把"产物图片 X 张、数据表 Y 个"元信息写进正文（prompt 措辞待打磨）。
- 双后端对比材料已留档 cnv_infercnvpy_backup/（注意随 ds TTL 7 天被 GC，需留存请转存）。

## 追加九：guardian 排查与崩溃循环治理（2026-09-11 下午，commit b954118）

### 排查结论（反转：guardian 无故障）

- **"双 guardian 并存"系误报**：27552(.venv pythonw 壳) + 2036(C:\Python312 真实解释器)
  是同一逻辑 guardian 的进程对——Python 3.12 venv 重定向器行为（2026-09-02 总结已
  记载同模式：.venv 启动器派生 C:\Python312 实进程）。sys.executable 实测正确指向
  venv 路径；.ws_guardian.pid=2036 写的是真实进程 pid，单实例守卫一直有效
  （9/10 日志已有 "already running" 拒绝记录）。
- **今晨事故真相**：开机 8:18 Docker Desktop 未起 → ws_client 在依赖未就绪窗口
  起不来 → guardian 每分钟忠实拉起、连崩 189 次（08:18–11:18）直到 Docker 恢复。
  guardian 全程正常履职，**ws_client 死因证据丢失**才是真缺陷。
- **无声死亡根因**：_restart() 的 Popen 不重定向 stdout/stderr，pythonw 无控制台，
  189 次崩溃报错全进虚空；唯一线索 8:18:18 take-over 行还是开机前 stale pidfile 留的。
- **冷却窗漏洞**：min_restart_interval=30s 在 tick=60s 下永已过期，崩溃循环无升级机制。

### 修复内容（scripts/ws_guardian.py，TDD 12+9 passed）

1. **_restart 重定向**：子进程 stdout/stderr 追加写入 logs/ws_client.out.log/err.log，
   下次崩溃 60 秒内可见 traceback。
2. **崩溃循环退避**：consecutive_failures 计数，非 3 的倍数原间隔拉起；
   第 3/6/9... 次跳过并记 ERROR（consecutive_failures/interval/skipped_until 计数），
   实际间隔 = 1x→3x→6x→15x 封顶（60s→3m→6m→15m）；探活成功重置。
   Popen 抛错（解释器丢失）与"拉起后死亡"分轨：前者立即记录失败不进退避档。
3. 测试：_restart 用 fake_popen 断言命令/cwd/flags/双日志句柄；退避用例覆盖
   冷却期/失败序列/重置/封顶/异常计失败；默认参数等价性冒烟。

### 运维要点

- 换 guardian 时须**先杀 Python312 实进程再杀 .venv 壳**（先杀壳则实进程失去
  父进程等待语义可能残留）；本次按 2036→27552 顺序替换，ws_client(39524) 未受影响。
- 排障速查：.ws_client.pid/.ws_guardian.pid 是"逻辑单实例"的权威依据；
  看到 .venv+C:\Python312 进程对 ≠ 双实例，先核对 pidfile 再动手。

---

## 追加十（2026-09-11 下午）：ws_client 启动期依赖等待 + Docker 自启现状核查

### 背景

早 8:18 事故的根治轮：guardian 侧已修（追加九），本轮换 ws_client 侧——
依赖（pg 容器）未就绪时不再"起了就崩"，而是原地等待。

### 修复内容（gateway/startup_wait.py 新建 + ws_client 接线，TDD 7 passed）

1. **wait_for_dependency 纯函数**：probe/clock/sleep 注入；首轮就绪直通返回 0；
   失败每 5s 重试，probe 抛异常按未就绪处理；等待超 1800s 抛 SystemExit(3)
   交 guardian 重新拉起（避免带过期上下文无限挂起）；日志节流（首失败 WARNING、
   之后每 60s 一条、就绪 INFO 记耗时）。
2. **ws_client.wait_for_pg_ready**：sqlite 直通（开发/测试零影响）；pg 走
   health_monitor.probe_pg_url 短超时探针（复用 9/9 事故沉淀的黑端口有界探测）。
3. **main() 钩子**：acquire_single_instance 之后、build_runtime 之前等待——
   从根上消除"依赖未就绪窗口起进程即崩"的崩溃循环；guardian 退避（追加九）
   降级为兜底。

### Docker Desktop 自启核查（无需改动，已是启用态）

- settings-store.json `AutoStart: true`；HKCU\Run 有 `Docker Desktop` 项；
  StartupApproved\Run 首字节 0（启用）。9/11 早 8:18 未起更可能是
  "重启后未登录 / Docker WSL2 冷启动慢于 guardian .lnk" 的窗口问题——
  窗口问题由本次 ws_client 等待根治。
- Startup 目录确认：FeishuAgentWsGuardian.lnk 在位（.lnk 自启仍未真实重启终验，
  下次重启一并验：guardian 自起 + Docker 自起 + ws_client 等 pg 就绪后连通）。

### 验证

- 单测 7 passed（即刻就绪/重试后就绪/探针异常/超限退出/边界就绪/sqlite 直通/pg 探针）
- ruff + mypy 干净；全量回归 1246 passed（+7）

---

## 追加十一（2026-09-11 傍晚）：Phase 45 空间统计三分析 st_stats

### 内容

st_* 第 9 工具 st_stats（甲方案单工具三分析，spec 2026-09-11-st-stats-design.md）：
- **autocorr**：Moran's I / Geary's C 空间自相关，逐基因统计 csv + top4 基因空间分布 png
- **cooccurrence**：簇间空间共现长表 csv + 曲线 png
- **nhood_enrichment**：簇间邻域富集 zscore/count 双矩阵 csv + 热图 png

### 关键实现决策

1. **邻域图复用优先**：st_process 已建 spatial_connectivities 直接复用（domains.py
   同款），缺失才按 coord_type/n_neighs 兜底补建；processed.h5ad 只读。
2. **cluster_key 回退链**：spatial_domain→banksy_domain→leiden→clusters，全灭报
   ST_CLUSTER_KEY_MISSING 并列出可用 obs 列（sc_de 式自纠引导）。
3. **nhood top_pair 取 |z| 最强**：分离结构中强耗竭（负 z）本身就是核心信号，
   只取正 z 会漏报（冒烟实测两簇分离 A~B z=-11.32）。
4. **容器探针先行**（B1 纪律）：实施前 docker run 核实 squidpy 1.8.3 全部签名与
   uns 数据结构（occ/interval/zscore/count/moranI 列名），计划阶段即消灭猜测。

### 冒烟抓获三坑（合成 10x10 网格 + 注入空间梯度基因，断网）

1. `sq.pl.spatial_scatter` 需 `uns['spatial']`——无则补占位壳（空 images +
   scalefactors）+ `img=False`；visium 真实数据不受影响走默认带图。
2. `sq.pl.co_occurrence` / `sq.pl.nhood_enrichment` **无 show 参数**（**kwargs
   直通 Line2D.set 报 unexpected keyword 'show'）。
3. 脚本级 `fail()` 也是 exit 1 + stdout JSON——冒烟结果解析须对齐 BioRunner
   口径（先按 stdout JSON 解析，失败才算执行崩溃）。

### 验证

- 断网冒烟全绿：Moran's I 把注入梯度基因 G_grad 排第一（I=0.541），
  Geary's C 同（C=0.457）；共现 2 簇；邻域富集 A~B z=-11.32 强耗竭；
  INVALID_INPUT 正确拒绝。
- TDD 注册 5 用例（注册/透传/默认值/repr 串宽松解析/错误透传），
  全量回归 **1251 passed**，ruff + mypy 干净。
- SECTION_TITLES 33 项（st_stats=空间统计分析）。

### 后续建议

- 真实 Visium 数据（humantest 空间切片）跑一次三分析，验证带组织图渲染路径。
- SECTION_TITLES 覆盖测试已自动盯新工具映射，后续 st_* 新工具零记忆负担。
