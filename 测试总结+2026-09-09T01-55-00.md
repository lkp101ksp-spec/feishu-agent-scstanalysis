# 测试总结（滚动主文件）

> 2026-09-23 轮转：2026-09-09 至 2026-09-20 段已归档至
> docs/archive/测试总结-2026-09-09_至_2026-09-20.md（含教训 #1-16 原始现场）。
> 本文件自 2026-09-21 Phase 75 起续记。

---

## 2026-09-23 v0.3.0 发布 + 建议清单四项收官（Release/Temp ACL/轮转/dogfood）

- **① v0.3.0 正式 Release**：pyproject 0.1.0→0.3.0（7296401），tag+Release notes 经 gh CLI 发布（releases/tag/v0.3.0），pre-push 门禁全绿。
- **③ Temp ACL 根治**：`Temp\pytest-of-Administrator`（4 项残留）删除重建成功；默认 basetemp 复验 57 passed（test_skill_dirs/test_skill_installer/test_renew_card_callback）。check.ps1 的 `--basetemp` 钉保留但改注：ACL 已根治，留钉只为防 agent 沙箱拦 C 盘 Temp（2026-09-17 教训）。
- **④ 测试总结轮转**：09-09~09-20 段（原 1-2966 行，含教训 #1-16 原始现场）切至 docs/archive/测试总结-2026-09-09_至_2026-09-20.md，主文件 3325→367 行自 Phase 75 续记。
- **② 0.3.0 生产回路 dogfood PASS**：guardian（计划任务实例 pid=5664）心跳新鲜；ws_client（pid 23952，双目录新代码）wss 在线。以用户身份经 lark-cli 给「单细胞分析」bot 发 ping（13:03:56），bot（app_id=cli_aa1a8a41f378dcbc，与 ws_client 日志一致）13:04 回复"在线，已收到 v0.3.0 dogfood ping；未启动研究任务。"——IM→ws 长连接→gateway→orchestrator→LLM→回复全链实证。

---

## 2026-09-21 Phase 75 Task 4（CI 接入 st 双冒烟）

- **改动**：ci.yml bio-image-smoke job sc_cellfreq 步后追加 st_genescore/st_metabolism 两冒烟步（计划锚点原文照抄，BIO_WORKSPACE_ROOT=runner.temp/bio_ws）。
- **本地三重门禁**：ruff All checks passed；mypy 204 files 零 issue；pytest 1362 passed / 6 deselected（154s）零回归。
- **push**：53b62e6（共 4 commit），pre-push 质量门四连全绿（含 cov 88%）。
- **远端 CI 卡点**：run 35559887462 四 job 均未启动——GitHub 账户级账单问题（`recent account payments have failed`），与本次改动无关（前序 docs-only run 35557820043/35478627234 同症 4-5s 秒挂）。需用户在 GitHub Settings → Billing & plans 处理欠费/额度后 re-run 验收；新冒烟步位于 bio-image-smoke（workflow_dispatch 手动触发 job），账单恢复后建议 manual run 验证 step 级 success。

## 2026-09-21 Phase 75 收口回填（#20 全链：双工具 + L3 接线 + 联读/oscc 真机 + CI 账单挂账）

计划：`docs/superpowers/plans/2026-09-20-phase75-crossread-stgenescore-stmetabolism.md`。
按计划 Task 7 五段模板压缩回填（#20 编号顺延 09-19 总结"验证一览"表 #19），逐 Task 实录：

- **① 冒烟两脚本 6+6 场景全绿**：Task 1 st_genescore（commit 09ee762）
  6 场景——D1 注入 TGFb 组均值 **96.9 vs D2 −0.08** 真值回收 + spatial
  png 落盘 + 拒收四连（无 spatial/单域/缺列/低重叠）断网端到端；Task 2
  st_metabolism（commit 882afe4）6 场景——AUCell Glycolysis
  **D1=0.8928 vs D2=0.0532**、mean 口径 **D1=2.299 vs D2=−0.191**
  双口径真值回收 + 拒收同上。
- **② 契约 + L3 接线（Task 3，commit 1946ef8）**：TDD 红（ImportError）
  →绿；契约 test_l3_st_* 8 用例 + 三守卫 **36 passed**（st 清单
  18→20 工具 + 附录 A 超时行 + SECTION_TITLES 同步单 commit 落地）；
  ruff/mypy **204 files 零告警**；全量 pytest **1362 passed**
  （基线 1354+8，零回归）。
- **③ CI 接入（Task 4，push 53b62e6，挂账）**：ci.yml bio-image-smoke
  job 追加两冒烟步（见上节）；远端 CI run **35559887462 因 GitHub
  账户账单问题 job 未启动**（非代码问题）——账单处理后 re-run 验收；
  新冒烟步挂在手动触发的 bio-image-smoke job，建议另触发
  workflow_dispatch 补验。
- **④ 联读真机（Task 5，commit becbb09，599sub 大写影子桥绕 human
  卫兵）**：**C1 pass**（TGFb×TGFB1 **rho=+0.976 p<0.001**）；
  **C2 not-pass**（粗注无 CAF 簇命中——观察到 ident 键 str vs
  clusters int 不匹配，如实挂账）；**C3 pass**（**43 对 |rho|≥0.8**，
  最强 Androgen×CXCL12 **+0.976**；多重检验问题挂账）。
- **⑤ oscc 真机（Task 6，commit 6795409）**：**1749 spots × 8 域**
  跑通；st_genescore 各域 top3 以 **JAK-STAT/EGFR/TNFa** 为主；
  两口径对照 TGFb st(spatial_domain) top=1 vs
  sc(cluster_annotations) top=3、JAK-STAT st top=5 vs sc top=0——
  **域/簇编号体系不同，方向不可直接对齐，如实记录**；st_metabolism
  AUCell **315 通路**各域 top3=Ribosome/Coronavirus disease/
  Antigen processing。

### 后续建议 / 挂账

- **CI 账单复跑**：GitHub 账户账单处理后 re-run push run 35559887462，
  并另触发 workflow_dispatch 验 bio-image-smoke 内 st_genescore/
  st_metabolism 两新冒烟步（Task 4 挂账）。
- **C2 ident 键 int/str 不匹配修复**：联读脚本 clusters int 与 ident
  str 键不匹配致 CAF 簇未命中，下轮修键型对齐后复跑 C2（Task 5 挂账）。
- **43 对强相关多重检验校正**：C3 的 43 对 |rho|≥0.8 未做多重检验
  校正，解释与引用前需 BH/FDR 口径补算（Task 5 挂账）。
- **反卷积加权打分（Cell2Location q05 权重）仍挂账**——需先在 oscc
  跑 deconvolve（spec §6 非目标 1）。
- **联读如需复用为工具**，待更多数据集验证后另立 Phase（spec §0 原则 4）。

## 2026-09-21 #20 补记：联读两挂账当日修复（C2 键型 + C3 FDR）

- **C2 修复→pass**：`ident` 键 str vs `clusters` int 不匹配已修
  （查键统一 `str(c)`，real_genescore_cytosig_599sub.py L146-150）；
  复跑 CAF 簇 5（Col1a1）命中，**TGFb 排名 1/8 ≤3，C2 转 pass**——
  C1/C2/C3 三判据首次全绿。
- **C3 补 BH/FDR**：`scipy.stats.false_discovery_control(method="bh")`
  对全景全部有效 p 值统一校正，新增第五件产物
  `crossread_pathway_factor_qval.csv`；43 对 |rho|≥0.8 中仅 **4 对
  q<0.05** 存活（Androgen×CXCL12 q=0.007 / Androgen×VEGFA q=0.039
  等）——全景层引用须以 q 值口径为准，未过校正的对子不可直接引用。
- ruff 零告警；产物五件套重落盘（时间戳更新）。

## 2026-09-21 Phase 76 收口回填（#21 st_score_weight 反卷积加权打分）

计划：`docs/superpowers/plans/2026-09-21-phase76-st-score-weight.md`。
兑现 #20 挂账"反卷积加权打分（Cell2Location q05 权重）"——新独立
容器工具 st_score_weight 消费 deconv.h5ad × scores csv 产出
细胞型×通路丰度加权活性矩阵，逐 Task 实录：

- **① 工具+冒烟（Task 1，commit 4fed079）**：st_score_weight 冒烟
  **4 场景全绿**——主场景 **CellA×TGFb=4.920 全局最高**
  （>CellB×TGFb）+ CellC 全零剔除入 dropped_celltypes +
  n_spots_overlap=40 + 双产物落盘；三拒收（ST_WEIGHT_NO_DECONV
  引导 st_deconvolve / INVALID_INPUT 引导 st_metabolism /
  索引错位 0% 重合拒收）断网端到端。
- **② 契约+接线（Task 2，commit 724b0a9）**：TDD 红（ImportError）
  →绿；契约 4 用例+三守卫 **32 passed**（st 清单 20→21 工具+
  附录 A 600 档+SECTION_TITLES 单 commit 原子落地）；mypy
  **205 files 零告警**；全量 pytest **1366 passed**
  （基线 1362+4，零回归）。
- **③ CI 接入（Task 3，push 11d5270）**：ci.yml bio-image-smoke
  job 接入 st_score_weight 冒烟步（手动触发口径沿用 Phase 75）；
  pre-push 四连门全绿；gh CLI 无 token，远端 CI 状态由用户网页
  确认——仓库已转 public 并更名 feishu-agent-scstanalysis，
  Actions 免费额度恢复（Phase 75 账单挂账解除），public 后首次
  运行结果待确认。
- **④ deconvolve 前置（Task 4，commit 917af74）**：oscc
  deconv.h5ad 落盘，真机耗时 **3991s（~67min）**，
  n_cell_types=**14**、n_ref_cells=1844；14 型清单：
  CD16- NK cells/Classical monocytes/Endothelial cells/
  Epithelial cells/Fibroblasts/Macrophages/Mast cells/
  Memory B cells/Plasma cells/Regulatory T cells/
  Tem_Effector helper T cells/Tem_Trm cytotoxic T cells/
  gamma-delta T cells/pDC；mean_abundance 要点：Epithelial
  **1.68** 最高、Mast 1.437、Fibroblasts 0.707。
- **⑤ 真机验收（Task 5，commit a33724b）**：双 source 跑通
  （gs 14×14 / mb 14×315，overlap 1749，dropped 均空）；
  **S1 pass**（Epithelial top3=JAK-STAT/EGFR/TNFa 命中 2/3）；
  **S2 pass**（TGFb 排名 Epithelial 行内 5 = 列均值口径 5，
  ≤成立）；**S3 pass**（dropped 空）；summary json 落
  `bio_workspace/_eval/real_st_score_weight_summary.json`。
- **⑥ 重要观察（如实记录）**：gs 与 mb 两口径下 14 细胞型 top3
  **完全一致**（gs 全为 JAK-STAT/EGFR/TNFa，mb 全为
  Ribosome/Coronavirus disease/Antigen processing）——丰度加权后
  细胞型间区分度体现在数值而非 top 排名，全局高分通路压过
  细胞型特异信号。

### 后续建议 / 挂账（#21）

- **top3 同质化问题**：建议后续变体——先对 S 按通路列 z-score
  再加权，或输出"细胞型 vs 全局均值差分矩阵"以恢复细胞型区分度。
- **CAF（Fibroblasts）×TGFb 与 599sub 联读结论（TGFb×TGFB1
  rho=0.976）的跨数据集交叉验证**另立。
- **bio-image-smoke 手动触发验收**新冒烟步（st_score_weight，
  workflow_dispatch）。
- **远端 CI 确认**：public 后首次运行结果待用户网页确认。

## 2026-09-22 Phase 77 收口回填（#22 L1 主动式 skill 进化双入口）

计划：`docs/superpowers/plans/2026-09-21-phase77-skill-distiller.md`，
spec 7082208。落地 RSI-L1（人在环路的递归自进化）：**手动 zip 安装**
与**成功后自动复盘提炼**双入口合并到统一 SkillInstaller 校验/安装层。
逐 Task 实录：

- **① SkillInstaller（Task 1，commit 4bb5021）**：统一校验/安装层
  12 用例全绿——validate_name（`[a-z][a-z0-9_]{1,30}`）/validate_files
  （键白名单 SKILL.md|tools.yaml|*.py、单文件 1MB 帽）/validate_zip
  （路径穿越防护+frontmatter 必查 name+description）/install
  （覆盖先整目录 .bak.时间戳兜底）。
- **② SkillDistiller（Task 2，commit 866e73e）**：成功复盘 7 用例——
  触发过滤 3（低于阈值不进 LLM/达阈值触发/skill 工具调用不计数）+
  schema 4（worth=false 静默/缺字段/重名拒/坏 files 拒）；复用
  Phase 28 归因纪律与幻觉字段白名单（875b280）。
- **③ create 分支接线（Task 3，commit 3d4f112）**：skill_improve 卡
  新增 kind 字段（patch=Phase 27 存量/create=新建）+回调按 kind
  分派（create→installer.install，完整 suggestion 经
  `_pending_create[improve_id]` 进程内暂存——按钮 value 不嵌文件
  内容，code_approval 式无 DB 纪律）；契约测试红→绿，全量 pytest
  **1389 passed**（基线 1366+23）。
- **④ 手动 install 路由（Task 4，commit 86efbc8，计划外 7 文件
  适配）**：计划假设"zip+文本同消息、路由在 gateway/app.py、已有
  download_file"三处全部与现状不符——飞书单消息单类型实为
  **两段式交互**（发 zip→`_pending_zip[chat_id]` 暂存→发
  `/skill install` 消费）；normalizer 此前拒一切非文本消息→补文件
  消息透传（IncomingMessage.file_key/file_name）；IMAdapter 本无
  下载方法→补 `download_message_file`（SDK im.v1.message_resource
  .get，需 im:resource 权限，CLI 路径 NotImplementedError）；路由
  落 orchestrator/app.py process()。4 用例，全量 **1393 passed**。
- **⑤ README 自进化章节（Task 5，commit 38cbaec）**。
- **⑥ 真机验收三链（Task 6，当日修 4 真机 bug 后全过）**：
  - 链①（手动全链）✅：ok_zip→/skill install→批准→toast→/code
    调用 run_echostats 统计 test_data.csv **chars=2083 与降级
    执行交叉一致**；失败后 diagnoser patch 卡→批准→SKILL.md.bak
    落盘（patch 流同步验证）。
  - 链②（自动复盘）✅：3 次 python run_cmd 触发 distiller→LLM
    判 worth=true→create 卡（pyfile_size，归因"模板化重复手写"
    准确）→批准→install 落盘+toast（产物 command 约定违反见
    挂账①，验收后即删）。
  - 链③（拒绝路径）✅：缺 SKILL.md zip 与缺 description zip 均
    精确拒绝不出卡。
  - toast 证据口径：飞书 toast 为顶部 2-3 秒气泡，以
    `ws card handled {status: applied, message: ...}` 日志为准。
- **真机修复四连（验收驱动，当日提交）**：
  1. **runtime 接线遗漏（d5aa923）**：plan Task 3 改了
     CodingRunner.__init__ 签名却漏了 gateway/runtime.py 构造点，
     真机报"skill 安装器未配置"——补 distiller/installer 注入
     （共享 _skills_dir）。
  2. **ws 模式 toast 缺失（8e33907）**：卡片回调 toast 统一在
     ws_client.card_result_to_response 拼装，该函数无 skill_improve
     分支——app.py 补 message 字段（create/patch 分文案+.bak
     提示），ws_client 补 applied/apply_failed 分支消费。
  3. **distiller 真机永不触发（5c59d61，双 bug）**：(a) agent_loop
     的 tool_events 只记 {step,name,ok}，args 摘要算了 100 字符
     却未落事件；(b) _should_trigger 假设 args 为裸命令字符串，
     真机形态为 `{"cmd": [...]}` JSON 摘要（截断可成非法 JSON），
     整串 split 首词恒为 `{"cmd":`——单测喂裸字符串全绿、真机
     全灰，典型"测试形态≠真机形态"盲区。修法：事件落 args 摘要
     +CMD_FIRST_RE 正则取首元素（容忍截断），TDD 补真机形态
     正/反两测（反例 python/Rscript/node 不同解释器不得触发，
     修复前确实红）。全量 **1395 passed**。
  4. **测试 fixture command 约定违反（未提交，fixture 修复）**：
     验收 zip 的 `command: python /skill/echo_stats.py {path}`
     三处违反执行器约定——字符串 command 被 `list()` 炸成单
     字符（WinError 2 根因）、`/skill/` 是容器模式（配 image）
     挂载路径在本机模式不存在、`{path}` 占位符不做替换（实为
     `--k v` 旗标追加）。按内置 skill 约定（argv 列表+venv
     python+cwd=skill 目录+argparse）修复。
- **运维事故钉档（19:20-21:51 服务离线 2.5h）**：agent 误用
  StopCommand 杀 start.ps1 后台任务导致进程树连带 ws_client
  被杀；9/18 遗留 guardian **僵尸双实例**（单实例守卫应拒第二
  实例却并存，且均停止 tick 未拉起）未兜底。恢复：清双僵尸→
  start.ps1 自然退出（不 StopCommand）→新 guardian 单实例。
  **教训：start.ps1 拉起的后台任务绝不用 StopCommand 终止
  （杀树）；guardian 需存活自检**。

### 关键修复与教训沉淀（#22 新增）

16. **"单测形态≠真机形态"盲区**：distiller 触发过滤单测喂裸字符
    串 args 全绿，真机 agent_loop 根本不落 args 字段——消费方
    单测的数据形态必须与生产方实际产出逐字段核对（本次靠"真机
    验收必跑触发链"才暴露）；修法=生产方事件 schema 变更时同步
    消费方测试 fixture，或共享事件构造 helper。
17. **计划假设与现状的三处偏差**（Task 4）：飞书单消息单类型、
    normalizer 非文本拒收、download 方法缺失——**涉及外部平台
    约束的计划条目，执行前用最小探针验证假设**（发条文件消息
    看 normalizer 行为）比按计划写 7 文件再返工便宜。
18. **热加载不刷新**：register_tools 重名跳过使已注册 skill 的
    tools.yaml 变更（覆盖安装/patch）运行期不生效——"每任务
    scan"只拾新增不刷存量，重启才清；文档化前用户侧口径：
    **改 skill 后需重启服务或等 registry 替换机制落地**。

### 后续建议 / 挂账（#22）

- **① distiller/diagnoser prompt 未教 command 约定（实锤）**：
  真机提炼的 pyfile_size tools.yaml 三连错——字符串 command、
  `{{files}}` 占位、parameters 缺 `type: object` 包裹——装上即
  WinError 2。修法候选：PROMPT_TEMPLATE 注入约定（argv 列表/
  cwd=skill 目录/--k v 旗标/本机 vs 容器 /skill/ 路径）+
  installer validate_files 增 command 形态校验（字符串拒收或
  告警）双保险。
- **② registry 热刷新**：installer.install/diagnoser.apply 写回
  后应触发 registry 替换（或每任务重建 skill 注册），消除
  "改 skill 必重启"口径。**（2026-09-23 真机验证闭环，见文末回填）**
- **③ .bak 备份目录污染**：install 覆盖与 diagnoser patch 的
  .bak 落在 skills/ 内，被 SkillLoader `*/SKILL.md` 扫描（重名
  工具跳过告警刷屏）且被 ruff/mypy 门禁扫到（未类型标注的旧
  脚本必挂 pre-push）——备份应移出 skills_dir 或门禁/扫描加
  `.bak*` 排除。
- **④ guardian 治理**：僵尸双实例原因待查（单实例守卫失效
  场景）；考虑 guardian 心跳落盘+ws_client 侧反向探活。
- **⑤ L2 进化质量自评估**（补丁效果度量+自动回滚）待使用数据
  积累；**L3 全自动明确不做**；定期批处理触发另议。
- **⑥ 第三方安装的 skill 会触发项目 lint/mypy 门禁**（skills
  包在 mypy strict 范围内）——长期需区分内置/安装 skill 目录
  或装后自动补类型桩。**（2026-09-23 双目录分离闭环，见文末回填）**

## 2026-09-22 挂账①-④ 修复回填（Phase 77 次日加固轮）

当日 4 项挂账一次性修复，门禁 ruff/mypy（win32+linux 反模拟）/
pytest 1416 passed（基线 1395+21）全绿：

- **① command 约定双保险**：distiller/diagnoser 的 PROMPT_TEMPLATE
  注入硬性约定（argv 列表/cwd=skill 目录/--k v 旗标/禁占位符/本机
  无 /skill/ 路径）；installer.validate_files 增 `_check_tools_yaml`
  硬校验——字符串 command、占位符 command、非法 YAML 一律拒收
  （错误文本自带正确示例）。测试 9 例（installer 4 + distiller 2
  prompt 契约 + 端到端拦截 1 + 既有回归 2）。
- **② registry 热刷新**：ToolRegistry 增 unregister；SkillLoader 增
  `reload_skill(registry, name)`——按 `[skill:<name>]` 描述前缀卸载
  旧工具（含新版已删工具）→重扫目录→强制重注册（register 同名覆写）；
  gateway/app.py skill_improve 回调在 create/patch 写回成功后调用，
  失败仅记日志不回滚落盘。测试 5 例（新增/覆写/删工具/他 skill
  不受影响/目录删除清注册）。**自此改 skill 免重启**。
- **③ .bak 移出 skills/**：install 覆盖备份改落
  `skills/../.skill_backups/<name>.bak.<ts>`（gitignore+ruff exclude
  双排除；mypy packages 白名单天然不扫）；SkillLoader.scan 防御性
  跳过 `.`/`.bak` 目录兜历史残留。diagnoser 文件级 .bak（SKILL.md
  .bak 等）不匹配 `*/SKILL.md` 与 `*.py` 扫描面，保持原位。
- **④ guardian 治理**（最终实现 7a71d67+）：`kill_other_guardians()`
  启动时按命令行特征（含 ws_guardian）**psutil 进程内**清扫同族
  僵尸（零子进程；非 win32 返回 0；AccessDenied/竞态退出跳过；
  清扫数落盘 logs/ws_guardian_sweep.log；**自杀防护：排除自身
  venv pythonw 启动器父进程**——其 cmdline 同样含 ws_guardian.py，
  误杀会被启动器作业对象级联带走 guardian（09:59 真机实测
  swept=1 后本体 30s 内静默死亡）；`write_heartbeat()`
  每 tick 落 ISO 时间戳至 logs/ws_guardian.heartbeat（外部可判
  假死）。测试 4 例（heartbeat 格式/非 win32 no-op/清扫计数与
  误伤排除/AccessDenied 容忍）。
  **教训 #20（沙箱作业对象连坐）**：在 agent 沙箱（父链
  trae-sandbox.exe→agent-tool-host.exe→TRAE SOLO CN.exe）内
  spawn powershell 子进程会被作业对象击杀且无声（同步版连
  guardian 一起 exit -1；DETACHED_PROCESS 独立进程组 detach 版
  子进程仍被杀——detached 只脱控制台不脱作业对象）；且
  pythonw -Command Stop-Process 组合疑似触发安全软件静默拦截
  （沙箱外计划任务实例的子进程同样无声消失）。**最终结论**：
  系统管理类操作优先纯进程内实现（psutil），子进程方案在
  沙箱/AV 双重拦截面下不可观测不可依赖。
  **教训 #21（沙箱拦截 OpenProcess → 探活恒真）**：沙箱内对死 pid
  调 OpenProcess 竟能开出句柄（Get-Process 已判死）→ ws_client
  `_pid_alive` 恒真 → guardian 单例守卫把陈旧 pidfile 误判为活
  实例反复拒启。修复：acquire_guardian_singleton 加心跳新旧
  交叉验证（`_heartbeat_stale`，超 3×tick=180s 未更新即判假死
  接管；心跳缺失=旧版实例则保守拒绝）。测试 +2（心跳新鲜拒绝/
  心跳超龄接管），ws_guardian 共 13 例。结论：沙箱内一切依赖
  OS 进程语义的判断都需带旁路佐证（文件心跳/mtime），不可单信
  Win32 探活。

### 教训沉淀（#22 增补）

19. **挂账当日清最划算**：真机验收刚跑完、上下文最热时修复挂账，
    四处修复+21 测试一次门禁通过；若隔周再做，fixture 细节与事故
    现场都已冷却。

## 2026-09-23 挂账②⑥闭环 + CI 红点钉档 + guardian 生产化回填

当日三件：②真机验证、⑥双目录分离、CI 历史红点钉档，另完成
guardian 计划任务一键安装与三轮真机 bug 修复。门禁 pytest
**1431 passed**（基线 1416+15），ruff/mypy（win32+linux 反模拟）全绿。

- **② 热刷新真机验证闭环**：app.py skill_improve 回调热加载接线
  此前零测试覆盖，补 2 例集成测试（test_renew_card_callback.py：
  reload_skill 以 `(registry, "reportgen")` 被调且落盘生效；reload
  抛错时仍落盘不回滚）。新建 `scripts/_verify_skill_hot_reload.py`
  真机全链探针：真实 SkillInstaller 装 probe skill v1 → scan+register
  → 调用得 `v1:hello` → 改写 run.py 为 v2 → reload_skill →
  **同一 registry 对象**再调得 `v2:hello`（removed/added 均为
  `['probe_echo']` 列表断言）→ finally 清理，PASS。另真机观测到
  guardian 计划任务实例自动拉起 ws_client 新进程（旧 36936 死亡→
  10:18:59 以新代码重启为 23952，"stale pidfile -> take over"，
  wss 连接正常、心跳新鲜）——守护链路与热部署回路同时得到实证。
- **⑥ 内置/安装 skill 双目录分离闭环**：内置 `skills/`（门禁覆盖）
  vs 安装 `skills_installed/`（门禁豁免的同级目录），
  `skill_loader.installed_dir_for()` 为唯一定位来源；scan 双目录
  遍历且**同名内置优先**（安装副本告警跳过）；reload_skill 先解
  安装目录再回落内置；installer 只写安装目录且**拒装与内置同名**
  （同名安装副本永不生效，属死重）；diagnoser `_iter_tools_yamls()`
  与 distiller `_skill_dirs()` 均覆盖双目录；ruff extend-exclude
  与 .gitignore 加 `skills_installed/`；echostats 迁入安装目录。
  测试 test_skill_dirs.py 9 例钉约定 + 3 处旧 fixture 随新落点
  修正。自此第三方安装/提炼的 skill 不再触碰 ruff/mypy 门禁。
- **CI 历史红点钉档**（0b8c422/96db405/7a71d67）：日志下载被
  GitHub API 403（"Must have admin rights"）且本机无 GH_TOKEN，
  改用 `git show <sha>:...` 静态钉案——三红点均含 creationflags
  版 guardian 清扫代码，其测试 monkeypatch `sys.platform`→win32
  **绕过** `if sys.platform != "win32": return` 提前返回守卫后，
  在 Linux 上求值 `subprocess.DETACHED_PROCESS | CREATE_NO_WINDOW`
  等 Windows-only 常量 → AttributeError（非 OSError，except 不接）
  → ubuntu CI 测试错误。gateway/ws_client.py 的 WinDLL 导入为
  if/else 语句块守卫（mypy 可达性特判生效），Linux 安全，排除。
  后续 psutil 纯进程内重构移除了全部 creationflags 求值面，
  master 最新 3 连绿=自愈确认，无需代码回修。
- **guardian 生产化**：`scripts/install_guardian_task.ps1`（+
  同名 .bat）一键注册计划任务 FeishuAgentGuardian（AtLogOn、
  UAC 自提权、立即启动、90s 日志确认）。真机三连修：
  ① PS 5.1 将 UTF-8 无 BOM 的 ps1 按 GBK 解析，中文注释字节吞掉
  字符串引号 → 存为 UTF-8 BOM、bat 注释纯 ASCII；② 根级任务需
  管理员 → UAC 自提权重启自身；③ psutil 清扫误杀自身 venv
  pythonw 重定向启动器父进程（cmdline 同含 ws_guardian.py），
  启动器作业对象级联带走 guardian → 排除 `ppid()`（教训 #21
  同族）。终态真机验证：pid=67772 存活 133s+、心跳新鲜、
  sweep log swept=0、父链 svchost（计划任务上下文）。

### 教训沉淀（#22 再增补）

22. **monkeypatch 平台=绕过守卫的测试是跨平台炸弹**：为测 win32
    分支把 `sys.platform` 打成 win32，同时也关掉了代码里的提前
    返回守卫，Windows-only API 常量遂在非 Windows 上被真实求值。
    要么 patch 掉整个函数/模块属性，要么把 Windows 常量引用收进
    运行时函数体内并在测试侧一并 mock——本例 CI 三红点皆源于此。
23. **`git add -A` 会吞未跟踪的临时目录**：tmp_skill_accept/（9 个
    测试 zip）未 gitignore 时被一把提交进 4e62325，事后只能
    `git rm -r --cached`+补 ignore 修复（b369e64）。add 前先
    `git status --short` 扫一眼未跟踪区，临时产物目录一律先落
    .gitignore 再开工。
