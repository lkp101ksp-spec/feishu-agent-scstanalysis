# 测试总结+2026-09-04T14-35-36（Phase 26 真机验收 + sc_load 修复 + 生信 skill 沉淀）

## 本次测试范围

Phase 26（A' 方案 coding agent）真机验收 5 项 + sc_load 错误信息优化 + 3 个生信 skill 沉淀与真机全链验收。

## 测试记录

### 1. sc_load 不存在路径错误信息优化（618d41f）

**根因**：`compute_dataset_id`（bio_runner.py:34）单文件版漏存在性检查，`os.stat` 对不存在路径抛原生 `FileNotFoundError`（WinError 2），sc_load handler 只捕 `BioRunError` 捕不到 → 被 tool_handler 兜底吞掉输出 `TOOL_EXEC_FAILED: [WinError 2]`。

**修复**：入口加 `os.path.isfile` 检查抛 `BioRunError("SC_FILE_NOT_FOUND")`（对齐目录版 `compute_dataset_id_dir` 的 `is_dir` 检查）。

**测试**：新增 `test_compute_dataset_id_missing_file_raises` 验证不存在路径抛 `BioRunError` 而非原生 OSError。24 用例全绿。

### 2. 生信 skill 沉淀（3608ca8）

沉淀 3 个 skill（宿主 scanpy 直跑，无容器隔离）：
- **bio_10x_merge**：10x 多样本合并（read_10x_mtx → concat → batch 列 → 去重 → h5ad）
- **bio_preprocess**：标准预处理（QC min_genes=600/min_cells=3/mt<20 → normalize_total 1e4 → log1p → HVG 2000 → scale → PCA 50 → neighbors 15 → UMAP → leiden 1.0 igraph）
- **bio_markers**：按 leiden 簇 wilcoxon 差异分析（use_raw=True）→ 每簇 top10 markers + dotplot

**宿主依赖安装**：scanpy 1.12.4 + igraph 1.1.2 + leidenalg（清华源）。

**本地冒烟**（bio_test_data\10x 10 样本 → 工作区）：
- merge：93665 细胞 × 32285 基因，batch 分布 M96=16513/M13_14=13698/M2=13347 等全对上
- preprocess：93665→59899 细胞 × 2000 HVG，27 簇
- markers：生物学合理（簇 0 成纤维 Col1a1/Col3a1/Sparc、簇 7 肌肉 Acta1/Tnnt3/Myl1、簇 25 B 细胞 Cd79a/Igkc/Bank1、簇 16/20/21 T 细胞 Cd3g/Itk/Skap1、簇 26/13 内皮 Cldn5/Flt4/Pecam1/Cdh5、簇 10 髓系 Lyz2/Ctss、簇 5 增殖 Mki67/Top2a、簇 23 干扰素应答 Rsad2/Isg15）

### 3. 真机 skill 全链验收（/code 用 3 个 skill）

**指令**：`/code 用 bio_10x_merge 把 I:\飞书agent\bio_test_data\10x 合并到工作区 merged.h5ad，然后用 bio_preprocess 预处理到 processed.h5ad，最后用 bio_markers 提取 markers 到工作区`

**结果**：步数 10 · 工具 9✓/0✗ · ~3581 tokens
- 词元命中 3 个 skill（"合并/预处理/markers" ∩ SKILL.md）
- 依次调 bio_10x_merge（审批）→ bio_preprocess（审批）→ bio_markers（审批）
- 数字与本地冒烟完全一致：93665 合并 → 59899 预处理 27 簇 → markers dotplot
- 产物：merged.h5ad（1.77GB）+ processed.h5ad（2.58GB）+ markers_dotplot.png（1.03MB）

**意外**：模型 initially 尝试写到 `C:\Users\kj04ke\Documents\feishu_test_workspace`（PermissionError），自动切换到实际工作区 `code_workspace\code_016355f0\`——WorkspaceManager 防逃逸生效。

### 4. Phase 26 全量回归

- 基线 927（Phase 26 收官时）
- 新增 3：sc_load 不存在路径 1 + skill 相关 2
- **当前 930 绿**（53.13s）

## 后续建议

1. **Phase 27 skill 失败诊断**（已写计划 `docs/superpowers/plans/2026-09-04-phase27-skill-diagnoser.md`）：/code 任务失败时自动分析轨迹、定位 skill 缺陷、生成改进建议，经人工审批卡写回 skill 目录。成本 1-2 天。
2. **Working Memory**（Recuris 思想提炼）：AgentLoop 加任务状态层，长任务（>10 步）不再丢目标。留给 Phase 28。
3. **skill 子进程容器隔离**：现在 skill 直跑宿主 python（需装包），复用 bio 沙箱即可容器化（防恶意 skill）。
4. **「他人不可批」群聊验证**：安全闭环最后一块，需第二账号。
5. **skill 语义检索**：词元匹配命中率低时换 embedding。
