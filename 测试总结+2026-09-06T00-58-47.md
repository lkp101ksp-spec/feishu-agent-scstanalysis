# 测试总结 2026-09-06T00-58-47 — pySCENIC 兼容性实测（Phase 36 前置研究）

## 任务
在宿主 venv（Python 3.12.10 + pandas 2.3.3 + scanpy 1.12.4 + liana 1.10.0）实测 pyscenic 兼容性，
为 Phase 36 `sc_scenic` 工具选型提供依据。未改动任何项目代码；探针脚本位于
`code_workspace/probe_pyscenic.py`（配套 `code_workspace/sitecustomize.py`，仅经 PYTHONPATH 生效）。

## 安装结果（清华源）
- 装上 **pyscenic 0.12.1**（arboreto 0.1.6 / ctxcore 0.2.0 / loompy 3.0.8 / pyarrow 25.0.1 / dask+distributed 2026.8.0 新装）。
- `pip check` 无冲突。
- **setuptools 被装成 84.0.0 → ctxcore 依赖 `pkg_resources` 崩溃，已降级 setuptools==80.9.0 修复**。

### 核心包版本对比（安装前 → 安装后）
| 包 | 前 | 后 | 变动 |
|---|---|---|---|
| pandas | 2.3.3 | 2.3.3 | 无 |
| numpy | 2.5.2 | 2.5.2 | 无 |
| scanpy | 1.12.4 | 1.12.4 | 无 |
| liana | 1.10.0 | 1.10.0 | 无 |
| scikit-learn | 1.9.0 | 1.9.0 | 无 |
| numba | 0.67.0 | 0.67.0 | 无 |
| dask / distributed | 未安装 | 2026.8.0 | 新增（pyscenic 拉入） |
| setuptools | （旧/无） | 84.0.0 → 手动降 80.9.0 | 需锁定 <81 |

## 四步实测（120 细胞 × 300 基因合成矩阵，真实 hg38 500bp feather + motif tbl）
| 步骤 | 结果 | 耗时 | 说明 |
|---|---|---|---|
| A grnboost2 | OK（需绕路） | 10.6s | vanilla 炸；改用 `arboreto.core.create_graph(..., include_meta=True)` 成功，adjacencies=(4485,3)，SPI1→LST1/CTSS/S100A9 注入信号正确回收 |
| B modules_from_adjacencies | OK | 0.1s | 7 个模块（adj 先过滤到 DB 可映射基因） |
| C prune2df + df2regulons | OK（需绕路） | 20.0s | vanilla 炸；monkeypatch `pyscenic.prune.from_delayed`（generator→list）后 df=(187,8)，**regulons=2**（RUNX1(+) 48 基因、STAT1(+) 45 基因） |
| D aucell | OK | 1.8s | AUC 矩阵 (120, 2) |

## 发现的硬兼容问题与绕法
1. **arboreto 0.1.6 `grnboost2()` 与 dask≥2024.3（dask-expr）不兼容**：`create_graph` 无条件对空列表
   调 `from_delayed` → `TypeError: Must supply at least one delayed object`。
   绕法：直接调 `arboreto.core.create_graph(..., include_meta=True)` + `client.compute(sync=True)`。
2. **pyscenic 0.12.1 `pyscenic/prune.py` 的 dask 路径同样不兼容**：向新版 `from_delayed` 传 generator →
   `TypeError: object of type 'generator' has no len()`。绕法：monkeypatch 物化为 list（约 5 行）。
3. **`pyscenic/transform.py:42-44` 引用 `np.object`（numpy≥1.24 已删）**：import 期即炸；`diptest.py:64`/`rss.py:21`
   引用 `np.float`。绕法：运行时 shim（`np.object=object` 等）；因子进程 spawn 重导入，需经
   `sitecustomize`（PYTHONPATH 注入）使子进程同样生效。
4. **`multiprocessing-on-dill` 3.5.0a4 与 Python 3.12 spawn 协议不兼容**（`BaseProcess._bootstrap() takes 1
   positional argument but 2 were given`）→ `prune2df(client_or_address="custom_multiprocessing")` 路径不可用，且
   子进程死亡会致父进程挂死在 `recv()`。sc_scenic 必须走 dask 路径。
5. **ctxcore 0.2.0 import 依赖 `pkg_resources`** → 需 `setuptools<81`（已锁 80.9.0）。
6. 良性警告：`pkg_resources` deprecation、`logger.warn` deprecated、pandas `DataFrameGroupBy.apply`
   include_groups FutureWarning（pyscenic/utils.py:244，未来 pandas 版本可能行为变化，需关注）。

## 结论
**可用，但需"锁版本 + 3 处绕路"**：
- 锁：`setuptools<81`（其余核心包 pandas/numpy/scanpy 均未被 pyscenic 改动）。
- 绕：① grnboost2 → `create_graph(include_meta=True)`；② prune2df → monkeypatch `from_delayed`；
  ③ numpy 别名 shim（需覆盖子进程，建议 sitecustomize 或 worker initializer）。
- pandas 2.3.3 下四步全绿，无 DataFrame.append / np.Inf 历史坑残留。
- 勿用 `custom_multiprocessing`（Python 3.12 不可用且会挂死）。

## 后续建议
1. sc_scenic 实现时将上述 shim/monkeypatch 封装进工具初始化（含 dask worker initializer 注入 numpy 别名 shim）。
2. 真实数据回归：用真实 h5ad（≥2000 细胞）跑一遍计时，评估 grnboost2 与 prune 的耗时/内存（feather 1.2GB 全量入内存，workers 各持副本）。
3. 关注 pandas `DataFrameGroupBy.apply` include_groups 语义变化，升级 pandas 前回归 modules 步骤。
4. 考虑给 `requirements-lock.txt` 增补 setuptools<81、pyscenic==0.12.1 及 shim 说明。

---

# Phase 36 Task 5 本机冒烟（2026-09-06 01:2x，宿主 venv + 真实 hg38 500bp 库）

## 结果：SMOKE PASS
- 输出：`[scenic] n_cells_used=200, n_genes=300, n_tfs=1839, n_adjacencies=4186, n_modules=8, n_regulons=2 (CEBPA(+), SPI1(+))`；`[ok] SPI1 module recovered, overlap = 6/9`；`aucell (200, 2)`。
- 耗时：grn 12.0s / ctx 16.0s / aucell 2.2s（远快于预估 1-3 分钟）。
- ruff：sandbox/sc_tools/scenic.py All checks passed。

## 本轮新踩两坑（已修，均同步进 sandbox/sc_tools/scenic.py）
1. **prune2df 默认 dask_multiprocessing 调度 spawn 子进程不继承进程内 numpy shim**
   （worker import pyscenic.transform 时 np.object 即炸）。修复：_prune 自建线程式
   LocalCluster(processes=False) 并把 Client 传给 prune2df(client_or_address=...)。
   sitecustomize 仍作容器兜底。
2. **RSS 方向假设错误**：pyscenic 0.12.1 regulon_specificity_scores 返回
   cell_type × regulon（行=簇、列=regulon），scenic.py 原计划按 列=簇 取值导致
   KeyError。修复：改用 rss.index 迭代簇、rss.loc[c].nlargest(...) 取 regulon。
3. 冒烟脚本自身：REAL 基因列表漏了 SPI1（genes.index 报错），已补；
   断言增强为 ok=False 时附 stderr 尾部。
- 观察记录：修复 1 后第一次冒烟曾出现一次性 `KeyError: Field "CST7" exists 2 times in schema`
  （pyarrow 重复列名），直跑 driver 及后续重跑均未复现，疑为 dask-expr 序列化偶发；留待容器集成验证观察。

## 产物
- code_workspace/smoke_phase36.py（untracked）
- code_workspace/smoke_ws36/ds36/scenic/{adjacencies.csv, regulons_enriched.csv, regulons.json, regulon_auc.csv, rss.csv, scenic_heatmap.png}

## 后续建议
- Task 6 容器断网集成时重点观察：dask LocalCluster loopback、CST7 schema 偶发是否再现。
- 真机回归时关注 threaded prune 在大模块数下的内存（线程共享主进程，feather 不再多副本，反而省内存）。

---

# Phase 36 实施节（sc_scenic 调控网络，2026-09-06）

## 交付
- `sandbox/sc_tools/scenic.py`：pySCENIC 0.12.1 三幕（GRNBoost2→cisTarget→AUCell）参数化脚本，
  counts 走 filtered/raw 链，基因过滤 count>=3 且 >=3 细胞，按簇分层抽样 max_cells（默认 3000）；
  TF 列表从 motif tbl gene_name 派生（hg38 ~1839）。产物落 `WS/{id}/scenic/`：
  adjacencies.csv / regulons_enriched.csv / regulons.json / regulon_auc.csv / rss.csv /
  scenic_heatmap.png。不写回 processed.h5ad（抽样是子集），AUC 可用 sc_meta merge_csv 回挂。
- `sandbox/scenic_site/sitecustomize.py`：numpy 别名 shim 兜底（site 自动导入，覆盖 dask worker 子进程）。
- `sandbox/bio.Dockerfile`：pyscenic 层（锁 setuptools<81 + COPY sitecustomize）。
- `config/settings.py`：新增 `bio_scenic_db_root`（env `BIO_SCENIC_DB_ROOT`，空=仓库根）。
- `orchestrator/app.py`：register_l3_singlecell 接线 bio_scenic_db_root。
- `orchestrator/tools/builtin/l3_singlecell.py`：sc_scenic handler + ToolSpec（L1_compute，timeout 3600）。
  **运行时只读挂载设计**（DB 不进镜像）：handler 挂 `cisTarget_databases→/scenic_db/cistarget`、
  `motifAnnotations→/scenic_db/motifannot`（均 :ro）；DB 目录缺失直接 SC_CONFIG 不进容器。
- `tests/unit/test_l3_singlecell.py`：TDD 4 用例（注册可见/转发+挂载断言/DB 缺失 SC_CONFIG/schema enum）。
- sc_* 工具数 20→21。

## 五坑绕法全录（宿主 probe 实测 + 冒烟期新增）
1. **setuptools 锁 <81**：84+ 删 `pkg_resources`，ctxcore 0.2.0 import 即炸；宿主降 80.9.0，
   Dockerfile 构建层同锁。
2. **grnboost2() 与 dask-expr 不兼容**：`create_graph` 对空 meta 列表调 from_delayed 炸
   `TypeError: Must supply at least one delayed object` → 改 `arboreto.core.create_graph(
   include_meta=True)` + `client.compute(sync=True)`。
3. **prune2df 把 generator 传给新版 from_delayed**（无 len）→ monkeypatch
   `pyscenic.prune.from_delayed` 先物化 list（约 5 行）。
4. **np.object/np.float（numpy>=1.24 已删）**：pyscenic transform/diptest/rss 仍引用 → 别名 shim；
   容器内经 site-packages/sitecustomize.py 覆盖 dask worker 子进程（spawn 全新解释器也生效）。
5. **prune2df 默认 dask_multiprocessing 调度 spawn 子进程不继承进程内 shim**（冒烟期新坑）：
   worker import pyscenic.transform 时 np.object 即炸 → `_prune` 自建线程式
   `LocalCluster(processes=False)` 并把 Client 传给 `prune2df(client_or_address=...)`。
   sitecustomize 仍作容器兜底。
- 另禁用 `custom_multiprocessing`（multiprocessing-on-dill 3.5.0a4 与 Py3.12 spawn 不兼容，
  且子进程死亡挂死父进程 recv()）。
- 另修 **RSS 方向**：pyscenic 0.12.1 `regulon_specificity_scores` 返回 行=簇、列=regulon，
  原计划按列=簇取值致 KeyError，改用 rss.index 迭代簇。

## 冒烟结果（宿主 venv，真实 hg38 500bp 库，200 细胞×300 基因 + SPI1 注入模块）
- SPI1 注入模块 top-20 回收 **6/9**（CTSS/FCER1G/LYZ/S100A8/S100A9/TYROBP）；
  2 regulons（CEBPA(+)/SPI1(+)）；AUC (200, 2)；RSS/热图产物齐。
- 耗时：grn 12.0s / ctx 16.0s / aucell 2.2s。

## 镜像与断网容器验证
- 镜像重建：**e18ef7b3d7aa**（修复：Dockerfile COPY 路径须相对构建上下文——
  `COPY scenic_site/sitecustomize.py ...` 而非 `sandbox/scenic_site/...`）。
- 断网（--network none）三项验证全过：
  ① `np.object shim: True`（sitecustomize 生效，worker 子进程覆盖证明）；
  ② `import pyscenic/ctxcore/arboreto/common/scenic ok`（0.12.1）；
  ③ 带挂载真跑 scenic.py：`ok: true`，grn 14.7s/ctx 21.0s/aucell 1.0s，2 regulons 与宿主一致——
  **dask LocalCluster 走 loopback 在 --network none 下正常**；CST7 schema 偶发未再现。

## 测试与回归
- 单测：47 passed（43+4）；ruff 全文件零告警。
- 全量回归：**1045 passed**（1041+4，51.80s），落 `.pytest_last.log`。

## 事故与教训
- **settings 字段声明被子代理并行写竞争丢失**：实施中 `bio_scenic_db_root` 字段声明一度被并行
  子代理覆盖丢失，致 9 个测试失败；补回声明后全绿。教训：**subagent-driven 并行写同一文件时，
  关键配置字段必须在合并后跑目标测试验证存在性，不能只看 ruff 通过**。

## 待办
- 真机验收延后统一测试轮；**累计待真机验收 16 个工具（Phase 31-36）**。
